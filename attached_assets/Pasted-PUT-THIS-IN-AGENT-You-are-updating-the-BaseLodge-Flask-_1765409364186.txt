PUT THIS IN AGENT

You are updating the BaseLodge Flask app to use a unified global design system. Follow these steps exactly and use checkpoints sparingly:

────────────────────────────────────────
1. CREATE GLOBAL STYLE FILE
────────────────────────────────────────
Create a new file: /static/styles.css

Extract ALL CSS from public/index.html (the BaseLodge design system file) EXCEPT:
• preview-only layout (the page header, preview sections)
• spacing demo boxes
• example section wrappers

KEEP:
• :root design tokens
• typography classes
• button components
• card components
• chips / pills
• segmented control
• profile summary card
• trip rows
• friend rows
• bottom navigation
• spacing utilities if needed

Clean, organize, and compress where appropriate.

────────────────────────────────────────
2. APPLY GLOBAL STYLES
────────────────────────────────────────
In base template (create if needed), or in every top-level template that should inherit global layout, ensure:

<link rel="stylesheet" href="{{ url_for('static', filename='styles.css') }}">

Remove any old unused CSS in static/style.css or inline blocks if the new design system replaces them.

────────────────────────────────────────
3. CREATE REUSABLE COMPONENT PARTIALS
────────────────────────────────────────

Add a new folder: templates/components/

Create the following files:

A) templates/components/profile_summary.html
Contains the BaseLodge profile summary card using:
• bl-card
• bl-profile-summary classes
Variables:
{{ user.first_name }} {{ user.last_name }}
{{ user.pass_type }}
{{ user.skill_level }}
{{ user.snow_type }}
{{ user.mountains_visited }}

B) templates/components/stats_card.html
A compact BaseLodge stats card with:
• Pass Type
• Skill Level
• Skier/Snowboarder
• Mountains count

C) templates/components/segmented_trips_tabs.html
Use the BaseLodge segmented control UI.
It must support three tabs:
"My Trips", "Friends' Trips", "All Trips"
Use Jinja variables to determine active tab.

D) templates/components/trip_row.html
A reusable row:
<span class="trip-date">{{ date }}</span> — <span class="trip-name">{{ user_name }}</span> <span class="trip-info">@ {{ mountain }}, {{ pass_type }}</span>

E) templates/components/friend_row.html
A compact friend display row.

F) templates/components/bottom_nav.html
Create the full bottom nav using BaseLodge styles.
Nav items:
Home → url_for('home')
Trips → url_for('my_trips')
Friends → url_for('friends')
Profile → url_for('profile')
Use an "active" class based on current endpoint.

────────────────────────────────────────
4. UPDATE ALL EXISTING PAGES TO USE NEW UI
────────────────────────────────────────

A) HOME PAGE (/home or /)
Rewrite home.html to follow this structure:

1. Profile Summary Card (call the partial)
2. Stats Card
3. Segmented Trips Tabs
4. Trip Feed (loop with trip_row.html)
5. "+ Add a Trip" button using bl-btn-primary
6. Include bottom nav partial at bottom

Remove old UI and duplicate layout.

B) PROFILE PAGE (/profile)
Use only:
• Profile summary card
• Editable profile form (restyle form inputs to match BaseLodge spacing & radius)
• Bottom navigation

Do NOT show trip cards on profile page.

C) FRIENDS PAGE (/friends)
Use:
• Page title "Friends"
• Filter chips using bl-chip classes
• Loop over friend list using friend_row.html
• Include bottom nav

D) MY TRIPS PAGE
If /my-trips exists, either:
• Redirect to /home
OR
• Convert to a standalone trip management page

Follow the BaseLodge card + trip row system.

────────────────────────────────────────
5. LOGIN FLOW FIX
────────────────────────────────────────
After successful login, redirect users to:

return redirect(url_for('home'))

Ensure @login_required decorations still function properly.

────────────────────────────────────────
6. REMOVE OLD UI
────────────────────────────────────────
Delete:
• any floating + buttons
• outdated blue buttons
• duplicated CSS
• mismatched padding
• old dropdown styling where segmented controls should be used

────────────────────────────────────────
7. VERIFY MOBILE-FIRST BEHAVIOR
────────────────────────────────────────
Ensure:
• All cards have correct max-width behavior
• Bottom nav is fixed at bottom
• Fonts match system-ui
• Buttons and segmented controls are finger-friendly
• No overflow issues on iPhone 12+ sizes

────────────────────────────────────────
8. FINAL CHECK
────────────────────────────────────────
• Ensure no template errors
• Ensure all profile/trips/friends routes still function
• Ensure new components are rendering properly
• Ensure bottom nav appears on all non-login pages
• Ensure global styles load on all pages

ONLY use checkpoints if a large multi-file change is necessary.
