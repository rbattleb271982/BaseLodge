Investigate BL-128 only. Do not make any code changes yet.

The issue: `POST /api/trip/create` can return an HTTP 500 when the request body contains valid JSON that is not a JSON object, such as an array, scalar, string, number, boolean, or null.

Please inspect the current create-trip API flow and report:

1. How `/api/trip/create` currently reads and validates the JSON request body.
2. Exactly why non-object JSON can reach an unhandled exception or 500.
3. Which non-object JSON types are affected, including:

   * arrays;
   * strings;
   * numbers;
   * booleans;
   * null.
4. How malformed JSON is currently handled and whether that behavior should remain unchanged.
5. The safest minimal fix so the endpoint accepts only a JSON object and returns a controlled HTTP 400 for any other JSON body.
6. The existing JSON error-response convention for this endpoint that should be preserved.
7. Where validation should occur so no trip, attendance, related record, notification, analytics event, or other side effect can occur from an invalid request.
8. Whether any shared request-validation helper already exists that should be reused rather than introducing duplicate logic.
9. What route-level tests currently cover `/api/trip/create` and what regression tests should be added for:

   * array JSON;
   * string JSON;
   * numeric JSON;
   * boolean JSON;
   * null JSON;
   * malformed JSON;
   * empty/missing body where relevant;
   * valid JSON object.
10. Whether the fix can be entirely server-side with no migration, dependency change, data backfill, or iOS/Android build.
11. Any compatibility risk to existing valid create-trip requests.

Keep the scope strictly to BL-128.

Do not redesign trip creation, change required trip fields, alter the API contract for valid object requests, modify unrelated JSON endpoints, or make general validation cleanup changes.

If you discover adjacent issues, identify them separately but do not include them in the proposed BL-128 implementation unless they are necessary to fix this specific bug.

Return an investigation report only. Do not implement anything until I explicitly approve it.
