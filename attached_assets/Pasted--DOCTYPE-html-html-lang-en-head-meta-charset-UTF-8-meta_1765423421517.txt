<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>BaseLodge Design System</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    :root {
      /* Color Tokens */
      --bl-color-primary: #8F011B;
      --bl-color-primary-soft: #B31633;
      --bl-color-bg: #FAFAFA;
      --bl-color-surface: #FFFFFF;
      --bl-color-border-soft: #F1F1F1;
      --bl-color-border: #D9D9D9;
      --bl-color-text: #1E1E1E;
      --bl-color-text-muted: #6E6E6E;
      --bl-color-accent-blue: #E7F3FF;
      --bl-color-success: #3E7E3F;
      --bl-color-error: #C62828;
      --bl-shadow-soft: 0 1px 3px rgba(0,0,0,0.06);

      /* Spacing (8pt scale) */
      --bl-space-2: 4px;
      --bl-space-3: 6px;
      --bl-space-4: 8px;
      --bl-space-5: 12px;
      --bl-space-6: 16px;
      --bl-space-7: 20px;
      --bl-space-8: 24px;

      /* Radius */
      --bl-radius-sm: 6px;
      --bl-radius-md: 12px;
      --bl-radius-lg: 20px;
    }

    body {
      font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
      background: var(--bl-color-bg);
      color: var(--bl-color-text);
      line-height: 1.6;
    }

    main {
      max-width: 900px;
      margin: 0 auto;
      padding: var(--bl-space-8);
    }

    /* TYPOGRAPHY CLASSES */
    .bl-heading-xl { font-size: 24px; font-weight: 700; line-height: 1.2; }
    .bl-heading-l { font-size: 20px; font-weight: 600; line-height: 1.3; }
    .bl-heading-m { font-size: 18px; font-weight: 600; line-height: 1.3; }
    .bl-body { font-size: 15px; font-weight: 400; line-height: 1.6; }
    .bl-label { font-size: 13px; font-weight: 500; line-height: 1.4; }
    .bl-caption { font-size: 12px; font-weight: 400; color: var(--bl-color-text-muted); }

    /* SECTION STYLES */
    section {
      margin-bottom: var(--bl-space-8);
      padding-bottom: var(--bl-space-8);
      border-bottom: 1px solid var(--bl-color-border-soft);
    }

    section:last-child {
      border-bottom: none;
    }

    .section-title {
      font-size: 20px;
      font-weight: 600;
      margin-bottom: var(--bl-space-6);
      color: var(--bl-color-text);
    }

    /* COLOR SWATCHES */
    .color-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
      gap: var(--bl-space-6);
      margin-top: var(--bl-space-6);
    }

    .color-swatch {
      border-radius: var(--bl-radius-md);
      overflow: hidden;
      box-shadow: var(--bl-shadow-soft);
    }

    .color-sample {
      height: 80px;
      width: 100%;
    }

    .color-label {
      background: var(--bl-color-surface);
      padding: var(--bl-space-5);
      border-top: 1px solid var(--bl-color-border-soft);
    }

    .color-var { font-size: 12px; font-weight: 600; color: var(--bl-color-text); }
    .color-hex { font-size: 11px; color: var(--bl-color-text-muted); }

    /* BUTTON STYLES */
    .bl-btn-primary {
      background: var(--bl-color-primary);
      color: white;
      border: none;
      border-radius: var(--bl-radius-lg);
      padding: 10px 14px;
      font-size: 15px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.2s ease;
      box-shadow: var(--bl-shadow-soft);
    }

    .bl-btn-primary:hover {
      background: #720115;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
    }

    .bl-btn-primary:disabled {
      background: var(--bl-color-text-muted);
      cursor: not-allowed;
    }

    .bl-btn-secondary {
      background: white;
      color: var(--bl-color-text);
      border: 1px solid var(--bl-color-border);
      border-radius: var(--bl-radius-lg);
      padding: 10px 14px;
      font-size: 15px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.2s ease;
    }

    .bl-btn-secondary:hover {
      background: var(--bl-color-bg);
      border-color: var(--bl-color-text-muted);
    }

    .bl-btn-secondary:disabled {
      opacity: 0.5;
      cursor: not-allowed;
    }

    .bl-btn-ghost {
      background: none;
      color: var(--bl-color-primary);
      border: none;
      padding: var(--bl-space-4) var(--bl-space-5);
      font-size: 15px;
      font-weight: 600;
      cursor: pointer;
      transition: opacity 0.2s ease;
    }

    .bl-btn-ghost:hover {
      opacity: 0.8;
    }

    .bl-btn-ghost:disabled {
      opacity: 0.4;
      cursor: not-allowed;
    }

    .button-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: var(--bl-space-6);
      margin-top: var(--bl-space-6);
    }

    .button-example {
      padding: var(--bl-space-6);
      border: 1px solid var(--bl-color-border-soft);
      border-radius: var(--bl-radius-md);
      background: var(--bl-color-surface);
      display: flex;
      flex-direction: column;
      gap: var(--bl-space-5);
    }

    .button-label {
      font-size: 12px;
      font-weight: 600;
      color: var(--bl-color-text-muted);
      text-transform: uppercase;
    }

    /* CARD STYLES */
    .bl-card {
      background: var(--bl-color-surface);
      border-radius: var(--bl-radius-md);
      border: 1px solid var(--bl-color-border-soft);
      box-shadow: var(--bl-shadow-soft);
      padding: var(--bl-space-6);
    }

    .bl-card-compact {
      padding: var(--bl-space-5);
    }

    .card-example {
      margin-top: var(--bl-space-6);
    }

    /* CHIP / PILL STYLES */
    .bl-chip {
      background: white;
      border: 1px solid var(--bl-color-border);
      border-radius: 999px;
      padding: 4px 10px;
      font-size: 13px;
      color: var(--bl-color-text-muted);
      display: inline-flex;
      gap: var(--bl-space-3);
    }

    .bl-chip-active {
      background: var(--bl-color-primary);
      color: white;
      border-color: var(--bl-color-primary);
    }

    .bl-chip-muted {
      background: var(--bl-color-bg);
      color: var(--bl-color-text-muted);
    }

    .chip-row {
      display: flex;
      flex-wrap: wrap;
      gap: var(--bl-space-5);
      margin-top: var(--bl-space-6);
    }

    /* SEGMENTED CONTROL */
    .bl-segmented-control {
      background: var(--bl-color-surface);
      border-radius: var(--bl-radius-md);
      padding: 4px;
      border: 1px solid var(--bl-color-border);
      display: flex;
      gap: 0;
      width: fit-content;
    }

    .bl-segment {
      border-radius: var(--bl-radius-md);
      padding: 6px 10px;
      font-size: 14px;
      font-weight: 600;
      border: none;
      background: transparent;
      color: var(--bl-color-text-muted);
      cursor: pointer;
      transition: all 0.2s ease;
    }

    .bl-segment.active {
      background: var(--bl-color-primary);
      color: white;
    }

    /* PROFILE SUMMARY */
    .bl-profile-summary {
      display: flex;
      flex-direction: column;
      gap: var(--bl-space-5);
    }

    .profile-name {
      font-size: 20px;
      font-weight: 600;
      color: var(--bl-color-text);
    }

    .profile-meta {
      font-size: 15px;
      color: var(--bl-color-text-muted);
    }

    .profile-mountains {
      font-size: 13px;
      color: var(--bl-color-text-muted);
    }

    .profile-action {
      color: var(--bl-color-primary);
      font-weight: 600;
      font-size: 13px;
      cursor: pointer;
      text-decoration: none;
    }

    /* TRIP ROW */
    .bl-trip-row {
      padding: var(--bl-space-5) 0;
      border-bottom: 1px solid var(--bl-color-border-soft);
      font-size: 14px;
      color: var(--bl-color-text);
    }

    .bl-trip-row:last-child {
      border-bottom: none;
    }

    .trip-date {
      font-weight: 600;
      color: var(--bl-color-text);
    }

    .trip-name {
      font-weight: 600;
    }

    .trip-info {
      color: var(--bl-color-text-muted);
    }

    /* FRIEND ROW */
    .bl-friend-row {
      padding: var(--bl-space-5) 0;
      border-bottom: 1px solid var(--bl-color-border-soft);
      display: flex;
      flex-direction: column;
      gap: var(--bl-space-3);
    }

    .bl-friend-row:last-child {
      border-bottom: none;
    }

    .friend-name {
      font-weight: 600;
      font-size: 15px;
      color: var(--bl-color-text);
    }

    .friend-meta {
      font-size: 13px;
      color: var(--bl-color-text-muted);
    }

    /* BOTTOM NAV */
    .bl-bottom-nav {
      background: var(--bl-color-surface);
      border-top: 1px solid var(--bl-color-border-soft);
      display: flex;
      justify-content: space-around;
      height: 56px;
      padding: var(--bl-space-4) 0;
      position: relative;
    }

    .bl-nav-item {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      gap: var(--bl-space-2);
      color: var(--bl-color-text-muted);
      font-size: 11px;
      font-weight: 600;
      text-decoration: none;
      cursor: pointer;
      transition: color 0.2s ease;
    }

    .bl-nav-item.active {
      color: var(--bl-color-primary);
    }

    .nav-icon {
      font-size: 20px;
    }

    /* SPACING VISUALIZER */
    .spacing-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(100px, 1fr));
      gap: var(--bl-space-6);
      margin-top: var(--bl-space-6);
    }

    .spacing-demo {
      text-align: center;
    }

    .spacing-box {
      background: var(--bl-color-primary);
      border-radius: var(--bl-radius-sm);
      margin: var(--bl-space-4) auto;
    }

    .spacing-label {
      font-size: 12px;
      font-weight: 600;
      color: var(--bl-color-text);
    }

    .spacing-value {
      font-size: 11px;
      color: var(--bl-color-text-muted);
    }

    /* PAGE HEADER */
    header {
      border-bottom: 1px solid var(--bl-color-border-soft);
      padding-bottom: var(--bl-space-8);
      margin-bottom: var(--bl-space-8);
    }

    .page-title {
      font-size: 32px;
      font-weight: 700;
      color: var(--bl-color-text);
      margin-bottom: var(--bl-space-3);
    }

    .page-subtitle {
      font-size: 15px;
      color: var(--bl-color-text-muted);
    }

    /* EXAMPLE SECTIONS */
    .example-section {
      background: var(--bl-color-surface);
      border: 1px solid var(--bl-color-border-soft);
      border-radius: var(--bl-radius-md);
      padding: var(--bl-space-6);
      margin-top: var(--bl-space-6);
    }

    .example-label {
      font-size: 12px;
      font-weight: 600;
      color: var(--bl-color-text-muted);
      text-transform: uppercase;
      margin-bottom: var(--bl-space-5);
    }
  </style>
</head>
<body>
  <main>
    <!-- PAGE HEADER -->
    <header>
      <h1 class="page-title">BaseLodge Design System</h1>
      <p class="page-subtitle">Complete visual design reference for the ski & snowboard social web app</p>
    </header>

    <!-- 1. COLORS -->
    <section>
      <h2 class="section-title">Color System</h2>
      <div class="color-grid">
        <div class="color-swatch">
          <div class="color-sample" style="background: #8F011B;"></div>
          <div class="color-label">
            <div class="color-var">--bl-color-primary</div>
            <div class="color-hex">#8F011B</div>
          </div>
        </div>
        <div class="color-swatch">
          <div class="color-sample" style="background: #B31633;"></div>
          <div class="color-label">
            <div class="color-var">--bl-color-primary-soft</div>
            <div class="color-hex">#B31633</div>
          </div>
        </div>
        <div class="color-swatch">
          <div class="color-sample" style="background: #FAFAFA;"></div>
          <div class="color-label">
            <div class="color-var">--bl-color-bg</div>
            <div class="color-hex">#FAFAFA</div>
          </div>
        </div>
        <div class="color-swatch">
          <div class="color-sample" style="background: #FFFFFF; border: 1px solid #D9D9D9;"></div>
          <div class="color-label">
            <div class="color-var">--bl-color-surface</div>
            <div class="color-hex">#FFFFFF</div>
          </div>
        </div>
        <div class="color-swatch">
          <div class="color-sample" style="background: #F1F1F1;"></div>
          <div class="color-label">
            <div class="color-var">--bl-color-border-soft</div>
            <div class="color-hex">#F1F1F1</div>
          </div>
        </div>
        <div class="color-swatch">
          <div class="color-sample" style="background: #D9D9D9;"></div>
          <div class="color-label">
            <div class="color-var">--bl-color-border</div>
            <div class="color-hex">#D9D9D9</div>
          </div>
        </div>
        <div class="color-swatch">
          <div class="color-sample" style="background: #1E1E1E;"></div>
          <div class="color-label">
            <div class="color-var">--bl-color-text</div>
            <div class="color-hex">#1E1E1E</div>
          </div>
        </div>
        <div class="color-swatch">
          <div class="color-sample" style="background: #6E6E6E;"></div>
          <div class="color-label">
            <div class="color-var">--bl-color-text-muted</div>
            <div class="color-hex">#6E6E6E</div>
          </div>
        </div>
        <div class="color-swatch">
          <div class="color-sample" style="background: #E7F3FF;"></div>
          <div class="color-label">
            <div class="color-var">--bl-color-accent-blue</div>
            <div class="color-hex">#E7F3FF</div>
          </div>
        </div>
        <div class="color-swatch">
          <div class="color-sample" style="background: #3E7E3F;"></div>
          <div class="color-label">
            <div class="color-var">--bl-color-success</div>
            <div class="color-hex">#3E7E3F</div>
          </div>
        </div>
        <div class="color-swatch">
          <div class="color-sample" style="background: #C62828;"></div>
          <div class="color-label">
            <div class="color-var">--bl-color-error</div>
            <div class="color-hex">#C62828</div>
          </div>
        </div>
      </div>
    </section>

    <!-- 2. TYPOGRAPHY -->
    <section>
      <h2 class="section-title">Typography</h2>
      <div style="margin-top: var(--bl-space-6);">
        <div style="margin-bottom: var(--bl-space-8);">
          <div class="bl-label" style="text-transform: uppercase; margin-bottom: var(--bl-space-3);">Heading XL (24px, 700)</div>
          <div class="bl-heading-xl">The quick brown fox jumps over the lazy dog</div>
        </div>
        <div style="margin-bottom: var(--bl-space-8);">
          <div class="bl-label" style="text-transform: uppercase; margin-bottom: var(--bl-space-3);">Heading L (20px, 600)</div>
          <div class="bl-heading-l">The quick brown fox jumps over the lazy dog</div>
        </div>
        <div style="margin-bottom: var(--bl-space-8);">
          <div class="bl-label" style="text-transform: uppercase; margin-bottom: var(--bl-space-3);">Heading M (18px, 600)</div>
          <div class="bl-heading-m">The quick brown fox jumps over the lazy dog</div>
        </div>
        <div style="margin-bottom: var(--bl-space-8);">
          <div class="bl-label" style="text-transform: uppercase; margin-bottom: var(--bl-space-3);">Body (15–16px, 400)</div>
          <div class="bl-body">The quick brown fox jumps over the lazy dog. This is the primary body text used throughout the interface for readable content.</div>
        </div>
        <div style="margin-bottom: var(--bl-space-8);">
          <div class="bl-label" style="text-transform: uppercase; margin-bottom: var(--bl-space-3);">Label (13px, 500)</div>
          <div class="bl-label">THE QUICK BROWN FOX JUMPS OVER THE LAZY DOG</div>
        </div>
        <div>
          <div class="bl-label" style="text-transform: uppercase; margin-bottom: var(--bl-space-3);">Caption (12px, 400)</div>
          <div class="bl-caption">The quick brown fox jumps over the lazy dog</div>
        </div>
      </div>
    </section>

    <!-- 3. SPACING & RADIUS -->
    <section>
      <h2 class="section-title">Spacing & Radius</h2>
      <h3 class="bl-heading-m" style="margin-top: var(--bl-space-6); margin-bottom: var(--bl-space-5); color: var(--bl-color-text-muted);">8pt Spacing Scale</h3>
      <div class="spacing-grid">
        <div class="spacing-demo">
          <div class="spacing-label">2px</div>
          <div class="spacing-box" style="width: 20px; height: 20px;"></div>
          <div class="spacing-value">--bl-space-2</div>
        </div>
        <div class="spacing-demo">
          <div class="spacing-label">4px</div>
          <div class="spacing-box" style="width: 24px; height: 24px;"></div>
          <div class="spacing-value">--bl-space-3</div>
        </div>
        <div class="spacing-demo">
          <div class="spacing-label">8px</div>
          <div class="spacing-box" style="width: 32px; height: 32px;"></div>
          <div class="spacing-value">--bl-space-4</div>
        </div>
        <div class="spacing-demo">
          <div class="spacing-label">12px</div>
          <div class="spacing-box" style="width: 44px; height: 44px;"></div>
          <div class="spacing-value">--bl-space-5</div>
        </div>
        <div class="spacing-demo">
          <div class="spacing-label">16px</div>
          <div class="spacing-box" style="width: 56px; height: 56px;"></div>
          <div class="spacing-value">--bl-space-6</div>
        </div>
        <div class="spacing-demo">
          <div class="spacing-label">20px</div>
          <div class="spacing-box" style="width: 72px; height: 72px;"></div>
          <div class="spacing-value">--bl-space-7</div>
        </div>
        <div class="spacing-demo">
          <div class="spacing-label">24px</div>
          <div class="spacing-box" style="width: 80px; height: 80px;"></div>
          <div class="spacing-value">--bl-space-8</div>
        </div>
      </div>

      <h3 class="bl-heading-m" style="margin-top: var(--bl-space-8); margin-bottom: var(--bl-space-5); color: var(--bl-color-text-muted);">Border Radius</h3>
      <div class="spacing-grid">
        <div class="spacing-demo">
          <div class="spacing-label">Small</div>
          <div class="spacing-box" style="width: 60px; height: 60px; border-radius: var(--bl-radius-sm);"></div>
          <div class="spacing-value">--bl-radius-sm (6px)</div>
        </div>
        <div class="spacing-demo">
          <div class="spacing-label">Medium</div>
          <div class="spacing-box" style="width: 60px; height: 60px; border-radius: var(--bl-radius-md);"></div>
          <div class="spacing-value">--bl-radius-md (12px)</div>
        </div>
        <div class="spacing-demo">
          <div class="spacing-label">Large</div>
          <div class="spacing-box" style="width: 60px; height: 60px; border-radius: var(--bl-radius-lg);"></div>
          <div class="spacing-value">--bl-radius-lg (20px)</div>
        </div>
      </div>
    </section>

    <!-- 4. BUTTONS -->
    <section>
      <h2 class="section-title">Buttons</h2>
      <div class="button-grid">
        <div class="button-example">
          <div class="button-label">Primary - Enabled</div>
          <button class="bl-btn-primary">Button</button>
        </div>
        <div class="button-example">
          <div class="button-label">Primary - Disabled</div>
          <button class="bl-btn-primary" disabled>Button</button>
        </div>
        <div class="button-example">
          <div class="button-label">Secondary - Enabled</div>
          <button class="bl-btn-secondary">Button</button>
        </div>
        <div class="button-example">
          <div class="button-label">Secondary - Disabled</div>
          <button class="bl-btn-secondary" disabled>Button</button>
        </div>
        <div class="button-example">
          <div class="button-label">Ghost - Enabled</div>
          <button class="bl-btn-ghost">Button</button>
        </div>
        <div class="button-example">
          <div class="button-label">Ghost - Disabled</div>
          <button class="bl-btn-ghost" disabled>Button</button>
        </div>
      </div>
    </section>

    <!-- 5. CARDS & CHIPS -->
    <section>
      <h2 class="section-title">Cards & Chips</h2>

      <h3 class="bl-heading-m" style="margin-top: var(--bl-space-6); margin-bottom: var(--bl-space-5); color: var(--bl-color-text-muted);">Card Variants</h3>
      <div class="example-section">
        <div class="bl-card">
          <div class="bl-heading-m" style="margin-bottom: var(--bl-space-3);">Standard Card</div>
          <p class="bl-body">This is a standard card with padding, border, and shadow. Used for content grouping and elevation.</p>
        </div>
      </div>
      <div class="example-section" style="padding: var(--bl-space-5);">
        <div class="bl-card bl-card-compact">
          <div class="bl-label">Compact Tile</div>
          <p class="bl-body">Smaller padding variant for denser layouts.</p>
        </div>
      </div>

      <h3 class="bl-heading-m" style="margin-top: var(--bl-space-6); margin-bottom: var(--bl-space-5); color: var(--bl-color-text-muted);">Chips</h3>
      <div class="chip-row">
        <span class="bl-chip">Ikon Pass</span>
        <span class="bl-chip">Epic Pass</span>
        <span class="bl-chip bl-chip-active">Active Chip</span>
        <span class="bl-chip bl-chip-muted">Muted Chip</span>
      </div>
    </section>

    <!-- 6. SEGMENTED CONTROL -->
    <section>
      <h2 class="section-title">Segmented Control</h2>
      <div class="example-section">
        <div class="example-label">Apple-style Tab Control</div>
        <div class="bl-segmented-control">
          <button class="bl-segment active">My Trips</button>
          <button class="bl-segment">Friends' Trips</button>
          <button class="bl-segment">All Trips</button>
        </div>
      </div>
    </section>

    <!-- 7. PROFILE SUMMARY -->
    <section>
      <h2 class="section-title">Profile Summary Card</h2>
      <div class="example-section">
        <div class="bl-profile-summary">
          <div class="profile-name">Me You</div>
          <div class="profile-meta">Epic — Intermediate — Skier</div>
          <div class="profile-mountains">0 mountains visited</div>
          <a href="#" class="profile-action">Edit Profile →</a>
        </div>
      </div>
    </section>

    <!-- 8. TRIP ROWS -->
    <section>
      <h2 class="section-title">Trip Rows</h2>
      <div class="example-section">
        <div class="example-label">Inline Trip Feed</div>
        <div class="bl-trip-row">
          <span class="trip-date">Jan 12</span> — <span class="trip-name">Parker Glide</span> <span class="trip-info">@ Snowbird, Ikon</span>
        </div>
        <div class="bl-trip-row">
          <span class="trip-date">Jan 18</span> — <span class="trip-name">Powder Peak</span> <span class="trip-info">@ Jackson Hole, Epic</span>
        </div>
        <div class="bl-trip-row">
          <span class="trip-date">Jan 25</span> — <span class="trip-name">Alpine Run</span> <span class="trip-info">@ Aspen, Epic</span>
        </div>
      </div>
    </section>

    <!-- 9. FRIENDS LIST -->
    <section>
      <h2 class="section-title">Friends List</h2>
      <div class="example-section">
        <div class="example-label">Friend Row Items</div>
        <div class="bl-friend-row">
          <div class="friend-name">Sarah Chen</div>
          <div class="friend-meta">Epic — Snowboarder</div>
        </div>
        <div class="bl-friend-row">
          <div class="friend-name">James Robinson</div>
          <div class="friend-meta">Ikon — Skier</div>
        </div>
        <div class="bl-friend-row">
          <div class="friend-name">Alex Torres</div>
          <div class="friend-meta">Epic — Intermediate</div>
        </div>
      </div>
    </section>

    <!-- 10. BOTTOM NAVIGATION -->
    <section>
      <h2 class="section-title">Bottom Navigation</h2>
      <div class="example-section" style="padding: 0; border-radius: 0;">
        <div class="bl-bottom-nav">
          <a href="#" class="bl-nav-item active">
            <div class="nav-icon">🏠</div>
            <span>Home</span>
          </a>
          <a href="#" class="bl-nav-item">
            <div class="nav-icon">⛷️</div>
            <span>Trips</span>
          </a>
          <a href="#" class="bl-nav-item">
            <div class="nav-icon">👥</div>
            <span>Friends</span>
          </a>
          <a href="#" class="bl-nav-item">
            <div class="nav-icon">👤</div>
            <span>Profile</span>
          </a>
        </div>
      </div>
    </section>

    <!-- 11. COMPLETE PAGE LAYOUT EXAMPLE -->
    <section>
      <h2 class="section-title">Complete Page Layout Example</h2>
      <div class="example-section" style="max-width: 480px;">
        <!-- Profile Summary -->
        <div class="bl-card" style="margin-bottom: var(--bl-space-6);">
          <div class="bl-profile-summary">
            <div class="profile-name">Me You</div>
            <div class="profile-meta">Epic — Intermediate — Skier</div>
            <div class="profile-mountains">0 mountains visited</div>
            <a href="#" class="profile-action">Edit Profile →</a>
          </div>
        </div>

        <!-- Stats -->
        <div class="bl-card" style="margin-bottom: var(--bl-space-6);">
          <div style="display: grid; grid-template-columns: 1fr 1fr; gap: var(--bl-space-6);">
            <div>
              <div class="bl-label">Pass Type</div>
              <div class="bl-heading-m" style="margin-top: var(--bl-space-3);">Epic</div>
            </div>
            <div>
              <div class="bl-label">Skill Level</div>
              <div class="bl-heading-m" style="margin-top: var(--bl-space-3);">Intermediate</div>
            </div>
          </div>
        </div>

        <!-- Segmented Control -->
        <div style="margin-bottom: var(--bl-space-6);">
          <div class="bl-segmented-control">
            <button class="bl-segment active">My Trips</button>
            <button class="bl-segment">Friends</button>
            <button class="bl-segment">All</button>
          </div>
        </div>

        <!-- Trip Feed -->
        <div class="bl-card">
          <div class="bl-trip-row">
            <span class="trip-date">Jan 12</span> — <span class="trip-name">Parker Glide</span> <span class="trip-info">@ Snowbird</span>
          </div>
          <div class="bl-trip-row">
            <span class="trip-date">Jan 18</span> — <span class="trip-name">Powder Peak</span> <span class="trip-info">@ Jackson Hole</span>
          </div>
        </div>

        <!-- Add Trip Button -->
        <button class="bl-btn-primary" style="width: 100%; margin-top: var(--bl-space-6);">+ Add a Trip</button>
      </div>
    </section>
  </main>
</body>
</html>
