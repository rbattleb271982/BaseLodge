Investigate BL-42 — Add friend availability overlap to mountain pages.
This is an investigation only. Do not make any code changes yet.
The goal is to determine the smallest way to surface useful friend availability overlap on mountain detail pages through the currently deployed web/backend architecture without requiring a new iOS or Android build.
Hard constraint
Do not propose or make changes to:
Native iOS project files
Native Android project files
Capacitor configuration
Native plugins
App permissions
Entitlements
Anything requiring a new Apple App Store or Google Play binary
If any portion would require a native rebuild, identify it clearly.
Desired behavior
On a mountain detail page, help the signed-in user understand whether any of their confirmed friends are available during relevant dates for that mountain.
The intent is to make mountain pages more actionable by connecting existing availability data with existing trip/mountain context.
Do not create a new availability system as part of BL-42.
Do not infer availability from:
lack of a trip
lack of an RSVP
wishlist state
Been Here
Going This Winter
Use only explicit availability data BaseLodge already stores.
What I want you to investigate
Determine:
Where mountain detail pages are implemented.
What social/trip data the mountain-detail route already loads.
Where user availability is currently stored.
What fields/models represent availability today.
Whether availability is:
date-specific
date-range based
season-specific
mountain-specific
global
How users currently create, edit, or remove availability.
Whether the mountain-detail route already receives any availability data.
Whether confirmed friends’ availability can be determined from existing loaded friend objects.
If not, identify the smallest additional query needed.
What BL-42 specifically requires based on the existing task/spec.
Whether BL-42 is intended to show overlap against:
the signed-in user’s own planned trip dates at that mountain
current-season dates generally
any explicit availability at that mountain
another existing definition
Whether the signed-in user must already have a trip at that mountain for overlap to be meaningful.
If the user has multiple trips at the same mountain, how overlap should be handled.
Whether participant-specific trip dates should affect the overlap calculation if they already exist.
How date overlap should be calculated:
inclusive start/end
partial overlap
full containment
Whether BaseLodge already has a reusable date-overlap helper.
How to handle:
no own trip at the mountain
one own trip
multiple own trips
no friends available
one friend available
multiple friends available
one friend overlapping multiple times
Whether a friend who is already Going This Winter should also appear in availability overlap.
Whether Interested / wishlist / Been Here state should affect the overlap display at all.
Whether availability itself has privacy settings.
Whether only confirmed direct friends may see each other’s availability.
Whether pending, removed, non-friend, or friend-of-friend users can enter the current availability data path.
Whether any schema migration is required.
Whether any new API endpoint is required.
Whether any additional server query is required.
Whether any aggregation/helper logic is required.
Whether any portion requires native iOS/Android work.
What automated tests should be added or updated.
Any mountain-page, availability, trip, BL-39, BL-40, or BL-41 behavior that could regress.
Important implementation preference
Favor the smallest possible implementation.
Reuse:
existing availability data
existing mountain-detail friend preload
existing trip dates
existing date-overlap helpers
existing social-summary styling
existing confirmed-friend/privacy rules
Do not:
redesign the mountain page
create a new availability workflow
create a new calendar system
infer availability from inactivity
add schema unless genuinely necessary
create a new API if existing route data can support the feature
expose non-confirmed connections
change native navigation or native UI
Return your findings in this format
Current availability implementation
Identify the relevant model(s), route(s), template(s), fields, and how availability is currently stored and edited.
Current mountain/trip context
Explain what dates and friend data the mountain-detail route already has available.
BL-42 behavior recommendation
Based on the existing task/spec and current architecture, define exactly what should count as a friend availability overlap.
Be explicit about:
own trip requirement
date-overlap rule
multiple trips
multiple availability ranges
duplicate friends
Display recommendation
Recommend the smallest UI treatment using the existing mountain-page social structure.
Include examples for:
0 friends
1 friend
multiple friends
Overlap/deduplication recommendation
Explain how a friend who also appears in Going This Winter, Interested, wishlist, or Been Here should be handled.
Privacy assessment
Explain which connection states may appear and how explicit availability privacy is preserved.
Requirement-by-requirement feasibility
For each BL-42 requirement state:
Web/backend only: Yes / No
Existing component/data to reuse
Required change
Risk/dependency
Data/API assessment
State whether BL-42 requires:
schema migration
new API endpoint
additional server query
aggregation/helper logic
additional page data
Explain any Yes answer.
Native-build assessment
State explicitly:
Can all of BL-42 ship without a new iOS or Android build? Yes / No
If no, identify exactly why.
Recommended implementation scope
List the minimum files/components/routes expected to change.
Testing
List tests required for:
no own trip
one own trip
multiple own trips
no/one/multiple overlapping friends
partial and full date overlap
duplicate overlap ranges
confirmed vs non-confirmed connections
privacy behavior
regression protection for BL-39, BL-40, and existing mountain social summaries
Risks / open questions
Only include genuine issues discovered in the codebase. Do not invent product questions if the existing task or architecture already answers them.
Do not implement anything until I review the investigation.