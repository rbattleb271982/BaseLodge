PUT THIS IN AGENT

Update the seed data so that test5@gmail.com is fully connected to all seed users and can see a rich, realistic environment from their account.

Do not remove existing seed users.
Do not remove existing trips.
Only adjust relationships and ownership where needed.

---

GOAL

Make test5@gmail.com the primary demo user with:
- full friend visibility
- incoming and outgoing requests
- shared trips
- overlap opportunities
- ideas surface populated

---

REQUIRED CHANGES

1. Identify test user

- Find or create user:
  test5@gmail.com

- This should be the central demo account

---

2. Connect ALL seed users to test5

For every existing seed user:

Create ONE of the following connection types relative to test5:

A. Confirmed friends (majority)
- Bidirectional accepted friendship

B. Incoming request (at least 1–2 users)
- Other user → test5 (pending)

C. Outgoing request (at least 1–2 users)
- test5 → other user (pending)

D. No connection (optional: 1 user)
- Leave one user unconnected for edge case testing

---

3. Ensure visibility of trips from test5

For all users connected to test5:

- If they have trips:
  - ensure test5 can see them where appropriate
- Ensure:
  - accepted trips appear in feeds
  - friend trips appear in Friends tab
  - overlaps can be computed

---

4. Add shared/overlapping scenarios

Create at least:

- 1 exact overlap:
  test5 + another user same resort + same dates

- 1 near overlap:
  test5 arrives within 1–2 days of another user

- 1 different resort, same dates:
  useful for Ideas tab logic

---

5. Ensure test5 has a strong profile

test5 should have:
- rider type(s)
- pass(es)
- skill level
- multiple upcoming trips across months

This ensures:
- identity line renders correctly
- Snapshot works
- Ideas tab has data to work with

---

6. Avoid duplicates / bad states

- Do NOT create duplicate friendships
- Do NOT create conflicting invitation states
- Ensure relationships are clean and consistent

---

7. Make it idempotent

- Running seed multiple times should not:
  - duplicate friendships
  - duplicate trips
  - break relationships

---

After updating, return:

1. List of users connected to test5 and how (confirmed / incoming / outgoing / none)
2. Number of overlapping trips created
3. Number of upcoming trips test5 now has
4. Confirmation that Friends, Trips, and Ideas tabs all show meaningful data
5. Any assumptions made