Implement Task #379 / BL-176 — Bound Home Ideas Retrieval.

The Plan is approved.

Do not create another planning task.
Do not remain in Draft/Plan mode.
Build the approved architecture now.

GOAL

Replace Home’s unbounded Python materialization of Ideas candidates with one exact bounded relational retrieval path.

Home must receive at most five final Ideas rows while preserving all currently defined:

- eligibility
- privacy
- suppression
- deduplication
- ranking
- dismissal
- card contract
- presentation behavior

Do not approximate the existing engine.

1. USE TASK #379 AS THE BUILD CONTRACT

Implement the approved Task #379 architecture.

The critical rule is:

Do NOT apply LIMIT 5, source limits, friend limits, finite overfetch, or any other cardinality bound until AFTER:

- all three candidate sources are constructed
- semantic source-level reduction is complete
- booked-window suppression is complete
- per-resort / mountainless deduplication is complete
- dismissal keys are constructed and excluded
- exact cross-source ranking is complete

Only then may the database apply the final five-row limit.

2. FREEZE EXISTING SEMANTICS FIRST

Before replacing Home’s path, ensure focused tests capture the existing Ideas engine as the semantic oracle.

Cover all three sources.

Friend trips:
- direct friends only
- public trips only
- current/future trips
- resort-backed trips only
- friend owner qualifies
- actively attending friend qualifies
- Interested qualifies
- Going qualifies
- pending excluded
- declined excluded
- removed excluded
- inactive legacy attendance excluded
- effective attendance dates override trip dates where currently supported
- past effective attendance windows excluded
- private trips excluded
- resortless trips excluded
- non-direct friends excluded

Availability:
- mutual explicit openness required
- future dates only
- normalized rows take precedence over legacy JSON
- legacy JSON fallback preserved exactly
- trip dates do not establish availability
- consecutive dates merge into windows
- friend duplication within dates/windows is removed
- mountainless windows remain valid
- shared wishlist may pin an overlap to a resort
- pass overlap is context only
- pass overlap must never restrict eligibility/ranking

Wishlist:
- direct friends required
- shared wishlist resort required
- no dates required
- no trips required
- missing/inactive resorts excluded
- friend groups preserved

3. BUILD DEDICATED BOUNDED RETRIEVAL

Prefer:

services/ideas_retrieval.py

Use one SQLAlchemy Core statement or one composed relational statement executed as one Ideas-specific SQL query.

The statement should derive:

A. friend-trip candidates
B. availability-overlap candidates
C. wishlist-overlap candidates

Then perform all exact semantic reduction before returning rows.

Do not introduce multiple source queries that are later merged in Python.

4. PRESERVE CANONICAL CROSS-SOURCE RANKING

Existing defined global order is:

1. friend count descending
2. viewer date overlap first
3. closest dated candidate first
4. signal priority:
   - friend trip
   - availability overlap
   - wishlist overlap

Preserve this exactly.

Per-resort winner preference remains:

1. larger friend group
2. earlier date
3. stronger signal type

Do not change these priorities.

For ties that were previously undefined, add deterministic stable-ID tie-breaks only.

Document those tie-breaks in code/tests.

Do not introduce a new product ranking rule.

5. PRESERVE SUPPRESSION

Maintain booked-window suppression exactly:

- same-resort dated Ideas near an already-booked viewer trip remain suppressed under the existing seven-day rule
- candidates with at least three friends retain the existing override
- undated wishlist candidates remain eligible

Do not broaden or narrow suppression.

6. PRESERVE DEDUPLICATION

Maintain:

- one winning Idea per resort across all sources
- mountainless availability dedupe by existing semantic concept/start-date behavior
- friend dedupe within availability dates/windows
- current friend grouping behavior
- existing representative social information used by the card contract

Do not add cross-section suppression between Ideas and Happening.

The current intentional absence of Ideas/Happening duplicate suppression must remain unchanged.

7. MOVE DISMISSAL INTO THE BOUNDED QUERY

Current Home dismissal must be incorporated before the final LIMIT.

Canonical dismissal keys remain:

Resort:
`idea_type:resort_id`

Mountainless:
`idea_type:sorted_friend_ids:start_date`

Requirements:

- dismissal is viewer-scoped
- friend IDs used in mountainless keys are deterministic
- dismissed winners are removed before final five-row cap
- later eligible Ideas must backfill
- Home must no longer need to materialize a complete feed just to remove dismissed cards

Do not change dismissal behavior or key semantics.

8. PRESERVE CARD CONTRACT

Return only the final bounded rows but shape them so existing Home Ideas formatting can remain equivalent.

Preserve all fields currently required for:

- card key
- idea type
- resort
- dates
- friend IDs
- friend count
- social/supporting text
- destinations
- dismissal
- existing Home downstream handoff

Where practical, continue using pure formatting helpers from `services/ideas_engine.py` rather than duplicating presentation semantics.

Do not change templates.

Do not change copy.

Do not change card destinations.

9. HOME ROUTE

Switch only Home’s Ideas retrieval path to the bounded service.

Remove the obsolete Home-side full-feed dismissal/filter/slice path once equivalent behavior is covered.

Do not change unrelated Home assembly.

Do not optimize:

- shared friend loading
- profile loading
- other Home sections

Those are outside BL-176.

10. HAPPENING IS OFF LIMITS

Do not modify:

- services/happening.py
- Happening eligibility
- Happening ranking
- Happening dismissal
- Happening five-card bound
- Happening presentation
- BL-177 behavior

BL-176 must remain architecturally independent.

11. CROSS-DIALECT SUPPORT

Preserve support for both database dialects currently used by the project.

Pay particular attention to:

- legacy JSON availability expansion
- wishlist JSON expansion if applicable
- malformed legacy values
- normalized-row precedence
- aggregation ordering
- deterministic friend ID lists

Use dialect-specific SQL construction only where necessary, while exposing the same service contract.

Do not add a dependency.

12. QUERY / MATERIALIZATION CONTRACT

The completed implementation must prove:

- exactly one Ideas-specific SQL statement per Home request
- at most five final candidate rows materialized in Python
- no complete source candidate collections materialized in Python
- no N+1
- no ORM lazy relationship loading
- no finite overfetch
- no source-level LIMIT
- no post-limit dismissal
- no cache/background precomputation

13. TESTS

Add focused tests for equivalence and bounds.

At minimum cover:

Friend trips:
- owner
- Interested
- Going
- excluded attendance states
- effective attendance dates
- past effective dates
- private
- non-direct
- resortless

Availability:
- normalized rows
- legacy fallback
- normalized precedence
- malformed legacy values
- mutual openness
- consecutive-date merge
- friend dedupe
- mountainless windows
- wishlist-pinned resort
- pass context without pass eligibility

Wishlist:
- shared resort
- no dates
- no trips
- inactive/missing resort
- multiple friends

Ranking:
- mixed-source candidates
- group-size priority
- dated vs undated behavior
- signal priority
- per-resort competition
- deterministic formerly-undefined ties

Suppression:
- booked-window suppression
- seven-day behavior
- three-friend override

Dismissal:
- resort dismissal key
- mountainless dismissal key
- leading candidate dismissed
- later candidate backfills
- five-card result still fills where enough eligible cards exist

Performance:
- large direct-friend population
- large trip population
- large availability population
- large wishlist population
- one Ideas query
- <=5 returned rows
- no N+1
- no lazy relationships

Regression:
- BL-177 Happening unchanged
- Home empty states unchanged
- Ideas presentation unchanged
- privacy/authorization unchanged
- existing dismissal behavior unchanged

14. EXPECTED FILE SCOPE

Likely:

- NEW: services/ideas_retrieval.py
- app.py
- services/ideas_engine.py only if small pure helper extraction/reuse is necessary
- focused new Ideas retrieval/equivalence tests
- existing Home tests where query/materialization assertions belong

Expected untouched:

- services/happening.py
- templates/*
- models.py unless an import/type-only adjustment is genuinely necessary
- migrations/*
- native configuration

If implementation requires schema/migration work, STOP.

15. STOP CONDITIONS

Stop and return BL-176 NEEDS PRODUCT/ARCHITECTURE DECISION if implementation reveals that:

- exact ranking cannot be represented relationally
- finite overfetch becomes necessary
- defined ranking semantics would have to change
- dismissal cannot occur before final limit
- privacy behavior would change
- schema/migration becomes necessary
- Happening must be modified or re-coupled
- cross-dialect equivalence cannot be preserved

Do not silently compromise the contract.

16. VALIDATION

Run the focused BL-176 suite plus relevant:

- existing Ideas tests
- Home empty-state tests
- Home dismissal tests
- BL-177 Happening retrieval tests
- Home presentation regressions
- privacy/authorization regressions
- git diff --check

Do not commit yet.

RETURN

BL-176 IMPLEMENTATION RESULT

- Files changed:
- New retrieval service:
- Ideas-specific SQL statements per Home request:
- Maximum final rows materialized:
- Friend-trip semantics preserved:
- Availability semantics preserved:
- Wishlist semantics preserved:
- Normalized availability precedence preserved:
- Legacy JSON fallback preserved:
- Booked-window suppression preserved:
- Three-friend override preserved:
- Per-resort deduplication preserved:
- Mountainless deduplication preserved:
- Cross-source ranking preserved:
- Stable tie-breaks added:
- Dismissal moved before final limit:
- Dismissal keys preserved:
- Dismissed-card backfill preserved:
- Card contract preserved:
- Ideas/Happening suppression behavior changed:
- services/happening.py changed:
- Home templates changed:
- New N+1:
- Lazy relationship loading:
- Source-level limits:
- Finite overfetch:
- New dependency:
- Privacy/authorization changed:
- Schema/migration changed:
- PostgreSQL validation:
- SQLite validation:
- Large-population bound test:
- Tests run:
- Tests passed:
- git diff --check:
- Scope deviations:
- Commit created: NO

End with exactly one:

BL-176 READY TO MERGE

or

BL-176 NEEDS PRODUCT/ARCHITECTURE DECISION