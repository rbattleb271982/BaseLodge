PUT THIS IN AGENT

Update the Trips screen UI to improve clarity, hierarchy, and overall polish. Do not redesign the entire page. Make targeted UI improvements only.

1. Remove the “YOUR TRIPS” eyebrow label completely.
   - Delete the label and the count from that row.
   - Do not replace it with another label.
   - Add slightly increased top spacing above the first trip card to maintain visual balance.

2. Keep tab structure but update labeling:
   - “My Trips” stays as is.
   - Change “Friend Trips” to “Friends’ Trips”.

3. Improve tab presentation:
   - Active tab should have a subtle underline or weight change.
   - Avoid heavy borders or clutter.
   - Maintain good spacing between tabs.

4. Upgrade the Snapshot CTA in the Trips header:
   - Replace the current plain text “Snapshot →” link with a compact pill-style button.
   - Use copy: “Season Snapshot”.
   - Do not include an arrow.
   - Position it in the same top-right header area.
   - Make it visually more prominent, but not heavier than the primary “Plan another trip” button.
   - Style guidance:
     - Soft cream or transparent background
     - Thin burgundy border: #6B1F2B
     - Burgundy text: #6B1F2B
     - Fully rounded pill shape
     - Comfortable horizontal padding, at least 12–16px
     - Slightly increased font size vs standard body text
   - Ensure the entire pill is tappable.
   - Keep it aligned with the Trips page title.
   - Do not move it into the tab bar.

5. Preserve the trip card as the primary focus:
   - Do not shrink or de-emphasize the trip card.
   - Ensure it sits clearly under the tabs with proper spacing.
   - The entire trip card should remain fully clickable/tappable.
   - Do not change backend trip logic.

6. Update empty state copy on the Friends’ Trips tab:
   - Replace existing copy with:
     Headline: “No trips yet”
     Subtext: “Plan a trip. Invite your friends.”
   - Improve typography hierarchy:
     - Headline should be larger and more prominent.
     - Subtext should be readable, not tiny helper text.
     - Increase line spacing slightly for a more premium feel.

7. Do not:
   - Add new components beyond the Snapshot pill styling.
   - Change backend logic.
   - Introduce new data.
   - Add additional labels or duplicate headers.
   - Create a checkpoint unless absolutely necessary.

8. QA requirements:
   - Verify spacing looks balanced on mobile.
   - Confirm tabs still switch correctly.
   - Confirm the trip card remains fully clickable.
   - Confirm the Season Snapshot pill is fully tappable.
   - Confirm no layout shift or broken alignment.
   - Confirm the page still works for empty and non-empty trip states.

Return a concise summary of changes and the files modified.