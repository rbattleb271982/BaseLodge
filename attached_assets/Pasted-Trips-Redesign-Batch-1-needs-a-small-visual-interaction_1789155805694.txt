Trips Redesign Batch 1 needs a small visual/interaction correction pass before approval.

Do NOT redesign the screen and do NOT start Both or Friends’.

Make only the corrections below.

1. REMOVE THE SEASON COUNTS ROW

The approved final header does not include:

“9 trips ahead · 2 already skied”
“5 trips ahead”
“No trips yet”

Remove that Mine summary/count copy.

The top structure should be:

Trips                         Season Snapshot ›

                              + Plan a trip

Mine 9 [invite count]   Both   Friends’

Keep this compact and consistent with the approved synthesis.

Do not replace the removed counts with new copy.

--------------------------------------------------

2. REMOVE LINK STYLING FROM INVITATION ROWS

Pending invitation rows currently render mountain names, dates, and inviter copy as visibly underlined browser-style links.

Examples include:

Copper, CO
14–15
Friend04 invited you

and equivalent invitation rows.

They must visually use the same ledger typography as ordinary trip rows.

Do not show default hyperlink underlines or browser-link styling.

Preserve any required navigation/accessibility semantics without changing the approved visual treatment.

--------------------------------------------------

3. TIGHTEN INVITATION ROW HEIGHT

The approved invitation treatment is I1: compact buttons on line two.

Current invitation rows are too tall and read as approximately three lines.

Return them toward the approved two-row ledger footprint:

Mountain                         Date
Inviter/context        Decline   Accept

Do not create an action strip below the row.

Keep accessible touch targets, but achieve those without unnecessarily increasing the visual row height.

Preserve the subtle invitation/blush treatment.

--------------------------------------------------

4. SIMPLIFY EMPTY STATE

The current empty state is too card-like and promotional.

Remove:

- GET STARTED eyebrow
- bordered/blush promotional card
- large serif “Plan your first trip”
- large full-width CTA
- unnecessary explanatory marketing treatment

Use a restrained BaseLodge ledger-style empty state.

The existing top-level `+ Plan a trip` action remains available.

A small amount of quiet explanatory copy is acceptable, for example:

No trips yet

Plan a trip to start building your season.

Do not add another dominant CTA.

The empty state should feel like an empty Trips ledger, not an onboarding card.

--------------------------------------------------

5. VERIFY ACCEPT BEHAVIOR

Do not change the approved RSVP rule.

Tapping Accept on an invitation must NOT immediately mean Going.

It must expose the choice:

Going
Interested

and then submit the selected canonical RSVP through the existing response path.

Verify this behavior in the running app.

If it is already correct, make no behavioral change.

If it is not correct, fix only this interaction according to the Batch 1 specification.

Decline remains directly actionable.

--------------------------------------------------

6. DO NOT CHANGE

Do not change:

- month grouping
- row ordering
- relationship semantics
- +N others semantics
- next-trip emphasis
- Earlier this season
- bottom navigation
- RSVP backend semantics beyond the Accept UI requirement
- fixture meaning
- Both
- Friends’
- Home
- Trip Detail

No migration.
No deployment.
No publishing.
No Git push.

--------------------------------------------------

7. VERIFY AND RECAPTURE

Run the relevant targeted tests after the corrections.

Then regenerate:

1. Mine normal — 390px
2. Mine heavy — 390px
3. Mine with 3 invitations — 390px
4. Mine heavy — 360px
5. Mine Earlier this season visible
6. Mine empty/sparse

Also provide one capture showing the Going / Interested choice after tapping Accept.

Report exact tests/results and display the new captures for review.

Stop there.