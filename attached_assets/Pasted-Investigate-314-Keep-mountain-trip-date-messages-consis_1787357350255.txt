Investigate #314 — Keep mountain trip-date messages consistent and tested.
This is an investigation only. Do not change code or tests yet.
Goal
Resolve the three failing assertions in tests/test_mountain_detail.py that currently block the #313 regression gate.
The current template renders same-month trip ranges compactly, for example:
Sep 1–3
while the failing tests expect:
Sep 1–Sep 3
Determine which behavior is actually canonical and whether the implementation or the tests are stale.
Investigate
Identify the exact three failing tests and assertions.
Identify the function/template/helper that formats these mountain availability trip ranges.
Trace when the compact same-month formatting was introduced.
Review the BL-42 implementation/spec, tests, commit history, and nearby date-format conventions.
Determine whether BL-42 intentionally established:
same month → Sep 1–3
cross month → Aug 31–Sep 2
Check whether any currently passing tests already protect that compact convention.
Determine whether changing the template to Sep 1–Sep 3 would regress any approved behavior or UI.
Determine whether the three failing tests simply need to be updated to match the existing approved formatting.
Verify the failures are unrelated to the #313 importer changes.
Hard constraints
Do not touch production.
Do not restart Flask.
Do not modify #313 importer code.
Do not import reference data into the real development DB.
Do not resume BL-77.
Do not change push/OneSignal behavior.
Return
Failing tests
List the three failures and their expected vs actual values.
Canonical formatting
State exactly what same-month and cross-month date ranges are supposed to look like and why.
Root cause
State whether:
tests are stale, or
implementation is wrong.
Smallest fix
Identify the minimum file(s) that should change.
#313 impact
Confirm whether resolving #314 should unblock the full regression gate for #313.
End with exactly one:
SAFE TO UPDATE TESTS ONLY
SAFE TO UPDATE IMPLEMENTATION ONLY
DO NOT CHANGE YET: [reason]
Do not implement or commit anything during this investigation.