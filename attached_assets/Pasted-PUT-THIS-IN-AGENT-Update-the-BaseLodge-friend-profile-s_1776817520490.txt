PUT THIS IN AGENT

Update the BaseLodge friend profile screen so it matches the newer Home/Profile editorial system and closely follows the attached mockup direction. This is a visual and structural refactor first, not a data model rewrite.

Goal:
Make the friend profile feel like the sibling of the user’s own profile screen. Same design language, same hierarchy, same premium editorial feel. No profile images. No generic social-app styling. Keep it text-led, calm, and mobile-first.

What to change:

1. Top friend summary should mirror the user profile summary style

* Keep the back link at the top.
* Refactor the top section so it feels like a contained profile summary module, similar to the updated Home/Profile direction.
* Use this structure:
    * friend name
    * rider type / level / pass line
    * a clear primary contextual action such as “Invite to trip →” when relevant
    * a larger, more visually separated stats band beneath
* The stats band should display:
    * Trips
    * Mountains or Mountain
    * Wishlist
* Increase visual hierarchy so the name, descriptor line, CTA, and stats feel intentional and connected.
* Use soft containment, subtle rules, and spacing. Do not add heavy modern card styling.

2. No profile photo or avatar support

* Do not add profile images, initials circles, or avatar placeholders.
* This app is text-first and editorial. Preserve that.

3. Add a shared-context module below the top summary

* Introduce a section/module that gives relational context between the current user and this friend.
* This should feel like a product insight block, not a notification card.
* Example content patterns:
    * You both have Vail on your wishlists
    * You’ve both been to Mammoth Mountain
* Title can be something like “Shared with Jake” or equivalent based on the friend’s name.
* Style it as a soft, elegant module consistent with the rest of the page.
* Reuse real existing overlap data if available. If some shared-context data is not currently available in the template context, add only the minimum supporting logic needed.

4. Upgrade the upcoming trips section

* Keep the friend’s upcoming trips section, but restyle it to match the newer Home module system.
* It should feel closer to the upgraded “My Next Trip” treatment and less like a plain list row.
* Recommended structure:
    * section label like “JAKE’S UPCOMING TRIPS”
    * a more intentional trip module with:
        * destination
        * dates
        * status such as Planning
        * right-side arrow/action affordance
        * subtle left accent rule if that fits the current Home system
* Preserve existing navigation behavior when tapping through.

5. Simplify and elevate the profile details section

* Keep profile details lightweight and secondary.
* Good candidates to keep:
    * Pass
    * Board
    * Boots
* Remove redundant summary rows if they duplicate information already shown above, for example “Upcoming trips 1.”
* Restyle this section so it feels like supporting information, not the main attraction.
* Use editorial spacing and simple dividers, not a dense settings-page feel.

6. Refine the remove friend action

* Keep Remove friend functionality.
* Visually demote it so it feels secondary and cleaner, while still accessible.
* It should fit the new design system and not feel visually harsh or like a destructive admin button.

7. Visual design rules

* Match the existing BaseLodge editorial system:
    * light cream background
    * serif-forward hierarchy where already used
    * restrained burgundy accent usage
    * soft borders or subtle rules
    * mobile-first spacing
    * premium, calm presentation
* Avoid introducing a different design language.
* Avoid chunky cards, dark blocks, oversized pills, or social-media-style patterns.

8. Functional constraints

* Do not break current friend profile routing or existing actions.
* Preserve:
    * back navigation
    * friend relationship state
    * invite to trip action if already wired
    * upcoming trip links
    * remove friend action
* Reuse existing template/view logic as much as possible.
* Add only minimal new supporting logic if needed for shared-context content.

9. Files to update

* Update the relevant friend profile template(s), CSS, and any minimal route/context code required so this screen matches the mockup direction closely.
* Keep implementation clean and production-appropriate.
* Only use checkpoints if I explicitly ask for them.

Do not finish until you have fully tested the updated friend profile screen in the browser and confirmed:

* the top section now mirrors the upgraded profile-summary system
* there are no profile images or avatar elements
* shared-context content appears in a cohesive module
* upcoming trips feels upgraded and visually aligned with Home
* profile details are simplified and secondary
* the screen looks correct on mobile widths.