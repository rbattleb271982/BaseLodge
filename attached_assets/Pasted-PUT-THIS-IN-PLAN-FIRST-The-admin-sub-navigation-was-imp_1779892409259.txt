PUT THIS IN PLAN FIRST

The admin sub-navigation was implemented in the wrong place.

I do NOT want section pills at the top of the page content.

Correct direction:
Subsections should live inside the admin menu/sidebar as nested submenu items under each main section. On mobile, those submenu items should appear inside the hamburger menu when the section is active/open.

Example:
User Insights
  - Overview
  - Rider Mix
  - Pass Ownership
  - Equipment

Dashboard
  - Overview
  - Trips
  - Mountains
  - Engagement
  - Platform
  - Passes

Mountains
  - Overview
  - Destination Intelligence
  - Resort Operations
  - Data Quality

The page should not show horizontal pill nav at the top.

Also, the Mountains tab currently still looks like an older standalone admin page. It does not match the current BaseLodge admin console visual system.

We need a plan to:
1. Remove the top in-page pill subnav from Dashboard and User Insights.
2. Move section navigation into the admin sidebar/menu as nested submenu links.
3. On mobile, expose these submenu links inside the hamburger menu.
4. Convert the Mountains page from standalone styling into the shared admin_console.html layout/system.
5. Give Mountains its own internal structure and submenu:
   - Overview
   - Destination Intelligence
   - Resort Operations
   - Data Quality
6. Preserve all existing Mountains editing functionality:
   - add resort
   - edit resort
   - activate/deactivate
   - pass brand editing
   - merge duplicates
   - delete
   - Excel import/export
   - bulk actions
   - canonical export
7. Do not break mobile resort card editing.
8. Do not change user-facing app screens.
9. No migrations unless absolutely required.

Please inspect:
- admin_console.html
- admin_dashboard.html
- admin_user_insights.html
- admin_resorts.html
- admin sidebar/mobile menu implementation
- current admin routes

Planning deliverables:
1. Explain how the current sidebar and mobile hamburger menu are structured.
2. Propose how nested submenu items should be rendered for each active admin section.
3. Explain how anchor links should work from the sidebar submenu.
4. Explain how to remove the top content pills safely.
5. Propose how to migrate admin_resorts.html into the shared admin_console.html layout without breaking its existing JS/AJAX editing behavior.
6. Propose the Mountains page structure:
   - Overview
   - Destination Intelligence
   - Resort Operations
   - Data Quality
7. Identify exact files that need changes.
8. Identify risks, especially around the standalone Mountains JS and mobile cards.
9. Do not implement until I approve the plan.

After the plan, stop and wait for approval.