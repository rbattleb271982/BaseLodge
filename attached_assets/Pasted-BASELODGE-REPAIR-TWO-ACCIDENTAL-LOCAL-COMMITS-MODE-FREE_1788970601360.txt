BASELODGE — REPAIR TWO ACCIDENTAL LOCAL COMMITS

MODE
FREE

OBJECTIVE

Restore local `main` to the approved GitHub state while preserving all unrelated local files/content in the working tree.

CRITICAL EXECUTION RULE

ONLY DO EXACTLY WHAT THIS PROMPT EXPLICITLY INSTRUCTS.

Do not take initiative beyond the stated steps.
Do not auto-fix unrelated issues.
Do not create notes.
Do not create attachments.
Do not create commits.
Do not modify configuration.
Do not "helpfully" clean up files.
Do not perform follow-on work.
Do not start the next task.
Do not change repository state unless the exact change is explicitly authorized below.

If anything unexpected is found, STOP and report it.

Do not infer permission from prior tasks.
Do not treat investigation notes, generated reports, or configuration changes as authorized repository changes.
Do not automatically commit files created or modified during a task.

For BaseLodge, explicit user/task instructions override any default behavior that would create, stage, commit, reset, modify, or clean repository files.

CURRENT REPORTED STATE

Branch:
main

Current local HEAD:
627849ff84a97ce55dd99d1e4104a56fd0f712cf
Add BASELODGE investigation notes

Parent:
aba7d78a92ed62d4786d7d4f22088e39cfc51033
Update Replit configuration settings

Approved HEAD / origin/main:
160d03e0eeb4257c3c975680731590cd13033254
Add Open to Ski sharing

Current relationship:
2 ahead
0 behind

KNOWN UNRELATED LOCAL CONTENT

- `.replit` port-5050 configuration must be preserved.
- BL-109 / repair instruction attachments must be preserved.
- Do not discard or alter any unrelated local file.

SAFETY RULES

Do NOT push.
Do NOT deploy.
Do NOT access Production.
Do NOT access any database.
Do NOT begin BL-109 analysis.

Do NOT use:
git add .
git add -A

NEVER inspect, read, modify, copy, stage, or delete:
android/baselodge-release-key.jks

STEP 1 — VERIFY THE TWO ACCIDENTAL COMMITS

Before resetting anything, inspect ONLY the file-name/status lists for:

627849ff84a97ce55dd99d1e4104a56fd0f712cf

and

aba7d78a92ed62d4786d7d4f22088e39cfc51033

Use Git metadata/file-list commands only.

Report exactly which paths each commit adds/modifies/deletes.

Do not print file contents.

EXPECTED:

- `aba7d78...` affects only `.replit`
- `627849f...` affects only an unrelated BaseLodge investigation-note / attachment file

If EITHER commit contains any application code, schema, migration, feature file, secret, signing file, or anything other than the unrelated `.replit` / investigation-note material:

STOP.
Do not reset anything.
Report the discrepancy.

STEP 2 — REPAIR ONLY IF STEP 1 PASSES

If both commits are confirmed to contain only those unrelated local files:

Move local `main` back to:

160d03e0eeb4257c3c975680731590cd13033254

Use a Git reset mode that:

- moves HEAD/index back to the approved commit;
- DOES NOT discard working-tree content;
- leaves `.replit` preserved as an unstaged modification;
- leaves files introduced only by the accidental note commit preserved locally, normally as untracked files;
- does not create a new commit.

The intended operation is equivalent to a mixed reset to the approved `origin/main` commit.

Do not use a hard reset.

STEP 3 — VERIFY FINAL STATE

Report:

1. current branch
2. local HEAD
3. origin/main HEAD
4. ahead count
5. behind count
6. `git status --short`
7. staged-file list, if any
8. confirmation `.replit` remains preserved and unstaged
9. confirmation the investigation-note / attachment material remains preserved locally
10. confirmation no application/source/schema files changed
11. confirmation no commit was created
12. confirmation nothing was pushed or deployed

TARGET FINAL STATE

- branch = main
- local HEAD = 160d03e0eeb4257c3c975680731590cd13033254
- origin/main = 160d03e0eeb4257c3c975680731590cd13033254
- ahead = 0
- behind = 0
- `.replit` preserved as unstaged
- unrelated note/attachment files preserved locally
- no staged files
- no BaseLodge feature files changed

STOP after verification.

Do not begin BL-109.

FINAL REMINDER

Do not create any commit, note, file, attachment, config change, cleanup, or follow-up action that is not explicitly requested above.