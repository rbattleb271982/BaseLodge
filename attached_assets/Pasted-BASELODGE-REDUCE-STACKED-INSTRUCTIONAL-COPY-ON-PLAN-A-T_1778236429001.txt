BASELODGE — REDUCE STACKED INSTRUCTIONAL COPY ON PLAN A TRIP

This is a copy/hierarchy cleanup, not a redesign.

The Plan a trip page currently has too much stacked instructional text. The flow now works, so we should remove redundant labels and helper copy.

Do NOT change the core layout, calendar logic, staged-date behavior, validation, save behavior, bottom nav, or styling system.

━━━━━━━━━━━━━━━━━━━━
GOAL
━━━━━━━━━━━━━━━━━━━━

Make the Plan a trip page feel calmer, more native, and less like a web form.

Reduce repeated instructional copy while preserving clarity.

━━━━━━━━━━━━━━━━━━━━
1. SIMPLIFY PAGE HERO
━━━━━━━━━━━━━━━━━━━━

Keep:

Plan a trip

Replace the current subhead:
“Tell us where and when. We'll handle the rest.”

With:

“Pick your mountain and dates.”

━━━━━━━━━━━━━━━━━━━━
2. SIMPLIFY WHERE SECTION
━━━━━━━━━━━━━━━━━━━━

Remove redundant stacked copy:

Remove:
- “WHERE”
- “Where are you going?”
- “Search any mountain.”

Keep the existing mountain search/selected mountain UI exactly as-is.

Do NOT change resort search behavior.

━━━━━━━━━━━━━━━━━━━━
3. SIMPLIFY WHEN SECTION
━━━━━━━━━━━━━━━━━━━━

Remove redundant stacked copy:

Remove:
- “WHEN”
- “When?”
- “Tap your start day, then your end day.”

Keep:
- calendar
- selected range confirmation block
- staged dates list
- inline errors
- dynamic helper text after a range is staged

The helper text:
“Tap a start date to add another range.”

should only appear after at least one range has been staged.

Do NOT show this helper text before the user has selected the first range.

━━━━━━━━━━━━━━━━━━━━
4. SIMPLIFY STATUS SECTION
━━━━━━━━━━━━━━━━━━━━

Remove:
- “STATUS”
- “Trip status”

Keep only the segmented control:

Planning / Going

Do not change the underlying status behavior.

━━━━━━━━━━━━━━━━━━━━
5. SIMPLIFY SHARING SECTION
━━━━━━━━━━━━━━━━━━━━

Remove:
- “SHARING”
- “Sharing settings”
- “Only your friends can see your trip details.”

Keep only:

“Friends can see this trip”

with the existing checkbox behavior.

Do not change privacy/sharing logic.

━━━━━━━━━━━━━━━━━━━━
6. PRESERVE FUNCTIONALITY
━━━━━━━━━━━━━━━━━━━━

Do NOT break:
- resort selection/search
- date range selection
- Add this range confirmation
- single-day trip behavior
- staged date list
- remove staged range
- duplicate validation
- overlap validation
- CTA copy: “Save trip”, “Save 2 dates”, etc.
- Planning / Going state
- Friends can see this trip checkbox
- group/proposal flow

━━━━━━━━━━━━━━━━━━━━
7. VISUAL RULES
━━━━━━━━━━━━━━━━━━━━

Do NOT:
- redesign the page
- add new cards
- change colors
- change typography system
- change global spacing
- change bottom nav
- introduce Tailwind
- do dead CSS cleanup yet

This should be a surgical copy/hierarchy cleanup.

Use checkpoints sparingly.

Do not finish until you have tested:
- empty/new Plan a trip state
- selected mountain state
- first date range selection
- staged range helper behavior
- multiple ranges
- Planning / Going toggle
- Friends can see this trip checkbox
- successful save