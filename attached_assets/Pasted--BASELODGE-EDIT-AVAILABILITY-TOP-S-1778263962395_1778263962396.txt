╔════════════════════════════════════════════════════════════╗
║ BASELODGE — EDIT AVAILABILITY TOP SECTION + MODAL POLISH ║
╚════════════════════════════════════════════════════════════╝

GOAL

Make the top of the Edit Availability screen clearer, lighter, and less visually stacked.

The current version feels too boxed-in and instructional.
We want it to feel more editorial and conversational while still clearly explaining:
- how to select dates
- that multiple ranges are supported
- why entering availability improves the app experience

This should match the premium BaseLodge aesthetic:
minimal, warm, clean, Apple-like.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TOP SECTION CHANGES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

REMOVE:
- the bordered container around the explainer text
- the tinted background behind the explainer text
- the icon/checkmark entirely

The explanatory content should sit directly on the page background with no card/container.

Keep:
- "Edit availability" title
- back button
- "How it works" link in the top-right

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
REPLACE THE COPY WITH:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Choose the days you're free to ski

Select any start date and end date.
You can choose multiple ranges.

Adding your availability helps us suggest trips and mountains that fit your schedule and makes it easier to plan with friends.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TYPOGRAPHY / SPACING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Requirements:
- Reduce stacked feeling
- Improve vertical rhythm
- Use spacing more intentionally
- Heading should feel editorial, not like a system alert
- Body text should be lighter and easier to scan
- Keep overall layout mobile-first
- Preserve existing color system and typography system

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
HOW IT WORKS MODAL
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Implement a lightweight modal/sheet opened from the "How it works" link.

Style:
- premium iOS-style sheet/modal
- rounded corners
- soft shadows
- scrollable if needed
- dismiss X in top-left
- no heavy borders

Top intro copy:

Your availability helps BaseLodge make better suggestions and connect you with friends.

Then add these sections:

1.
TITLE:
Share when you're free

BODY:
Select any start date and end date. You can add multiple ranges throughout the season.

2.
TITLE:
Get better suggestions

BODY:
We'll use your availability to suggest trips, mountains, and plans that fit your schedule.

3.
TITLE:
Coordinate with friends

BODY:
See when your friends are available so it's easier to plan trips together.

4.
TITLE:
Stay in the loop

BODY:
We'll notify you about trip opportunities that match the dates you're free.

Bottom privacy note in a softer/light rounded box:

Only you can see your availability

Your availability is private and only used to improve your experience.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
IMPLEMENTATION NOTES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- Use existing icon set if helpful inside modal only
- NO icon in the top explainer section
- DO NOT redesign the calendar itself
- DO NOT alter save button behavior
- DO NOT change availability logic
- Focus only on:
  - hierarchy
  - spacing
  - copy
  - modal experience

Need:
- updated HTML
- updated CSS
- any lightweight JS needed for modal open/close
- fully responsive/mobile-safe implementation

Use checkpoints sparingly.