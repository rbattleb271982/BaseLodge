You are working inside the existing BaseLodge Flask + SQLAlchemy app.
🎯 GOAL (ONE CHECKPOINT ONLY)
Implement social functionality for GroupTrips, including:
GroupTrip invitations
Accepting / leaving trips
Host vs guest permissions
Automatic global friend connections
Contextual “Connect” button logic from shared trips
⚠️ Do NOT implement UI styling polish
⚠️ Do NOT touch Equipment, Accommodations, or Transportation UI yet
⚠️ Do NOT add notifications or feed events
🔒 HARD CONSTRAINTS
You MUST:
Use the existing Friend model for global friendships
Use the existing User model
Use the new GroupTrip and GroupTripGuest models from Step 1
Preserve all existing data
You MUST NOT:
Modify or rename existing models (SkiTrip, Friend, User)
Add new tables
Add feed events
Add seed data
Add admin features
🧩 FEATURES TO IMPLEMENT (COMBINED)
A️⃣ GroupTrip Invitations
Backend behavior
Implement routes/services to support:
Invite user to GroupTrip
Only GroupTrip.host_id can invite
Creates GroupTripGuest(status="invited")
Prevent duplicate invites
Accept invite
Changes status to accepted
Automatically creates a global friend connection using the existing Friend model
No approval flow
No feed event
Leave trip
Guest can leave silently
Deletes their GroupTripGuest row
Does NOT affect friendships
Remove guest
Host can remove a guest
No feed event
B️⃣ Permissions Enforcement (Backend)
Enforce the following rules server-side:
Only host can:
Edit GroupTrip fields
Invite or remove guests
Guests:
Can view GroupTrip details
Cannot edit anything
If a guest attempts a restricted action:
Return a safe 403 or redirect
C️⃣ Shared-Trip “Connect” Logic
Eligibility rule
A user can connect globally with another user if and only if:
Both users have GroupTripGuest.status = "accepted"
They share at least one upcoming GroupTrip
They are not already friends
Implementation
Add a helper function that returns True/False for this eligibility
Expose this boolean to the profile view context
Action
Clicking Connect:
Creates a global friend connection via existing Friend model
No feed event
No approval flow
Safe against duplicates
D️⃣ Minimal UI Wiring (NO DESIGN WORK)
You may:
Add basic buttons/links:
“Invite to Trip”
“Accept Invite”
“Leave Trip”
“Connect”
You must:
Reuse existing templates where possible
Avoid CSS or layout refactors
Keep everything functional but minimal
🧪 REQUIRED CHECKS BEFORE STOPPING
Before finishing, you must verify:
Host can invite users
Invitee can accept
Accepting creates a Friend relationship
Guests can leave silently
Host can remove guests
Shared-trip users see “Connect” button
Clicking “Connect” creates friendship
No feed events are created
App boots with no errors
Existing SkiTrip and Friend functionality still works
🛑 STOP CONDITION
STOP immediately after:
GroupTrips are socially functional
All logic above works
No UI polish added
No extra features started
