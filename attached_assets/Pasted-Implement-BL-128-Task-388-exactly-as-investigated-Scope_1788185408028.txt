Implement BL-128 / Task #388 exactly as investigated.

Scope this change only to `POST /api/trip/create`.

Requirements:

1. Keep the existing `request.get_json()` parsing behavior unchanged.
2. Immediately after JSON parsing, verify that the parsed top-level value is a dictionary/object.
3. If the parsed JSON is not an object, return HTTP 400 using the endpoint’s existing error envelope:

`{"success": false, "error": "Invalid request body."}`

4. The guard must reject:

   * arrays;
   * strings;
   * integers;
   * floats;
   * booleans;
   * JSON `null`.

5. Perform this validation before:

   * reading any request fields;
   * friend authorization queries;
   * resort queries;
   * overlap checks;
   * trip or participant creation;
   * lifecycle updates;
   * commits;
   * notifications;
   * messaging events;
   * analytics events;
   * any other side effect.

6. Preserve the existing behavior for malformed JSON. Do not convert malformed JSON into the new endpoint-specific response.

7. Preserve current empty/missing-body behavior according to Flask’s existing parser behavior.

8. Preserve all current behavior for valid JSON object requests, including:

   * required field validation;
   * optional fields;
   * solo trip creation;
   * friend invite authorization;
   * success response shape;
   * authentication;
   * CSRF;
   * persistence;
   * notifications;
   * analytics and lifecycle behavior.

9. Do not introduce a generalized JSON validation helper for this task. Use the smallest local guard consistent with existing route conventions.

10. Do not modify unrelated JSON endpoints.

11. Do not change required trip fields, date validation, invite rules, trip semantics, database schema, dependencies, data, or native applications.

12. The adjacent malformed-date issue identified during investigation is explicitly out of scope for BL-128.

Add focused route-level regression coverage for:

* array JSON;
* string JSON;
* integer JSON;
* float JSON;
* boolean JSON;
* JSON `null`;
* malformed JSON;
* empty JSON body;
* missing/non-JSON body where applicable;
* valid JSON object.

For invalid non-object bodies, verify:

* HTTP 400;
* JSON response with `success: false`;
* correct error shape;
* no Trip persisted;
* no participant persisted;
* no lifecycle mutation;
* no notification;
* no messaging/event emission;
* no analytics event;
* no other observable side effect.

Do not use a shared test helper where it would turn `None` into `{}` and hide the JSON-null case. Use direct client requests where needed.

Run:

* focused BL-128 tests;
* adjacent trip-create regression tests;
* broader relevant suite;
* full suite if practical;
* compilation/syntax verification;
* diff integrity checks.

Do not make unrelated cleanup changes.

When complete, report:

* exactly what behavior changed;
* tests added and results;
* confirmation that malformed JSON behavior remains unchanged;
* confirmation that valid object requests remain compatible;
* whether any files outside BL-128 scope changed;
* confirmation that no migration, dependency change, data backfill, schema change, or native build is required.

Final commit message must include BL-128.
