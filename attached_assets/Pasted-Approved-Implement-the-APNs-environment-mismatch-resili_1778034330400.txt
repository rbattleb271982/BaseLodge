Approved. Implement the APNs environment mismatch resilience plan exactly as described.

Current issue:
The /admin/test-push endpoint is selecting the newest active iOS token and sending to production APNs because APNS_USE_SANDBOX=false. The selected token row says apns_environment=production, but Apple returns:

BadEnvironmentKeyInToken
status_code: 403
server host: api.push.apple.com

This means Apple considers the token to belong to the opposite APNs environment. Apple’s response is the source of truth.

Implementation requirements:

1. Refactor send_apns_push so it can retry the opposite APNs environment once when Apple returns BadEnvironmentKeyInToken.

Add an optional keyword argument:

prefer_sandbox: bool | None = None

Behavior:
- prefer_sandbox=None: keep current behavior and derive from APNS_USE_SANDBOX
- prefer_sandbox=True: try sandbox first
- prefer_sandbox=False: try production first

2. Retry logic:
- Send first attempt to the selected host
- If first attempt succeeds, return success immediately
- If first attempt returns BadEnvironmentKeyInToken:
  - Do NOT deactivate the token yet
  - Retry once against the opposite APNs host
  - If retry succeeds:
    - Keep token active
    - Update the matching PushDeviceToken row’s apns_environment to the environment that actually worked
    - Return success with retry metadata
  - If retry fails:
    - Only then mark the token inactive
    - Return failure with both attempt results

3. Do not permanently mark a token inactive on the first BadEnvironmentKeyInToken response.
Only mark inactive if BOTH APNs environments fail.

4. Add structured return data from send_apns_push, including:
- success
- first_attempt_environment
- first_attempt_host
- first_attempt_status_code
- first_attempt_error
- retry_attempted
- retry_environment
- retry_host
- retry_status_code
- retry_error
- retry_success
- env_corrected
- corrected_token_environment
- final_success

5. Update /admin/test-push so the response clearly shows:
- selected token id
- selected token preview
- stored apns_environment before send
- first APNs host used
- first APNs result
- retry APNs host used, if applicable
- retry APNs result, if applicable
- final success/failure
- whether the token environment was corrected

6. Update /admin/test-push to accept an optional user_id query parameter:

/admin/test-push?user_id=6

Keep it admin-only. If no user_id is provided, preserve the existing default behavior.

7. Update push_register_token so it does not overwrite a token environment that was corrected by a successful APNs retry with a weaker client-side hint.

Specifically:
- Do not let Capacitor.DEBUG=false or APNS_USE_SANDBOX=false automatically relabel a token back to production if APNs previously proved it works against sandbox
- Keep the existing upsert behavior
- Do not create duplicate rows for the same user_id/token pair

8. Apply this fix to the shared APNs send logic, not only the debug route, so future real push notifications benefit from the same fallback behavior.

9. Add logging for:
- BadEnvironmentKeyInToken detected
- retrying opposite APNs environment
- retry succeeded
- token environment corrected
- retry failed and token deactivated

10. Remove any temporary APNs DEBUG print statements that are now replaced by structured logging.

Do not:
- Change Replit secrets
- Change APNS_USE_SANDBOX
- Make UI changes
- Touch Xcode/native iOS files
- Create duplicate token rows
- Use checkpoints unless I explicitly ask

Testing before marking complete:
1. Run the app and confirm no syntax/import errors
2. Hit /admin/test-push with the current TestFlight token
3. Confirm that if BadEnvironmentKeyInToken happens:
   - first attempt fails
   - opposite environment retry happens
   - response clearly shows both attempts
4. If retry succeeds:
   - confirm the push notification arrives
   - confirm token stays active
   - confirm apns_environment is corrected to the environment that worked
5. Hit /admin/test-push a second time:
   - confirm it uses the corrected environment first
   - confirm no unnecessary retry happens
6. Hit /admin/test-push?user_id=6:
   - confirm it targets user_id=6
7. Confirm no duplicate token rows were created
8. Confirm logs clearly show the fallback/correction behavior

Do not finish until you have tested the endpoint behavior and confirmed there are no syntax/import errors.