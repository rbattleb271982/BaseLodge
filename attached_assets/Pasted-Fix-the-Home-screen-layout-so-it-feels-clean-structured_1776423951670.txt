Fix the Home screen layout so it feels clean, structured, and premium. Right now it still looks messy.

Main problems to solve:
1. Too much vertical stacking
2. The top card/banner is too tall
3. Card hierarchy is unclear
4. Section spacing is inconsistent
5. Some cards are rendering 4 rows high when they should be 2 rows max

Do NOT redesign the page.
Do NOT change the color system again.
Do NOT add new features.
This is strictly a layout and hierarchy cleanup pass.

GOALS
- Make the Home screen feel calm and editorial
- Reduce height and vertical sprawl
- Enforce 2-row max for compact cards where appropriate
- Tighten spacing between sections
- Make the page feel intentionally composed, not stacked

TASKS

1. Fix the trip invite banner/card at the top
- The invite card is too tall
- Rebuild it into a compact 2-row layout max

Target layout:
Row 1:
- Left: "Quinn invited you to Cerro Catedral"
- Right: Accept button, Decline button

Row 2:
- Left: "Apr 23–26"
- Below or inline secondary text: "+1 more invite"
- Do not let this become a 4-line block unless the mountain name is extremely long
- If title is too long, truncate intelligently after 2 lines max
- Buttons should stay right-aligned and vertically centered with the content block
- Reduce vertical padding inside this card

2. Normalize the card stack
The order should be:
- Invite banner/card
- Welcome/profile summary block
- Stats row
- User trip card
- Best Match card
- Friend activity row/card

Do not make the user trip card feel like a hero.
It should feel like the first card in the stack.

3. Tighten vertical spacing across the full page
Reduce whitespace between:
- Banner and Welcome
- Welcome and profile nudge
- Profile nudge and stats
- Stats and first card
- Section labels and cards
- Card and next section label

The page should scroll less and feel denser without feeling cramped.

4. Standardize section headers
Current headers like:
- NEXT UP
- BEST MATCH
- FRIEND ACTIVITY

Need to be visually consistent:
- same font size
- same tracking
- same spacing above and below
- same left alignment

5. Fix the trip card layout
The Vail card should be compact and clean:
Row 1:
- "IN 3 DAYS" on left
- "Edit →" on right
Row 2:
- "Vail, CO"
Row 3:
- "Apr 20–24 · No Pass"

Do not let padding make this card oversized.
Tighten it.

6. Fix the Best Match card
This card is too tall.
Reduce internal padding and tighten layout.

Target structure:
- Small eyebrow / supporting line
- Large date line
- Small supporting line
- CTA button

Do not leave large empty space below the button.
The card should hug its content more closely.

7. Fix the Friend Activity row
This should feel lighter and more list-like, not like a giant block.
Keep:
- NEW CONNECTION
- Dale wants to connect
- Connect to plan trips together
- Accept button on right

But tighten vertical spacing so it reads like a compact activity row.

8. Global rule: compact cards should be 2 rows max when possible
Audit all compact cards/banners on Home.
If any card is rendering as 4 stacked rows because of padding, line breaks, or layout structure, refactor it.
Compact cards should use horizontal space better before growing vertically.

9. Mobile-first checks
Validate on narrow mobile width.
Make sure:
- buttons do not wrap awkwardly
- long resort names do not break the layout
- compact cards stay compact
- text truncates gracefully where needed

10. Output
After making changes, provide:
- a short list of layout issues fixed
- confirmation that compact cards now render at 2 rows max where appropriate
- confirmation that the Home page is visually tighter and cleaner

IMPORTANT
- Only use checkpoints if I explicitly say so
- Do not overengineer this
- Keep changes minimal but high impact