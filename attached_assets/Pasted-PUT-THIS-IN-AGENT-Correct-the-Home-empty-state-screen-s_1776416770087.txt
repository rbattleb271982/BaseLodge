PUT THIS IN AGENT

Correct the Home empty-state screen so it matches the intended direction.

I’m attaching:

1. the current incorrect version
2. the visual reference
3. the homepage, where the BaseLodge tagline already exists

This is not a new redesign.
This is a correction to match the intended editorial direction.

Only update the empty Home state (no trips). Do not change populated Home.

---

1. Re-center the entire empty-state content block

The empty-state content below the stats row must be:

* centered overall
* visually composed as one editorial column
* not left-aligned like a dashboard

Apply:

* centered container
* readable max width
* centered paragraph
* centered closing line

Important:
The numbered list can remain internally structured, but the overall composition must feel centered and intentional.

---

2. Increase font sizes and improve typography

The current fonts still feel too small and too plain.

Adjust typography so the screen feels premium and editorial on mobile:

* Name should stay the largest element
* Supporting identity line should be slightly larger and cleaner
* Intro paragraph should be noticeably larger than it is now
* Numbered list text should be larger and easier to read
* Spacing between all elements should be opened up

Use the existing brand typography system where possible.
Aim for a more premium, designed look — not small UI utility text.

---

3. Add the BaseLodge tagline

Pull the exact existing BaseLodge tagline from the homepage and place it into this empty Home experience.

Requirements:

* Use the exact tagline already used on the homepage
* Do not invent a new one
* Place it naturally within the editorial block
* It should support the mood of the page, not feel like a marketing banner

---

4. Remove the bottom CTA

Remove:

* "Add your first friend →"

Do NOT replace it with another link.

Replace the closing moment with:

* "Explore the menu below"

This should be plain text, centered, calm, and understated.

No button.
No link.
No CTA treatment.

---

5. Preserve the intended tone

This screen should feel:

* centered
* editorial
* premium
* calm
* welcoming

It should NOT feel:

* like onboarding SaaS
* like a task list
* like a dashboard
* like a funnel to “add friend”

---

6. Keep the current structural wins

Keep:

* "Richard."
* shortened stat labels
* unified stats band
* numbered list format

But refine the presentation so it feels more elegant and aligned with the original vision.

---

7. Scope

Only change the empty Home state.
Do not alter:

* populated Home
* route logic
* other tabs
* account/profile logic

---

Goal:

The Home empty state should feel like a premium, centered editorial welcome to the product.

It should use:

* stronger typography
* the homepage tagline
* centered composition
* calm closing text: "Explore the menu below"

And it should stop trying to push "Add your first friend" as the action.
