You are working inside the existing BaseLodge Flask + SQLAlchemy application.
🎯 GOAL
Seed a fully realistic demo dataset for BaseLodge that supports end-to-end testing and demos.
The dataset must include:
Two known users (Richard + Jonathan)
50 additional realistic users
Fully completed profiles for all users
Equipment for all users
4 SkiTrips per user
Friend connections linking every dummy user to both Richard and Jonathan
GroupTrips that Richard and Jonathan are attending
Open Dates populated for users so overlap logic can be tested
This is data seeding only.
Do NOT modify UI, auth logic, or schema.
🔒 NON-NEGOTIABLE RULES
Use existing models: User, Friend, SkiTrip, GroupTrip, TripGuest, EquipmentSetup, OpenDate (or equivalent)
Hash passwords using the app’s existing utilities
Do NOT overwrite existing records
Do NOT duplicate data
Script must be idempotent
Respect all foreign keys and constraints
Use bidirectional Friend rows
👤 FIXED USERS (CREATE IF NOT EXISTS)
Richard
First name: Richard
Last name: Battle-Baxter
Email: richardbattlebaxter@gmail.com
Password: 12345678
Jonathan
First name: Jonathan
Last name: Schmitz
Email: jonathanmschmitz@gmail.com
Password: 12345678
Reuse existing records if they already exist.
👥 ADDITIONAL USERS (50 TOTAL)
Create 50 additional users, each with all profile fields filled:
Profile fields
first_name (realistic)
last_name (realistic)
email (unique)
password = 12345678
rider_type (Skier or Snowboarder)
skill_level (Beginner, Intermediate, Advanced, Expert)
pass_type (1–3 passes, comma-separated)
home_state (valid US state)
home_mountain (random existing Resort.name)
profile_setup_complete = True
🎿 EQUIPMENT (EVERY USER)
Create EquipmentSetup records:
Primary (required)
discipline derived from rider_type
brand (realistic, alphabetical list)
length_cm (reasonable range)
width_mm (reasonable range)
slot = PRIMARY
Secondary (optional)
Create for ~50% of users
slot = SECONDARY
Do NOT overwrite existing equipment.
🧳 SKI TRIPS (EVERY USER)
Each user must have exactly 4 SkiTrips.
Requirements
resort_id (random existing Resort)
future start/end dates
non-overlapping per user
pass_type matches one of user’s passes
is_public = True
Skip trip creation if user already has ≥4.
🤝 FRIEND CONNECTIONS
For each of the 50 dummy users, create bidirectional Friend connections with:
Richard
Jonathan
Do NOT connect dummy users to each other.
🧑‍🤝‍🧑 GROUP TRIPS (REQUIRED)
Create 3–5 GroupTrips with realistic future dates.
GroupTrip requirements
host_id alternates between Richard and Jonathan
title (e.g., “March Tahoe Trip”, “Utah Powder Week”)
start_date / end_date (future)
optional resort_id
Attendance
For each GroupTrip:
Richard → ACCEPTED
Jonathan → ACCEPTED
Randomly select 5–10 dummy users → ACCEPTED
Use TripGuest (or equivalent) with correct status.
Do NOT duplicate GroupTrips or guests if script is re-run.
📆 OPEN DATES (REQUIRED)
Create Open Dates so overlap logic can be tested.
Rules
Richard and Jonathan:
Each get 6–10 open date ranges
Dummy users:
Each get 3–6 open date ranges
Dates should:
Be future-dated
Overlap across users intentionally
Vary in length (1–4 days)
Use existing Open Dates model / storage format.
Do NOT overwrite existing open dates.
🧩 IMPLEMENTATION STRUCTURE
Create a single Flask CLI command, e.g.:
flask seed-full-demo-world
Execution order:
Create fixed users
Create dummy users
Fill profiles
Create equipment
Create SkiTrips
Create Friend connections
Create GroupTrips + guests
Create Open Dates
Commit + verify
✅ VERIFICATION OUTPUT (MANDATORY)
After execution, print:
Total users
Total dummy users
Total SkiTrips
Total GroupTrips
Total TripGuests
Total EquipmentSetup records
Total Open Dates
Sample output (5 users):
email
passes
home_mountain
trip_count
equipment_present
open_dates_count
group_trips_count
connected_to_richard
connected_to_jonathan
🚫 EXPLICITLY OUT OF SCOPE
Do NOT:
Modify onboarding or profile UI
Touch auth logic
Delete existing data
Refactor models
Add tests
Add feeds or notifications
Create admin tooling
📋 FINAL RESPONSE BACK
Return:
Confirmation script ran successfully
Summary counts
Any skipped records (with reason)
