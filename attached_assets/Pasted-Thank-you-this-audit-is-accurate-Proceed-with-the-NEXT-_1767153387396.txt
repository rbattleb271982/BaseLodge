Thank you — this audit is accurate.

Proceed with the NEXT STEP ONLY.
Do not introduce new features.
Do not redesign the UI.

Goal:
Stabilize filtering + restore ALL bulk actions so the admin panel is reliable again.

================================
STEP 1 — RESTORE BULK ACTIONS
================================

Frontend:
• Restore missing buttons in the Bulk Actions panel:
  - Activate Selected
  - Deactivate Selected
  - Delete Selected

• Ensure these buttons:
  - Operate ONLY on selectedResorts (JS Set)
  - Do NOT rely on filters or DOM visibility
  - Are enabled only when selectedResorts.size > 0

Backend:
• Reuse existing endpoints for:
  - Activate / Deactivate
  - Delete
• Do NOT create new routes
• Use ORM updates only

================================
STEP 2 — FIX BULK + FILTER INTERACTION
================================

Rules:
• Selection is the source of truth for bulk actions
• Filters control visibility only
• If a bulk action changes a row so it no longer matches filters:
  - That row should disappear from the table
• Filters/search must NOT reset after bulk actions

Fix:
• After a bulk action completes:
  - Update each affected row’s data-* attributes
  - Re-run filterResorts()

================================
STEP 3 — EXPORT FILTER SYNC (CRITICAL)
================================

Problem:
Excel export does NOT always match the visible table.

Fix:
• When “Export Filtered to Excel” is clicked:
  - Read current filter state from JS:
    • search text
    • country
    • status
    • state/region
    • pass brand
  - Construct export URL with query params dynamically
  - Navigate to that URL

Backend:
• Trust query params ONLY
• Do not re-implement JS filtering logic in Python

================================
STEP 4 — SELECT ALL CONSISTENCY
================================

Fix Select All behavior:
• “Select All” selects only currently visible rows
• Clearing filters does NOT auto-select newly visible rows
• selectedResorts is never implicitly modified by filtering

================================
STEP 5 — VERIFICATION
================================

Verify the following flows:

1. Filter → Select rows → Bulk Deactivate → Rows disappear
2. Switch Status = Inactive → Rows reappear
3. Bulk Activate → Rows disappear again
4. Export Filtered to Excel → Matches visible rows exactly
5. Inline edits still preserve filters
6. No JS console errors

Stop after completing this step.
List files changed.
