Implement BL-82 — Add “Joined BaseLodge” month and year to Profile using the completed investigation and Task #302.
This is an implementation task.
Goal
Show a quiet, non-interactive join-date line on the signed-in user’s /profile:
Joined BaseLodge · Month YYYY
Example:
Joined BaseLodge · March 2025
Hard constraint
This must ship through the existing Flask/webview architecture only.
Do not modify:
Native iOS project files
Native Android project files
Capacitor configuration
Native plugins
App permissions
Entitlements
Anything requiring a new Apple App Store or Google Play binary
Canonical date source
Use the existing:
User.created_at
This is the canonical account creation timestamp already used by BaseLodge.
Do not derive or infer the join date from:
onboarding completion
first login
first trip
friendships
equipment
other activity
If created_at is null, omit the line entirely.
Do not backfill or invent a value.
Display behavior
Render the line only on the signed-in user’s own /profile.
Friend profiles must remain unchanged.
Place it inside the existing Profile identity card:
after the rider type / skill level / pass details
before gear information
The line should be:
visually quiet
non-interactive
subordinate to the primary identity content
consistent with existing Profile metadata styling
Do not create a new card or section.
Date formatting
Format using full month and year only:
created_at.strftime('%B %Y')
Example:
March 2025
Never render:
day
date number
time
timezone
Keep the existing server-side month/year convention.
Scope
The investigation confirmed BL-82 requires:
No schema migration
No new database field
No API endpoint
No additional server query
No additional page data
No friend-profile changes
No native work
current_user.created_at is already available to templates/profile.html.
Keep the implementation as small as possible.
Expected implementation surface
Focus changes around:
templates/profile.html
static/styles.css only if a small metadata style adjustment is genuinely needed
tests/test_profile_consolidation.py
Do not change app.py unless the current implementation proves it is genuinely necessary.
Do not modify templates/friend_profile.html except, if needed, for a regression test fixture or non-user-visible reason. Friend-profile behavior must remain unchanged.
Legacy/null behavior
Some legacy accounts may have a null created_at.
For those accounts:
do not infer a join date
do not display placeholder text
do not display “Unknown”
simply omit the join-date line
Testing
Add or update focused tests covering:
signed-in Profile renders Joined BaseLodge · March 2025 for a fixed March 2025 timestamp
full month name is correct
year is correct
exact day is not exposed
time is not exposed
null created_at omits the entire line
normal /profile behavior remains intact
friend-profile output remains unchanged
Use explicit fixed timestamps in tests rather than relying on the model default.
Run:
focused Profile tests
full test suite
Regression protections
Do not change:
Profile routing
friend-profile routing
User.created_at semantics
signup behavior
gear behavior
rider-type behavior
Profile stats
account-history behavior
native iOS/Android/Capacitor code
Completion report
After implementation, return:
Files changed
List every changed file.
Behavior changed
Confirm the own-Profile join month/year behavior and null behavior.
Tests
List focused and full-suite results.
Native-build verification
State explicitly:
Native iOS/Android files changed: No
New app-store build required: No
Commit
Provide the Git commit hash for the completed BL-82 implementation.