Implement the finalized Season Snapshot share card design.

Frontend only unless existing template data needs minor formatting.
Do not change backend logic or routes.

Files:
- Season Snapshot template
- Relevant CSS only

Goal:
Create an Instagram-shareable 1:1 Season Snapshot card.

Design requirements:

1. The share card must be square / Instagram 1:1 friendly.
   - Use a fixed square share-card container or aspect-ratio: 1 / 1
   - It should be screenshot-ready on mobile
   - No long vertical card

2. Remove the page title “Season Snapshot.”
   - Do not show that title anywhere on this page.

3. Page headline above the card:
   “Your winter, made to be shared.”

4. Card title:
   “{First name}’s Winter”
   - Must always stay on one line
   - Use nowrap
   - Use responsive font sizing if needed
   - “Winter” should keep the burgundy italic serif styling

5. Identity block:
   Split into two lines:

   Line 1:
   “{rider_type} · {skill_level}”

   Line 2:
   “{pass_type}”

   Rules:
   - Line 1 should stay on one line
   - Line 2 should be smaller/lighter
   - Pass should NOT be crammed into Line 1

6. Keep:
   “— on BaseLodge —”

7. Remove from the card:
   - Exact dates
   - Countries
   - “20 mountains · 10 months” summary
   - Any footer stats

8. Trip display:
   Group trips by month and year.

   Format:
   DEC 2026
   vertical burgundy bar
   Aspen Snowmass
   Vail

9. Use a 2-column month grid for now.
   - Months should flow naturally left to right, top to bottom
   - Keep spacing generous but compact enough for 1:1

10. Styling:
   - Cream background
   - Light cream card
   - Burgundy accents
   - Serif card title
   - Sans metadata
   - Rounded card corners
   - Soft shadow
   - Premium/editorial feel

11. Preserve existing data and logic:
   - Use the user’s real first name
   - Use actual rider type, skill level, and pass type fields
   - Use actual trips
   - Group by trip month/year
   - Only show mountain/resort names, not countries or dates

12. Do not add:
   - Buttons
   - Interactions
   - Download/export logic
   - Carousel/slides
   - AI references

Goal:
The final card should look like a polished Instagram carousel image, not an app data table.

Only use checkpoints if I explicitly say so.