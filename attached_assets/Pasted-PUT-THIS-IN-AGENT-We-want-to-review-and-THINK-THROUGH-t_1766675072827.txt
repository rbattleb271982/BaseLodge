PUT THIS IN AGENT

We want to review and THINK THROUGH the invite-link behavior.
This is a DESIGN + DIAGNOSTIC task, not a bug fix yet.

BACKGROUND
- Invite links are permanent URLs (/r/<token>).
- A user may click the same invite link:
  - before signup
  - after signup
  - after already being logged in
- Current behavior appears consistent but may not be intentional.

GOAL
Map the CURRENT behavior and present RECOMMENDED options so we can choose the desired UX before changing code.

========================
TASK 1 — Document current behavior
========================

For the invite route (/r/<token>), document what happens today in each case:

1. Logged-out user who has never signed up
2. Logged-in user who has already signed up via the invite
3. Logged-in user who did NOT sign up via the invite
4. Logged-out user who already has an account (but is not logged in)

For each case, specify:
- Which page is rendered
- Whether invite/session logic runs
- Whether any state is set or modified
- Where the user ends up

========================
TASK 2 — Identify UX options
========================

Based on common patterns, outline 2–3 reasonable approaches for invite links AFTER signup, for example:

Option A:
- Invite links only matter for logged-out users
- Logged-in users are redirected to home/dashboard

Option B:
- Invite links always show landing page
- Logged-in users can log out or switch accounts

Option C:
- Invite links detect “already connected” state and redirect accordingly

Describe pros/cons of each in the context of this app.

========================
TASK 3 — Recommendation
========================

Recommend ONE option that best fits:
- A social app with friend connections
- Durable invite relationships
- Minimal confusion for non-technical users

DO NOT implement yet.
This task is analysis and recommendation only.

========================
OUTPUT REQUIRED
========================

Return:
- A short behavioral matrix (scenario → result)
- A clear recommended option
- Confirmation whether the current behavior is intentional or accidental
