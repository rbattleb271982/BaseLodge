I want to refactor the current profile page into a Home screen with a minimal inline header and move the detailed profile card into a separate Edit Profile screen.

Please follow these steps carefully and only change what’s specified.

========================================
PART 1 — Create an Edit Profile screen
========================================

1) Open templates/profile.html (or whichever template is currently used for the “Me You” page with the big Name / Rider Type / Pass Type / Friends card and the two buttons: "View Friends" and "Invite a Friend").

2) Locate the top “profile card” markup. It will look something like:

<div class="auth-card profile-card">
    <h1 class="auth-title">...</h1>
    <div class="profile-info">...</div>
    <div class="profile-actions">...</div>
</div>

Copy this **entire** auth-card block (including the h1, profile-info, and profile-actions) and use it to create a new template:

- New file: templates/edit_profile.html

For now:
- Keep the same outer layout container as profile.html (e.g., the surrounding .profile-container / .profile-page structure) so the page matches the current look.
- You don’t have to wire up actual editing behavior beyond what already exists in profile.html; just reuse the same content.

3) In app.py, create a new route called edit_profile that:

- Requires the user to be logged in (same session/user checks as the current profile route).
- Loads the current user object.
- Loads any context data that the current profile route already uses (e.g., friends count, trips if needed).
- Renders "edit_profile.html" with the same context variables that profile.html received before (e.g., user, friends_count, etc.).

Example skeleton (adapt to match existing style):

@app.route("/edit_profile")
def edit_profile():
    if "user_id" not in session:
        return redirect(url_for("auth"))
    user = User.query.get(session["user_id"])
    if not user:
        session.pop("user_id", None)
        return redirect(url_for("auth"))
    # Load any other context you already use on the profile page (friends count, etc.)
    return render_template("edit_profile.html", user=user, friends_count=friends_count, state_abbr=STATE_ABBR, my_trips=my_trips, friends_trips=friends_trips, friend_map=friend_map)

Use the actual variable names and context that your existing profile route uses; do not invent new ones if they already exist.

==================================================
PART 2 — Simplify the top of the profile.html page
==================================================

Now we treat profile.html as the **Home screen**.

1) In templates/profile.html, inside the main container (e.g., .profile-container), locate the existing auth-card/profile-card at the top (the one you just copied to edit_profile.html). Replace that entire block with a new inline header.

New header structure should be:

<div class="profile-header">
    <h1 class="auth-title">{{ user.first_name }} {{ user.last_name }}</h1>

    <p class="profile-inline-meta">
        {{ user.rider_type or 'Rider type not set' }} •
        {{ user.pass_type or 'No pass selected' }} •
        {{ friends_count }} friend{% if friends_count != 1 %}s{% endif %}
    </p>

    <div class="profile-inline-links">
        <a href="{{ url_for('friends') }}" class="link-button">View Friends</a>
        <button type="button" class="link-button" onclick="copyInviteLink()">Invite a Friend</button>
        <a href="{{ url_for('edit_profile') }}" class="link-button">Edit Profile</a>
    </div>
</div>

Notes:
- Use whatever variable you currently use to display the friends count (e.g., friends_count, total_friends); adjust the Jinja expression accordingly.
- Leave the “Upcoming Trips” card (with the My Trips / Friends’ Trips toggle) exactly where it is, just below this new header.

2) At the bottom of profile.html, before </body>, add a small script to support the Invite link.

If you already have a copyInviteLink() implementation on the Friends page, reuse the logic here. For example:

<script>
function copyInviteLink() {
    const currentUserId = {{ user.id }};
    const inviteUrl = `${window.location.origin}/auth?ref=${currentUserId}`;
    navigator.clipboard.writeText(inviteUrl).then(() => {
        alert('Invite link copied to clipboard!');
    }).catch(() => {
        alert('Failed to copy invite link');
    });
}
</script>

Only add this if it doesn’t already exist in profile.html.

====================================================
PART 3 — Add CSS for the new inline header (styles.css)
====================================================

In static/styles.css, add the following near the bottom (before the media query or after, as long as it doesn’t conflict):

/* --- Home / Profile Inline Header --- */
.profile-header {
  margin-bottom: 16px;
}

.profile-inline-meta {
  font-size: 0.9rem;
  color: var(--text-muted);
  margin-top: 8px;
}

.profile-inline-links {
  margin-top: 10px;
  display: flex;
  flex-wrap: wrap;
  gap: 12px;
}

.profile-inline-links .link-button {
  font-size: 0.85rem;
  text-decoration: underline;
  padding: 0;
}

@media (min-width: 480px) {
  .profile-inline-meta {
    font-size: 0.95rem;
  }
}

This should keep the top section lightweight and inline, with:
- Name in the existing big header style (auth-title),
- A subtle one-line meta row (rider type • pass • friend count),
- Three inline text links: View Friends, Invite a Friend, Edit Profile.

====================================================
PART 4 — Keep Upcoming Trips + Toggles As-Is
====================================================

Do NOT remove or restructure the "Upcoming Trips" card or the My Trips / Friends’ Trips toggle that was previously implemented. That card should remain below the new inline header and continue working with:

- My Trips → full trip-card layout
- Friends’ Trips → inline-trip layout

====================================================
GENERAL
====================================================

- Do not use checkpoints unless absolutely required.
- Do not alter unrelated routes, templates, or CSS.
- Make sure /edit_profile works and uses the old detailed card layout from the original profile.html.
- Make sure /profile still loads as before but now with the inline header instead of the big card.
