Update the post-create trip detail screen.

Goal:
Make the trip detail page feel simpler, more social, and more aligned with BaseLodge. Remove duplicate “Planning” treatment and replace the top flash banner with a dismissible success modal.

Changes needed:

1. Success confirmation
- Remove the current top-page flash/banner placement for “Trip added.”
- On successful trip creation, show a centered dismissible modal/lightbox instead.
- Modal copy:
  Title: “Trip planned.”
  Body: “Your Vail weekend is on the calendar.”
- Include an X close button.
- User should be able to dismiss it.
- Keep it subtle, minimal, and BaseLodge-styled.
- Do not add confetti or illustrations.

2. Hero/header section
- Keep the main mountain name large.
- Make the main mountain name a link to that mountain’s detail page.
  Example: tapping “Vail” opens the Vail mountain page.
- Do not use hero images or destination photography.
- Remove the duplicate “Planning” pill/eyebrow at the top of the hero.
- Keep Planning only once, in the metadata row near visibility/status.

3. Social proof near the top
- Add a short social proof pill/line under the date if data exists:
  “On 3 friends’ wishlists”
- Only show it when the count is greater than 0.
- Keep it short and subtle.

4. Trip setup section
- Keep the setup rows:
  Pass
  Equipment
  Transportation
  Accommodations
  Lessons
- Keep existing values and edit behavior.
- Clean up the section so it feels like trip setup, not a settings dump.
- Keep copy:
  “Let everyone know the plan.”

5. What’s next section
- Remove the extra action cards:
  Invite friends
  Share trip
- Keep only one action row/card:
  Title: “See who’s going”
  Subtitle: “See friends heading to Vail.”
- This should route to the relevant friends/trips/mountain social view using existing functionality if available.
- If there is no exact route yet, wire it to the best existing related page and do not invent a new backend system.

6. Preserve
- Existing trip detail logic
- Edit link behavior
- Back navigation
- Bottom nav
- Existing row edit/dropdown behavior
- Existing trip creation success behavior, except changing the visual confirmation from banner to modal

QA:
- After creating a trip, user lands on the trip detail page.
- Success modal appears and can be dismissed with X.
- No top success banner remains.
- “Planning” appears only once.
- Mountain name links to the mountain detail page.
- Social proof appears only when count > 0.
- Bottom section shows only “See who’s going” with subtitle “See friends heading to Vail.”
- No photos/images added.