PUT THIS IN AGENT

Objective
Fix the Home Mountain dropdown on the Profile screen so it populates correctly and is filtered by Home State.

This is a data wiring + UI logic fix.
Do NOT change database schemas unless strictly required.

⸻

Scope
Apply changes to:
	•	Profile edit screen (“About You” section)
	•	Home State dropdown
	•	Home Mountain dropdown

⸻

Required behavior (LOCKED)
	1.	Home State dropdown

	•	Populate with all valid states
	•	Selection must be saved correctly on profile save

	2.	Home Mountain dropdown

	•	Must be populated from the canonical Resort/Mountain table
	•	Do NOT use hardcoded lists
	•	Dropdown must be disabled until a Home State is selected

⸻

	3.	State → Mountain filtering (critical)

	•	When a user selects a Home State:
	•	Populate Home Mountain dropdown with only mountains in that state
	•	When no state is selected:
	•	Home Mountain dropdown shows placeholder text:
“Select your home state first”
	•	Dropdown is disabled

⸻

	4.	State change handling

	•	If a user changes Home State after selecting a Home Mountain:
	•	Clear the previously selected Home Mountain
	•	Repopulate the dropdown with mountains for the new state

⸻

	5.	Empty-state handling

	•	If a selected state has no mountains:
	•	Show placeholder:
“No mountains available in this state”
	•	Keep dropdown disabled

⸻

	6.	Data integrity

	•	Home Mountain must be stored as a foreign key / ID reference to the Resort table
	•	Do NOT store mountain names as strings
	•	Ensure existing users with invalid or missing Home Mountain data do not error

⸻

	7.	Accessibility & UX

	•	Disabled dropdown must be visually clear
	•	Enabled dropdown must be clearly tappable
	•	Do NOT allow users to open an empty dropdown

⸻

Acceptance Criteria
	•	Home Mountain dropdown is initially disabled
	•	Selecting a Home State enables the Home Mountain dropdown
	•	Only mountains from the selected state appear
	•	Changing state clears the selected mountain
	•	Profile can be saved successfully
	•	No console or server errors

⸻

Testing Required (DO NOT SKIP)
	•	Test with a state that has many mountains (e.g., Colorado)
	•	Test with a state that has zero mountains
	•	Test changing state after selecting a mountain
	•	Test saving and reloading profile
	•	Test existing users with no Home Mountain set

Do not finish until all tests pass.
