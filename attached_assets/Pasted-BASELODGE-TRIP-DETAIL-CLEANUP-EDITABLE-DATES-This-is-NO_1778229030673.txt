BASELODGE — TRIP DETAIL CLEANUP + EDITABLE DATES

This is NOT a redesign.

Make these targeted changes only.

━━━━━━━━━━━━━━━━━━━━
1. REMOVE BAD SUBHEAD COPY
━━━━━━━━━━━━━━━━━━━━

Remove this subtitle under “Trip setup”:
“Let everyone know the plan.”

Remove this subtitle under “What’s next?”:
“Keep the stoke going.”

Do not replace them with anything.

The section titles should stand alone:
- Trip setup
- What’s next?

━━━━━━━━━━━━━━━━━━━━
2. ADD WISHLIST SOCIAL PROOF
━━━━━━━━━━━━━━━━━━━━

Inside the “See who’s going” card:

If wishlist_count > 0:
show a small secondary metadata line:

“3 on wishlist”
“1 on wishlist”

Placement:
- beneath “See friends heading to [Mountain].”
- smaller/subdued than headline
- lightweight/editorial
- maroon accent is acceptable

If wishlist_count == 0:
render nothing.

Do NOT:
- add pills
- add badges
- add heavy containers
- add new sections

Keep it minimal.

━━━━━━━━━━━━━━━━━━━━
3. MAKE DATES EDITABLE
━━━━━━━━━━━━━━━━━━━━

The trip date row beneath the mountain name:

Example:
Feb 14–20, 2027 · 6 nights

should become tappable for trip owners.

Owner behavior:
- date row is tappable
- opens a native-feeling bottom-sheet modal
- NOT a full page redirect

Modal contents:
- Start date picker
- End date picker
- Save button
- Cancel/close

Requirements:
- pre-populate current trip dates
- preserve existing validation
- prevent invalid ranges
- preserve existing trip ownership permissions
- preserve existing grouped/batch trip behavior

On save:
- update trip dates
- recalculate night count
- refresh/re-render cleanly
- avoid full hard reload if possible
- preserve scroll position if possible
- avoid jumpy flash behavior

Non-owner behavior:
- date row remains static text

━━━━━━━━━━━━━━━━━━━━
4. MOUNTAIN NAME SHOULD FEEL TAPPABLE
━━━━━━━━━━━━━━━━━━━━

The mountain name itself should visually read as tappable too.

Example:
Courchevel

IMPORTANT:
If the mountain name already links to a resort/mountain detail page:
- preserve that behavior
- do not break existing navigation

The mountain title and the dates should feel like connected interactive metadata.

Visual treatment should feel:
- subtle
- elegant
- editorial
- iOS-native

Do NOT:
- use bright blue links
- use ugly web underlines
- make it feel like a desktop website

Optional:
- subtle opacity/color treatment
- tiny chevron/edit affordance

Think:
Apple Settings row behavior
NOT:
traditional hyperlink styling

━━━━━━━━━━━━━━━━━━━━
5. MOBILE-FIRST QA
━━━━━━━━━━━━━━━━━━━━

Verify behavior on:
- iPhone Safari
- Capacitor iOS app
- Android webview/app

Ensure:
- bottom sheet respects safe-area insets
- keyboard/date picker does not overlap buttons
- modal works correctly on smaller screens

━━━━━━━━━━━━━━━━━━━━
6. IMPORTANT GUARDRAILS
━━━━━━━━━━━━━━━━━━━━

Do NOT:
- redesign the page
- change global spacing
- change bottom nav
- change card styling
- introduce Tailwind
- create duplicate edit flows

The lightweight date interaction beneath the title should become the canonical lightweight date-edit entry point.

Use the existing design system already implemented.

Use checkpoints sparingly.