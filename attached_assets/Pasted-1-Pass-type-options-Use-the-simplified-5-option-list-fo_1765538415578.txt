1️⃣ Pass type options
Use the simplified 5-option list for onboarding.
Answer to Replit:
Use the simplified list for onboarding: Epic / Ikon / Indy / Other / None.
The more granular pass types can be handled later on the profile or pass selection screen.
Why this is correct
Onboarding = identity, not configuration
Reduces decision fatigue
Future-proof (you already plan to expand passes later)
Matches your Base Lodge V1 scope
2️⃣ UI style for selections
Use segmented controls for both Rider Type and Pass Type.
Answer to Replit:
Use segmented controls for both Rider Type and Pass Type. This should visually match the Sign Up / Log In segmented control styling.
Why
Matches the auth experience
Mobile-friendly
Makes “required selection” obvious
Faster than dropdowns or radios
3️⃣ Existing users behavior
Existing users with both values set should skip /setup-profile automatically.
Answer to Replit:
Users who already have both rider_type and pass_type set should skip /setup-profile automatically.
They should still be able to edit these values later via the Profile screen.
Why
No surprises for existing users
No forced re-onboarding
Editing belongs in Profile, not Setup
4️⃣ Redirect scope
Apply the redirect to all protected routes.
Answer to Replit:
Apply the /setup-profile redirect check to all protected routes, not just Home.
Users must complete rider_type and pass_type before accessing any authenticated content.
Why
Prevents weird edge cases (Friends, Invite, etc.)
Keeps data integrity clean
Enforces a single mental model: “I must pick my basics first”
5️⃣ Setup completion indicator
Use the presence of both rider_type AND pass_type.
Do NOT add a new field.
Answer to Replit:
Use the presence of both rider_type and pass_type as the setup completion indicator.
Do not add a new setup_completed field.
Why
Zero schema changes
Clear, deterministic logic
Avoids duplicate state bugs
Scales cleanly
🔒 Optional implementation guidance (you don’t have to send this, but it helps)
If they ask how to implement the redirect cleanly:
Use a before_request or login-required wrapper
Exclude:
/logout
/setup-profile
/auth/*
Check:
if current_user.is_authenticated and (not current_user.rider_type or not current_user.pass_type):
    redirect('/setup-profile')