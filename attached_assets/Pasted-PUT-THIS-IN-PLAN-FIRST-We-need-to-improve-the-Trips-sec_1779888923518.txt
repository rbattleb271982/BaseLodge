PUT THIS IN PLAN FIRST

We need to improve the Trips section of the BaseLodge admin dashboard, but stay grounded in data that actually exists today.

Do not implement yet. First inspect the current admin dashboard route/template/models and propose the safest plan.

Goal:
Upgrade the Admin Dashboard Trips section from basic trip counts into a useful operational + destination intelligence view.

Important constraints:
- Do not invent metrics that the current data model cannot support.
- Do not include “trips where users connected afterward.”
- Do not build the future “gold mine” analytics layer yet.
- Do not redesign the entire admin dashboard.
- Do not change user-facing app screens.
- Keep the admin mobile overflow fix intact.
- Use existing models/routes/data where possible.
- Avoid migrations unless absolutely required.
- Use checkpoints sparingly.

Trips section should include only feasible metrics such as:

1. Core Trip Metrics
- Total Trips
- Trips This Month
- Upcoming Trips
- Past Trips
- Users With Trips
- Avg Trips Per User

2. Destination Intelligence
- Most Planned Resorts
- Most Wishlisted Resorts
- Resorts With Highest Trip Count
- Resorts With Highest User Count
- Trending Resorts This Month
- Resorts With No Planned Trips

3. Social Density / Invite Behavior
Only use data that exists today.
Possible metrics:
- Trips with invited friends
- Trips with accepted invites
- Avg invites per trip
- Invite acceptance %
- Trips with more than 1 participant
- Most social resorts
- Friend trips by resort

4. Planning Behavior
Only if derivable from existing trip dates.
Possible metrics:
- Avg trip length
- Avg days booked ahead
- Most common trip month
- Weekend vs weekday distribution
- Most common trip duration

5. Operational Tables
Useful admin tables:
- Upcoming trips
- Recently created trips
- Trips with no participants
- Trips missing resort metadata
- Trips with highest invite activity

Planning deliverables:
1. Identify which of these metrics are currently feasible from existing models.
2. Identify the exact models/fields needed for each metric.
3. Identify which metrics should be deferred because data is missing or unreliable.
4. Propose the best layout for the Trips section on desktop and mobile.
5. Identify exact files/routes/templates that need changes.
6. Explain any performance risks from aggregate queries.
7. Propose the safest implementation sequence.
8. Do not implement until I approve the plan.

After creating the plan, stop and wait for approval.