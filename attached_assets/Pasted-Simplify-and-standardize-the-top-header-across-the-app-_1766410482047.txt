Simplify and standardize the top header across the app by replacing the repeated “Base Lodge” brand text with the active section name (Trips, Friends, Invite, Settings), using the brand color, and removing redundant page titles.

This is a UI hierarchy and consistency update. Do not change navigation logic or database schema.

⸻

SECTION 1: Header behavior (global)
	1.	Replace app name with section title
Update the shared top header so that it displays the current section name instead of “Base Lodge”.

The header title should be:
	•	Trips
	•	Friends
	•	Invite
	•	Settings

depending on the active screen.
	2.	Styling

	•	Use the same color, font, size, and weight currently used for “Base Lodge”
	•	Center the title horizontally
	•	This title becomes the primary page identifier

Do not introduce a second title below it.

⸻

SECTION 2: Remove redundant page titles
	3.	Remove duplicate page headers
Remove the large black page titles that currently appear below the header, such as:

	•	“Trips”
	•	“Friends”
	•	“Invite a Friend”
	•	“Settings”

Once the header shows the section name, these titles are redundant and should not render.

Do not leave empty spacing where these titles were.

⸻

SECTION 3: Back navigation behavior
	4.	Back button handling

	•	Keep the existing Back button behavior exactly as-is
	•	The Back button should appear when navigating into sub-pages (e.g. Friend Profile, Edit Trip)
	•	The Back button should NOT appear on top-level tab screens

Do not change routing or navigation logic.

⸻

SECTION 4: Scope of change
	5.	Screens affected
Apply this change to all top-level tab screens:

	•	Trips
	•	Friends
	•	Invite
	•	Settings

Sub-pages (e.g. Profile Details, Friend Profile, Trip Detail) may continue to use their existing headers unless they already rely on the shared header.

⸻

SECTION 5: Non-goals
	•	Do not add the app name elsewhere
	•	Do not redesign the header layout
	•	Do not change tab bar behavior
	•	Do not modify fonts or colors beyond reusing the existing brand style
	•	Do not refactor unrelated layout code

⸻

SECTION 6: Testing requirements
	•	Each top-level tab shows the correct section name in the header
	•	No screen shows both a header title and a page title
	•	Header color and typography match the original “Base Lodge” styling
	•	Back navigation works as before
	•	No layout gaps appear where titles were removed
	•	Mobile layout remains clean and consistent

⸻

Agent instructions
	•	Update the shared header component first
	•	Remove page titles second
	•	Make the smallest possible changes
	•	Fully test before marking complete
	•	Do not refactor unrelated files
