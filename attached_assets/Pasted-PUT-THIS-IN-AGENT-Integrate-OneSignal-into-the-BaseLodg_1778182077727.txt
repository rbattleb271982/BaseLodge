PUT THIS IN AGENT

Integrate OneSignal into the BaseLodge Capacitor mobile app as the new push notification orchestration layer, without removing the existing direct APNs code yet.

Goal:
Add OneSignal support side-by-side so we can test iOS and Android push through OneSignal while keeping the current APNs implementation available as a fallback.

Requirements:
1. Add the official OneSignal Capacitor/Cordova-compatible SDK dependency appropriate for this project.
2. Initialize OneSignal only when running inside the native Capacitor app, not on normal web browsers.
3. Use the existing environment variable:
   ONESIGNAL_APP_ID
4. Do not hardcode secrets or app IDs directly into templates or JavaScript.
5. Add a safe server-rendered config value or endpoint that exposes only the public OneSignal App ID to the native client.
6. Keep existing APNs direct push code intact for now.
7. Add clear logging so we can verify:
   - native platform detected
   - OneSignal initialization attempted
   - OneSignal login/external ID set for the current user, if supported
   - OneSignal permission prompt result
8. Associate the logged-in BaseLodge user with OneSignal using a stable external user ID, preferably the internal user ID as a string.
9. Do not expose private emails, APNs tokens, FCM tokens, or OneSignal REST API keys in client-side code.
10. Use the existing backend environment variables:
    ONESIGNAL_APP_ID
    ONESIGNAL_REST_API_KEY
11. Add a backend helper function for sending a OneSignal push later, but do not replace existing push routes yet unless there is already a safe admin-only test route pattern to copy.
12. Add a new admin-only OneSignal test route:
    /admin/test-onesignal-push
13. Protect the new test route with the existing admin/login protection.
14. Do not use checkpoints unless I explicitly ask for one.

Important:
This is a Capacitor app where iOS currently has direct APNs working. Do not break:
- existing APNs direct push
- push permission registration
- AppDelegate foreground notification behavior
- existing Android project setup
- login/session behavior
- production web behavior at app.baselodgeapp.com

Implementation details:
- Prefer the official OneSignal Capacitor/Cordova integration path compatible with Capacitor v8.
- Only initialize OneSignal inside native app runtime checks.
- Avoid browser console errors when running the production web app normally.
- Preserve all existing push token registration flows until OneSignal is verified working.

After implementation, fully test:
- app starts cleanly
- web browser does not throw OneSignal errors
- native detection still works
- OneSignal initialization code is present only for native app use
- Python syntax passes
- templates compile
- no secrets are exposed client-side
- existing /admin/test-push and /admin/test-push-broadcast still work
- new /admin/test-onesignal-push route is admin-only

Do not finish until you have fully tested the implementation and summarized exactly what files changed.