We need to extend and stabilize the Admin → Resort Data Curation dashboard.

This work has TWO parts:
(A) Inline + bulk editing for Pass Brands
(B) Fix Excel export crash related to merged cells

DO NOT redesign the admin UI.
Extend existing patterns only.

================================
PART A — PASS BRANDS INLINE + BULK EDIT
================================

Pass Brands allowed values:
• Epic
• Ikon
• Indy
• Other
• None

----------------
A1. Inline edit (single row)
----------------

Frontend:
• Replace static Pass Brands text with a <select> dropdown in each row
• Default selected value = current pass brand
• On change:
  - Send AJAX request
  - Do NOT reload page
  - Preserve filters and search state
  - Update UI inline on success

Backend:
Add protected endpoint:
POST /api/admin/resorts/update-pass-brand

Payload:
{
  "resort_id": <int>,
  "pass_brand": "<string>"
}

Behavior:
• Validate pass_brand against allowed values
• Update Resort.pass_brands using ORM (no Core updates)
• Commit
• Return JSON { "success": true }

----------------
A2. Bulk edit
----------------

Frontend:
Extend EXISTING Bulk Actions bar:
• Add dropdown: “Set Pass Brand to…”
• Add button: “Apply Pass Brand”

Behavior:
• Disabled if no rows selected
• On click:
  - Collect selected resort IDs
  - POST to bulk endpoint
• Page reload OR inline update is acceptable

Backend:
POST /api/admin/resorts/bulk-update-pass-brand

Payload:
{
  "resort_ids": [int],
  "pass_brand": "<string>"
}

Behavior:
• Validate inputs
• ORM bulk update ONLY:
  Resort.query.filter(Resort.id.in_(ids)).update(
      {Resort.pass_brands: pass_brand},
      synchronize_session=False
  )
• Commit
• Return JSON { "updated_count": X }

Constraints:
• Admin-only (@login_required + @admin_required)
• No canonical JSON writes
• No page redesign
• Preserve filters/search where possible

================================
PART B — FIX EXCEL EXPORT ERROR
================================

Current error:
{"message":"Export failed: 'MergedCell' object has no attribute 'column_letter'"}

Root cause:
openpyxl code is iterating over merged header cells and accessing cell.column_letter.

----------------
B1. Required fix
----------------

In the Excel export implementation:

DO NOT:
• Use cell.column_letter
• Iterate over header cells
• Use merged cells at all

Instead:

1. Define headers explicitly:
headers = [
  "ID",
  "Name",
  "Country",
  "State / Region",
  "Pass Brands",
  "Status"
]

2. Write headers by numeric index ONLY:
for col_idx, header in enumerate(headers, start=1):
    ws.cell(row=1, column=col_idx, value=header)

3. Write data rows using numeric column indexes ONLY
4. Do NOT merge any cells
5. Keep the Excel output flat and simple

----------------
B2. Production safety
----------------

• Generate Excel file in-memory using BytesIO
• Do NOT write to disk
• Return file using send_file()
• Handle missing/empty filters defensively
• If export fails, return JSON error (not HTML error page)

----------------
B3. Verification
----------------

After fixing:
• Export Filtered to Excel works
• File downloads successfully
• Excel opens without errors
• Filtered rows match visible table
• No “Something went wrong” page appears

================================
FINAL CHECKLIST
================================

Confirm all of the following:
• Inline Pass Brands dropdown works
• Bulk Pass Brands update works
• Filters/search are preserved
• Excel export no longer throws MergedCell error
• No SQLAlchemy Core updates are used anywhere
• List all files changed

Do not implement any additional features beyond this scope.
