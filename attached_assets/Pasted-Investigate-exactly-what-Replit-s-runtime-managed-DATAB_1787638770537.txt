Investigate exactly what Replit’s runtime-managed DATABASE_URL represents in this project and what happens if it is removed from the Secrets pane.
Do not remove it. Do not detach anything. Do not Publish.
Objective
Determine whether removing runtime-managed DATABASE_URL is actually the supported and safe way to detach the old Replit-managed development database association now that BaseLodge development uses external Supabase Dev.
Investigate
What system currently creates/manages DATABASE_URL?
Is DATABASE_URL itself the database association, or merely one runtime variable created by a deeper Replit Database integration?
What exact UI action is available beside DATABASE_URL in Secrets, and what does Replit documentation say that action does?
Would removing that key:
only remove the environment variable;
detach the managed database from the project;
delete the managed development database;
affect stored development data;
affect Publish schema synchronization;
be recreated automatically by Replit?
Is there a safer supported detach/remove path in:
Database tool;
Integrations;
project settings;
deployment settings;
Secrets?
Can the old Replit-managed development database remain intact while its Publish/schema-sync association is removed?
After the correct detach action, would normal Publish become application-code-only, or would Replit still try to provision a managed production database?
What is the rollback path if the detach is performed and something unexpected happens?
Current architecture to preserve
Development runtime → external BaseLodge Dev Supabase
Production runtime → existing BaseLodge Prod Supabase
Old Replit-managed development database → currently unused by BaseLodge runtime
Do not change any of those during this investigation.
Hard boundaries
Do not:
delete or edit DATABASE_URL;
delete or detach the Replit-managed DB;
change any Supabase secret;
modify either Supabase database;
restart production;
run migrations;
publish/deploy;
run legacy maintenance;
touch push/OneSignal/APNs;
resume BL-77.
Use current official Replit documentation and read-only workspace behavior where available.
Return
Report:
what DATABASE_URL actually represents;
the exact supported detach mechanism, if one exists;
whether data is preserved;
whether the key is recreated;
whether Publish schema sync stops afterward;
rollback/recovery path;
the exact user action you recommend, or state that no safe supported action exists.
End with exactly:
SAFE DETACH PATH IDENTIFIED — READY FOR USER ACTION
or
DETACHMENT BLOCKED: [reason]