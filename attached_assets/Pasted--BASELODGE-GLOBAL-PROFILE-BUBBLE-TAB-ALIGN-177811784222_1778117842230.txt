╔════════════════════════════════════════════════════╗
║ BASELODGE GLOBAL PROFILE BUBBLE + TAB ALIGNMENT   ║
╚════════════════════════════════════════════════════╝

Add the existing Home profile bubble/avatar affordance consistently across the main logged-in tabs.

Goal:
The profile bubble should appear in the same top-right location on:
- Home
- Trips
- Friends
- Mountains

This should make Profile feel globally accessible and make the app shell feel more consistent.

────────────────────────────────────
PART 1 — GLOBAL PROFILE BUBBLE
────────────────────────────────────

Use the same profile bubble styling and behavior that already exists on the Home page.

Apply it to:
- Trips page
- Friends page
- Mountains page

Rules:
- Place profile bubble in the top-right of the page header
- Keep page title left-aligned
- Bubble should route to the user’s profile/settings destination exactly like it does from Home
- Do not create a new visual style
- Reuse existing CSS/classes where possible
- Do not add a second competing top-right action

Preferred header structure:

Page Title                         Profile Bubble

Then secondary actions/tabs/search/content below.

────────────────────────────────────
PART 2 — MOVE EXISTING TOP-RIGHT ACTIONS
────────────────────────────────────

Because the profile bubble now owns the top-right position, move existing buttons out of that area.

Trips page:
- Move “Season Snapshot” out of the top-right
- Keep it available as a secondary action
- Place it below the title/header area, preferably near the tabs or just above them
- It should remain visually secondary to the page title
- Do not remove functionality

Friends page:
- Move “Invite →” out of the top-right
- Keep it available as a secondary action
- Place it below the title/header area, preferably near the tabs or search area
- It should remain visually secondary
- Do not remove functionality

Mountains page:
- Add profile bubble top-right
- Keep search and filters below the header
- Do not disrupt existing search/filter functionality

────────────────────────────────────
PART 3 — TAB SPACING CONSISTENCY
────────────────────────────────────

Make the Friends page tab spacing match the Trips page tab spacing.

Current issue:
- Trips page tabs (“My Trips” / “Suggested”) have better spacing and balance
- Friends page tabs (“Friends” / “Friends’ Trips”) feel cramped and uneven

Update Friends tab styling/layout so it matches the Trips tab rhythm:
- same horizontal spacing behavior
- same underline behavior
- same vertical spacing
- same visual weight
- same overall feel

Do not change tab labels unless necessary.

────────────────────────────────────
PART 4 — DESIGN RULES
────────────────────────────────────

Preserve BaseLodge design language:
- premium
- editorial
- Apple-like
- cream background
- maroon accents
- minimal UI
- no extra icons
- no dashboard styling
- no heavy shadows

Do not redesign the full pages.
This is a header/app-shell consistency pass only.

────────────────────────────────────
PART 5 — QA
────────────────────────────────────

Verify:
- profile bubble appears on Home, Trips, Friends, and Mountains
- profile bubble appears in the same relative top-right position
- profile bubble routes correctly
- Trips “Season Snapshot” still works
- Friends “Invite” still works
- Mountains search still works
- Mountains filters still work
- Friends tabs match Trips tab spacing
- no header content overlaps on iPhone widths
- no bottom-nav/safe-area regressions
- no unrelated page layout changes

Deployment notes:
- Replit testing required
- Replit publish/deploy required after verification
- GitHub push required after Replit verification
- No Xcode/TestFlight rebuild needed unless native files changed

Use checkpoints sparingly. Only use them if absolutely necessary.