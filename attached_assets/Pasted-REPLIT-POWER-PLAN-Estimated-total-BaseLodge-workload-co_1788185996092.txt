REPLIT: POWER + PLAN
Estimated total BaseLodge workload completed after this step: ~37%

BL-127 — Investigate incorrect wishlist removal at_limit response

PLAN / INVESTIGATION ONLY.
Do not modify code, tests, database state, migrations, configuration, or dependencies.
Do not commit anything.

Context:
BL-127 is a Medium-priority bug.

Current reported behavior:
Wishlist removal reports `at_limit=true` at three items even though the actual/canonical wishlist maximum is 15.

Done looks like:
- `at_limit` uses the canonical wishlist maximum.
- Clients receive correct state for 0 through 15 items.
- Tests cover counts below, at, and above the relevant boundary.

Notion identifies likely code:
- `api_wishlist_remove()` in `app.py`, historically around lines 13112–13123.

Important recent context:
- BL-124 is complete.
- BL-126 is complete.
- BL-132 and Task #385 are complete from the code side.
- BL-70 is implemented and committed, but its migration/backfill is DEFERRED.
- Do not apply or depend on the BL-70 migration.
- BL-123 normalized wishlist destination IDs and is complete at commit:
  b24c6041c694b8242e2f0043c098417708fbb158
- There is one known unrelated migration test failure involving SQLite vs the PostgreSQL migration safety requirement. Do not expand BL-127 to fix it.

Investigate the current codebase and report:

1. Locate `api_wishlist_remove()` and show exactly how `at_limit` is currently calculated after a removal.

2. Identify the canonical wishlist maximum used by the ADD path and anywhere else wishlist capacity is enforced.
   - Determine whether the value 15 is represented by a shared constant/helper/configuration or duplicated literals.
   - Identify the authoritative source we should use for BL-127.

3. Search the repository for:
   - `at_limit`
   - wishlist limit/capacity constants
   - relevant literals such as 3 and 15 where they relate specifically to wishlist capacity
   - wishlist add/remove API consumers

4. Trace the remove response contract.
   - What fields does the endpoint return?
   - Which frontend/native code consumes `at_limit`?
   - What behavior changes when it is true vs false?
   - Confirm whether correcting the boolean alone is sufficient or whether another field/consumer is inconsistent.

5. Establish expected boundary behavior after a successful removal.
   Specifically determine expected `at_limit` values for resulting wishlist counts:
   - 0
   - 1
   - 14
   - 15
   - and, defensively, >15 if legacy/corrupt data somehow exists.

6. Review existing wishlist tests, especially tests covering:
   - add capacity
   - removal
   - response payloads
   - BL-123 normalization/validation behavior
   Determine the smallest regression-test additions needed for BL-127.

7. Check whether fixing BL-127 requires:
   - database/schema changes
   - migration work
   - native iOS/Android changes
   - API contract changes
   - generalized refactoring

The expected answer for those should be NO unless the actual architecture demonstrates otherwise.

8. Assess whether this is safely implementable as a narrow bug fix without touching unrelated wishlist behavior.

Return a concise implementation plan containing:
- root cause
- exact files/functions involved
- canonical limit source
- proposed code change
- proposed tests
- expected boundary semantics
- risks/regressions to guard against
- recommended Replit tier for BUILD

STOP after the investigation.

Do not implement BL-127 yet.
Do not create a generalized wishlist refactor unless the existing architecture makes one necessary.
Do not touch BL-70 migration/backfill work.
Do not touch Production or Supabase.
Do not fix unrelated failures.