PUT THIS IN AGENT

Goal 1: Create 10 additional dummy users (User Test31–User Test40) whose trips overlap with:
- the main BaseLodge user (richardbattlebaxter@gmail.com), AND
- the test user (jonathanmschmitz@gmail.com),
and connect all these dummy users as friends to BOTH accounts.

Goal 2: Enhance the Friend Trip Details page to show a smart overlap message such as:
“You’ll be there 1 day during this time.”
or
“You’ll be there 3 days during this time.”

Use checkpoints sparingly.

==================================================
PART A — CREATE 10 OVERLAPPING DUMMY USERS
==================================================

STEP A1 — Add a debug route to create extra dummy users
-------------------------------------------------------
File: app.py (or whichever file stores your seeding/debug routes).

At the top, ensure:

import random
from datetime import timedelta

Then add this route:

@app.route("/debug/create-extra-dummy-users")
def create_extra_dummy_users():
    # 1. Look up anchor users by email
    main_email = "richardbattlebaxter@gmail.com"
    jon_email = "jonathanmschmitz@gmail.com"

    main_user = User.query.filter_by(email=main_email).first()
    jon_user = User.query.filter_by(email=jon_email).first()

    if not main_user or not jon_user:
        return f"Anchor users not found. main={bool(main_user)}, jon={bool(jon_user)}", 400

    # 2. Load anchor trips
    from models import SkiTrip
    anchor_trips = SkiTrip.query.filter(
        SkiTrip.user_id.in_([main_user.id, jon_user.id])
    ).all()

    if not anchor_trips:
        return "No anchor trips found to create overlaps.", 400

    # 3. Random helper lists
    rider_types = ["Skier", "Snowboarder", "Telemark", "Adaptive", "Other"]
    pass_types = ["Epic", "Epic Local", "Ikon", "Ikon Base", "Indy", "Other", "None"]
    skill_levels = ["Beginner", "Intermediate", "Advanced", "Expert"]

    # 4. Create 10 new dummy users
    created_users = []
    for idx in range(31, 41):
        email = f"usertest{idx}@example.com"
        existing = User.query.filter_by(email=email).first()
        if existing:
            continue

        u = User(
            first_name="User",
            last_name=f"Test{idx}",
            email=email,
            rider_type=random.choice(rider_types),
            pass_type=random.choice(pass_types),
            skill_level=random.choice(skill_levels),
        )
        u.set_password("skitest123")
        db.session.add(u)
        created_users.append(u)

    db.session.commit()

    if not created_users:
        return "All dummy users Test31–Test40 already exist.", 200

    # 5. Create 1–3 overlapping trips for each dummy
    for u in created_users:
        num_trips = random.randint(1, 3)
        for _ in range(num_trips):
            template_trip = random.choice(anchor_trips)

            overlap_trip = SkiTrip(
                user_id=u.id,
                state=template_trip.state,
                mountain=template_trip.mountain,
                start_date=template_trip.start_date,
                end_date=template_trip.end_date,
                visible_to_friends=True,
            )

            if hasattr(template_trip, "resort_id"):
                overlap_trip.resort_id = template_trip.resort_id

            db.session.add(overlap_trip)

    db.session.commit()

    # 6. Add friendships to both main + jonathan
    from models import Friend

    def ensure_friendship(a, b):
        existing = Friend.query.filter_by(user_id=a.id, friend_id=b.id).first()
        if not existing:
            db.session.add(Friend(user_id=a.id, friend_id=b.id))

        existing_rev = Friend.query.filter_by(user_id=b.id, friend_id=a.id).first()
        if not existing_rev:
            db.session.add(Friend(user_id=b.id, friend_id=a.id))

    for u in created_users:
        ensure_friendship(main_user, u)
        ensure_friendship(jon_user, u)

    db.session.commit()

    return f"Created {len(created_users)} extra dummy users with overlapping trips and friendships.", 200


STEP A2 — Run the route
-------------------------------------------------------
Visit:

/debug/create-extra-dummy-users

Example:
https://base-lodge-app--rbattleb.replit.app/debug/create-extra-dummy-users

Check response.

Verify new overlaps in:
- Home → Friends’ Trips
- Overlaps tab


STEP A3 — Safety Note
-------------------------------------------------------
This route should be disabled after seeding is complete.

==================================================
PART B — ADD OVERLAP MESSAGE TO FRIEND TRIP DETAILS
==================================================

STEP B1 — Update the friend_trip_details route
-------------------------------------------------------
In app.py, find:

@app.route("/friend-trip/<int:trip_id>")
@login_required
def friend_trip_details(trip_id):

Add THIS logic BEFORE render_template:

trip = SkiTrip.query.get_or_404(trip_id)
friend = User.query.get(trip.user_id)

# Must be friends
is_friend = Friend.query.filter_by(
    user_id=current_user.id,
    friend_id=friend.id
).first()

if not is_friend:
    return "Unauthorized", 403

# Find your overlapping days
your_trips = SkiTrip.query.filter_by(user_id=current_user.id).all()

def overlapping_days(a_start, a_end, b_start, b_end):
    latest_start = max(a_start, b_start)
    earliest_end = min(a_end, b_end)
    delta = (earliest_end - latest_start).days + 1
    return max(0, delta)

your_overlap_days = 0
your_overlap_ranges = []

for yt in your_trips:
    days = overlapping_days(
        trip.start_date,
        trip.end_date,
        yt.start_date,
        yt.end_date
    )
    if days > 0:
        your_overlap_days += days
        your_overlap_ranges.append(yt)

has_overlap = your_overlap_days > 0


Then modify the return:

return render_template(
    "friend_trip_details.html",
    trip=trip,
    friend=friend,
    has_overlap=has_overlap,
    overlap_days=your_overlap_days,
    your_overlap_ranges=your_overlap_ranges
)


STEP B2 — Update friend_trip_details.html
-------------------------------------------------------
In templates/friend_trip_details.html, directly BELOW the trip date area, insert:

{% if has_overlap %}
<div class="overlap-box">
    {% if overlap_days == 1 %}
        <p><strong>You’ll be there 1 of the same day.</strong></p>
    {% else %}
        <p><strong>You’ll be there {{ overlap_days }} days during this time.</strong></p>
    {% endif %}
</div>
{% endif %}


STEP B3 — Add optional styling
-------------------------------------------------------
Add at bottom of friend_trip_details.html or your stylesheet:

<style>
.overlap-box {
    background: #F5F9FF;
    border: 1px solid #D0E2FF;
    padding: 12px 14px;
    border-radius: 10px;
    margin-top: 12px;
    color: #223;
}
</style>


==================================================
END OF PROMPT
==================================================

