I need you to fully audit and fix the social seeding functionality.  
Perform these steps in order, and make all corrections directly in the codebase:

1. Verify that there is a debug blueprint (debug_bp) and that it is correctly registered in app.py via app.register_blueprint(debug_bp).  
   If it is missing, add the registration.

2. Search the repo for any existing seed route(s) like /seed_social_data or /seed_friends.  
   If they exist and are incorrect, replace them entirely with the version below.

3. Create (or replace) a route called /seed_social_data inside routes/debug_routes.py.  
   This route must:
      - Look up the user with email "me@example.com" and assign to main_user.
      - If main_user does not exist, return an explanatory message.
      - Create 10 fake users if they do not already exist.  
        Each fake user must have:
           * first_name and last_name (use Faker if available; otherwise hardcode)
           * email in the format fakeX@example.com
           * pass_type randomly chosen from ["Epic","Ikon","Other","None"]
           * rider_type randomly chosen from ["Skier","Snowboarder"]
      - For each fake user, create TWO Friend rows:
           Friend(user_id=main_user.id, friend_id=fake_user.id)
           Friend(user_id=fake_user.id, friend_id=main_user.id)
        Only create rows if they do not already exist.
      - Create 1–3 SkiTrip objects for each fake user:
           * random state from ["CO","UT","CA","VT","NH","BC"]
           * random mountain from ["Vail","Breckenridge","Park City","Whistler Blackcomb","Mammoth","Stowe","Snowbird","Aspen"]
           * start_date = random date within the next 120 days
           * end_date = start_date + random 1–3 days
      - Import all required models (User, Friend, SkiTrip, db).
      - Commit everything.
      - Print detailed logs to the console:
            print("Creating fake user:", email)
            print("Creating friendship:", main_user.id, fake_user.id)
            print("Creating trip for:", fake_user.id)
      - Return "Social data seeded successfully."

4. After implementing, run the route yourself inside the Replit preview environment and make sure no errors occur.

5. If any imports are missing or the file structure needs adjustment, fix whatever is required so the route loads and works at runtime.

Do not use checkpoints unless I explicitly request them.
