BASELODGE — POST-PUBLISH MOBILE REGRESSION QA

Test the live production app like a real mobile user.

Focus on:
- interaction quality
- regressions
- confusing UX
- broken flows
- visual inconsistencies
- mobile usability

Recently changed areas to specifically test:

━━━━━━━━━━━━━━━━━━━━
1. TRIP DETAIL PAGE
━━━━━━━━━━━━━━━━━━━━

Test:
- hero layout
- mountain title tap behavior
- editable date row
- date edit bottom sheet
- wishlist count in “See who’s going”
- Trip setup section
- What’s next section
- bottom sheet safe-area handling
- scroll behavior after editing dates

Check for:
- duplicate/old layout structures
- spacing inconsistencies
- broken links
- awkward tap targets
- web-like instead of native-feeling interactions

━━━━━━━━━━━━━━━━━━━━
2. ADD / EDIT TRIP DATE FLOW
━━━━━━━━━━━━━━━━━━━━

Test:
- selecting one date range
- auto-staging after end-date tap
- selecting multiple ranges
- removing staged ranges
- overlap validation
- duplicate validation
- CTA text (“Save trip”, “Save 2 dates”, etc.)
- calendar usability on small screens

Verify:
- user never has to scroll upward after selecting dates
- “+ Add date” no longer exists as a required action
- staged ranges remain stable
- group flow still works

━━━━━━━━━━━━━━━━━━━━
3. PLAN A TRIP FLOW
━━━━━━━━━━━━━━━━━━━━

Test the full “Plan a trip” experience end-to-end.

Verify:
- resort selection/search
- date selection flow
- auto-staging behavior
- multi-date trip flow
- pass selection
- save/create flow
- redirect after save
- newly created trip detail page rendering

Check for:
- confusing order of operations
- broken staged-date behavior
- duplicate trips accidentally created
- incorrect trip counts
- missing pass/resort data
- broken mobile spacing
- awkward keyboard/calendar overlap
- stale UI state after save

Confirm the overall flow feels:
- mobile-first
- smooth
- intuitive
- app-like instead of web-form-like

━━━━━━━━━━━━━━━━━━━━
4. ACCOUNT DELETION
━━━━━━━━━━━━━━━━━━━━

Test:
- delete-account modal
- email confirmation behavior
- mismatch rejection
- successful deletion flow
- redirect after deletion
- logout/session clearing behavior

Check for:
- broken redirects
- silent failures
- server errors
- stale authenticated state

━━━━━━━━━━━━━━━━━━━━
5. GENERAL MOBILE QA
━━━━━━━━━━━━━━━━━━━━

Test:
- Home
- Trips
- Friends
- Ideas
- Profile
- login/logout
- flash messages
- bottom nav
- mobile spacing
- iOS safe-area behavior

━━━━━━━━━━━━━━━━━━━━
RETURN FORMAT
━━━━━━━━━━━━━━━━━━━━

Please separate findings into:

1. Critical bugs
2. Medium-priority UX issues
3. Minor polish issues
4. Possible regressions
5. Things that are working especially well

Do NOT make changes.
Only test and report.