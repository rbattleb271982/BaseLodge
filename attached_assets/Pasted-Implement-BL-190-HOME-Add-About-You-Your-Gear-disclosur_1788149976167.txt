Implement BL-190 — HOME — Add About You & Your Gear disclosure.

This is BUILD.

Do not create another planning task.
Use the completed BL-187 Home summary contract and BL-188 disclosure foundation.
Do not redesign the broader Home page.

Goal:
Replace the current Home profile/gear summary presentation with one compact, collapsed “About You & Your Gear” disclosure that gives the user a useful snapshot without consuming excessive vertical space.

PRODUCT CONTRACT

Collapsed state:

About You & Your Gear
Skier · Advanced · No pass added

Use the user’s actual existing values, not hardcoded text.

The collapsed summary should communicate, when available:
- rider type
- skill level
- pass

Examples:
Skier · Advanced · Epic
Snowboarder · Intermediate · Ikon
Skier · Advanced · No pass added

Use existing canonical display semantics from BL-187.
Do not create new rider/pass normalization rules.

Expanded state:

Use a clean SINGLE-COLUMN presentation.

Show the relevant existing information for:
- Rider Type
- Skill Level
- Pass
- Skis
- Boots
- Bindings

Use the existing canonical gear selection and rental-state behavior already represented by Home/BL-187.

IMPORTANT DESIGN CONSTRAINTS

- Use the BL-188 disclosure macro/foundation.
- Collapsed by default.
- Do not add custom disclosure JavaScript.
- Do not persist disclosure state.
- Do not add analytics in this task.
- Keep the module vertically compact.
- Use a single-column/list layout.
- Do NOT use a two-column label/value grid that causes wrapping.
- Do NOT add photographs.
- Do NOT add image placeholders.
- Do NOT add thumbnails.
- Do NOT add profile bubbles or avatars.
- Do NOT add decorative mountain illustrations.
- Do NOT add new decorative icons merely to fill space.
- Preserve the existing warm/clean Home visual language rather than creating a separate design system.

EMPTY / MISSING VALUES

Handle incomplete profiles gracefully.

Use concise user-facing empty language where necessary rather than exposing Python/null/database values.

For example:
- no pass → “No pass added”
- no gear for a category → use the existing appropriate Home/profile empty behavior

Do not invent gear or profile information.

Do not introduce new profile-editing behavior in this task.

DATA CONTRACT

Consume the existing BL-187 `home_summary["about_you"]` contract.

Do not introduce new Home database queries.

The disclosure should use already-resolved values including:
- authenticated user
- display rider type
- skill level
- pass
- rider disciplines
- selected gear by discipline
- rental state

Do not reinterpret those values in the template if canonical presentation logic already exists.

CURRENT CONTENT

This task may replace/restructure the existing Home profile/gear summary content necessary to create the new disclosure.

Do not alter unrelated Home modules:
- Your Activity
- Friends’ Passes
- Your Next Trip
- Ideas
- Happening
- Friends’ Trips
- navigation/header

Do not implement BL-191 or BL-192 while doing BL-190.

PRIVACY / PRODUCT SEMANTICS

This is presentation work.

Do not change:
- profile semantics
- gear selection semantics
- pass semantics
- trip semantics
- friend semantics
- privacy/authorization
- Home queries
- Ideas/Happening logic
- schema/migrations
- native configuration

TESTS

Add or extend focused tests covering at minimum:
- disclosure exists
- collapsed by default
- collapsed summary uses actual rider type
- collapsed summary uses actual skill level
- collapsed summary uses actual pass
- missing pass displays “No pass added”
- expanded content exposes appropriate Rider Type
- Skill Level
- Pass
- Skis
- Boots
- Bindings
- missing gear handled gracefully
- rental behavior remains consistent
- no new Home queries are introduced
- existing BL-187 and BL-188 behavior remains intact

Prefer extending existing Home/gear/disclosure test coverage rather than creating redundant broad suites.

VALIDATION

Run:
- focused BL-190 tests
- BL-188 disclosure regression tests
- BL-187 Home summary regression tests
- directly affected Home/gear tests
- `git diff --check`

Do not commit yet.

Return:

BL-190 IMPLEMENTATION RESULT

- Files changed:
- Existing Home content replaced:
- Collapsed summary implemented:
- Single-column expanded layout confirmed:
- BL-187 summary contract used:
- New Home queries added:
- Custom disclosure JavaScript added:
- Product semantics changed:
- Privacy/authorization changed:
- Other Home modules changed:
- Schema/migration changed:
- Tests run:
- Tests passed:
- git diff --check:
- Scope deviations:
- Commit created: NO

If implementation is complete and clean, end with exactly:

BL-190 READY TO MERGE