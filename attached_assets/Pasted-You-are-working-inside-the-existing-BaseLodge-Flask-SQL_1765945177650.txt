You are working inside the existing BaseLodge Flask + SQLAlchemy app.
🎯 Goal
Upgrade onboarding and profile editing to support multi-pass selection using a “Select all that apply” pattern, while preserving existing data and avoiding scope creep.
This is v1-safe, data-correct, and minimal.
✅ PRODUCT RULES (DO NOT DEVIATE)
Users can select multiple ski passes
Selection method = checkboxes (“Select all that apply”)
Store passes as a comma-separated string on User.pass_type
Example: "Ikon,MountainCollective"
Existing users with a single pass must continue to work
Changing passes affects future browsing only
Do NOT retroactively modify trips
Do NOT add join tables or refactor schema beyond User.pass_type
🧩 IMPLEMENTATION TASKS
1️⃣ Update Canonical Pass List (Backend)
Confirm or define a single canonical list of passes used everywhere:
CANONICAL_PASSES = [
    "Epic",
    "Ikon",
    "MountainCollective",
    "Indy",
    "PowderAlliance",
    "Freedom",
    "SkiCalifornia",
    "Other",
    "None"
]
Use these exact string values
No spaces in stored values
Display labels may include spaces
2️⃣ Update Onboarding UI (setup_profile.html)
Replace the single-select pass dropdown with:
Checkbox list
Header text:
Which passes do you have?
Select all that apply.
Example HTML structure:
{% for pass in CANONICAL_PASSES %}
  <label>
    <input type="checkbox" name="pass_type" value="{{ pass }}">
    {{ pass.replace("_", " ") }}
  </label>
{% endfor %}
Notes:
Allow multiple selections
“None” can be selected alone or with others (no validation enforcement in v1)
3️⃣ Update Onboarding POST Handler (/setup-profile)
Read multiple values from request:
passes = request.form.getlist("pass_type")
Persist as comma-separated string:
user.pass_type = ",".join(sorted(set(passes)))
If no passes selected:
user.pass_type = "None"
Preserve all existing onboarding logic
4️⃣ Update Profile Edit UI (edit_profile.html or equivalent)
Mirror the same checkbox UI
Pre-check boxes for existing user values:
user_passes = set(current_user.pass_type.split(","))
<input type="checkbox"
       name="pass_type"
       value="{{ pass }}"
       {% if pass in user_passes %}checked{% endif %}>
5️⃣ Update Profile Edit POST Handler
Same logic as onboarding:
getlist("pass_type")
join → comma-separated string
fallback to "None"
6️⃣ Backward Compatibility (CRITICAL)
Existing users with:
pass_type = "Ikon"
must continue to work without modification
Do NOT migrate existing rows unless user edits their profile
All code that reads User.pass_type must tolerate:
single value
comma-separated values
7️⃣ Minimal Validation Only
Do NOT:
Enforce mutual exclusivity
Prevent “None + others”
Validate pass compatibility
This is informational v1 behavior only.
8️⃣ Smoke Verification Checklist (Required)
After implementation, manually verify:
New user can select multiple passes during onboarding
Profile edit shows correct boxes checked
Saving profile persists correctly
Existing users with single pass still load correctly
App boots cleanly
No crashes in trip creation or friends pages
🚫 EXPLICITLY OUT OF SCOPE
Do NOT:
Add join tables
Refactor pass filtering logic
Block users based on passes
Modify existing trips
Add UI polish beyond checkboxes
Add automated tests beyond manual smoke checks
📋 FINAL REPORT BACK
Return:
Files modified
Confirmation of backward compatibility
Any edge cases observed