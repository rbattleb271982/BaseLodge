You are modifying the BaseLodge Flask app.

FEATURE:
Allow users to delete a trip directly from the Edit Trip screen.

UX REQUIREMENTS (STRICT):
- Delete must be visible but secondary
- Delete must NOT look like a primary button
- Delete must require confirmation
- Confirmation text must read EXACTLY:
  "Are you sure you want to delete this trip?"

DO NOT redesign the page.

--------------------------------------------------
STEP 1: ADD DELETE LINK (UI)
--------------------------------------------------

On the Edit Trip page (edit_trip.html or equivalent):

- Add a text-only link below the "Save changes" button
- Left-aligned
- Red text
- Smaller than primary CTA
- Copy must be exactly:
  "Delete trip"

This link must NOT submit the form.

--------------------------------------------------
STEP 2: CONFIRMATION POP-UP
--------------------------------------------------

When "Delete trip" is tapped:

- Show a confirmation dialog
- Use native browser confirm OR existing modal pattern
- Confirmation message must be exactly:
  "Are you sure you want to delete this trip?"

If user cancels:
- Do nothing

If user confirms:
- Proceed to delete

--------------------------------------------------
STEP 3: BACKEND DELETE LOGIC
--------------------------------------------------

Locate the existing trip delete logic OR create it if missing.

Requirements:
- Delete the trip by ID
- Ensure the trip belongs to the current user
- Prevent deleting trips owned by other users
- Use POST or DELETE (not GET)

--------------------------------------------------
STEP 4: REDIRECT + FEEDBACK
--------------------------------------------------

After successful deletion:
- Redirect user to:
  - Home screen OR
  - Trips list (use existing pattern)
- Show success message:
  "Trip deleted."

--------------------------------------------------
STEP 5: SAFETY CHECKS
--------------------------------------------------

- Do NOT allow deletion without confirmation
- Do NOT allow deletion via direct URL without auth
- Do NOT expose trip IDs in client-side JS
- Do NOT break edit/save functionality

--------------------------------------------------
STEP 6: TESTING
--------------------------------------------------

Manually verify:
1. Delete link is visible but subtle
2. Confirmation dialog appears
3. Cancel keeps trip intact
4. Confirm deletes trip
5. User is redirected correctly
6. Deleted trip no longer appears anywhere

STOP when complete.
