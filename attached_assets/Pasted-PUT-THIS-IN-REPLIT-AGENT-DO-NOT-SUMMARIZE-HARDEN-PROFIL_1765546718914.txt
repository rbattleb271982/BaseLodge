PUT THIS IN REPLIT AGENT — DO NOT SUMMARIZE
🔒 HARDEN PROFILE CONSOLIDATION + ADD TRIP DURATION DISPLAY + REGRESSION SAFEGUARDS
This task has three objectives:
Lock in the /more profile consolidation (prevent regressions)
Prevent deleted templates/routes from ever breaking the app again
Add trip duration (number of days) to all trip rows/cards
This is not a redesign.
This is structural hardening + small data enhancement.
1️⃣ PROFILE CONSOLIDATION — HARD LOCK (MANDATORY)
❌ /profile MUST NOT EXIST
Remove (or fully comment out) any route rendering profile.html
Remove all references to:
render_template("profile.html")
redirect(url_for("profile"))
✅ Defensive Redirect (Required)
Add a guard so /profile can never crash the app:
@app.route("/profile")
def deprecated_profile():
    return redirect(url_for("more"))
2️⃣ STANDARDIZE ALL PROFILE SAVE REDIRECTS
For every POST handler that modifies profile data:
Save data
Redirect to /more
Never render a template
Applies to:
Rider type
Pass type
Skill level
Home state
Home mountain
Mountains visited
✅ Required pattern:
db.session.commit()
return redirect(url_for("more"))
❌ Forbidden:
render_template(...)
redirect(url_for("profile"))
3️⃣ ADD TRIP DURATION (NEW REQUIREMENT)
🎯 Goal
Each trip row/card should display how many days long the trip is, based on start and end dates.
Rules:
Trip length = (end_date - start_date) + 1
Example:
Feb 10 – Feb 13 → 4 days
Display must be inline with existing trip text
Do NOT change layout, spacing, or card structure
Backend (preferred)
Compute duration when preparing trip data:
trip.duration_days = (trip.end_date - trip.start_date).days + 1
Template
Append duration text, e.g.:
Feb 10–Feb 13 — Whistler British Columbia • 4 days
or equivalent inline phrasing.
❌ Do NOT:
Stack vertically
Add icons or badges
Increase row height
4️⃣ AUTOMATED REGRESSION TESTS (REQUIRED)
Create tests/test_profile_consolidation.py
Test 1: /profile always redirects
def test_profile_redirects(client):
    response = client.get("/profile", follow_redirects=False)
    assert response.status_code in (301, 302)
    assert "/more" in response.location
Test 2: Profile saves redirect to /more
def test_profile_save_redirects(client, logged_in_user):
    response = client.post("/update-skill-level", data={"skill_level": "Advanced"})
    assert "/more" in response.location
Test 3: No deleted templates referenced
def test_no_profile_template_reference():
    import pathlib
    code = pathlib.Path("app.py").read_text()
    assert "profile.html" not in code
5️⃣ DOCUMENTATION GUARD (MANDATORY)
At the top of app.py, add:
# PROFILE CONSOLIDATION NOTE:
# The app no longer uses /profile or profile.html.
# All profile-related UI lives under /more.
# Do NOT reintroduce profile routes or templates.
6️⃣ ACCEPTANCE CRITERIA (ALL REQUIRED)
Before marking complete, verify:
✅ /profile redirects to /more
✅ No TemplateNotFound errors
✅ Saving profile fields always returns to /more
✅ Trip cards show correct number of days
✅ No layout changes
✅ App runs after restart
✅ Tests pass
⛔️ ABSOLUTELY DO NOT
Recreate profile.html
Add a second profile page
Change trip card layout
Add icons/badges
Skip tests
🧠 CONTEXT (DO NOT IGNORE)
The /more page is the single source of truth for profile state.
Trips are shared objects and must remain visually consistent across:
My Trips
Friends’ Trips
Overlaps
This task locks that in permanently.
If you want, next steps after this are:
Fix auth/session persistence cleanly
Seed dummy users once DB is stable
Add overlap logic tests
But do not proceed until this is complete and verified.