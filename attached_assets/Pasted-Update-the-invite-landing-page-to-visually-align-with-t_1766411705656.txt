Update the invite landing page to visually align with the Trips page by using a familiar card-based layout and surfacing lightweight profile and trip information in a clear, scannable format.

This is a UI and copy update only. Do not change invite logic, routing, or database schema.

⸻

SECTION 1: Layout structure
	1.	Card-based layout
Refactor the invite landing page to use a single, centered card styled consistently with Trips page cards.

Requirements:
	•	Same card width, padding, border radius, and shadow as Trips cards
	•	Neutral background behind the card
	•	No tabs or navigation elements

⸻

SECTION 2: Card content
	2.	Headline
At the top of the card, display:

“{First Name} {Last Name} wants to connect”

Do not include the app name in the headline.
	3.	Identity summary
Below the headline, display a one-line identity summary using the same format as profile cards:

“{Rider Type} · {Skill Level} · {Home State (if available)}”

Rules:
	•	Omit missing fields cleanly
	•	Use bullet separators (·)
	•	Match ordering used elsewhere in the app

	4.	Trips planned stat
Below the identity summary, display a single stat styled similarly to Trips page stats:

“{X} trips planned”

Rules:
	•	Show only if X > 0
	•	Use this exact phrasing
	•	Do not show dates or destinations

	5.	Supporting copy
Below the stat (optionally separated by a light divider), display:

“See when you’re both heading to the mountains and plan ski trips together.”

⸻

SECTION 3: Call to action
	6.	Primary CTA
Place a primary button inside the card with text:

“Create account to connect”
	7.	Secondary CTA
Below the primary CTA, include:

“Already have an account? Log in”

Ensure the secondary CTA is visually subordinate.

⸻

SECTION 4: Scope control
	8.	What NOT to include
Do NOT show:

	•	Trip lists
	•	Equipment
	•	Terrain preferences
	•	Pass type
	•	Photos
	•	Friends or social graphs

This page should preview value, not replicate the app.

⸻

SECTION 5: Testing requirements
	•	Card layout matches Trips page styling
	•	Inviter data renders correctly
	•	Trips planned stat shows only when > 0
	•	CTAs work as before
	•	Page feels visually consistent with Trips
	•	Mobile layout is clean and centered

⸻

Agent instructions
	•	Reuse existing Trips card styles
	•	Make the smallest possible layout changes
	•	Fully test before marking complete
	•	Do not refactor unrelated files
