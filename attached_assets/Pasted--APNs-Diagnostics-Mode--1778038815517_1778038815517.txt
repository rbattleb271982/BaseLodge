╭────────────────────────────────────────────╮
│ APNs Diagnostics Mode                      │
╰────────────────────────────────────────────╯

Add an admin-only APNs diagnostics mode for `/admin/test-push`.

Goal:
Verify the backend APNs credentials and headers without changing iOS, Capacitor, Xcode, TestFlight, token selection, or normal push behavior.

Important:
- This is Replit backend only.
- Replit publish/deploy is required after the change.
- No GitHub/Mac/Xcode/TestFlight steps are required for this diagnostic.
- Do not archive or rebuild anything.
- Do not use checkpoints unless absolutely necessary.

Implement this in the backend APNs test-push flow only:

1. In the `/admin/test-push` route, add diagnostics output for the single test push run.
2. Do not add noisy logs to every push send.
3. Do not print full tokens during normal push sends.
4. Do not change the APNs sending logic unless needed to expose diagnostics.
5. Do not change token registration, token selection, or retry behavior.

For the admin test push, print/log these values:

- `APNS_TEAM_ID`
- `APNS_KEY_ID`
- `APNS_BUNDLE_ID`
- JWT `iss`
- JWT header `kid`
- APNs `apns-topic` header
- selected token length
- selected token preview only, not full token
- APNs environment used
- APNs response status
- APNs response body / reason
- APNs `apns-id` response header if available

Expected values:
- `APNS_TEAM_ID` should be `9X2B8QDJ3D`
- `APNS_BUNDLE_ID` should be `com.baselodge.app`
- JWT `iss` should match `APNS_TEAM_ID`
- JWT header `kid` should match `APNS_KEY_ID`
- APNs topic should be `com.baselodge.app`
- selected token length should be `64`

Make the diagnostics clearly labeled in the Replit logs, for example:

[APNs DIAGNOSTICS] APNS_TEAM_ID=...
[APNs DIAGNOSTICS] APNS_KEY_ID=...
[APNs DIAGNOSTICS] APNS_BUNDLE_ID=...
[APNs DIAGNOSTICS] jwt_iss=...
[APNs DIAGNOSTICS] jwt_kid=...
[APNs DIAGNOSTICS] apns_topic=...
[APNs DIAGNOSTICS] selected_token_length=...
[APNs DIAGNOSTICS] selected_token_preview=...
[APNs DIAGNOSTICS] environment=production|sandbox
[APNs DIAGNOSTICS] response_status=...
[APNs DIAGNOSTICS] response_body=...
[APNs DIAGNOSTICS] apns_id=...

If the APNs send function currently builds the JWT internally, add a safe way for `/admin/test-push` to request diagnostic metadata back from that function without changing normal behavior.

Recommended approach:
- Add an optional parameter like `diagnostics=False`
- Only collect or print diagnostic metadata when `/admin/test-push` passes `diagnostics=True`
- Leave all normal push sends unchanged

After implementation, confirm:
- Normal push sends behave the same as before
- `/admin/test-push` still sends the test notification
- `/admin/test-push` now prints the diagnostics above
- No full device token is printed
- No APNs private key contents are printed
- No unrelated frontend, iOS, Capacitor, GitHub, Mac, Xcode, or TestFlight files were changed