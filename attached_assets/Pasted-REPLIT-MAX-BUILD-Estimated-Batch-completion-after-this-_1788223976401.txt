REPLIT: MAX + BUILD
Estimated Batch completion after this step: ~100%

TASK: BL-159 — Bound Friends and Friends’ Trips retrieval

Task #408 investigation is approved.

Proceed directly to implementation.
Do not create another plan.

==================================================
PRIMARY GOAL
==================================================

Implement bounded progressive loading for:

1. Authenticated Friends directory
2. Friends’ Trips on /my-trips
3. Grouped Friends’ Trips detail sheets

Preserve all existing:

- reciprocal-friend authorization
- social-trip visibility
- lifecycle behavior
- RSVP behavior
- friend-centric grouping
- counts
- filters
- labels
- links
- actions
- empty states
- visual design

Do not redesign these surfaces.

==================================================
1. FRIENDS DIRECTORY PAGING
==================================================

Page only reciprocal-current friends.

Initial page size:

20 friends

Subsequent pages:

20 friends

Ordering must be deterministic:

1. first name ascending
2. last name ascending
3. Friend ID ascending

Use a strict opaque keyset cursor containing every ordering component required to resume safely.

Do not use OFFSET pagination.

==================================================
2. FRIEND AUTHORIZATION
==================================================

BL-133 remains a hard boundary.

A displayed friend requires reciprocal CURRENT Friend rows.

The following must NOT authorize:

- one-sided Friend row
- Pending relationship
- Declined relationship
- former friendship
- stale friendship state
- FriendConnectionEvent/history rows

Every page request must recompute current reciprocal authorization.

Cursor position never grants access.

==================================================
3. FRIENDS DIRECTORY CARDINALITY
==================================================

The initial /friends request must hydrate at most:

- 20 displayed friend Users
- 20 current reciprocal Friend rows

Do not hydrate all reciprocal friends just to render the first page.

The total authorized friend count may be calculated separately and must remain the COMPLETE count.

Do not substitute loaded-row count for total friend count.

==================================================
4. REMOVE DIRECTORY TRIP MATERIALIZATION
==================================================

The current Friends directory should not hydrate Trip, Resort, or participant ORM collections merely to render friend rows.

For visible friend rows only, batch any displayed upcoming-trip count using a bounded aggregate query.

If the current template does not display another trip/overlap presentation field:

- confirm it has no template/client/server consumer
- remove that dead computation

Do not retain large trip collections solely for unused context.

==================================================
5. FRIEND DIRECTORY ROW DATA
==================================================

Preserve existing row content:

- Name
- New badge
- Rider type
- Skill
- Pass
- complete displayed upcoming-trip count
- Friend Profile link

Do not add new information.

Do not introduce avatar/profile bubbles or image content.

==================================================
6. FRIENDS LETTER HEADERS
==================================================

Preserve alphabetical letter grouping.

Progressively appended pages must handle boundaries correctly.

Example:

Page 1 ends with several B names.
Page 2 begins with another B name.

Do not emit a duplicate B header unnecessarily.

If page 2 begins with C, append the C header correctly.

The server response may include enough boundary metadata to allow deterministic section/header merging.

==================================================
7. FRIEND SEARCH AND FILTERS
==================================================

Existing Friends controls must search/filter the COMPLETE authorized friend set on the server.

This includes current:

- name search
- pass filters
- rider filters
- skill filters

Do NOT filter only currently loaded DOM rows.

When search/filter criteria change:

- reset pagination
- return the first matching page
- update total matching count/messages
- preserve current matching semantics
- preserve empty-state behavior

Do not invent new filters.

==================================================
8. GLOBAL MEMBER SEARCH
==================================================

Keep global member search separate from the paginated reciprocal Friends directory.

Do not accidentally constrain global member search to:

- loaded friends
- reciprocal friends
- current directory filter state

Preserve its existing authorization and behavior.

==================================================
9. PENDING REQUESTS
==================================================

Pending Friend Requests remain COMPLETE and unpaginated.

They must:

- remain separate from reciprocal friend pages
- not consume a 20-row directory slot
- keep current counts
- keep current actions
- keep current ordering

Do not change request lifecycle semantics.

==================================================
10. SUGGESTED FRIENDS
==================================================

Suggested Friends remain complete under current behavior and separate from the reciprocal Friends page.

Do not redesign or paginate them unless required by an existing exact implementation dependency.

If untouched, explicitly confirm that.

==================================================
11. FRIENDS DIRECTORY ENDPOINT
==================================================

Add a focused authenticated page endpoint for Friends directory pagination/filtering.

Return server-rendered fragments plus small paging metadata such as:

- HTML
- has_more
- next_cursor
- complete matching count
- authorized displayed friend IDs if useful for dedupe
- first/last grouping boundary metadata if needed

Keep response minimal.

Do not expose sensitive fields.

==================================================
12. FRIENDS DIRECTORY CLIENT BEHAVIOR
==================================================

Use minimal vanilla JavaScript.

Load more behavior:

- disable button while loading
- show concise loading state
- append rows
- avoid duplicate friend IDs
- merge alphabetical headers correctly
- update cursor
- remove button when complete
- preserve existing rows on request failure
- expose retry state
- maintain keyboard/focus usability

No infinite scroll.
No frontend framework.

==================================================
13. NORMAL MY TRIPS BEHAVIOR
==================================================

The normal My Trips tab must NOT eagerly build the complete Friends’ Trips collection.

BL-158 viewer feeds remain unchanged.

When the user is on the standard My Trips tab:

- Upcoming remains BL-158 behavior
- Pending Invites remain unchanged
- Past remains BL-158 behavior
- do not perform unnecessary Friends’ Trips collection work

Direct:

/my-trips?tab=friends

must load the FIRST bounded Friends’ Trips page.

==================================================
14. FRIENDS’ TRIPS AUTHORIZATION
==================================================

Preserve exact current rules.

Friend presence requires:

- reciprocal-current friendship

AND an existing qualifying social-trip path such as:

- direct friend organizer of qualifying public trip
- direct friend with Going participation where current behavior allows

Preserve existing public/private rules.

Private trips must not become visible merely because a friend relationship exists.

Every page/detail request must reauthorize current state.

==================================================
15. FRIENDS’ TRIPS RSVP RULES
==================================================

Preserve exact existing behavior:

Going
- may establish friend presence according to current rules

Interested
- does NOT establish friend-presence row if currently excluded

Pending
- excluded

Declined
- excluded

Removed
- excluded

Do not broaden social visibility.

==================================================
16. FRIENDS’ TRIPS LIFECYCLE
==================================================

Preserve current Friends’ Trips eligibility.

Active/legacy current/future behavior remains authoritative.

Completed, Cancelled, and ordinary past-only entries remain excluded where current Friends’ Trips behavior excludes them.

BL-80 terminal history must not become social authorization.

==================================================
17. FRIENDS’ TRIPS DISPLAY UNIT
==================================================

Do NOT paginate raw Trip or participant source rows.

The top-level pagination unit is the COMPLETE existing display row after current friend-centric grouping.

Current grouping identity:

friend
+ destination
+ status

Preserve exact current semantics.

Different friends on the same Trip remain separate friend-centric presences.

==================================================
18. GROUP THRESHOLD
==================================================

Preserve current grouping threshold exactly:

- 1 qualifying trip → individual row
- 2 qualifying trips → individual rows under current behavior
- 3 or more qualifying trips for the same display grouping → one grouped row

Do not change this threshold.

Grouped rows must show:

- complete authorized total
- correct date range
- current labels/content
- existing group-sheet affordance

The count must be COMPLETE, not just loaded-detail count.

==================================================
19. FRIENDS’ TRIPS TOP-LEVEL PAGE SIZE
==================================================

Page after conceptual grouping.

Page size:

10 display rows

Fetch enough lightweight information to determine:

- 10 complete display rows
- whether another display row exists
- deterministic continuation cursor

Do not hydrate every source Trip before taking 10 grouped display rows.

The implementation must genuinely bound underlying source/materialization work.

==================================================
20. TOP-LEVEL ORDERING
==================================================

Codify the current deterministic Friends’ Trips display order.

Use the investigation findings and current behavior to include every ordering component needed, including where applicable:

- month/effective start
- destination
- friend identity
- status
- group identity
- stable final tie-breakers

Do not change visible ordering merely to simplify SQL.

Cursor must encode the complete deterministic ordering key.

==================================================
21. GROUPING CORRECTNESS
==================================================

A top-level page must never:

- split one conceptual grouped row across pages
- partially count a grouped row
- duplicate a grouped row
- omit trips from its complete count/range
- merge different friends into one row
- merge distinct statuses incorrectly

Build deterministic reference output before/alongside implementation so paged output can be compared against current unbounded semantics.

==================================================
22. MONTH / DESTINATION CONTAINER MERGING
==================================================

Existing month and destination hierarchy must remain intact.

If a later page continues an already rendered:

- month
- destination

append into that existing section instead of producing visually duplicated containers.

If a new page begins a new month/destination, create the appropriate hierarchy.

Server fragments may expose boundary metadata if necessary.

Do not redesign the grouping UI.

==================================================
23. FRIENDS’ TRIPS MOUNTAIN FILTER
==================================================

Current destination/mountain filtering must operate over the COMPLETE authorized Friends’ Trips result universe.

Do NOT filter only loaded DOM rows.

The filter options must represent all authorized destinations under current semantics.

Selecting a destination must request complete paginated results for that destination.

Reset pagination when filter changes.

Preserve current labels and empty states.

==================================================
24. GROUPED DETAIL PAYLOAD
==================================================

Remove embedded all-trip detail payloads from grouped rows.

A grouped row should contain only enough server-rendered state to:

- display the summary
- identify the authorized group opaquely
- request grouped details

Do not serialize/embed hundreds of underlying trips into initial HTML.

==================================================
25. GROUPED DETAIL PAGING
==================================================

Opening a grouped row should request the first authorized detail page.

Page size:

20 trip details

If more exist, show Load more within the existing detail sheet.

The detail page must hydrate at most:

- 20 Trips
- 20 Resorts where distinct

Do not hydrate the entire grouped trip history.

==================================================
26. GROUP DETAIL ORDERING
==================================================

Preserve current detail ordering.

Use deterministic keyset pagination based on:

- effective attendance/trip start as appropriate
- Trip ID final tie-breaker
- any other current ordering component required for exact equivalence

Null-date behavior must be explicit and cross-dialect safe.

==================================================
27. GROUP DETAIL AUTHORIZATION
==================================================

Opaque group identity and detail cursor never authorize access.

Every detail request must re-evaluate:

- reciprocal-current friendship
- public visibility
- organizer/Going presence
- lifecycle
- attendance timing
- current destination/status group membership

If a trip no longer qualifies, it must not be returned.

If friendship is removed, the endpoint must fail closed.

==================================================
28. FRIENDS’ TRIPS ENDPOINT
==================================================

Add a focused authenticated endpoint for top-level Friends’ Trips display pages.

Accept:

- optional cursor
- current supported destination/mountain filter

Return:

- server-rendered display-row HTML
- has_more
- next_cursor
- small grouping/container boundary metadata if required
- displayed unit IDs/opaque identities if useful for dedupe

No sensitive raw data.

==================================================
29. GROUP DETAIL ENDPOINT
==================================================

Add a separate authenticated endpoint for grouped details.

Accept:

- opaque validated group identity
- optional cursor

Return:

- server-rendered authorized detail HTML
- has_more
- next_cursor
- authorized Trip IDs if needed for client dedupe

Strictly validate malformed/tampered identifiers.

Invalid values must produce a clear safe error, not an empty successful authorization bypass.

==================================================
30. CURSOR VALIDATION
==================================================

All cursors must be:

- opaque
- URL-safe
- versioned
- section/type scoped
- strictly validated

Reject:

- malformed cursor
- unsupported version
- mismatched endpoint/type
- invalid payload
- impossible ordering shape

Return HTTP 400 for invalid cursor/request shape unless an existing endpoint convention requires another explicit safe status.

Never treat cursor contents as authority.

==================================================
31. BL-158 REUSE
==================================================

Reuse BL-158 conventions where they naturally apply:

- opaque cursor approach
- validation style
- server-rendered fragments
- disabled/loading/retry behavior
- client dedupe
- focus handling

Do NOT:

- refactor BL-158 substantially
- modify BL-158 eligibility/order
- create a generalized pagination framework
- force Friends’ Trips into the My Trips viewer-feed service

Small shared primitives are acceptable only if truly narrow and regression-safe.

==================================================
32. FRIEND DIRECTORY BUDGETS
==================================================

For 10/50/100/500 reciprocal friends:

Initial /friends page:

<=20 displayed reciprocal friend Users
<=20 displayed reciprocal Friend rows

No Trip/Resort/participant ORM collections solely for rendering directory cards.

Visible upcoming-trip counts:

one bounded aggregate retrieval for visible rows

Warm /friends target:

<=14 total SQL statements

Statement count must not grow with total reciprocal friend population.

==================================================
33. FRIENDS’ TRIPS BUDGETS
==================================================

For 10/50/100/500 eligible source/display populations:

Top-level Friends’ Trips page:

<=10 display rows

Hydration target:

<=10 visible friend identities

For individual rows:

<=10 visible Trip/Resort rows

Grouped rows must NOT hydrate/embed all group detail Trips.

Warm direct:

/my-trips?tab=friends

target:

<=11 total SQL statements

Query/cardinality must remain stable across source population growth.

==================================================
34. GROUP DETAIL BUDGETS
==================================================

Grouped detail endpoint:

<=20 hydrated Trip rows
<=20 Resort rows if all distinct

Focused page endpoint target:

<=4 total statements including authentication

No per-detail-row lazy loads.

No ORM/materialization growth based on total group size.

==================================================
35. REFERENCE EQUIVALENCE
==================================================

Create deterministic test-only unbounded reference builders for:

A. reciprocal Friends directory
B. Friends’ Trips display units
C. grouped detail membership

For paginated results:

concatenating all pages must reproduce the reference exactly.

Verify:

- IDs
- order
- grouping
- friend identity/context
- Trip membership
- labels
- links
- counts
- date ranges
- destination/status
- filter output

==================================================
36. FRIENDS BOUNDARY TESTS
==================================================

Cover at minimum:

- 0
- 1
- 19
- 20
- 21
- 40
- 41
- final partial page

Also cover:

- same first names
- same first + last names
- deterministic Friend-ID tie
- letter-header continuation
- filter reset
- search reset
- complete total count
- no duplicates
- no missing friends

==================================================
37. FRIENDS’ TRIPS BOUNDARY TESTS
==================================================

Cover top-level display units at:

- 0
- 1
- 9
- 10
- 11
- 20
- 21
- final partial page

Also test:

- one friend / many trips
- many friends / one trip
- organizer sources
- Going sources
- mixed organizer + participant sources
- same friend/trip duplicate source path
- grouped threshold 1/2/3
- continued month
- continued destination
- destination filter
- no duplicates
- no missing display units

==================================================
38. GROUP DETAIL BOUNDARY TESTS
==================================================

Cover group sizes:

- 3
- 19
- 20
- 21
- 40
- 41
- 500

Verify:

- first page <=20
- subsequent page <=20
- complete group total remains accurate
- concatenated details equal reference
- no duplicated Trip
- no missing Trip
- no unauthorized Trip

==================================================
39. LARGE NETWORK TESTS
==================================================

Use disposable fixtures for:

10
50
100
500

reciprocal friends.

Also:

- 500 friends each owning one qualifying trip
- one friend with 500 same-group qualifying trips
- 500 friends attending one shared trip
- 100 friends with multiple qualifying trips
- mixed organizer/Going sources

Measure:

- SQL statements
- User loads
- Friend loads
- Trip loads
- Resort loads
- participant loads
- rendered display rows
- HTML size where useful

Timing is diagnostic only.

==================================================
40. AUTHORIZATION MUTATION TESTS
==================================================

Between page/detail requests test:

- reciprocal friendship removed
- relationship becomes one-sided
- trip changes public → private
- trip becomes Completed
- trip becomes Cancelled
- trip becomes past/ineligible
- Going changes to Interested
- Going changes to Declined
- Going changes to Removed
- new eligible trip inserted

Every endpoint must re-evaluate current state.

No initial-page authorization snapshot may be trusted later.

==================================================
41. CROSS-DIALECT VALIDATION
==================================================

Verify keyset expressions and ordering under:

- SQLite runtime
- PostgreSQL-compatible compilation/runtime using existing disposable infrastructure where available

Pay particular attention to:

- NULL ordering
- alphabetical ordering
- case handling matching current semantics
- descending/ascending dates
- source/status rank
- composite keyset predicates
- duplicate source dedupe
- grouped boundaries

Do not touch persistent databases.

==================================================
42. OUT OF SCOPE
==================================================

Do not implement:

- Home changes
- Ideas changes
- Friend Profile changes
- Mountain Detail changes
- Trip Detail changes
- Season Snapshot changes
- BL-158 viewer-feed changes
- new friend-search features
- Suggested Friends redesign
- new trip filters
- infinite scrolling
- frontend framework
- generalized pagination framework
- global caching
- schema denormalization

==================================================
43. DATABASE SAFETY
==================================================

Expected:

NO schema change
NO migration
NO index change

Do not:

- create Alembic revisions
- apply BL-70
- mutate Supabase Development
- mutate persistent Replit
- access Production

If any schema/index/migration appears necessary:

STOP and report it separately.

==================================================
44. REGRESSION SUITES
==================================================

Run focused BL-159 tests plus relevant adjacent suites including:

- Friends page
- My Trips
- BL-80
- BL-133
- BL-158
- BL-161 authenticated query budgets
- lifecycle
- attendance
- social visibility
- navigation

Then run the full suite.

Also run:

- py_compile
- git diff --check

==================================================
45. RUNTIME VALIDATION
==================================================

Restart Flask workflow.

Check:

- /friends
- Friends Load more
- Friends search/filter behavior
- Pending Requests
- Suggested Friends
- /my-trips
- /my-trips?tab=friends
- Friends’ Trips Load more
- destination filter
- grouped trip sheet
- grouped detail Load more
- browser console

No browser errors.

==================================================
46. INDEPENDENT REVIEW
==================================================

Before commit perform independent:

- correctness review
- privacy/authorization review
- performance/cardinality review

Verify:

- full concatenation equivalence
- exact grouping semantics
- no duplicates/missing rows
- all filters operate on complete server-side authorized sets
- group details no longer embed full histories
- current-state authorization on every request
- stable query counts
- bounded ORM loads
- BL-158 unchanged
- no schema/index/migration
- no persistent DB writes
- no Production access

If any material issue remains:

STOP.

==================================================
47. COMMIT
==================================================

If implementation and validation pass:

Commit with:

BL-159 Bound Friends retrieval

Exclude unrelated pasted prompt/build assets.

Do not begin another backlog task.

==================================================
RETURN
==================================================

Return:

A. files changed
B. Friends paging architecture
C. reciprocal authorization behavior
D. Friends ordering/cursor
E. friend page size/lookahead behavior
F. Friends search/filter server behavior
G. total-count behavior
H. Pending Requests verification
I. Suggested Friends verification
J. directory trip-count batching
K. Friends’ Trips paging architecture
L. Friends’ Trips eligibility
M. top-level grouping boundary
N. top-level order/cursor
O. destination filter behavior
P. grouped-sheet lazy-loading architecture
Q. grouped-detail cursor/order
R. month/destination append behavior
S. BL-158 reuse
T. boundary-test results
U. 10/50/100/500 directory ORM/SQL measurements
V. 10/50/100/500 Friends’ Trips ORM/SQL measurements
W. 3/20/21/500 grouped-detail measurements
X. reference concatenation equivalence
Y. privacy/lifecycle/RSVP/regression/full-suite/review results
Z. commit hash and whether BL-159 is code-side complete and safe to close

Also explicitly confirm:

- no schema/index/migration
- BL-70 untouched
- persistent Development/Replit databases untouched
- Production untouched