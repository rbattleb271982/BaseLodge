Proceed with #298 exactly as planned.

Do not change scope and do not implement anything.

One additional emphasis for the final report:

When documenting the BaseLodge data graph and especially the final **BASELODGE CONTEXT FOR PRODUCT IDEATION** section, be extremely explicit about the intersections between entities and what the current code/data can already answer or derive.

I do not just want an inventory of fields and features.

I want another AI to understand the actual computational/social graph available inside BaseLodge.

For example, investigate and explicitly state whether the current implementation can determine things such as:

* Which friends share a specific pass
* Which friends share the user's pass
* Which friends are skiers, snowboarders, or both
* Which friends match specific skill levels
* Combinations such as Advanced + Skier + Ikon
* Which friends have visited a specific mountain
* Which friend has visited a mountain the most
* How many times each friend has visited a mountain
* Which friends have a mountain on their wishlist
* Which friends are attending or interested in a trip
* Which friends are available during specific dates
* Whether overlapping availability between multiple friends can be derived
* Which mountains are accessible through a user's passes
* Which mountains are accessible through friends' passes
* Which friends share wishlist mountains
* Which friends share mountain history
* Which friends share future trips
* Which people are connected through mutual friends
* Which network statistics can be calculated from existing profile fields
* What combinations the current Ideas engine already evaluates
* What ranking/scoring inputs already exist
* Which useful intersections are technically possible from existing stored data even if no UI currently exposes them

These examples are not an exhaustive checklist. Discover additional intersections from the actual schema and application logic.

For each meaningful intersection, distinguish:

**LIVE** — already calculated/surfaced
**DERIVABLE** — data exists and current architecture can reasonably calculate it
**PARTIAL** — some required data or logic exists but there are gaps
**NOT CURRENTLY POSSIBLE** — important required data is not stored

Also make sure the audit explains any meaningful temporal/history limitations. For example, distinguish between data that records historical events versus only the user's current state.

The final context section should make it possible for a product strategist to reason about BaseLodge as:

**PEOPLE × CONNECTIONS × PASSES × MOUNTAINS × VISITS × WISHLISTS × AVAILABILITY × TRIPS × GEAR × ACTIVITY**

rather than merely as a collection of screens.

Proceed with the audit and return the completed report in chat.
