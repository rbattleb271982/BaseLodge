1. FIX ONBOARDING RIDER TYPES (CRITICAL)
Problem
During onboarding, the “I am a…” step only shows:
Skier
Snowboarder
But the app supports additional rider types that are already defined elsewhere and should be selectable.
Required Rider Types
Update onboarding to display all supported rider types, including:
Skier
Snowboarder
Both (Skier + Snowboarder)
Other
Rules
Do not hardcode rider types directly in the template.
Reuse the same rider type source / constant / enum that is used in:
Profile editing
Friend profile views
Matching logic
Onboarding and profile editing must stay perfectly in sync going forward.
2. REMOVE “MOUNTAIN SUMMARY — ACTIVE”
Problem
The Home header currently displays:
“Mountain Summary — Active”
This label is unnecessary and confusing.
Required Fix
Remove this label entirely.
Do not replace it with any other label.
Ensure no leftover spacing or empty containers remain.
3. UPDATE HOME HEADER GREETING COPY
Change
Replace:
“Welcome back, Richard 👋”
With:
“Welcome, Richard 👋”
Apply this everywhere the Home greeting appears.
4. TYPOGRAPHY ADJUSTMENT — PROFILE SUMMARY LINE
Target Line
Skier · Ikon · Skill level TBD
(or equivalent values)
Required Fix
Increase font size slightly (one step larger than current).
Keep font weight normal (not bold).
Preserve dot separators.
This line should read as a primary profile summary, not helper text.
5. DEFAULT VALUE FOR “MOUNTAINS VISITED”
Problem
The “Mountains visited” row shows inconsistent placeholder text such as:
“None select…”
Truncated values
Required Fix
When no mountains are selected, always display “None”
No ellipses
No truncation
Apply this consistently to:
More screen
Profile summary
Friend profile views
6. CONSISTENCY & SAFETY CHECKS (DO NOT SKIP)
After implementation:
Onboarding rider types == Profile rider types == Matching logic rider types
No duplicate rider type definitions
No references to “Mountain Summary — Active” remain
No regressions to login, onboarding flow, or navigation
7. VALIDATION STEPS (MANDATORY)
Before finishing:
Create a brand-new user
Go through onboarding
Confirm all rider types appear
Land on Home and verify:
Greeting says “Welcome, Name”
No “Mountain Summary — Active”
Profile summary line is larger and readable
Visit /more and confirm:
Mountains Visited shows “None” by default
IMPORTANT
Do not add new features, copy, or UI beyond what is listed here.
This is a cleanup + alignment pass, not a redesign.