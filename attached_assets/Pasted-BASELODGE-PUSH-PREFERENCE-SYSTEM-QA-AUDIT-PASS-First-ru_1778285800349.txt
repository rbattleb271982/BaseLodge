BASELODGE — PUSH PREFERENCE SYSTEM QA + AUDIT PASS

First run this as an audit/QA task before making any edits.

Only apply small fixes if the audit proves they are necessary.

DO NOT:
- add new architecture
- redesign the system
- add categories
- add inboxes
- add queues/background workers
- add unread counts
- add new DB tables
- redesign push architecture

Use checkpoints sparingly.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
GOAL
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Verify the already-existing BaseLodge push opt-out system works cleanly end-to-end on production/TestFlight.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. USER MODEL
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Confirm:
- push_notifications_enabled exists
- default = TRUE
- migration is safe/idempotent

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2. UI
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Audit:
- Profile page entry point renders correctly
- push_settings.html toggle page works
- wording/status text is consistent
- spacing/layout feels production-ready on mobile
- no broken states or visual regressions

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
3. API
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Validate:
- /api/push/preferences works correctly
- toggle persists after refresh
- toggle persists after logout/login
- invalid payload handling is safe
- no unhandled exceptions

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
4. ONESIGNAL SYNC
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Validate:
- blSetPushEnabled() correctly calls:
  - optOut()
  - optIn()
- no JS console errors
- no race conditions
- no duplicate listeners
- no repeated OneSignal registration loops

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
5. SERVER-SIDE SUPPRESSION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Audit send_onesignal_push:

Confirm:
- opted-out users are suppressed correctly
- opted-in users are NOT accidentally suppressed
- skipped-send behavior is logged clearly
- suppression logic is defensive/safe

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
6. ADMIN BYPASS BEHAVIOR
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Confirm these intentionally bypass preferences:
- /admin/test-push
- /admin/test-push-broadcast

Document this clearly in findings.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
7. BADGE INTERACTION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Validate:

OPTED-IN USERS
- push arrives
- badge appears
- badge clears on app open/resume

OPTED-OUT USERS
- no OneSignal push sent

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
8. REGRESSION AUDIT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Confirm NO accidental impact to:
- APNs direct path
- Android push delivery
- email systems
- auth/session flows
- analytics_head.html startup logic

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ALLOWED FIXES ONLY IF NECESSARY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Allowed:
- wording cleanup
- tiny UI polish
- tiny logging improvements
- defensive null/JS guards
- obvious bug fixes

NOT allowed:
- architecture rewrites
- new services
- queue systems
- new notification frameworks
- schema redesign
- notification inbox systems

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
AFTER QA, PROVIDE:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Exact files touched (if any)
2. What was tested
3. Any bugs found
4. Any fixes applied
5. Remaining risks
6. Whether the system is now production-ready