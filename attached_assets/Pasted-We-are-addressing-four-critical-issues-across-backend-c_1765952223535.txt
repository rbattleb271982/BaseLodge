We are addressing four critical issues across backend correctness and mobile UX.
This work must be completed together, cleanly, and without scope creep.
This prompt supersedes all previous partial prompts.
🔴 ISSUE 1 — INVITE BUTTON CRASH (SCHEMA DRIFT)
Error observed
Clicking the Invite button causes a 500 error:
(psycopg2.errors.UndefinedColumn)
column invite_token.uses_count does not exist
🎯 INTENDED INVITE DESIGN (LOCKED)
Invite links are unique
Invite links are single-use
Invite links exist to prevent spam
Once used, an invite is invalid forever
There is NO multi-use invite functionality
🧠 ROOT CAUSE
Code references uses_count / max_uses
Database schema does not contain uses_count
Code drifted toward a multi-use design that is not intended
This mismatch causes a hard crash
✅ REQUIRED FIX — INVITES (NON-NEGOTIABLE)
STEP 1️⃣ Remove multi-use logic entirely
Remove all references to:
uses_count
max_uses
Any logic that increments or checks usage counts
This includes:
SQLAlchemy models
Queries
Invite validation logic
Templates and conditionals
STEP 2️⃣ Enforce single-use via used_at
Invite validity must be determined only by:
used_at IS NULL
(Optional) expires_at IS NULL OR expires_at > now()
On successful acceptance:
Set used_at = now()
Persist immediately
Any future attempt must fail gracefully
STEP 3️⃣ Align model and schema
Update the InviteToken model so it exactly matches the database table.
Allowed fields only:
id
token
inviter_id
created_at
used_at
expires_at (if present)
❌ Do NOT add new columns
❌ Do NOT leave unused fields in the model
STEP 4️⃣ Fix Invite button crash
Clicking Invite must no longer crash
No SELECTs referencing non-existent columns
No silent exception suppression
STEP 5️⃣ Friendly failure handling
If an invite is invalid or already used:
Show a human-readable message
Do NOT crash
Example:
“This invite link has already been used.”
🟡 ISSUE 2 — ADD TRIP: RESORTS DROPDOWN + VALIDATION
🐛 PROBLEM A — Resorts dropdown does not populate
Required behavior
When a State is selected:
Fetch resorts for that state
Populate the Resorts dropdown dynamically
Reset the resort selection if the state changes
Implementation
Use existing endpoint (e.g. /api/resorts/<state>) if available
Otherwise add a minimal JSON endpoint
JavaScript must:
Listen for state <select> changes
Populate resorts <select>
Disable resorts dropdown until a state is selected
Clear previous resort on state change
🛑 PROBLEM B — “Set this as my home mountain” validation
Required behavior
If:
“Set this as my home mountain” is checked
AND
No resort is selected
Then:
Block form submission
Show a clear inline error
Error copy (use verbatim):
“Please choose a resort before setting it as your home mountain.”
Validation requirements
Frontend JS validation (no alerts)
Backend validation as safety net
Invalid state must never persist
🟢 ISSUE 3 — MOUNTAINS VISITED: NO VISUAL FEEDBACK FOR MULTI-SELECT
🐛 Current problem
User can select multiple mountains
There is no clear visual summary of what’s selected
✅ REQUIRED UX FIX
Add a Selected Mountains summary using pill / chip components.
Behavior
Selected mountains appear as pills above the list
Each pill shows:
Mountain name
✕ remove icon
Tapping ✕ removes the selection
If no mountains are selected:
The pill area is hidden
Rules
Do NOT show “X selected”
Do NOT require scrolling
Mobile-first
Frontend-only (no backend changes)
🔵 ISSUE 4 — EDIT PROFILE PAGE IS NOT MOBILE-FRIENDLY
🐛 Current behavior
“More → Change” opens a desktop-style form
Page is long, cluttered, and overwhelming on mobile
Lacks visual hierarchy
This is unacceptable for a mobile-first app.
🎯 REQUIRED FIX — MOBILE-FIRST LAYOUT CLEANUP
This is layout and presentation only.
Do NOT change what data is collected.
1️⃣ Constrain layout
Add a mobile container with:
Max width
Consistent horizontal padding
Prevent full-bleed desktop stretching
2️⃣ Clear visual sections
Group fields into clearly separated sections:
Rider Info
Passes
Home Mountain
Skill Level
Equipment
Each section should feel distinct and scannable.
3️⃣ Reduce visual noise
Remove redundant labels
Reduce excessive vertical stacking
Improve spacing hierarchy
4️⃣ Save interaction — FLOATING BOTTOM SAVE (REQUIRED)
Implement a floating Save bar at the bottom of the screen:
Always visible on mobile
Clear primary action (e.g. “Save Profile”)
Does not block form fields
Includes safe-area padding (iOS)
Subtle background / shadow separation
This avoids:
Scrolling fatigue
Guessing where to save
Over-dominant buttons mid-form
5️⃣ No redesign / no new flows
🚫 Do NOT:
Add tabs
Add modals
Remove fields
Change backend logic
Change navigation
🧪 ACCEPTANCE CRITERIA (ALL MUST PASS)
Invites
Invite button works
No uses_count references
Single-use enforced
Graceful failure
Works in published app
Add Trip
Resorts populate on state change
Resort resets correctly
Home mountain validation enforced
Inline errors shown
Mountains Visited
Selected mountains always visible
Easy removal
No clutter when empty
Edit Profile
Clean mobile layout
Clear sections
Floating Save works
Page feels intentional, not “admin”
🚫 SCOPE LIMITS
No new models
No schema changes beyond invite fix
No redesigns
No suppressed errors
📝 REQUIRED OUTPUT
After completion, report:
Root cause of invite crash
Files changed
How resorts are fetched
Validation locations
How selected-mountain pills work
How floating Save is implemented