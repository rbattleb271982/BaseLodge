MAKE THE FOLLOWING CHANGES TO THE BASELODGE APP
Use NO checkpoints unless absolutely required.
Modify only the affected areas.
Keep all naming conventions, templates, and display styling consistent.
✅ PART 1 — Add mountains_visited to User model (MUST persist in DB)
1. Modify models.py:
Add a new field under the profile fields:
mountains_visited = db.Column(db.JSON, default=[])
This must be stored as a JSON list (e.g., ["Vail", "Park City", "Palisades Tahoe"]).
2. Generate a migration:
Autogenerate migration
Apply migration
Ensure no existing user profiles break
Existing users should default to empty list []
3. Update save logic in /mountains-visited route:
Replace session storage with persistent model storage:
user.mountains_visited = selected_mountains_list
db.session.commit()
Remove all references to session["selected_mountains"].
4. Update template behavior:
When loading /mountains-visited, pre-check boxes based on user.mountains_visited
Ensure UI continues to support:
State → mountain filtering
Search within selected state
Multi-select
5. Update everywhere that displays mountains:
profile.html
friend_profile.html
Future trip logic does not need changes
If user has no mountains, show "No mountains selected"
✅ PART 2 — Add gear field to User model
1. Modify models.py:
Add:
gear = db.Column(db.String(200))
2. Migration:
Autogenerate migration
Apply migration
3. Update /edit_profile:
Add new input row “Gear”
Save to database exactly like other text fields
4. Update display pages:
profile.html → show Gear row
friend_profile.html → show Gear row
If empty → show "Not specified"
✅ PART 3 — Add Skill Level to Onboarding (Step 3)
1. Update setup_profile.html:
Modify Step 3 to ask:
Home State (dropdown)
Birth Year (scroll dropdown)
Skill Level (segmented buttons or dropdown)
Skill level options:
Beginner
Intermediate
Advanced
Expert
2. Update /setup-profile POST logic:
Add:
user.skill_level = request.form.get("skill_level")
3. Update validation:
All three fields must be required:
home_state
birth_year
skill_level
If any missing → reload Step 3 with error message.
✅ PART 4 — Display + Logic Updates Everywhere Skill + Gear + Mountains Should Appear
Update the following templates:
1. profile.html ("My Info")
Sections must include:
Rider Type
Pass Type
Skill Level
Home State
Birth Year
Gear
Mountains Visited (# count + clickable to edit)
2. friend_profile.html
Display same fields read-only.
3. home.html
Ensure the header line displays:
{Pass Type} — {Rider Type} — {Skill Level}
Example:
Epic — Snowboarder — Advanced
Use larger font as previously discussed.
4. Friend list formatting (friends.html + component):
Each friend row must display inline:
Alex Peak — Snowboarder — Epic — Advanced
5. more.html
Do NOT use > icons — use the new row styling.
✅ PART 5 — Data Migration Safety
Before migrations, ensure:
Users missing birth_year, home_state, or skill_level are allowed to keep NULL values.
New users must complete all onboarding steps.
Do NOT delete existing rows.
Do NOT reset the database unless explicitly commanded.
⚠️ PART 6 — DO NOT BREAK ANYTHING ELSE
Keep all of these intact:
Login / Auth flow
Friends system
Trip system
Navigation bar (Home, Friends, Invite, More)
Styling via static/styles.css
🚀 DELIVERABLES
When finished, reply with:
Confirmation migrations ran correctly
All updated code blocks
Screenshots or HTML dumps of updated templates
Verification that:
Mountains persist after logout
Gear displays properly
Skill level appears in onboarding
Friend rows show inline formatting
My Info + More screens use Carv-style sectioned rows