Fix the Equipment Setup page based on the diagnostic report.

Goal:
Make the rent toggle, brand dropdown, and save flow actually work in the iOS/Capacitor WebView and browser.

Do these surgical changes only:

1. In templates/settings_equipment.html:
- Move the embedded JavaScript out of the {% block scripts %}.
- Place it at the very bottom of {% block content %}, after all Equipment page HTML and just before {% endblock %}.
- Remove the DOMContentLoaded wrapper.
- Run the setup code directly after the DOM elements exist.
- Keep null guards, but do not allow them to silently fail without console logging.

2. Brand dropdown:
- Ensure initBrands() runs immediately after the script loads.
- Ensure #primary-brand is populated with the correct options:
  - Skier → ski brands
  - Snowboarder → snowboard brands
- Ensure boot brand options populate too.
- The Brand dropdown must never open with “No Options” or be blank.
- If options cannot load, fall back to a regular text input instead of a dead dropdown.

3. Rent toggle:
- Ensure #pill-own and #pill-rentals both get click handlers.
- Tapping “I rent” must immediately:
  - set hidden equipment status input to needs_rentals
  - apply active maroon style to “I rent”
  - remove active state from “I have my own equipment”
  - hide the gear fields
  - show the rental confirmation message
- Tapping “I have my own equipment” must reverse this immediately.

4. Save flow:
- Update /settings/equipment/save in app.py so it reads equipment_status from the form.
- Save current_user.equipment_status in the same route/commit as equipment setup.
- If equipment_status == needs_rentals:
  - save that status
  - do not require discipline or gear fields
  - return {"success": true}
- If equipment_status == have_own_equipment:
  - save status and gear fields together as currently intended.

5. Do not rely on the separate /settings/equipment-status route for the main save flow anymore.
- Leave the old route in place if other code uses it.
- But this page’s Save button should submit status through /settings/equipment/save.

6. Preserve:
- Current page design
- Cream background
- SKIS/BOARD logic
- Bottom nav
- Back button
- Save button styling

7. Test:
- Load page
- Tap “I rent” and confirm it visually changes immediately
- Tap “I have my own equipment” and confirm it changes back
- Tap Brand and confirm options appear
- Save while renting
- Reload page and confirm renting state persists
- Save with own equipment
- Reload page and confirm brand/status persists
- Confirm Home no longer shows “Add your gear” when user saved “I rent”

Do not redesign the page.
Do not create unnecessary checkpoints.