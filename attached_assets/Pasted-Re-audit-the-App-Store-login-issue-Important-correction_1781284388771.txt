Re-audit the App Store login issue. Important correction: Google authentication was not enabled when this user created their beta account, and they are using the same email account. Do not assume this is a Google OAuth/auth_provider mismatch.

Problem:
A prior beta tester downloaded the production App Store version of BaseLodge and cannot log in with the same email/password they used during beta.

Known facts:
- App Store and TestFlight appear to point to the same backend: https://app.baselodgeapp.com
- Same production Supabase PostgreSQL database appears to be used
- The user says they are using the same email account
- Google auth was not enabled at the time, so this is likely not a Google OAuth issue

Please investigate the real cause.

Check:

1. Confirm the user's record exists
- Use the new admin/user-lookup endpoint or database query
- Confirm:
  - email
  - auth_provider
  - has_password
  - created_at
  - last_active_at
  - login_count
  - is_verified
  - is_seeded
  - lifecycle_stage

2. Compare beta-era auth behavior vs current auth behavior
- Inspect the signup/login/password hashing code history if available
- Look for changes to:
  - password hashing method
  - password field name
  - password length/validation
  - email normalization
  - seeded/test account behavior
  - verification requirements
  - app-only vs web login handling

3. Check whether old beta users had valid password_hash values
- Identify whether password_hash is NULL, malformed, generated by an older method, or incompatible with the current check_password logic
- Compare a working current user vs this beta user

4. Check production logs for this user's login attempt
- Find the exact failure reason if logged
- Confirm whether the request reaches POST /auth
- Confirm whether the failure is:
  - user not found
  - password mismatch
  - missing password_hash
  - CSRF/session issue
  - mobile Safari/App Store webview cookie issue
  - account status issue

5. Check whether App Store build has any cookie/session/login differences
- Confirm login POST works from native iOS App Store shell
- Check SameSite, Secure, domain, session cookie, redirect, and Capacitor webview behavior
- Compare with browser login using the same account

6. Improve diagnostics if needed
- Add safe temporary logging around failed login attempts:
  - email found: yes/no
  - has_password: yes/no
  - auth_provider
  - password_hash prefix/type only, not full hash
  - check_password result
  - user agent/source
- Do not log raw passwords or full password hashes.

Return:
- The actual root cause
- Whether this affects all beta users or only some
- Whether users need password resets, account migration, or a code fix
- The safest production fix
- Any QA steps to confirm the fix