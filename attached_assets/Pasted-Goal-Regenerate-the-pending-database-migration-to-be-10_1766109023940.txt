Goal
Regenerate the pending database migration to be 100% non-destructive, preserving all existing production data — especially ski_trip rows — while still applying the intended schema changes.
🔴 Critical Requirements (Must Follow)
DO NOT truncate any tables
Explicitly remove:
truncate table "ski_trip" cascade;
All existing ski_trip rows must be preserved.
Do NOT add NOT NULL columns to tables with existing rows unless a default is provided
Specifically:
ski_trip.trip_duration must be nullable for now.
Regenerate the migration after fixing the model definitions so the generated SQL is safe.
✅ Required Schema Adjustments
Apply the following changes without data loss:
1. resort table
Add country column:
ADD COLUMN country varchar(2);
Increase state column length:
ALTER COLUMN state TYPE varchar(50);
2. ski_trip table
Add new columns nullable:
ADD COLUMN trip_duration varchar(20);
ADD COLUMN trip_equipment_status varchar(20);
⚠️ Do not make trip_duration NOT NULL in this migration.
3. user table
Add:
ADD COLUMN equipment_status varchar(20);
4. Dropping NOT NULL constraints
These are acceptable and should remain if already intended:
ALTER COLUMN ... DROP NOT NULL;
(Keep existing behavior for email preferences, lifecycle fields, etc.)
🧠 Implementation Guidance
If SQLAlchemy models currently define:
trip_duration = db.Column(db.String(20), nullable=False)
→ change to:
trip_duration = db.Column(db.String(20), nullable=True)
Regenerate migrations after adjusting models.
If Replit tries to introduce TRUNCATE again, stop and reassess — that indicates an unsafe migration strategy.
✅ Validation Checklist (Must Confirm Before Publish)
 No TRUNCATE statements in migration
 No NOT NULL added to columns with existing data
 All existing ski_trip rows remain intact
 Resort dropdowns still work
 App boots with no errors
Outcome
After this change:
Production data is preserved
Europe + country modeling remains correct
Schema is future-proofed for later backfills
You avoid irreversible data loss