Implement BL-122 / Task #387 exactly as investigated.

Scope this change only to the existing equipment measurement handling for `length_cm` and `width_mm` in:

* `/settings/equipment/save`
* `/profile/equipment`

Requirements:

1. Add or reuse a small shared nullable-integer validation helper.
2. Preserve current behavior where:

   * missing/null values clear the optional field to NULL;
   * empty strings clear the optional field to NULL;
   * numeric strings such as `"170"` save successfully as integers.
3. Reject malformed nonempty values such as:

   * alphabetic text;
   * decimal values;
   * scientific notation;
   * mixed text/numbers.
4. Enforce these broad sanity bounds:

   * `length_cm`: 50–250 inclusive;
   * `width_mm`: 50–400 inclusive.
5. Validate both measurement fields before mutating an existing EquipmentSetup, creating/flushing a new one, or committing anything.
6. Invalid creates must leave no persisted setup row.
7. Invalid updates must leave the existing saved values unchanged.
8. Preserve each endpoint’s existing JSON error shape:

   * settings endpoint: `{"error": "..."}`
   * profile endpoint: `{"success": false, "error": "..."}`
     Return HTTP 400 for invalid measurement input.
9. Preserve existing authentication, CSRF, ownership filtering, transaction behavior, and success response shapes.
10. Do not change the schema, database constraints, UI, dependencies, or native application.
11. Do not modify `setup_id`, `boot_flex`, `purchase_year`, unrelated JSON type handling, or other numeric routes as part of BL-122.

Add route-level regression tests for both endpoints covering both measurement fields, including:

* malformed text;
* decimal/scientific/non-integer text;
* below-minimum and above-maximum values;
* invalid create with no persisted row;
* invalid update with unchanged existing database state;
* numeric-string success;
* empty/null clearing;
* existing success response shape;
* ownership behavior.

Run the relevant tests plus the broader test suite needed to verify there are no regressions.

Do not make unrelated cleanup changes.

When complete, report:

* exactly what behavior changed;
* tests added and results;
* whether any files outside the expected BL-122 scope changed;
* confirmation that no migration, native build, dependency change, or data backfill is required.

Final commit message must include BL-122.
