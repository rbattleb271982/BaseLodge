# Batch 2 UX/UI — Planning Only

We are resuming the BaseLodge UX/UI work after the database recovery incident.

IMPORTANT CONTEXT:
- Batch 1 has already been implemented and is intact in the current local workspace.
- Do NOT redo, revert, modify, or "clean up" Batch 1.
- Batch 2 findings/scope were previously documented and approved, but Batch 2 implementation was interrupted before code changes were made.
- The current local workspace is the source of truth.
- The recovered Supabase database is connected and working.
- Startup migrations have been safely re-enabled.
- This task is PLANNING ONLY.

DO NOT modify any files.
DO NOT run tests.
DO NOT start/restart the application.
DO NOT modify Secrets.
DO NOT query or modify the database.
DO NOT commit, push, publish, reset, rebase, cherry-pick, or otherwise alter Git history.

## Objective

Recover the exact previously approved Batch 2 UX/UI scope from the existing project documentation/attached assets, verify each proposed change against the CURRENT source code, and produce a precise implementation plan for my approval.

Do not invent a new Batch 2.

The goal is to continue the design work exactly where we left off before the database incident.

## Step 1 — Recover the previously approved Batch 2 scope

Search the repository, attached_assets, planning/task documentation, and relevant local commit history for the Batch 2 UX/UI findings and approval documentation.

Pay particular attention to:

- commit `dc00394` — "Add Batch 2 findings approval documentation"
- any attached asset introduced by that commit
- Batch 1 audit documentation if it provides context for the batching system
- any planning notes describing Batch 2
- any documentation identifying findings as approved, deferred, rejected, or assigned to another batch

Extract the EXACT Batch 2 items that had been approved.

For each item report:

1. Finding/change
2. Screen/page/component affected
3. Original rationale
4. Original proposed solution
5. Whether it was explicitly approved
6. Any constraints or exceptions that were documented

Do not expand the scope beyond what was previously approved.

If something is ambiguous, flag it as ambiguous rather than guessing.

## Step 2 — Compare the approved Batch 2 scope against current code

Inspect the CURRENT versions of every template, component, CSS file, JS file, or Python route relevant to those approved findings.

Remember that Batch 1 has already modified many of these files.

For each Batch 2 item determine:

- Is the issue still present?
- Did Batch 1 already solve any portion of it?
- What exact current file(s) would need modification?
- What current classes/components/layout structures are involved?
- Could the proposed Batch 2 change conflict with or undo a Batch 1 change?
- Does the implementation require template-only changes, CSS-only changes, JS changes, backend changes, or some combination?

Do not assume the old plan can simply be applied verbatim. Reconcile the approved intent against the CURRENT code while preserving that intent.

## Step 3 — Protect Batch 1

Identify the Batch 1 changes in the same files Batch 2 would touch.

For every overlapping file, explicitly state:

- what Batch 1 changed there
- what Batch 2 proposes to change
- how the Batch 2 implementation can be made without regressing Batch 1

If an approved Batch 2 item now conflicts with Batch 1, flag it for my decision rather than choosing one automatically.

## Step 4 — Produce the proposed Batch 2 implementation plan

Give me a numbered implementation plan.

For EACH proposed change include:

### Item
Short descriptive name.

### Current problem
What exists in the current UI/code.

### Approved Batch 2 change
What the previously approved plan says should change.

### Files
Exact files expected to change.

### Implementation
Describe precisely what code/template/CSS behavior would change.

Do not write the code yet.

### Batch 1 regression risk
LOW / MEDIUM / HIGH

Explain why.

### Functional regression risk
LOW / MEDIUM / HIGH

Explain whether this could affect forms, routes, JavaScript behavior, authentication, navigation, data rendering, or other application functionality.

### Validation
Explain exactly how we should verify this item after implementation.

## Step 5 — Scope summary

At the end provide:

1. Total number of approved Batch 2 items recovered
2. Total number still requiring implementation
3. Number already effectively resolved by Batch 1, if any
4. Number requiring clarification, if any
5. Exact list of files expected to change
6. Confirmation whether any Python/backend code needs to change
7. Confirmation whether any JavaScript needs to change
8. Confirmation whether any database/schema/data work is involved
9. Overall Batch 1 regression risk
10. Recommended implementation order

## Hard safety rules

This is investigation and planning only.

DO NOT:

- edit files
- create files
- delete files
- run tests
- run BaseLodge
- restart BaseLodge
- access either Supabase database
- modify Secrets
- alter environment variables
- run migrations
- commit
- push
- publish
- reset Git
- checkout another commit
- cherry-pick
- rebase

Source inspection and read-only Git inspection are allowed.

## Final line

End the report with exactly:

BATCH 2 PLAN READY — NO CHANGES MADE

Then stop and wait for approval.