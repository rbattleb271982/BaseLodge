BASELODGE — TIGHTEN PLAN A TRIP VERTICAL RHYTHM + PRESERVE MOUNTAIN ON VALIDATION ERRORS

This is a layout polish + UX continuity cleanup.

Do NOT redesign the page.

━━━━━━━━━━━━━━━━━━━━
1. REDUCE EXCESSIVE VERTICAL WHITESPACE
━━━━━━━━━━━━━━━━━━━━

After removing instructional copy, the page now has too much empty vertical space.

Tighten the vertical rhythm throughout the Plan a trip page.

Reduce vertical spacing around:
- mountain search area
- selected mountain card
- calendar section
- staged date list
- Planning/Going segmented control
- sharing checkbox row

Target:
~30–40% less vertical whitespace overall.

The page should feel:
- tighter
- calmer
- more intentional
- more app-native

Do NOT:
- make it cramped
- reduce tap target sizes
- change typography system
- change colors
- redesign components

━━━━━━━━━━━━━━━━━━━━
2. REMOVE UNNECESSARY DIVIDER LINES
━━━━━━━━━━━━━━━━━━━━

The horizontal divider lines now exaggerate the empty space.

Reduce/remove unnecessary dividers where appropriate.

Prefer:
- fewer separators
- more natural grouping through spacing

Keep the page visually clean and editorial.

━━━━━━━━━━━━━━━━━━━━
3. REMOVE REDUNDANT SEARCH HELPER TEXT
━━━━━━━━━━━━━━━━━━━━

Remove:

Type a mountain name or state — e.g. "Vail" or "Colorado"

The placeholder text already explains the interaction.

Preserve the existing search behavior.

━━━━━━━━━━━━━━━━━━━━
4. COMPRESS STATUS + SHARING SECTIONS
━━━━━━━━━━━━━━━━━━━━

The Planning/Going segmented control should feel visually connected to the surrounding content instead of floating alone.

Reduce:
- top margin before the segmented control
- bottom margin after it

The “Friends can see this trip” checkbox should feel like a compact settings row, not a large standalone section.

Tighten:
- row padding
- spacing around the checkbox row

Do NOT change functionality.

━━━━━━━━━━━━━━━━━━━━
5. PRESERVE SELECTED MOUNTAIN ON DATE VALIDATION ERRORS
━━━━━━━━━━━━━━━━━━━━

Current issue:

If the user:
- selects a mountain
- selects dates
- hits an overlap validation error

…the page clears/reset state in a way that forces the user to re-select the mountain again.

That is frustrating UX.

Fix:
If a date validation error occurs:
- preserve the selected mountain
- preserve the search state
- preserve already-staged valid date ranges
- only reject the invalid range attempt

The user should NOT have to:
- re-search the mountain
- re-select the mountain
- rebuild the entire trip

This applies especially to:
- overlap validation
- duplicate range validation
- server-side validation re-renders

━━━━━━━━━━━━━━━━━━━━
6. GUARDRAILS
━━━━━━━━━━━━━━━━━━━━

Do NOT:
- redesign the page
- change trip creation logic
- remove batch/group flow behavior
- change calendar interaction model
- change CTA logic
- introduce Tailwind
- do dead CSS cleanup yet

This is a polish + continuity pass only.

Use checkpoints sparingly.

Do not finish until you have tested:
- overlap validation preserves selected mountain
- duplicate validation preserves selected mountain
- staged date ranges persist after validation errors
- Planning/Going still works
- sharing checkbox still works
- mobile spacing feels tighter and cleaner