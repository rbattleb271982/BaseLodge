PUT THIS IN AGENT

Goal: Add a single protected route that deletes ALL database records tied to 
- "richardbattlebaxter@gmail.com"
- "jonathanmschmitz@gmail.com"

This includes:
- User records
- Friend relationships (both directions)
- SkiTrip records
- Any relational objects tied to these users
- mountains_visited or similar fields (if applicable)

Do NOT delete other users or the entire database.
Make route admin-protected.

Use minimal checkpoints.

------------------------------------------------------------
1. ADD ADMIN CHECK HELPER IF NOT ALREADY PRESENT
------------------------------------------------------------

In app.py, ensure this exists:

from functools import wraps

def admin_required(f):
    @wraps(f)
    def wrapper(*args, **kwargs):
        admin_email = "richardbattlebaxter@gmail.com"
        if not current_user.is_authenticated or current_user.email != admin_email:
            return "Admin privileges required.", 403
        return f(*args, **kwargs)
    return wrapper

------------------------------------------------------------
2. ADD THE DELETE ROUTE
------------------------------------------------------------

In app.py, add:

@app.route("/delete-account-data")
@login_required
@admin_required
def delete_account_data():
    target_emails = [
        "richardbattlebaxter@gmail.com",
        "jonathanmschmitz@gmail.com"
    ]

    deleted_summary = {}

    for email in target_emails:
        user = User.query.filter_by(email=email).first()
        if not user:
            deleted_summary[email] = "User not found"
            continue

        # Delete SkiTrip records
        SkiTrip.query.filter_by(user_id=user.id).delete()

        # Delete Friend connections (both directions)
        Friend.query.filter_by(user_id=user.id).delete()
        Friend.query.filter_by(friend_id=user.id).delete()

        # Clear mountains_visited if stored in model
        if hasattr(user, "mountains_visited"):
            user.mountains_visited = []
        
        # Finally delete the user
        db.session.delete(user)
        db.session.commit()

        deleted_summary[email] = "All associated data deleted"

    return {
        "status": "success",
        "details": deleted_summary
    }

------------------------------------------------------------
END