PUT THIS IN AGENT

Make a final product polish pass on /season-snapshot to align it with the rest of the app and improve readability for real-world screenshots.

Do not change backend logic.
Do not change grouping logic.
Do not change trip data.
Do not redesign the card structure.
Only update layout, formatting, and typography where specified.

---

REQUIRED CHANGES

1. Add bottom navigation (global app consistency)

- Add the standard bottom navigation to this page:
  Home, Trips, Ideas, Friends, Profile
- Use the existing shared component (templates/components/bottom_nav.html)
- It should behave exactly like other logged-in pages
- Ensure it does NOT visually interfere with the screenshot card:
  - keep clear spacing between the card and the nav
  - nav should feel like app chrome, not part of the artifact

---

2. Add page title

- Add a page title at the top:
  Season Snapshot

- Follow the same style as other pages (e.g., Trips, Friends, Profile)
- Place it ABOVE the instruction text
- Maintain proper spacing so hierarchy reads:

  Season Snapshot
  Take a screenshot of the card below
  Share your season wherever you want

---

3. Fix identity formatting

- Replace comma-separated values:
  Skier,Snowboarder

- With:
  Skier + Snowboarder

- Ensure formatting is clean and consistent with identity_line output
- Keep the rest of the identity structure intact:
  SKIER + SNOWBOARDER · INTERMEDIATE · EPIC · IKON

---

4. Improve font size for screenshot readability

The card must be readable when:
- screenshotted
- viewed in iMessage
- viewed in Instagram feed

Adjust typography accordingly:

- Increase headline slightly if needed
- Increase trip row font size slightly
- Increase date size slightly
- Increase identity line slightly

Do NOT:
- break hierarchy
- make it feel oversized
- change font families

Goal:
👉 everything should be legible at ~60–70% scale

---

5. Keep card clean (no UI noise)

Ensure:
- NO status labels (GOING, PLANNING, etc.)
- NO buttons inside the card
- NO navigation inside the card
- NO app-like elements

The card must remain a pure share artifact

---

6. Preserve existing design decisions

Do NOT change:
- cream card surface
- typography style
- “— on BaseLodge —”
- month abbreviations (DEC, JAN, FEB, etc.)
- date formatting (Dec 18 — 22, Dec 27 — Jan 2)
- trip grouping
- capture framing
- instruction text
- background treatment

---

7. Maintain composition

Ensure final layout reads:

[Season Snapshot]        ← page title
Take a screenshot...
Share your season...

[card — centered, prominent]

Back to Trips (optional, keep subtle)

[bottom nav]

Spacing should feel balanced and intentional.

---

After updating, return:

1. Confirmation bottom nav is added and positioned correctly
2. How page title was added and styled
3. Confirmation identity now uses "+"
4. What font size adjustments were made
5. Confirmation card readability improved for screenshots
6. Confirmation no UI elements were added inside the card
7. Any assumptions made