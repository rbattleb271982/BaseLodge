REPLIT: MAX + PLAN
Estimated Batch completion after this step: ~20%

TASK: BL-175 — Lazy-load Friends social metadata and suggestions

INVESTIGATE ONLY.
Do not implement yet.

CONTEXT

The Friends surface has already received:

- BL-133 canonical authenticated social visibility
- BL-159 bounded/paginated Friends retrieval
- BL-173 targeted Friends refreshes

BL-175 was originally defined to defer secondary Friends intelligence after the primary list became bounded.

GOAL

Determine what secondary Friends-page work still blocks the initial render today and identify the smallest safe lazy-loading improvement.

Focus on secondary data such as:

- trip/social context attached to friends
- availability overlap/context
- suggested-friend intelligence
- counts or metadata that are not required to render the primary bounded friend directory

INVESTIGATE

1. Trace the current authenticated Friends page request after BL-159 and BL-173.

2. Inventory all database/service work required for:
   A. primary Friends directory
   B. incoming requests
   C. suggestions
   D. trip/social metadata
   E. availability/pass/rider/skill metadata
   F. any other secondary intelligence

3. Identify which work blocks the initial HTML response.

4. Measure or characterize the current query/work split for:
   - initial Friends render
   - first bounded Friends page
   - suggestions
   - secondary social intelligence

Use focused instrumentation only.

5. Classify each piece as:
   A. required for first render
   B. safe to lazy-load after first render
   C. already lazy/bounded and should remain unchanged

6. Determine whether BL-173's shared targeted-refresh utility can be reused for lazy-loaded canonical regions.

Prefer reuse over a new frontend fetching architecture.

7. Preserve:
   - BL-133 authorization/visibility
   - BL-159 stable paging, filtering, ordering, and bounded retrieval
   - BL-173 targeted refresh behavior
   - active Friends tab/filter/search state
   - browser/Capacitor behavior

8. Confirm lazy loading will not:
   - fetch the full friend universe
   - increase paging/query budgets
   - expose data before authorization is rechecked
   - duplicate suggestion or availability logic in JavaScript
   - cause layout/state resets after the secondary region arrives

9. Determine whether suggestions should load:
   - immediately after primary render
   - only when their tab/section becomes visible
   - or remain synchronous because the current cost is already negligible

Base that decision on the current implementation, not the old task assumption.

DO NOT

- implement anything
- redesign Friends
- change Friends product rules or copy
- change paging size/order/filter semantics
- introduce a client-side state store
- add a frontend framework
- change schema/index/migrations
- touch Production or persistent data
- run the full test suite
- perform broad stress/browser testing

DELIVERABLE

Return:

A. current initial-render work breakdown
B. what remains synchronous
C. what is already bounded/lazy
D. actual remaining performance problem, if any
E. safe lazy-load candidates
F. regions/endpoints needed
G. whether BL-173 utility can be reused
H. authorization/privacy considerations
I. paging/filter/state considerations
J. exact narrow BUILD scope
K. focused tests required
L. files likely to change
M. schema/migration requirement
N. recommended BUILD tier

If BL-159/BL-173 already eliminated most of the original BL-175 problem, say so and recommend shrinking or closing the task rather than inventing work.

Do not implement during PLAN.
Do not run the full suite.