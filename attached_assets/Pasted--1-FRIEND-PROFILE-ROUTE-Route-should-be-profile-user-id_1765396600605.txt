✅ 1. FRIEND PROFILE ROUTE
✔️ Route should be:
/profile/<user_id>
Why?
Consistent with industry patterns
Easy to extend
Avoids creating redundant routes
✔️ Yes — include a Back button
Place it at the top of the friend profile page, above the Summary Card:
<a href="/friends" class="back-link">← Back to Friends</a>
This should be a simple text link styled like a mobile back button.
✅ 2. TRIP CARDS (Friend Profile View)
✔️ Show all trips, grouped into two sections:
Upcoming Trips
Past Trips
✔️ Sorting:
Automatically sort upcoming trips by start_date ascending
Sort past trips by start_date descending
❌ No filters needed here
The friend profile is for viewing, not filtering.
✅ 3. NAVIGATION ACTIVE STATE
✔️ Make nav highlight active route dynamically in Jinja
Example:
<a href="/profile" class="{% if request.path.startswith('/profile') %}active{% endif %}">
  Profile
</a>
No JavaScript needed.
✔️ The rule:
Home = active if path starts with /home
Trips = active if /trips
Friends = active if /friends
Profile = active if /profile AND not editing a friend’s profile
Friend profile pages should NOT activate Friends nav UNLESS you want them to
My recommendation:
✔️ Activate Friends when viewing a friend's profile.
So:
{% if '/profile/' in request.path and user_is_viewing_friend %}
   Friends is active
{% endif %}
✅ 4. SEGMENTED CONTROLS
✔️ Replace dropdowns with segmented buttons in BOTH:
/profile (edit profile page)
Onboarding / Setup profile page (if exists)
✔️ Selected button visual style:
Background color: brand red #8F011B
Text color: white
Unselected buttons: #F7F7F7 background + dark text
Border for all buttons: 1px #DDD
Example:
.segmented-control button.active {
    background: #8F011B;
    color: white;
}
✅ 5. FRIEND CARDS DATA ATTRIBUTE
✔️ Yes — add the data-pass attribute to every friend card in friends.html.
Example:
<div class="friend-card" data-pass="{{ friend.pass_type }}">
Required so filtering works instantly.
✅ 6. CURRENT vs NEW PROFILE PAGE
✔️ /profile stays as your own profile
Edit fields
Summary card with progress bar
Settings
Invite button
✔️ /profile/<user_id> becomes the friend profile
Same summary card (no edit/progress/settings)
Back button
Upcoming trips
Past trips
✔️ /home should remain your dashboard (if it exists)
If you currently do NOT have a /home route, then skip it — do not create anything new.
Summary:
Route	Purpose
/profile	Logged-in user's own profile (editable)
/profile/<user_id>	Friend’s profile (view only)
/friends	Friend list + filters
/home (if exists)	Landing dashboard
Everything stays consistent.