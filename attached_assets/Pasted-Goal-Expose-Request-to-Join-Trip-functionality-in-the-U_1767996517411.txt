Goal:
Expose “Request to Join Trip” functionality in the UI by extending the existing Invite toggle. This work is UI-only and should reuse existing backend logic that is already implemented and validated.

--------------------------------
STRICT SCOPE RULES
--------------------------------

- ❌ Do NOT modify backend logic
- ❌ Do NOT modify models or schemas
- ❌ Do NOT add routes
- ❌ Do NOT add notifications
- ❌ Do NOT add the “Request to Join” button yet
- ❌ Do NOT change My Trips
- ❌ Do NOT refactor unrelated UI

This task is limited to rendering and handling existing REQUEST-type invites in the Invite toggle UI.

--------------------------------
1) Invite Toggle: Rendering
--------------------------------

In the existing Invite toggle for a trip:

- Render BOTH:
  - OUTBOUND invites (existing behavior)
  - REQUEST invites (new)

Do NOT split into separate sections.

For REQUEST invites:
- Display requester name + avatar
- Display a small label/badge:
  “Requested to Join”

Ordering:
- Sort all invites (OUTBOUND + REQUEST) by most recent activity.

--------------------------------
2) Permissions
--------------------------------

- Only the trip owner may see Accept / Decline buttons.
- Accepted participants may see REQUEST invites in read-only mode.
- Non-participants should not see the Invite toggle at all (existing behavior).

--------------------------------
3) Actions
--------------------------------

Accept:
- Calls existing accept-invite logic
- Updates UI state immediately on success
- Requester becomes a participant (already handled backend-side)

Decline:
- Calls existing decline-invite logic
- Updates UI state immediately on success

No confirmation modals required.

--------------------------------
4) UI States
--------------------------------

- Pending REQUEST → visible with Accept / Decline (owner only)
- Accepted REQUEST → handled by existing accepted-invite UI
- Declined REQUEST → removed or shown consistent with existing declined behavior

--------------------------------
5) Guardrails
--------------------------------

- Do not introduce new copy beyond “Requested to Join”
- Do not add new tabs, toggles, or pages
- Do not touch legacy GroupTrip templates
- Keep styling consistent with existing Invite UI

--------------------------------
6) Validation
--------------------------------

After implementation, confirm:
- Owners can see and act on join requests
- Participants can see requests read-only
- Existing outbound invite behavior is unchanged
- No console errors
- No backend calls beyond existing endpoints

Use checkpoints sparingly.