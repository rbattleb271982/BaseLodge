Perform a consolidated polish update across Friends-related views to ensure consistent identity display (first + last name), consistent equipment display, and visibility of ride space availability on friend trips.

This is a UI consistency and correctness update only. Do not add new features or change database schema.

⸻

SECTION 1: Friend profile page
	1.	Full name display
On the friend profile page, display the friend’s full name (first name + last name) at the top of the profile.

Rules:
	•	Concatenate first_name and last_name from the User model
	•	If last_name is missing or empty, display first_name only
	•	Do not show extra spaces or placeholders

This should be the primary name shown on the friend profile.
	2.	Equipment display
Display the friend’s Equipment on their profile page using the same format, placement, and styling as the current user’s own profile card.

Rules:
	•	Reuse the same equipment field(s) and helper logic used on the user profile card
	•	Do not create new fields
	•	Do not duplicate logic
	•	If the friend has no equipment selected, display “Equipment · Add equipment”
	•	Do not add edit controls
	•	Do not redesign the card

Placement:
	•	Equipment should appear alongside identity attributes (rider type, pass, skill level)
	•	Maintain existing spacing and hierarchy

⸻

SECTION 2: Friends’ Trips view
	3.	Full name in Friends’ Trips
When viewing a friend’s trips (the Friends’ Trips view), display the friend’s full name (first name + last name) wherever the friend’s name appears.

Examples:
	•	“Jonathan Schmitz’s trips”
	•	Name headers showing “Jonathan Schmitz”

Fallback:
	•	If last_name is missing or empty, display first_name only

Do not change how the current user’s own trips are labeled.

⸻

SECTION 3: Friend trip detail page
	4.	Space availability indicator
When viewing a friend’s trip detail page, show whether the trip has space for additional people.

Placement:
	•	In the Ride / Trip Participation section
	•	Near existing ride or participation information

Data source:
	•	Reuse existing trip capacity / ride logic already present in the app
	•	Do not introduce new database fields

Display logic:
	•	If space is available: show “Has space for more” (or existing equivalent copy)
	•	If the trip is full: show “Trip is full” (or existing equivalent copy)

Rules:
	•	This is informational only
	•	Do not add join buttons or CTAs
	•	Do not change ride eligibility logic

⸻

SECTION 4: Non-goals
	•	Do not redesign friend cards
	•	Do not add or modify filters
	•	Do not add sorting
	•	Do not change database schema
	•	Do not refactor ride or trip logic
	•	Do not modify onboarding or edit flows

⸻

SECTION 5: Testing requirements
	•	Friend profile displays first and last name correctly
	•	Friend profile displays equipment correctly with fallback text
	•	Friends’ Trips view shows full name
	•	Friend trip detail page shows space availability correctly
	•	No regressions on the user’s own profile or trips
	•	Mobile layout remains intact

⸻

Agent instructions
	•	Make the smallest possible changes
	•	Reuse existing helpers and template logic
	•	Use minimal checkpoints
	•	Fully test before marking complete
	•	Do not refactor unrelated code
