Implement full Android push notification support for BaseLodge using Firebase Cloud Messaging (FCM) while preserving the existing working APNs-direct iOS implementation.

Context:
- iOS push notifications already work in production using direct APNs.
- Capacitor PushNotifications is already installed.
- android/app/google-services.json already exists.
- FIREBASE_SERVICE_ACCOUNT_JSON secret already exists in Replit.
- Existing push token registration + APNs infrastructure already exists.
- Existing admin route: /admin/test-push
- Existing model: PushDeviceToken

Critical rules:
- Do NOT break or rewrite the working iOS APNs flow.
- iOS must continue using APNs only.
- Android must use Firebase Cloud Messaging.
- Preserve existing listener timing behavior (listeners before register()).
- Never log full push tokens or secrets.
- Use checkpoints sparingly only if absolutely necessary.

Implementation requirements

1. Frontend push registration updates
Inspect the existing push registration flow in:
templates/components/analytics_head.html

Update it so:
- platform detection is native-aware
- iOS registers as:
  platform: "ios"
- Android registers as:
  platform: "android"

Requirements:
- Keep existing registration listener ordering
- Keep Capacitor native-platform detection
- Continue posting to /api/push/register-token
- Only send apns_environment for iOS
- Android should not send APNs-specific fields

2. Backend token registration support
Update backend token registration logic so:
- Android tokens are accepted cleanly
- platform="android" is fully supported
- Existing iOS behavior remains unchanged
- Duplicate active token handling works correctly
- Existing token lifecycle/deactivation behavior is preserved

3. Firebase Admin SDK server integration
Add server-side Firebase Admin initialization using:
FIREBASE_SERVICE_ACCOUNT_JSON

Requirements:
- Parse credentials securely from env
- Do not require service-account files on disk
- Initialize Firebase Admin only once
- Fail gracefully if secret missing
- Add required dependency/dependencies

4. Android push sending service
Create a reusable helper/service for Android FCM sends.

Requirements:
- Send notification title/body
- Support optional data payload
- Return structured success/failure responses
- Log:
  provider
  platform
  token preview only
  Firebase response ID
- Never log full token values

5. /admin/test-push upgrade
Upgrade /admin/test-push so:
- If latest active token platform == ios:
  use existing APNs logic
- If platform == android:
  use new FCM logic

The route should:
- clearly indicate provider used
- clearly indicate platform used
- handle failures safely
- not crash if no token exists

Use:
Title: BaseLodge
Body: Test push from BaseLodge

6. Android native validation
Verify Android project setup:
- google-services plugin applied correctly
- Firebase dependencies present
- AndroidManifest requirements correct
- Capacitor push plugin remains functional

Only make minimal native changes required.

7. Testing + QA
Run all reasonable tests:
- Python syntax
- Startup validation
- Import validation
- Existing APNs imports still work
- /admin/test-push does not regress for iOS
- Android send helper handles invalid tokens gracefully
- Missing Firebase secret handled safely
- No template syntax errors

At the end report:
- Files changed
- Dependencies added
- Any new env vars required
- Whether Replit deploy is required
- Whether GitHub push/pull + npx cap sync android are required
- Exact Android testing steps