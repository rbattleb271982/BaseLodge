BASELODGE — SELF-HEAL REPLIT ARTIFACT COMMITS + COMPLETE BL-109 INVESTIGATION

REPLIT AGENT
POWER — BUILD

TASK PROGRESS
BL-109: 0% → ~15%

IMPORTANT

ONLY DO EXACTLY WHAT THIS PROMPT AUTHORIZES.

The Replit environment has repeatedly created automatic local commits containing only:
- `.replit`
- `attached_assets/Pasted-*` investigation/repair prompt files
- Replit-generated local investigation/repair documentation

Those commits are ENVIRONMENTAL NOISE.

DO NOT STOP merely because local HEAD is ahead of origin/main if every ahead commit contains ONLY those allowed paths.

Instead, automatically repair that state as instructed below and CONTINUE THE TASK.

Do not create application code.
Do not change schema.
Do not create migrations.
Do not change tests.
Do not change dependencies.
Do not deploy.
Do not push.
Do not access Production.
Do not access any database.
Do not create a project/task record.
Do not search Notion or any task-management system.

NEVER inspect, read, modify, copy, stage, or delete:

android/baselodge-release-key.jks

Never use:

git add .
git add -A


==================================================
PHASE 1 — NORMALIZE REPLIT-GENERATED GIT NOISE
==================================================

Authoritative approved GitHub state:

origin/main should be:

160d03e0eeb4257c3c975680731590cd13033254
Add Open to Ski sharing

First inspect:

- current branch
- local HEAD
- origin/main
- ahead/behind counts
- git status --short
- every commit in origin/main..HEAD using Git metadata/file paths only

DO NOT print file contents.

ALLOWED ENVIRONMENTAL-NOISE PATHS:

1. `.replit`
2. `attached_assets/Pasted-*`
3. clearly Replit-generated investigation/repair note files created only from these prompts

If EVERY commit in origin/main..HEAD contains ONLY those allowed environmental-noise paths:

DO NOT STOP.

Immediately perform a MIXED reset to origin/main.

The reset must:

- move local main to origin/main
- reset the index
- preserve `.replit` in the working tree
- preserve `attached_assets/Pasted-*` locally
- leave those files unstaged/untracked as appropriate
- create no commit
- delete nothing
- push nothing

DO NOT use a hard reset.

If ANY ahead commit contains real BaseLodge work, including application code, templates, CSS, JS, Python, tests, schema, migrations, dependencies, legitimate configuration, feature work, or other non-noise paths:

STOP and report the unexpected commit and paths.

Otherwise CONTINUE AUTOMATICALLY into Phase 2.

Do NOT ask for confirmation between phases.


==================================================
PHASE 2 — BL-109 ARCHITECTURE INVESTIGATION
==================================================

TASK

BL-109 — Close the loop on suggested connections

INVESTIGATION ONLY.

Do not implement anything.

OBJECTIVE

Understand the smallest safe way to support this flow:

1. User A suggests User B knows User C.
2. BaseLodge retains the actual suggestion event.
3. B and C later become connected in BaseLodge.
4. BaseLodge recognizes the prior suggestion.
5. User A receives a lightweight Happening confirmation such as:

   "John and Sarah connected"
   "You suggested they knew each other."

6. No action is required from B or C.
7. Multiple qualifying suggesters may each receive one confirmation.
8. Do not assign exclusive credit.
9. Do not claim the suggester caused the connection.
10. Do not expose messages, conversations, private activity, or relationship details.
11. Do not create public scores, badges, counts, rankings, or leaderboards.

This must extend existing BaseLodge architecture.
Do not invent a standalone social system.


A. SUGGESTED CONNECTIONS

Trace the complete current implementation.

Identify:

- UI surfaces
- routes/endpoints
- frontend JS
- services/helpers
- models/tables
- migrations
- event/notification behavior
- Happening integration
- analytics if any
- tests

Answer:

1. What exactly happens today when A suggests B knows C?
2. What is persisted?
3. Is suggester identity durable?
4. Are B and C both stored durably?
5. Is a suggestion timestamp stored?
6. Is the pair ordered or unordered?
7. Are duplicates possible?
8. Is there lifecycle/status data?
9. Is suggestion data durable or merely disposable notification/event data?

Cite exact file paths, functions/classes, models, and fields.


B. FRIENDSHIP / CONNECTION CREATION

Trace EVERY authoritative production path by which two users become connected.

Identify:

- friendship model/table
- directed vs symmetric semantics
- statuses
- pending/accepted behavior
- every route/service/helper capable of creating an accepted connection
- friend-request acceptance
- direct connection paths
- invitation-derived connection paths if any
- any bypasses around a centralized service

Answer:

1. What exact state means B and C are connected?
2. What are ALL paths that cause that transition?
3. Is there one safest central hook for BL-109?
4. If not, what is the smallest reliable alternative?

Cite exact code locations.


C. HAPPENING

Trace the existing Happening architecture.

Determine:

- persisted vs dynamically derived events
- models/tables
- event types
- services/helpers
- recipient targeting
- visibility rules
- deduplication
- idempotency
- history behavior
- rendering/templates
- comparable social milestone events

Answer:

1. What existing pattern should BL-109 reuse?
2. Is a new Happening event type required?
3. How can the event be visible only to qualifying suggester(s)?


D. CURRENT SOCIAL EVENT FLOW

Inspect what currently happens when:

- a friend request is accepted
- two users become connected
- someone suggests a connection
- a suggested connection is acted upon
- similar social milestones occur

Determine whether BL-109 can reuse an existing event-generation mechanism.


E. DATA MODEL GAP

Determine whether existing persisted data already supports:

- introducer
- unordered suggested pair
- suggestion timestamp
- conversion state
- connection/conversion timestamp

DO NOT assume a migration is needed.

If a schema change is required, explain precisely why.


F. MATCHING RULE

Recommend the smallest deterministic rule.

Evaluate:

- suggestion predates accepted connection
- B/C equals C/B
- multiple suggesters
- repeated suggestions by same suggester
- duplicate friendship acceptance
- suggestion after pair already connected
- old suggestions
- removal and reconnection
- blocking/privacy
- idempotency

Do not infer causation.

Use language equivalent to:

"You suggested they knew each other."

Never:

"You introduced them."
"You connected them."
"You caused the connection."


G. MULTIPLE SUGGESTERS

Recommend how:

- A suggests B/C
- D separately suggests B/C
- B/C connect
- A gets exactly one confirmation
- D gets exactly one confirmation
- neither gets exclusive credit
- duplicates are prevented


H. PRIVACY / BLOCKING

Determine all relevant existing rules.

The introducer may learn only:

- that B and C connected
- that the introducer previously suggested they knew each other

Do not expose:

- messages
- conversations
- who initiated the friend request
- private activity
- trip information
- unrelated relationship information


I. IDEMPOTENCY

Determine whether BL-109 needs:

- converted flag
- converted_at
- notified/emitted state
- canonical pair fields
- unique constraint
- event dedupe key

Recommend the minimum required by current architecture.


J. TEST PLAN

At minimum cover:

- single suggester
- multiple suggesters
- reversed pair ordering
- duplicate suggestion
- repeated suggestion by same user
- duplicate acceptance execution
- suggestion before connection
- suggestion after existing connection
- unrelated pair connection
- Happening visible only to qualifying suggester
- privacy leakage prevention
- blocking case if applicable
- removal/reconnection if relevant
- idempotent confirmation creation

Identify existing test files that should be extended.


K. IMPLEMENTATION SLICES

Recommend the smallest safe sequence.

Prefer as few slices as architecture permits.

Possible structure only if needed:

1. persistence/matching foundation
2. conversion hook
3. Happening presentation
4. focused regression tests

Do not add unnecessary infrastructure.


==================================================
PHASE 3 — FINAL GIT HYGIENE
==================================================

After completing the READ-ONLY BL-109 investigation, inspect Git again.

If Replit has automatically produced new local commits during this run and EVERY new commit above origin/main contains ONLY:

- `.replit`
- `attached_assets/Pasted-*`
- Replit-generated investigation/repair documentation

then automatically perform another MIXED reset to origin/main.

Preserve those files locally as unstaged/untracked content.

DO NOT STOP merely because Replit created another allowed artifact commit.

If a commit contains ANY real BaseLodge source/schema/test/dependency/feature work, STOP and report it without resetting that legitimate work.

Because this task is investigation-only, the expected final Git history is:

local HEAD = origin/main

No feature commit should exist.


==================================================
FINAL REPORT
==================================================

Return ONLY in the Replit chat.

DO NOT save the report into the repository.

Include:

1. Repository/Git state before normalization
2. Environmental-noise commits found
3. Git normalization performed
4. Existing Suggested Connections architecture
5. Existing friendship/connection architecture
6. Existing Happening architecture
7. Exact persistence currently available
8. Gaps found
9. Recommended conversion/matching rule
10. Multiple-suggester handling
11. Idempotency recommendation
12. Privacy/blocking considerations
13. Recommended implementation slices
14. Focused test plan
15. Exact files likely to change
16. Database migration required: YES / NO / UNCERTAIN, with evidence
17. Overall implementation risk
18. Recommended Replit Agent level + PLAN/BUILD mode for first implementation slice
19. Final local HEAD
20. Final origin/main
21. Final ahead/behind
22. Final git status --short
23. Estimated BL-109 completion percentage

FINAL RULES

Do not create code.
Do not create tests.
Do not create schema changes.
Do not create migrations.
Do not commit feature work.
Do not push.
Do not deploy.
Do not access Production.
Do not access databases.
Do not search for or create a BL-109 task.
Do not save your report as a file.

MOST IMPORTANT:

DO NOT STOP BECAUSE REPLIT ITSELF CREATED AN ALLOWED `.replit` OR `attached_assets/Pasted-*` COMMIT.

SELF-HEAL THOSE ALLOWED ARTIFACT COMMITS WITH A MIXED RESET AND CONTINUE.