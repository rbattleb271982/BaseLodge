Replit Agent

We are refining BaseLodge UI and fixing remaining behavior issues.

This is a focused polish pass:
- design consistency
- interaction fixes
- navigation improvements

Use checkpoints sparingly.

---

PART 1 — HOME SCREEN STAT BAR (TAPPABLE)

Replace current vertical stats:

"1 Trips"
"2 Mountains"
"0 Wish list"

With a horizontal stat bar:

Trips       Mountains       Wishlist
1           2               0

REQUIREMENTS:

- Single horizontal row
- Even spacing across width
- Numbers:
  - larger
  - bold
- Labels:
  - smaller
  - muted color

INTERACTION (IMPORTANT):

Each stat is tappable:

- Trips → navigates to /my-trips
- Mountains → navigates to /mountains (or mountains visited page)
- Wishlist → navigates to /wishlist

UX RULES:
- Entire stat block is tappable (not just text)
- Add subtle tap feedback (opacity or background change)
- No heavy borders or boxes — keep it clean

---

PART 2 — TRIP DETAILS IMPROVEMENTS

1. Add “Other” option to Transportation

2. Update Lessons copy:
Replace:
"Maybe a lesson"

With:
"Considering a lesson"

3. Add explanation under “Your trip details”

Add subtext:
"Visible to friends when your trip is public."

Style:
- smaller text
- muted color
- directly under section header

---

PART 3 — CTA CONSISTENCY

Standardize buttons:

- “Invite friends”
- “Add availability”

Both must use:

→ outlined pill button (secondary style)

Rules:
- burgundy border
- burgundy text
- rounded (pill shape)
- consistent padding

---

PART 4 — ADD AVAILABILITY SCREEN FIXES

1. Restore bottom navigation
- Must always be visible

2. FIX CALENDAR RANGE SELECTION (CRITICAL)

Current issue:
- behaves like individual day selection
- cannot reliably select ranges

REQUIRED BEHAVIOR:

- First tap = start date
- Second tap = end date
- Same date twice = single-day selection
- Third tap = reset new range
- If second tap is earlier → auto reorder

DO NOT:
- rebuild calendar DOM

DO:
- update styles only

ADD RANGE SUMMARY:

- No selection → "Select your dates"
- Start only → "Apr 14 — pick end date"
- Single day → "Apr 14 · day trip"
- Range → "Apr 14–17"

Include:
- Clear button

---

PART 5 — INVITE FRIENDS SCREEN

1. Remove pink box around QR code

2. Add text under QR:

"Have your friends scan this to connect."

3. Keep centered layout
4. Maintain clean spacing

---

PART 6 — GLOBAL DESIGN RULES

Apply everywhere touched:

TYPOGRAPHY:
- Serif → titles only
- Sans-serif → everything else

COLORS:
- Burgundy = primary accent
- No additional accent colors

CARDS:
- white background
- soft border
- rounded corners

BUTTONS:
- Primary = filled burgundy
- Secondary = outlined burgundy
- Tertiary = text only

SPACING:
- consistent vertical rhythm
- no random spacing

---

DELIVERABLES

1. Tappable stat bar
2. Fixed calendar behavior
3. Updated trip details section
4. Consistent button styling
5. Invite screen cleanup
6. Short explanation
7. Manual test steps

Do not introduce new patterns.
Match existing Home screen design language.