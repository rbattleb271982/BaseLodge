PUT THIS IN AGENT

Create a read-only architecture and redesign brief for the Season Snapshot page so I can give it to Claude for visual redesign.

Do not implement changes.
Do not edit files.
This is a diagnostic + creative brief only.

Page intent:
The Season Snapshot page should be designed primarily for screenshot sharing, especially a polished 1:1 Instagram-style share card. It should feel like a premium social poster, not a utility dashboard.

Current issue:
The page works, but it does not yet feel share-worthy enough. The destination list truncates, the card hierarchy is flat, and the page needs a stronger poster-like composition.

Design direction:
- Full-screen poster feel
- 1:1 shareable snapshot area should be the primary design target
- Bottom nav can remain outside/under the poster experience
- Premium/editorial BaseLodge style
- Soho House x Apple energy
- Cream, maroon, charcoal palette
- Serif headline moments + clean sans body
- Minimal, warm, elevated
- No clutter
- No gradients unless already consistent with the app
- No new libraries
- Stay within current Flask/Jinja/CSS architecture

Data to include:
Only use what is currently visible/available on the page:
- User first name
- “Winter” title language
- Rider type
- Skill level
- Pass status
- “on BaseLodge”
- Month grouping
- Destination list / trips shown by month

Do NOT add:
- Wishlist
- Mountains visited
- Friends overlapping
- Social feed content
- New stats unless they can be derived cleanly from current visible data
- New database fields

Investigate current architecture:
1. Locate the Season Snapshot route.
2. Locate the template.
3. Locate CSS used by the page.
4. Identify all variables passed into the template.
5. Identify where rider type, skill level, pass type, month, and destination names come from.
6. Identify why “Big Sky Resort” is truncating and which selector causes it.
7. Identify whether the page currently supports screenshot/share behavior or only visual display.
8. Identify whether the snapshot card can be constrained to a 1:1 aspect ratio without breaking mobile layout.
9. Identify whether bottom nav is part of the same base template and how much control this page has over full-screen layout.

Redesign proposal requirements:
Provide 2–3 layout directions Claude could use, such as:
1. Editorial poster card
2. Passport/stamp-inspired winter card
3. Minimal luxury season recap card

Each direction should include:
- Page structure
- Visual hierarchy
- Typography recommendations using current app fonts/classes where possible
- Spacing strategy
- How to handle long destination names without truncation
- How to make the central snapshot area close to 1:1 for Instagram screenshots
- What should remain outside the shareable area
- Risks or constraints in the current code

Implementation constraints for later:
- Must remain Flask/Jinja/CSS
- No React
- No new frontend framework
- No schema changes
- No new backend data requirements
- Mobile-first
- iPhone safe-area aware
- Should not break logged-in base layout or bottom nav

Final output format:
1. Current architecture
2. Current data available
3. Current visual/layout problems
4. Recommended redesign constraints
5. 2–3 proposed design directions
6. Recommended direction
7. Exact files Claude should reference
8. What Claude should not change
9. Implementation risks