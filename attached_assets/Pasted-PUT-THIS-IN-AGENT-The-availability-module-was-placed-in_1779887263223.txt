PUT THIS IN AGENT

The availability module was placed in the wrong experience again.

Correct behavior:
- The Home screen should NOT permanently show the “You’re free” card.
- The Edit Availability page should NOT show the “You’re free” card above the calendar.
- Instead, when the user taps the “Edit availability” pill on Home, open the availability module/sheet on top of the Home screen, like the attached reference.

This should feel like a lightweight Home-screen overlay/module, not a full page takeover.

HOME BEHAVIOR

Keep Home clean by default:
- profile card
- pills
- Requests
- Happening
- Ideas/Opportunities

When user taps “Edit availability”:
- open a lightweight availability module/sheet on the Home screen
- show the “You’re free” summary if dates exist
- show saved date ranges
- show “+ N more dates” if needed
- show an “Edit availability” CTA inside the module that takes the user to the full calendar editor
- if no dates exist, show the add-availability nudge inside the module

IMPORTANT:
The summary module should appear only after tapping “Edit availability.”
It should not be permanently visible on Home.
It should not be placed on the calendar page.

FULL EDIT AVAILABILITY PAGE

The full calendar page should remain the compact editor:
- intro copy
- Learn how it works →
- compact calendar
- selected date summary
- Save availability
- Cancel

Remove the “You’re free” card from the full Edit Availability page.

PRESERVE:
- compact mobile-first calendar spacing
- existing date selection behavior
- existing save behavior
- current Home profile card
- current Home sections

DO NOT:
- permanently add the module to Home
- add the module above the calendar page
- redesign the whole Home page
- create a heavy modal
- add illustrations or profile bubbles

After implementation:
- test on iPhone viewport
- screenshot Home default state
- screenshot Home after tapping Edit availability with the module open
- screenshot full Edit Availability calendar page
- confirm the module only opens from the Home pill