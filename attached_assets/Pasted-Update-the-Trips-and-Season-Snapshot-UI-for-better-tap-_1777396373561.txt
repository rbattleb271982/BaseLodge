Update the Trips and Season Snapshot UI for better tap clarity, consistent typography, and screenshot quality.

Do not change backend logic, database models, auth, admin, onboarding, or routes unless a route already exists and only needs existing links wired correctly.

1. Make Trips screen titles consistent
- On templates/my_trips.html, update the top “Trips” page title so it uses the same global serif heading class/token used by Home, Profile, Friends, Ideas, and other main logged-in screens.
- Remove any local font-family, font-size, or font-weight overrides that make “Trips” render thinner or different from the rest of the app.
- Confirm the title visually matches the main screen-title style used elsewhere.

2. Make “Snapshot” look tappable
- On the Trips screen, update the “Snapshot” text link in the top-right so users understand it is tappable.
- Keep it as a secondary/text navigation action, not a primary button.
- Make it larger and clearer.
- Use burgundy text.
- Add a simple arrow: “Snapshot →”
- Ensure the whole text target is clickable, not just part of it.
- Keep placement top-right.

3. Make trip rows fully tappable
- On templates/my_trips.html, ensure each trip row in “Your trips” is fully tappable across the entire row.
- The row should link to the trip detail page.
- Keep the chevron/arrow on the right as the visual navigation cue.
- Do not make only the arrow clickable.
- Add hover/focus-visible states for accessibility.
- Ensure touch target height is at least 44px.

4. Home next-trip card tappability
- On the Home screen template, make the entire “My next trip” card tappable.
- It should navigate to the trip detail page for that trip.
- Keep the “Edit →” action if it currently exists, but the card itself must also be clickable.
- Avoid nested invalid anchors. If needed, use a single anchor wrapper for the card or JS-safe structure that keeps valid HTML.
- Add an accessible aria-label like “View trip details for Courchevel”.
- Do not show “No Pass” if the pass value is empty, missing, “None”, “No pass”, or “No Pass”.
- In that case, omit the pass segment entirely so metadata reads like:
  Feb 14–20 · In 292 days
  not:
  Feb 14–20 · No Pass · In 292 days

5. Season Snapshot screen improvements
- Find the Season Snapshot template.
- Make the snapshot card/component significantly larger and more screenshot-friendly on mobile.
- The card should be wider, centered, and sized so it fills more of the visible screen while still leaving breathing room.
- It should display the mountain as:
  Courchevel, France
  instead of truncating to “Courc...” or using the long region string.
- If the app has access to city/mountain and country separately, use those.
- If not, for now update the display logic so Courchevel renders as “Courchevel, France”.
- Avoid truncating the mountain name on the snapshot card unless absolutely necessary.
- Use responsive font sizing so common destination names fit cleanly.
- Keep the card visually premium and screenshot-ready.
- The user should be able to take a screenshot and clearly see:
  name
  winter label
  skier metadata
  BaseLodge attribution
  trip month
  Courchevel, France
  trip dates

6. Season Snapshot page layout
- Keep the page title “Season Snapshot”.
- Keep helper copy, but ensure it does not crowd the snapshot card.
- “Back to Trips” should look like a secondary text link, not a primary CTA.
- Ensure the bottom nav does not cover the snapshot card or back link.
- Add enough bottom padding using the existing safe-area system.

7. Interaction rules / guardrails
- Anything that looks tappable must actually be tappable.
- Anything that is tappable must have a clear visual cue:
  - rows use chevron/arrow
  - secondary nav links use burgundy text and optional arrow
  - primary actions use full-width burgundy buttons
- Do not introduce new pill-shaped CTAs.
- Pills are only for selection inputs.
- Do not introduce decorative cards except bounded objects like trip cards, calendar, or snapshot card.

8. QA checks before finishing
- Mobile viewport QA required for:
  /my-trips
  /home
  Season Snapshot route
- Confirm:
  - “Trips” title matches the global serif screen-title style
  - “Snapshot →” is visibly tappable and clickable
  - trip rows are fully clickable
  - Home next-trip card is fully clickable
  - “No Pass” does not appear in home next-trip metadata
  - Season Snapshot card is wider/larger
  - Season Snapshot shows “Courchevel, France”
  - bottom nav does not cover content
  - no console errors
  - no Flask/template errors
  - no route/backend/database changes beyond linking existing UI

Use checkpoints sparingly. Do not create unnecessary checkpoints.