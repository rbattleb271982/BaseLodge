BASELODGE — BL-125 FINAL COMMIT AND PUSH
BUG — Keep Mountains friend counts fresh after trip changes

REPLIT AGENT
POWER — BUILD

TASK PROGRESS
Starting: ~95%
Expected after successful commit + push: 100%

CRITICAL EXECUTION RULE

ONLY DO EXACTLY WHAT THIS PROMPT EXPLICITLY AUTHORIZES.

Do not take initiative.
Do not create notes.
Do not create documentation.
Do not create Replit tasks.
Do not create attachments.
Do not create follow-on work.
Do not modify unrelated files.
Do not deploy.
Do not access Production.
Do not access or mutate Production databases.

Reports belong only in Replit chat.

NEVER inspect, read, modify, copy, stage, or delete:

android/baselodge-release-key.jks

Never use:

git add .
git add -A

==================================================
APPROVED BASELINE
==================================================

Approved parent commit:

9bef821cce66451aef1cc4e3e570796efa6d5a01
Close suggested connection loop

Approved BL-125 changed files are exactly:

app.py
templates/mountains_tab.html
tests/test_mountains_data.py

No other legitimate files are authorized for this commit.

Known environmental-noise files may include:

.replit
attached_assets/Pasted-*

These must remain outside the commit.

==================================================
PHASE 1 — VERIFY REPOSITORY STATE
==================================================

Before staging, report:

1. current branch
2. local HEAD
3. origin/main
4. ahead/behind
5. git status --short

Expected:

branch = main
local HEAD = 9bef821cce66451aef1cc4e3e570796efa6d5a01
origin/main = 9bef821cce66451aef1cc4e3e570796efa6d5a01
ahead = 0
behind = 0

Expected legitimate modified files:

app.py
templates/mountains_tab.html
tests/test_mountains_data.py

Known unrelated environmental state may include:

.replit
attached_assets/Pasted-*

Do not stage those.

==================================================
SELF-HEAL ENVIRONMENTAL NOISE
==================================================

If Replit automatically created ahead commits containing ONLY:

.replit
attached_assets/Pasted-*

then:

1. inspect those commit file paths using Git metadata only;
2. confirm no legitimate application/source/test/schema/dependency/native files are included;
3. perform a safe MIXED reset to origin/main;
4. preserve environmental files locally;
5. preserve legitimate BL-125 working-tree changes;
6. create no extra commit;
7. continue automatically.

If ANY ahead commit contains legitimate BL-125 or other application work:

STOP and report the exact commit and paths.

Do not reset legitimate work.

==================================================
PHASE 2 — VERIFY APPROVED DIFF
==================================================

Inspect the full diff for ONLY:

app.py
templates/mountains_tab.html
tests/test_mountains_data.py

Confirm the diff implements only the approved BL-125 scope:

- remove personalized `_mountains_cache` response reuse
- keep stable resort reference caching
- recompute reciprocal friend IDs and grouped friend counts per request
- use `Cache-Control: private, no-store`
- remove stale ETag/304 response reuse
- frontend fetch uses `{ cache: 'no-store' }`
- every `pageshow`, including bfcache restoration, triggers fresh Mountains data
- filter-reset behavior remains
- existing friend_count semantics remain unchanged
- focused regression coverage added/updated
- no Mountain Detail implementation change
- no schema/migration/native/deployment change

If the diff contains anything outside this scope:

STOP and report it.

==================================================
PHASE 3 — FINAL SAFETY CHECKS
==================================================

Before staging, run:

git diff --check

Then run focused validation if needed to confirm nothing changed since the successful implementation run.

At minimum confirm:

- tests/test_mountains_data.py still passes
- changed Python compiles
- template still parses

Do not rerun the entire full suite unless necessary because it already passed during implementation.

If any validation fails:

STOP and report it.

==================================================
PHASE 4 — STAGE EXACTLY THREE FILES
==================================================

Stage ONLY:

git add app.py
git add templates/mountains_tab.html
git add tests/test_mountains_data.py

Do not stage anything else.

Then report:

git status --short

Verify:

- only the three approved BL-125 files are staged
- `.replit` is NOT staged
- no `attached_assets/Pasted-*` file is staged
- no signing/native/schema/migration file is staged

If anything else is staged:

STOP and unstage only the unintended file(s), then report.

==================================================
PHASE 5 — COMMIT
==================================================

Create exactly one commit with this exact subject:

Keep Mountains friend counts fresh

Do not add a body unless Git requires one.

Then report:

git rev-parse HEAD
git show --stat --oneline --summary HEAD
git diff-tree --no-commit-id --name-only -r HEAD

Verify the commit contains exactly:

app.py
templates/mountains_tab.html
tests/test_mountains_data.py

If it contains anything else:

STOP.

Do not amend automatically unless the only issue is an accidentally staged environmental-noise file and the correction is unquestionably safe.

==================================================
PHASE 6 — PUSH
==================================================

Push `main` normally.

No force push.

If the local HTTPS remote lacks credentials, use the existing authorized GitHub connection.

If publishing through the GitHub connection/API is necessary:

- preserve the exact approved three-file tree;
- preserve the parent relationship;
- do not force-update the branch;
- advance `main` only if the current remote parent is still the approved baseline;
- verify the resulting remote tree exactly matches the local approved commit contents.

Do not create a second feature commit.

==================================================
PHASE 7 — FINAL VERIFICATION
==================================================

After push, verify:

1. local HEAD
2. origin/main
3. ahead/behind
4. final commit subject
5. exact committed files
6. final git status --short

Expected final state:

- local HEAD = origin/main
- ahead = 0
- behind = 0
- exact commit subject = `Keep Mountains friend counts fresh`
- commit contains only:
  - app.py
  - templates/mountains_tab.html
  - tests/test_mountains_data.py

Known unrelated local environmental state may remain:

M .replit
?? attached_assets/Pasted-*

That is acceptable if outside the commit.

==================================================
PROHIBITED ACTIONS
==================================================

Do NOT:

- deploy
- access Production
- access Production database
- mutate any database
- create a migration
- modify schema
- touch native/iOS/Android work
- access signing assets
- create Replit tasks
- create notes/docs/attachments
- commit `.replit`
- commit `attached_assets/Pasted-*`
- force push
- perform unrelated cleanup

==================================================
FINAL REPORT
==================================================

Return only in Replit chat:

1. branch
2. commit hash
3. commit subject
4. exact committed files
5. local HEAD
6. origin/main
7. ahead/behind
8. final git status --short
9. validation results run before commit
10. confirmation `.replit` excluded
11. confirmation all `attached_assets/Pasted-*` excluded
12. confirmation no deployment occurred
13. confirmation no Production/database action occurred
14. confirmation no signing asset was accessed
15. BL-125 completion = 100%

STOP after the report.