PUT THIS IN AGENT

Update the Season Snapshot feature to match the FINAL approved design.

Do not modify backend logic.
Do not touch the route.
Only update the template and styling.

---

PROBLEM:

The current /season-snapshot view does not match the approved design.

It currently shows:
- a maroon header block
- incorrect name styling
- raw identity line formatting
- a UI-like card instead of an editorial share card

We need to replace this with the final editorial version.

---

REQUIRED CHANGES:

1. Remove the maroon header entirely
- No colored header block
- No dark top section
- Entire card should be a single cream surface

---

2. Update headline

Replace current name display with:

Richard’s <em>Winter</em>

Requirements:
- Serif font (Georgia or similar)
- One line
- “Winter” italic and slightly darker maroon tone
- Balanced sizing (not oversized)

---

3. Identity line

Replace current identity rendering with:

{{ current_user|identity_line }}

Requirements:
- Uppercase
- Letter spaced
- Clean formatting
- No commas (ensure filter outputs properly formatted string)

---

4. Branding line

Keep:

— on BaseLodge —

Requirements:
- Centered
- Small
- Subtle maroon tone
- No footer bar

---

5. Layout structure

Top section should be compact:

[headline]
[identity]
[on BaseLodge]
[thin rule]

Then trip list starts immediately after.

---

6. Remove footer

Delete:
- BASELODGE footer block
- year display (2026–27)

Add instead:
- small bottom spacing or subtle hairline rule

---

7. Background

Card:
- cream tone (#EDE4D3 or similar)

Page background:
- dark (to frame the card)

---

8. Trip list (keep existing logic)

Do NOT change:
- grouping
- formatting
- data

Only ensure:
- spacing matches editorial style
- no UI-like rows or heavy dividers

---

9. Typography cleanup

- Capitalize name properly (Richard, not richard)
- Ensure serif font throughout
- Improve spacing between sections

---

10. Keep everything else the same

- Route logic stays
- grouping helper stays
- date formatting stays

---

GOAL:

Make the Snapshot look like a clean, editorial, shareable card — NOT an app UI.

---

After updating, return:
1. Updated template code
2. What was removed
3. What was replaced
4. Confirmation that visual now matches the final design