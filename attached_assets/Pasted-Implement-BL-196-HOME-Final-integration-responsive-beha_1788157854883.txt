Implement BL-196 — HOME — Final integration, responsive behavior, and native-shell QA.

This is the final integration/QA pass for the current Home workstream.

The underlying Home modules have already been implemented and merged.

Do not redesign Home.
Do not create a new architecture.
Do not reopen completed Home tasks.
Do not change existing business logic unless a verified integration defect requires it.

GOAL

Validate the completed Home experience as one cohesive page across:

- server-rendered web
- supported mobile widths
- desktop
- native app shell / WebView behavior

Fix only genuine integration, responsive, accessibility, or presentation defects discovered during this pass.

The result should be a polished, stable Home screen using the already-approved product behavior.

CURRENT HOME CONTRACT

The integrated Home should contain, in this order:

1. Header
   - “Welcome, <first name>.”
   - existing profile button

2. About You & Your Gear
   - collapsed by default
   - compact collapsed summary
   - expanded single-column detail
   - Rider Type
   - Skill Level
   - Pass
   - Skis
   - Boots
   - Bindings

3. Your Activity
   - collapsed by default
   - compact collapsed summary
   - Trips
   - Mountains Visited
   - Wishlist Mountains

4. Friends’ Passes
   - collapsed by default
   - unique direct-friend count
   - Epic / Ikon / Other distribution
   - existing BL-19 filtering/navigation semantics preserved

5. Your Next Trip
   - compact card
   - View trip CTA
   - resort
   - dates
   - nights
   - confirmed direct friends going
   - countdown
   - optional “Actions to take · N” disclosure only when action_count > 0

6. Happening
   - horizontal native-scrolling carousel
   - cards approximately 75–80% available width on mobile
   - next-card peek
   - existing dismissal
   - max five bounded results

7. Ideas
   - existing presentation
   - exact BL-176 bounded retrieval
   - max five results
   - existing dismissal/ranking/matching behavior

8. Fixed bottom navigation
   - Home
   - Trips
   - Friends
   - Mountains

PRODUCT / VISUAL GUARDRAILS

Do not add:

- photographs
- image placeholders
- thumbnails
- mountain imagery
- decorative mountain illustrations
- friend avatars
- profile bubbles inside Home content
- gratuitous icons
- bookmarks
- new See all links without real destinations
- new Home modules

The existing profile button in the header may remain.

Maintain the current warm cream/bone background, burgundy accents, white rounded cards, restrained typography, and compact vertical rhythm.

Do not perform a broad design-system redesign.
That belongs to later design work.

1. FULL HOME INTEGRATION REVIEW

Render Home with realistic populated data and verify the complete vertical flow.

Check:

- module order
- spacing between modules
- card widths
- consistent horizontal gutters
- heading hierarchy
- disclosure spacing
- borders/radii/shadows
- alignment
- text wrapping
- tap targets
- visual density
- bottom-nav clearance

Look specifically for inconsistencies introduced because individual Home modules were built separately.

Fix only those integration inconsistencies.

2. MOBILE RESPONSIVE QA

Validate at minimum representative widths around:

- 320px
- 360px
- 390px
- 430px

Verify:

- no page-level horizontal overflow
- no clipped text
- no disclosure-summary collisions
- no arrow overlap
- no awkward multi-column behavior
- long resort names wrap safely
- long gear/pass names wrap safely
- activity metrics remain readable
- Friends’ Passes remains compact
- Next Trip metadata remains compact
- Actions to take remains usable
- Happening retains intentional next-card peek without causing document overflow
- Ideas cards remain usable
- bottom nav does not obscure content

Do not solve wrapping by shrinking text to unreadable sizes.

3. DESKTOP / WIDE-WIDTH QA

Validate representative desktop widths such as:

- 768px
- 1024px
- 1280px+

Ensure the Home page does not become visually stretched or sparse.

Preserve existing app-width/container conventions rather than inventing a desktop redesign.

Check:

- sensible max-width behavior
- consistent alignment
- disclosure rows
- Happening carousel
- Ideas
- fixed navigation behavior where applicable

4. DISCLOSURE INTEGRATION

Verify the shared Home disclosure primitive across:

- About You & Your Gear
- Your Activity
- Friends’ Passes
- Next Trip Actions

Confirm:

- collapsed by default
- keyboard accessible
- summary remains understandable
- down-arrow behavior consistent
- no chevrons introduced
- adequate tap target
- focus behavior works
- opening one does not break layout of another
- macro remains backward compatible

Do not add JavaScript disclosure state unless an actual blocker requires it.

5. NEXT TRIP QA

Verify all major states:

- owner
- Going guest
- Interested guest
- zero actions
- one action
- multiple actions where contract permits
- no next trip
- trip today
- trip tomorrow
- future trip
- trip currently in progress
- long resort name
- private authorized trip

Confirm:

- View trip remains separate from Actions to take
- no Add pass action
- no Edit trip presented as needs-attention
- canonical BL-189 destinations remain intact
- BL-193 friends-going semantics remain intact

Do not modify server-side action eligibility unless a failing test proves an integration regression.

6. HAPPENING QA

Confirm BL-177 + BL-195 remain intact.

Verify:

- bounded retrieval remains max five
- horizontal carousel works by native scrolling
- next-card peek appears on mobile
- X dismissal remains reachable
- dismissal does not distort layout
- no custom carousel dependency
- no page-level horizontal overflow
- desktop remains usable

Do not modify `services/happening.py` unless a genuine regression is proven.

7. IDEAS QA

Confirm BL-176 integration remains intact.

Verify:

- one bounded Ideas query
- max five materialized results
- dismissal/backfill works
- presentation is unchanged
- mountainless cards remain valid
- mixed source cards display correctly
- long friend/resort text wraps safely
- empty Ideas state remains correct

Do not change Ideas ranking, matching, suppression, deduplication, or privacy behavior.

Do not modify the retrieval architecture for presentation cleanup.

8. EMPTY / PARTIAL HOME STATES

Test realistic partial profiles and sparse accounts:

- brand-new user
- no gear
- no pass
- no trips
- no upcoming trip
- no friends
- friends but no passes
- no Happening
- no Ideas
- combinations of populated and empty sections

The page should remain intentional rather than looking broken or excessively blank.

Preserve existing empty-state product behavior unless there is an actual layout defect.

9. ACCESSIBILITY QA

Check:

- semantic heading order
- disclosure keyboard operation
- visible keyboard focus
- buttons vs links
- dismissal controls
- meaningful accessible labels
- adequate tap targets
- no duplicate interactive nesting
- bottom navigation semantics
- reasonable text contrast using existing palette

Fix straightforward accessibility defects within Home scope.

Do not initiate a whole-app accessibility refactor.

10. NATIVE-SHELL QA

Validate Home through the project’s supported native-shell/WebView workflow.

Check:

- viewport sizing
- safe-area behavior
- bottom navigation
- scroll behavior
- sticky/fixed elements
- horizontal Happening scroll
- disclosure interaction
- dismissal interaction
- View trip navigation
- Friends’ Passes navigation
- Ideas destinations
- Next Trip action anchor destinations

Look for differences between desktop browser/mobile emulation and the native shell.

Do not modify native configuration unless a verified Home blocker requires it.

If native configuration changes appear necessary, STOP and report before making them.

11. PERFORMANCE REGRESSION CHECK

Ensure the integration pass does not undo:

- BL-187 shared Home assembly improvements
- BL-177 bounded Happening retrieval
- BL-176 bounded Ideas retrieval

Confirm:

- no new Home query loops
- no N+1
- no new lazy loading
- no client dependency added for layout behavior
- no broad repeated data retrieval

Do not introduce caching.

12. TESTS

Run all relevant Home tests, including at minimum:

- Home empty states
- Home disclosure
- About You & Gear
- Your Activity
- Friends’ Passes
- Next Trip
- Next Trip Actions contract
- Next Trip Actions presentation
- Happening retrieval
- Happening carousel
- Ideas retrieval/equivalence
- dismissal regressions
- relevant privacy/authorization tests

Add focused BL-196 tests only for defects or integration behavior not already protected.

Avoid duplicating existing test coverage merely to increase test count.

13. VISUAL VALIDATION

Perform actual browser validation rather than relying only on unit tests.

Capture/check representative populated and sparse Home states at mobile and desktop sizes.

Verify console output.

Report any remaining visual defects rather than silently accepting them.

14. SCOPE CONTROL

This task may make small presentation/integration fixes in existing Home templates/CSS/tests where needed.

Do NOT:

- redesign modules
- change product semantics
- change matching/ranking
- add new features
- introduce new Home data
- add avatars/images
- redesign bottom navigation
- change schema
- run migrations
- add dependencies
- introduce caching
- start BL-184
- start BL-186

If a problem belongs to a different backlog task, document it rather than absorbing it into BL-196.

15. VALIDATION AND COMMIT

Once all genuine integration defects are fixed:

- run the complete relevant Home regression suite
- run git diff --check
- verify Flask workflow
- verify browser console
- verify mobile responsive behavior
- verify desktop behavior
- verify native-shell behavior if available

Do not commit unrelated files.

If BL-196 requires only validation and no code changes, explicitly say so rather than forcing a commit.

If changes are required and everything passes, do NOT commit yet.

RETURN

BL-196 IMPLEMENTATION / QA RESULT

- Files changed:
- Integration defects found:
- Integration defects fixed:
- Mobile widths validated:
- Desktop widths validated:
- Native-shell validated:
- Native-shell issues:
- Disclosure integration:
- Next Trip states validated:
- Happening regression:
- Ideas regression:
- Empty/partial states:
- Accessibility issues found:
- Accessibility issues fixed:
- Page horizontal overflow:
- Bottom-nav clearance:
- New Home queries:
- N+1 introduced:
- BL-177 bound preserved:
- BL-176 bound preserved:
- Templates changed:
- CSS changed:
- JavaScript changed:
- Native config changed:
- Schema/migration changed:
- New dependency:
- Tests run:
- Tests passed:
- git diff --check:
- Flask workflow:
- Browser console:
- Remaining issues / deferred tasks:
- Scope deviations:
- Commit created: NO

End with exactly one:

BL-196 READY TO MERGE

or

BL-196 NEEDS FOLLOW-UP