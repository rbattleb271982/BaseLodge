REPLIT: MAX + BUILD
Estimated total BaseLodge workload completed after this step: ~38%

TASK: Complete the BL-80 Supabase Development rollout

Context:

BL-80 code is complete and committed:
27514825 — BL-80 Preserve terminal trip history

Supabase Development backup gate is now satisfied.

Verified pre-migration backup:

Object:
bl135/development/pre-bl80-supabase-development_bl52_trip_stay_20260831T174817Z.dump

Checksum:
86dc82204d8afba8661699fffdd6f44ee5eeff0c1e800260c88a255a61af2f70

Backup verification:
- authenticated App Storage listing passed
- authenticated download passed
- size matched: 341,707 bytes
- SHA-256 matched
- PostgreSQL 17 pg_restore --list passed
- 673 restore inventory entries
- alembic_version, ski_trip, ski_trip_participant, and related schema confirmed
- Production untouched

Current Supabase Development state:

alembic_version = bl52_trip_stay

BL-78 absent
BL-79 absent
BL-80 absent
BL-70 absent

Existing trips: 15

The normal Flask workflow is:

env BASELODGE_RUNTIME_ENV=development python app.py

It intentionally targets Supabase Development.

==================================================
1. FINAL PREFLIGHT
==================================================

Immediately before migration, reverify:

- target is Supabase Development
- target identity matches:
  sha256:2e1590206884…
- alembic_version = bl52_trip_stay
- BL-78 absent
- BL-79 absent
- BL-80 absent
- BL-70 absent
- verified backup objects still exist in App Storage

Do not access Production.

If any migration/schema assumption changed since backup:

STOP before mutation.

==================================================
2. APPLY BOUNDED MIGRATION
==================================================

Migrate Supabase Development explicitly to:

bl80_trip_lifecycle

Approved migration path:

bl52_trip_stay
→ bl78_rsvp_transition
→ bl79_friend_history
→ bl80_trip_lifecycle

DO NOT apply:

bl70_user_season_pass

Do NOT run unrestricted:

alembic upgrade head

Use the guarded Development database target and explicit revision.

==================================================
3. VERIFY POST-MIGRATION DATABASE STATE
==================================================

After migration verify:

- alembic_version = bl80_trip_lifecycle
- BL-78 ski_trip_rsvp_transition exists
- BL-79 friend_connection_event exists
- ski_trip.lifecycle_state exists
- ski_trip.terminal_at exists
- ski_trip_lifecycle_event exists
- lifecycle constraints exist
- required FKs exist
- required indexes exist
- BL-70 user_season_pass remains absent

Also verify:

- all 15 existing trips still exist unless a pre-existing data condition explains otherwise
- existing trips were NOT assigned fabricated completed/cancelled lifecycle states
- no synthetic lifecycle events were created
- existing participant/history data remains intact

Do not manually edit trip data.

==================================================
4. RESTART NORMAL FLASK WORKFLOW
==================================================

Restart the existing saved workflow unchanged:

env BASELODGE_RUNTIME_ENV=development python app.py

Do not:
- change the workflow command
- point it at Replit DB
- change ports
- modify runtime architecture

Ensure port 5000 is owned only by the intended Flask process/reloader tree.

==================================================
5. AUTHENTICATED APPLICATION SMOKE TEST
==================================================

Using the normal Supabase Development runtime, verify:

- login/auth flow loads
- Home loads
- My Trips loads
- Friends loads
- Mountains loads
- at least one existing Trip Detail loads if safely accessible
- no generic “Something went wrong” page
- no missing lifecycle_state errors
- no missing lifecycle-event table errors
- no HTTP 5xx responses
- no browser console errors
- no server tracebacks

Do NOT perform destructive completion/cancellation against meaningful Development trips merely for smoke testing.

==================================================
6. REGRESSION / SAFETY
==================================================

No code changes should be necessary.

Run a focused post-migration smoke suite for BL-80 schema/runtime compatibility.

Also run:

git diff --check

Do not rerun the entire full suite unless a new defect appears.

If a code change appears necessary:

STOP and report the incompatibility rather than silently modifying BL-80.

==================================================
7. PRODUCTION ISOLATION
==================================================

Production must remain completely untouched.

Do not:
- query Production
- migrate Production
- test Production
- change Production secrets/configuration

==================================================
8. RETURN
==================================================

Return:

A. final pre-migration Supabase Development revision
B. confirmation verified backup still existed
C. exact Alembic command/revision applied
D. migrations applied
E. post-migration alembic_version
F. confirmation BL-70 was NOT applied
G. schema verification
H. existing trip/data preservation result
I. confirmation no lifecycle history was fabricated
J. Flask workflow restart result
K. authenticated smoke-test results
L. browser/server error status
M. focused post-migration test results
N. git diff --check result
O. confirmation Production was untouched
P. whether BL-80 is now fully rolled out and safe to close

Do not begin BL-87.
Do not implement Task #395 or #398 as separate scope.
Do not make unrelated changes.