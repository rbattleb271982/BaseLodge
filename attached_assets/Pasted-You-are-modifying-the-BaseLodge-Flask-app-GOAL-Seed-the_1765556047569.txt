You are modifying the BaseLodge Flask app.

GOAL:
Seed the database with:
1) Two real main users (Richard Battle-Baxter and Jonathan Schmitz)
2) 50 realistic dummy users
3) Bidirectional friend connections between:
   - Every dummy user ↔ Richard
   - Every dummy user ↔ Jonathan
4) Trips, overlaps, open days, and fully completed profiles

IMPORTANT RULES:
- DO NOT break existing routes, auth, or onboarding
- DO NOT modify templates
- DO NOT hardcode IDs — always query by email
- Use existing models only (User, Friend, SkiTrip or Trip model used in this app)
- This should be safe to run once (idempotent: skip creation if user already exists)

--------------------------------------------------
STEP 1: CREATE / ENSURE TWO MAIN USERS
--------------------------------------------------

Create or fetch users with the following exact attributes:

User 1:
- First name: Richard
- Last name: Battle-Baxter
- Email: richardbattlebaxter@gmail.com
- Password: 12345678 (use the app’s existing password hashing logic)
- Rider type: Skier
- Pass type: Epic
- Profile: fully completed (home_state, skill_level, birth_year if supported)

User 2:
- First name: Jonathan
- Last name: Schmitz
- Email: jonathanmschmitz@gmail.com
- Password: 12345678
- Rider type: Skier
- Pass type: Epic
- Profile: fully completed

If either user already exists, DO NOT recreate — just fetch them.

--------------------------------------------------
STEP 2: GENERATE 50 REALISTIC DUMMY USERS
--------------------------------------------------

Create 50 users with:
- Realistic first + last names (no placeholders like Test User)
- Unique emails (example: firstname.lastname+skiXX@gmail.com)
- Password: skitest123
- Rider type: Skier or Snowboarder (mix)
- Pass type: Epic or Ikon (mix)
- Skill level: Beginner / Intermediate / Advanced (random)
- Home state: realistic ski states (CO, UT, CA, WA, MT, VT, NY, WY)
- All profile fields filled out except optional gear fields

--------------------------------------------------
STEP 3: FRIEND CONNECTIONS
--------------------------------------------------

For EVERY dummy user:
- Create bidirectional Friend records:
  - Dummy ↔ Richard
  - Dummy ↔ Jonathan

Ensure:
- No duplicate Friend rows
- Friendship works both directions (A → B and B → A)

--------------------------------------------------
STEP 4: CREATE TRIPS (IMPORTANT)
--------------------------------------------------

For EACH dummy user:
- Create exactly 4 trips
- Trips must:
  - Be future-dated (next 6 months)
  - Use real ski mountains already in the database
  - Have realistic start/end dates (2–5 days)

OVERLAP REQUIREMENTS:
- At least 30% of users should share:
  - Same mountain
  - Overlapping dates
- At least 10 users should overlap with BOTH Richard and Jonathan
- Some trips should overlap across multiple users (to test social density)

OPEN DAY REQUIREMENTS:
- Some trips must include “open days” (flexible or partially open)
- Mark these using the app’s existing open/flexible logic if present

--------------------------------------------------
STEP 5: TRIPS FOR RICHARD & JONATHAN
--------------------------------------------------

Create 3–5 trips each for:
- Richard
- Jonathan

Ensure:
- They overlap with dummy users
- They share at least one common trip together

--------------------------------------------------
STEP 6: SAFETY + OUTPUT
--------------------------------------------------

- Wrap everything in a Flask app context
- Commit once at the end
- Print a short summary:
  - Users created
  - Friends created
  - Trips created
- Do NOT expose passwords in logs
- Do NOT touch migrations

STOP when complete.
