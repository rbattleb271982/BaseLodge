🚨 PUT THIS IN AGENT
Use checkpoints sparingly. Follow the steps in order.
Apply the “if there’s a check” rule everywhere:
If something already exists, update/modify it.
If it doesn’t exist, insert/add it.
Avoid duplicates.
STEP 1 — Update app.py (login redirect + new /home route)
Find the login route (likely @app.route('/login', methods=['GET', 'POST'])).
Inside the block where login is successful and you currently do a redirect (probably to /profile or /trips), change that redirect to:
return redirect(url_for('home'))
If there’s a check and the redirect already points to home, leave it.
Add or update the /home route.
If a /home route already exists, update it to load:
The current logged-in user’s upcoming trips
Friends’ upcoming trips
A combined list for “All Trips”
If no /home route exists, create one similar to:
@app.route('/home')
@login_required
def home():
    from datetime import date

    # Use existing models; adjust names if needed (SkiTrip, Trip, Friend, etc.)
    today = date.today()

    # My upcoming trips
    my_trips = SkiTrip.query.filter(
        SkiTrip.user_id == current_user.id,
        SkiTrip.start_date >= today
    ).order_by(SkiTrip.start_date.asc()).all()

    # Friends' upcoming trips
    # Use your existing Friend model / relationship.
    # If you already have helper functions, reuse them.
    friend_trips = []
    # TODO: populate friend_trips by querying friends' SkiTrip objects,
    # keeping only upcoming trips sorted by start_date ASC.

    # Combined list for All Trips (upcoming only)
    all_trips = (my_trips or []) + (friend_trips or [])
    # Sort combined by start_date if you have that field
    try:
        all_trips = sorted(all_trips, key=lambda t: t.start_date)
    except Exception:
        pass

    return render_template(
        'home.html',
        user=current_user,
        my_trips=my_trips,
        friend_trips=friend_trips,
        all_trips=all_trips
    )
If there’s a check and your model names are different (e.g., Trip instead of SkiTrip), adapt accordingly using the existing models and relationships in the project.
STEP 2 — Create or Update templates/home.html
If home.html does not exist, create it.
If it does exist, replace/merge the main content with this structure.
Use your existing base template layout (e.g. {% extends "base.html" %} and {% block content %}) if that’s how the app is structured.
Example:
{% extends "base.html" %}

{% block content %}
<div class="home-page">
    <h1 class="home-title">Home</h1>

    <div class="home-tabs">
        <button class="home-tab active" data-tab="my">My Trips</button>
        <button class="home-tab" data-tab="friends">Friends' Trips</button>
        <button class="home-tab" data-tab="all">All Trips</button>
    </div>

    <div class="home-actions">
        <a href="{{ url_for('trips') }}" class="primary-btn add-trip-btn">Add a Trip</a>
    </div>

    <!-- My Trips -->
    <div class="home-tab-panel active" id="tab-my">
        {% if my_trips %}
            {% for trip in my_trips %}
                <div class="trip-card">
                    <div class="trip-card-header">
                        <h3>{{ trip.mountain_name }}</h3>
                        <span class="trip-dates">{{ trip.start_date }} → {{ trip.end_date }}</span>
                    </div>
                    <div class="trip-pass-info">
                        Compatible Pass: <strong>{{ trip.pass_type }}</strong>
                    </div>
                </div>
            {% endfor %}
        {% else %}
            <p class="empty-state">You don’t have any upcoming trips yet.</p>
        {% endif %}
    </div>

    <!-- Friends' Trips -->
    <div class="home-tab-panel" id="tab-friends">
        {% if friend_trips %}
            {% for trip in friend_trips %}
                <div class="trip-card">
                    <div class="trip-card-header">
                        <h3>{{ trip.mountain_name }}</h3>
                        <span class="trip-dates">{{ trip.start_date }} → {{ trip.end_date }}</span>
                    </div>
                    <div class="trip-pass-info">
                        {{ trip.owner.first_name }} {{ trip.owner.last_name }} · Pass: <strong>{{ trip.pass_type }}</strong>
                    </div>
                </div>
            {% endfor %}
        {% else %}
            <p class="empty-state">Your friends don’t have any upcoming trips yet.</p>
        {% endif %}
    </div>

    <!-- All Trips (upcoming only) -->
    <div class="home-tab-panel" id="tab-all">
        {% if all_trips %}
            {% for trip in all_trips %}
                <div class="trip-card">
                    <div class="trip-card-header">
                        <h3>{{ trip.mountain_name }}</h3>
                        <span class="trip-dates">{{ trip.start_date }} → {{ trip.end_date }}</span>
                    </div>
                    <div class="trip-pass-info">
                        {% if trip.user_id == user.id %}
                            You · Pass: <strong>{{ trip.pass_type }}</strong>
                        {% else %}
                            {{ trip.owner.first_name }} {{ trip.owner.last_name }} · Pass: <strong>{{ trip.pass_type }}</strong>
                        {% endif %}
                    </div>
                </div>
            {% endfor %}
        {% else %}
            <p class="empty-state">No upcoming trips yet.</p>
        {% endif %}
    </div>
</div>

<script>
// Tab switching (JS-only, no reloads)
document.addEventListener('DOMContentLoaded', function () {
    const tabs = document.querySelectorAll('.home-tab');
    const panels = document.querySelectorAll('.home-tab-panel');

    tabs.forEach(tab => {
        tab.addEventListener('click', () => {
            const target = tab.getAttribute('data-tab');

            tabs.forEach(t => t.classList.remove('active'));
            tab.classList.add('active');

            panels.forEach(panel => {
                if (panel.id === 'tab-' + target) {
                    panel.classList.add('active');
                } else {
                    panel.classList.remove('active');
                }
            });
        });
    });
});
</script>
{% endblock %}
If there’s a check (e.g., some of this structure already exists), update/merge instead of duplicating.
STEP 3 — Update Global Navigation (templates/base.html or nav include)
Find where the main navigation bar is rendered (often in base.html or a partial like _nav.html).
Ensure the nav includes Home, Trips, Friends, Profile and uses dynamic active classes:
<nav class="main-nav">
    <a href="{{ url_for('home') }}"
       class="{% if request.path.startswith('/home') %}active{% endif %}">
       Home
    </a>

    <a href="{{ url_for('trips') }}"
       class="{% if request.path.startswith('/trips') %}active{% endif %}">
       Trips
    </a>

    <a href="{{ url_for('friends') }}"
       class="{% if request.path.startswith('/friends') or '/profile/' in request.path %}active{% endif %}">
       Friends
    </a>

    <a href="{{ url_for('profile') }}"
       class="{% if request.path == '/profile' %}active{% endif %}">
       Profile
    </a>
</nav>
If there’s a check and some links already exist:
Update URLs and class logic.
Avoid creating duplicate <nav> elements.
STEP 4 — Ensure /profile is NOT the landing page, and add Invite Button if missing
In templates/profile.html, confirm:
The summary card exists at the top (as previously implemented).
The profile edit section is wrapped with id="profile-edit".
Replace any remaining dropdowns for Pass Type and Skier/Snowboarder with segmented controls (if not already done). You can mirror the structure used in setup_profile.html.
Apply the “if there’s a check” rule:
If segmented controls already exist → leave them.
If selects still exist → replace them with segmented controls and use a hidden input field for the actual value.
If an Invite a Friend button is not yet present on /profile, add it above the Settings section:
<button id="invite-friend-btn" class="primary-btn invite-btn">
  Invite a Friend to BaseLodge
</button>

<script>
document.addEventListener('DOMContentLoaded', function () {
    const inviteBtn = document.getElementById('invite-friend-btn');
    if (inviteBtn) {
        inviteBtn.addEventListener('click', function() {
            const message = "Hey! I’m using BaseLodge to connect with friends for ski trips. You should join — it’s free and makes planning super easy: https://baselodge.app";

            if (navigator.share) {
                navigator.share({ text: message });
            } else {
                window.location.href = "sms:?body=" + encodeURIComponent(message);
            }
        });
    }
});
</script>
If there’s a check and an invite button already exists, update its styling/JS instead of adding a duplicate.
Confirm the Settings section with Log Out is at the bottom of /profile.
STEP 5 — Update static/styles.css for Home and Tabs (and reuse existing trip card styles)
Open static/styles.css and:
If .trip-card styles already exist, leave them and reuse.
If not, add:
.trip-card {
    background: #FFFFFF;
    border: 1px solid #E5E5E5;
    border-radius: 10px;
    padding: 14px;
    margin-bottom: 12px;
}

.trip-card-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 6px;
}

.trip-card-header h3 {
    margin: 0;
    font-size: 1.05rem;
}

.trip-dates {
    font-size: 0.85rem;
    color: #555;
}

.trip-pass-info {
    font-size: 0.9rem;
    color: #333;
}
Add or update styles for the Home page tabs:
.home-page {
    padding-bottom: 70px; /* room for bottom nav */
}

.home-title {
    font-size: 1.4rem;
    font-weight: 600;
    margin-bottom: 12px;
}

.home-tabs {
    display: flex;
    gap: 8px;
    margin-bottom: 12px;
}

.home-tab {
    flex: 1;
    padding: 8px 10px;
    border-radius: 999px;
    border: 1px solid #DDD;
    background: #F7F7F7;
    font-size: 0.9rem;
}

.home-tab.active {
    background: #8F011B;
    color: #FFF;
    border-color: #8F011B;
}

.home-tab-panel {
    display: none;
}

.home-tab-panel.active {
    display: block;
}

.home-actions {
    margin-bottom: 16px;
}

.add-trip-btn {
    display: inline-block;
    padding: 8px 12px;
    border-radius: 8px;
    background: #8F011B;
    color: #FFF;
    font-size: 0.9rem;
    text-decoration: none;
}

.empty-state {
    font-size: 0.9rem;
    color: #777;
    margin-top: 8px;
}
Make sure your .main-nav styles still look correct; if they’re missing, add:
.main-nav {
    display: flex;
    justify-content: space-around;
    padding: 10px 0;
    border-top: 1px solid #DDD;
    background: #FFF;
    position: fixed;
    bottom: 0;
    width: 100%;
}

.main-nav a {
    text-decoration: none;
    font-size: 0.85rem;
    color: #444;
}

.main-nav a.active {
    color: #8F011B;
    font-weight: 600;
}
If there’s a check (existing .main-nav etc.), update rather than duplicate.
STEP 6 — Save All Files
Use no more than 2–3 checkpoints across the whole operation.
Double-check that:
Logging in now lands you on /home.
Tabs switch between My / Friends’ / All trips without reloading.
Bottom nav is present and highlights Home on /home.
/profile is still accessible via nav, but no longer the initial landing page.