Fix the mountain search/add behavior on both pages:

- Mountains Visited / Where you've ridden
- Wishlist mountains

Problem:
Typing in the add/search field is currently filtering only the already-selected saved list. That is wrong.

Required behavior:
The search field should search the full available Resort dataset, not just the selected list.

Implementation:

1. Keep the saved mountain list as-is.
- It should show the user’s already-selected mountains.
- Keep grouping headers.
- Keep remove links.
- Do not use the saved list as the search source.

2. Add/restore a separate search results panel under the input.
- When the user types 1+ characters, show matching available resorts.
- Match against resort name only.
- Use case-insensitive includes().
- Example: typing “Wi” should show “Winter Park” if it exists in the Resort table.

3. The search results panel should use the full active resort dataset:
- Resort.is_active == true
- Resort.is_region == false

4. Exclude already-selected resorts from search results.
- If a user already added Courchevel, do not show Courchevel as an add result.
- But keep Courchevel visible in the saved list.

5. When a result is tapped:
- Call the existing add route:
  - Wishlist: /api/wishlist/add
  - Visited: /api/mountains-visited/add
- On success:
  - Add the mountain to the saved list
  - Remove it from the search results
  - Clear the input or leave it typed, whichever is already consistent with current behavior

6. Verify Winter Park:
- Confirm Winter Park exists in the Resort table.
- If it is missing, inactive, or marked as region, report that explicitly before changing seed data.
- If it exists and is active/non-region, “Wi” must show it.

7. Remove extra metadata from saved rows:
- Do not display state, province, region, or country under the saved mountain name.
- Saved rows should show:
  - Mountain name
  - remove action

8. Empty states:
- If search has no matches, show:
  “No matching mountains found”
- Do not show a blank screen.

9. Preserve styling:
- Cream background
- Existing typography
- Existing dividers
- Bottom nav
- Remove link styling

10. Test:
- Type “Wi” → Winter Park appears if active/non-region
- Tap Winter Park → it adds to saved list
- Winter Park disappears from search results after adding
- Type “Cour” after Courchevel is already selected → Courchevel should not appear as an add result
- Removing a mountain makes it eligible to appear in search results again
- Repeat for both Wishlist and Mountains Visited pages

Use Assistant, not Agent.
Keep the change surgical.