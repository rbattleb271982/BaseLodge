PUT THIS IN AGENT

CONTEXT
Desktop select for Country works.
Mobile Safari tap does NOT open picker.
We must NOT refactor logic or replace selects.
Fix mobile touch interception ONLY.

--------------------------------
ROOT CAUSE (DO NOT DEBATE)
--------------------------------
A transparent overlay, pseudo-element, or positioned child is intercepting touch events on iOS.
Desktop clicks pass through. iOS does not.

--------------------------------
MANDATORY FIX (STEP BY STEP)
--------------------------------

1. IDENTIFY THE COUNTRY SELECT

In templates/add_trip.html locate:

<select id="country" name="country">

Do NOT change its logic or options.

--------------------------------

2. REMOVE TOUCH INTERCEPTION (CRITICAL)

Immediately after the Country select, add:

<div class="select-touch-shield"></div>

Then update CSS in static/css/style.css:

/* ---- iOS touch interception fix ---- */

.select-touch-shield {
  position: absolute;
  inset: 0;
  pointer-events: none;
}

.native-select-wrapper {
  position: relative;
  z-index: 30;
}

select {
  pointer-events: auto !important;
  position: relative !important;
  z-index: 31 !important;
}

--------------------------------

3. KILL ALL OVERLAYS ABOVE SELECTS (MOBILE ONLY)

Add this MOBILE-ONLY override:

@media (hover: none) and (pointer: coarse) {
  .card::before,
  .card::after,
  .trip-basics-card::before,
  .trip-basics-card::after,
  .select-arrow,
  .dropdown-icon {
    display: none !important;
  }
}

--------------------------------

4. REMOVE EVENT INTERCEPTION (DO NOT TOUCH DESKTOP)

Search codebase for:

- preventDefault()
- stopPropagation()

If found on ANY wrapper around the select:
- Remove it ONLY for mobile using a media query or pointer check

Example safeguard:

if (window.matchMedia('(pointer: coarse)').matches) {
  // do not intercept clicks
}

--------------------------------

5. CONFIRM REAL SELECT RECEIVES TAP

Add TEMP debug (remove after verification):

<script>
document.getElementById('country').addEventListener('touchstart', () => {
  console.log('iOS touch reached country select');
});
</script>

--------------------------------

ACCEPTANCE CRITERIA (NON-NEGOTIABLE)

- Desktop behavior unchanged
- iOS Safari: tapping Country opens native picker
- No floating “Choose country” text bubble
- State unlocks after selection

If desktop breaks, rollback and reapply ONLY steps 2–3.

--------------------------------
END PROMPT
--------------------------------