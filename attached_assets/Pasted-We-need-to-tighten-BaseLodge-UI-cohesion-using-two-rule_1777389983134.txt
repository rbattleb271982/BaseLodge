We need to tighten BaseLodge UI cohesion using two rules:
A consistent interaction system
Apple-style copy principles: clear, concise, sentence case, action-first language
Do not redesign layouts. Do not move components. Do not change routes, backend logic, database logic, or product flows.
First, inspect the relevant logged-in templates and CSS. Then implement only the safe fixes below.
Interaction system rules
Primary buttons
Use one primary button style for main actions:
Save
Continue
Plan a trip
Invite via text
Save availability
Primary buttons should use the existing BaseLodge primary button system:
16px font size
600 weight
full width when in page flow
consistent burgundy fill
consistent radius and padding
Navigation links
Use one navigation link style for action links that move the user somewhere:
Add availability →
Invite friends →
View →
Invite →
Rules:
16px
500 weight
burgundy
no underline
trailing arrow where appropriate
Inline utility links
Use sparingly for utility actions:
Copy invite link
Rules:
keep visually secondary
no new color
avoid mixing underline + arrow
Muted/destructive/low-priority actions
Use one muted style for:
remove
cancel
remove friend
Rules:
muted color
smaller than primary action
no visual competition with primary actions
do not make destructive actions loud
Apple-style copy cleanup
Apply these safe copy fixes where they appear:
“Invite Friends” → “Invite friends”
“Add your open availability here” → “Add availability”
“Have your friends scan this to connect.” → “Scan to connect.”
“Add a mountain you've skied” → “Add a mountain”
Also review obvious title-case button labels and convert to sentence case where appropriate, but do not rewrite large blocks of copy.
Specific things to fix
Remove underline + arrow combinations.
If a link has an arrow, it should not be underlined.
Keep “Copy invite link” as a utility action.
It can remain underlined if that is the existing inline utility style, but it should not be bigger or visually stronger than the primary “Invite via text” button.
Normalize “remove” links.
Use a muted action style.
Do not underline unless already part of the muted destructive utility pattern.
Do not make them burgundy.
Preserve Home screen row cards.
Do not turn Home “Plan a trip” or “Invite a friend” into primary buttons.
They are navigation rows.
Preserve section labels and metadata.
Do not change uppercase section labels like PROFILE, ACCOUNT, MAIN SETUP.
Do not enlarge metadata just because it is small.
Output
Before changing anything, summarize:
Files you found with inconsistent button/link/copy patterns
Which changes are safe to implement
Which items you are intentionally leaving alone
Then implement only the safe fixes above.
After implementation, report:
Files changed
Classes updated
Copy changes made
Any items left unchanged and why
QA these screens:
Home
Trips
Ideas
Friends
Invite friends
Profile
Wish list
Mountains visited
Add availability
Equipment
Edit profile
Do not push or publish.