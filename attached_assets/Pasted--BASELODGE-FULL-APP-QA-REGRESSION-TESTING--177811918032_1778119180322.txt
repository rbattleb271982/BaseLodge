╔════════════════════════════════════════════════════╗
║ BASELODGE FULL APP QA + REGRESSION TESTING        ║
╚════════════════════════════════════════════════════╝

Run a comprehensive QA and regression test pass across the full BaseLodge app.

Goal:
Verify that recent changes did not break core app behavior, routing, layout, mobile usability, data integrity, or production safety.

Recent changes to specifically validate:
- Multi-date trip creation
- created_in_batch_id field
- Home Happening grouping
- Friends’ Trips grouping
- Grouped trip bottom sheet
- Tappable card system
- Removed inline arrows from tappable cards
- Profile metadata links
- Stats row affordances
- Mountain detail header refinement
- Global profile bubble on main tabs
- Trips/Friends header alignment
- Trips/Friends secondary actions converted from pills to text links

────────────────────────────────────
PART 1 — STARTUP + SERVER HEALTH
────────────────────────────────────

Verify:
- App starts cleanly with no Python errors
- No template errors
- No migration errors
- No console-breaking JavaScript errors
- No missing static assets
- No broken imports
- No broken route registration
- Startup migration for created_in_batch_id is safe/no-op on repeat startup

Check:
- /health
- /admin/version if available
- main logged-in app pages

────────────────────────────────────
PART 2 — AUTH + SESSION BASICS
────────────────────────────────────

Verify:
- Login works
- Logout works
- Signup still works if testable
- Existing users can access logged-in pages
- Unauthenticated users are redirected appropriately
- Admin/debug routes remain protected
- Session behavior is stable after refresh

Do not alter real production users.

────────────────────────────────────
PART 3 — GLOBAL NAVIGATION + APP SHELL
────────────────────────────────────

Verify all bottom nav tabs:
- Home
- Trips
- Friends
- Mountains

For each tab verify:
- Page loads
- Correct tab is active
- No layout overflow
- Bottom nav safe area works
- Page content is not hidden behind bottom nav
- Profile bubble appears where expected
- Profile bubble routes correctly
- Top spacing works on mobile widths

Verify:
- No old Ideas tab remains
- Header hierarchy is consistent across Trips, Friends, and Mountains
- Page title + profile bubble align horizontally where applicable
- Tabs/actions align horizontally where applicable

────────────────────────────────────
PART 4 — HOME PAGE QA
────────────────────────────────────

Verify:
- Home page loads for logged-in user
- Welcome/profile card renders correctly
- Profile metadata links work:
  - rider type
  - skill level
  - pass type
  - skis
  - boots
- Stats row links work independently:
  - Upcoming Trips
  - Mountains Visited
  - Wishlist Mountains
- Happening section renders
- Happening grouped repeated trips correctly when 3+ future trips exist for same friend/mountain/status
- Happening does not group 1–2 trips
- Happening splits groups by status
- Happening does not include past trips in grouped rows
- Opportunity cards are visually distinct cards
- Removed inline arrows do not break tap behavior
- Dismiss X works and does not accidentally open the card
- Press states feel subtle and do not cause layout jump

────────────────────────────────────
PART 5 — TRIPS PAGE QA
────────────────────────────────────

Verify:
- Trips page loads
- Trips title and RB bubble align horizontally
- Season Snapshot appears as lightweight text action in the tabs/action row
- Season Snapshot still works
- My Trips tab works
- Suggested tab works
- Tabs and Season Snapshot align on same row
- Tab spacing/underline are correct
- Existing trips display correctly
- Trip rows route to correct trip detail page
- “Plan another trip” works
- Trips tab still shows individual trips, not grouped rows
- Bottom nav does not cover content

────────────────────────────────────
PART 6 — ADD TRIP / MULTI-DATE CREATION QA
────────────────────────────────────

Test single-trip creation:
- Select mountain
- Pick one date range
- Save
- Confirm one trip is created
- Confirm redirect goes to trip detail
- Confirm trip appears in My Trips
- Confirm trip appears on profile where expected

Test multi-date creation:
- Select one mountain
- Add 2 arbitrary date ranges
- Save
- Confirm two separate trips are created
- Confirm redirect goes to My Trips
- Confirm each trip has its own detail page
- Confirm each trip can be edited independently

Test grouping threshold:
- Create 3+ future trips for same mountain/user/status
- Verify Friends/Home feed grouping where applicable
- Verify Trips page remains individual
- Verify Profile page remains individual

Validation tests:
- Overlapping ranges within same batch are rejected or skipped safely
- Date ranges overlapping existing trips are skipped safely
- Past dates are rejected
- Incomplete ranges cannot be submitted
- Remove staged date range works
- CTA text updates correctly:
  - Create trip
  - Save 2 dates
  - Save N dates
- date_ranges_json is populated only when appropriate
- group/friend proposal flow still uses single date range and is not broken

Data checks:
- Each created trip has normal trip fields populated
- Batch-created trips share created_in_batch_id
- created_in_batch_id is not used as a series object
- No parent/series/recurrence behavior exists

────────────────────────────────────
PART 7 — TRIP DETAIL QA
────────────────────────────────────

Verify:
- Trip detail page loads for normal trips
- Trip detail page loads for batch-created trips
- Editing one batch-created trip does not change the others
- Deleting/canceling one batch-created trip does not delete the others
- Friend trip detail links still work
- Back navigation works
- No layout overflow on mobile

────────────────────────────────────
PART 8 — FRIENDS PAGE QA
────────────────────────────────────

Verify:
- Friends page loads
- Friends title and RB bubble align horizontally
- Invite appears as lightweight text action in the tabs/action row
- Invite still works
- Friends tab works
- Friends’ Trips tab works
- Friends/Friends’ Trips tabs align with Invite on same row
- Friends tab spacing matches Trips tab spacing
- Search friends works
- Friend rows route correctly
- Friend trip counts display correctly

Friends’ Trips grouping:
- 1–2 trips at same mountain show individually
- 3+ trips same friend/mountain/status group into one row
- Grouped row displays compact date label like Dec–Mar
- Grouped row displays correct count
- Mixed statuses split into separate groups
- Past trips are excluded from grouped future rows
- Tapping grouped row opens bottom sheet
- Bottom sheet title is correct
- Bottom sheet subtitle/count is correct
- Each bottom sheet row links to correct /friend-trip/<trip_id>
- Bottom sheet closes correctly
- Bottom sheet does not break scrolling
- Non-grouped rows still route normally

────────────────────────────────────
PART 9 — FRIEND PROFILE QA
────────────────────────────────────

Verify:
- Friend profile page loads
- Friend upcoming trips display individually
- No grouping on profile pages
- Each individual trip routes correctly
- Friend profile tabs/sections still work
- Existing metadata displays correctly

────────────────────────────────────
PART 10 — MOUNTAINS PAGE QA
────────────────────────────────────

Verify:
- Mountains page loads
- Mountains title and RB bubble align horizontally
- Search works
- Country/state/pass filters work
- Clear filters works
- Mountain rows route correctly
- Friend count indicator works
- Chevron navigation rows remain intact
- No cards were incorrectly introduced
- No bottom nav overlap

Mountain detail page:
- Header now uses compact title format:
  - Beaver Creek, CO
- Standalone state eyebrow removed
- Metadata row appears as:
  - Not on your pass · 1 friend going · 12 trips planned
- No snow/weather/conditions row added
- Trip list below starts higher than before
- Existing trip list remains unchanged
- Back navigation works
- No layout overflow on small mobile widths

────────────────────────────────────
PART 11 — PROFILE PAGE QA
────────────────────────────────────

Verify:
- Profile page loads
- Profile identity links are maroon and subtly underlined
- Rider/skill/pass links route directly to correct edit destinations
- Ski/boot links route correctly
- Stats row labels show subtle interactive affordance
- Each stat is individually tappable
- Profile sections still display correctly
- No name/header clipping on scroll
- No floating save button overlap on settings pages

────────────────────────────────────
PART 12 — SETTINGS / EDIT FLOWS QA
────────────────────────────────────

Verify:
- Edit profile works
- Pass selector works
- Equipment editing works
- Account/settings rows retain true chevrons
- Accordion/disclosure arrows still work where intended
- Save buttons work
- Form validation still works
- Numeric inputs still use numeric keyboard where applicable
- Brand/model inputs avoid autocorrect where applicable

────────────────────────────────────
PART 13 — INTERACTION CONSISTENCY QA
────────────────────────────────────

Audit for incorrect arrows:
- No inline → remains on tappable cards where the card itself is the action
- True navigation rows still use chevrons where appropriate
- Back arrows remain
- Accordion/disclosure arrows remain
- Calendar navigation arrows remain

Audit card system:
- Opportunity/feed/invite surfaces are cards
- Settings/profile/mountains/friends utility rows remain row-based
- Dismiss X is visually secondary
- Dismiss X tap does not trigger parent card tap
- Press states are subtle scale + opacity
- No heavy shadows or dashboard styling introduced

────────────────────────────────────
PART 14 — MOBILE RESPONSIVENESS QA
────────────────────────────────────

Test at iPhone-like widths:
- 390px
- 430px
- narrow/small width if available

Verify:
- No horizontal scrolling
- No clipped headers
- No overlapping profile bubble
- No tab label wrapping issues
- No search/filter overflow
- Bottom sheets fit screen
- Bottom nav safe area works
- Primary CTAs remain visible and tappable
- Content does not hide behind bottom nav

────────────────────────────────────
PART 15 — JAVASCRIPT + CONSOLE QA
────────────────────────────────────

Check browser console while using:
- Home
- Trips
- Add Trip
- Friends
- Grouped bottom sheet
- Mountains
- Profile/settings

Verify:
- No uncaught JavaScript errors
- No broken event listeners
- No duplicate submit issues
- No missing element errors
- No console spam from production analytics
- PostHog still loads without noisy debug logs

────────────────────────────────────
PART 16 — SECURITY / PRODUCTION SAFETY QA
────────────────────────────────────

Verify:
- No localhost/staging URLs leaked into templates
- No secrets printed client-side
- No raw tokens logged
- No sensitive user data exposed in HTML unnecessarily
- Debug/admin routes remain protected
- Forms that should be POST remain POST
- CSRF still works where applicable
- Rate limiting remains intact where applicable

────────────────────────────────────
PART 17 — FINAL REPORT
────────────────────────────────────

Return a clear QA report with:

1. PASS / FAIL summary
2. Files inspected
3. Flows tested
4. Any bugs found
5. Severity for each bug:
   - blocker
   - high
   - medium
   - low
6. Recommended fixes
7. Whether it is safe to publish
8. Whether GitHub push is safe after publish
9. Whether native/Xcode/TestFlight rebuild is required

Do not mark complete until testing has actually been performed.

If anything fails, stop and report the issue clearly before making broad unrelated changes.

Use checkpoints sparingly. Only use them if absolutely necessary.