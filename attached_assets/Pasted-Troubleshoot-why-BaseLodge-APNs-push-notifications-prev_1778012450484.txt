Troubleshoot why BaseLodge APNs push notifications previously worked but now return:

{"error":"BadEnvironmentKeyInToken","status_code":403}

Do not make broad rewrites. Diagnose first, then apply the smallest safe fix.

Context:
- iOS uses direct APNs, not Firebase.
- TestFlight/App Store builds require APNs production.
- Xcode local/debug installs usually require APNs sandbox.
- This error usually means the APNs endpoint environment does not match the device token environment.
- Current env vars include:
  - APNS_KEY_P8
  - APNS_KEY_ID
  - APNS_TEAM_ID
  - APNS_BUNDLE_ID
  - APNS_USE_SANDBOX

Tasks:
1. Find the APNs sending code and confirm how it chooses sandbox vs production endpoint.
2. Add safe diagnostics to the admin test push output/logs:
   - APNS_USE_SANDBOX value
   - resolved APNs endpoint host
   - APNS_BUNDLE_ID
   - token platform
   - token created_at / updated_at if available
   - token active status
   - APNs status_code
   - APNs error reason
   - apns-id
3. Do not log the full APNs token or private key. If showing token, only show the first 8 and last 6 characters.
4. Confirm whether the latest active token being tested came from a TestFlight build or a local Xcode build if that data exists. If it does not exist, add a lightweight source/build_environment field or diagnostic note only if it can be done safely without breaking existing tokens.
5. If APNS_USE_SANDBOX is currently true while testing TestFlight, clearly report that production APNs is required and set the app to use production only if env config allows.
6. Make the admin test push page/report clearly explain:
   - “BadEnvironmentKeyInToken means this token belongs to the other APNs environment.”
   - “Use sandbox for local Xcode installs.”
   - “Use production for TestFlight/App Store installs.”
7. Ensure inactive/old tokens are not selected ahead of the latest valid active token.
8. Add defensive handling so if APNs returns BadDeviceToken or Unregistered, that token is marked inactive.
9. Do not change push permission flow, AppDelegate, Capacitor config, or frontend registration logic unless diagnostics prove the issue is there.
10. Use checkpoints sparingly.

After changes, test:
- Run the admin test push route.
- Confirm the response shows the resolved APNs environment.
- Confirm whether the selected token is active and latest.
- Confirm BadEnvironmentKeyInToken is either resolved or clearly diagnosed with the exact environment mismatch.
- Do not finish until the push test route has been run and the result is reported.