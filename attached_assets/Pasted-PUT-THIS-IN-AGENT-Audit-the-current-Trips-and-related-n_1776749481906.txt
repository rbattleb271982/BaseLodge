PUT THIS IN AGENT

Audit the current Trips and related navigation structure so I understand exactly what exists before making any changes.

Goal:
There has been discussion about a “Join a Trip” surface, but I am not sure if it actually exists in the current implementation. I want a clear, factual breakdown of what is currently built.

What to audit

1. Trips tab structure
- What tabs or sections currently exist inside the Trips page?
- Exact names of tabs, sections, or toggles
- Which route renders the Trips page
- Which template renders it
- Any supporting JS or service files involved

2. Any “Join a Trip” implementation
- Does a “Join a Trip” tab, section, route, partial, or card currently exist anywhere in the codebase?
- Search the codebase, do not assume
- If yes:
  - where is it defined
  - what route/template/file powers it
  - what data it shows
- If no:
  - explicitly confirm that it does not currently exist anywhere meaningful in the app

3. Discovery surfaces
List all current surfaces where a user can discover other people’s trips, including but not limited to:
- Ideas tab
- Friends > Upcoming
- Trips page
- Home screen
For each one, explain briefly what it currently shows and how it is intended to function.

4. Trip detail page capabilities
Audit the current trip detail page and tell me:
- what actions are currently available
- whether a user can only view
- whether a user can join
- whether a user can request to join
- whether a user can express interest
- whether owner/participant logic already exists

5. Data model support
Check the models and supporting logic and tell me whether anything already exists for:
- join requests
- trip participants
- pending approval states
- owner acceptance / decline flows

6. Current product reality
At the end, give me a plain-English summary of:
- what exists today
- what does not exist today
- whether “Join a Trip” is real, partial, or only conceptual right now
- where joining functionality currently lives, if anywhere

Important:
- Do not change any code
- Do not build anything yet
- This is a read-only audit only

Deliverable:
Give me a clean summary with:
- routes
- templates
- files involved
- current behavior
- missing behavior

Only use checkpoints if I explicitly ask for them.