PUT THIS IN PLAN FIRST.

# Onboarding Step 2 Redesign (Identity Setup)

We are redesigning Step 2 of onboarding to improve visual symmetry and readability.

IMPORTANT:
- Do NOT change any database fields, stored values, form names, backend logic, onboarding flow, validation, routes, or profile mappings.
- This is a UI/UX change only.
- Use checkpoints sparingly and only if truly necessary.

## Goal

Replace the current pill-style wrapping layout with a clean vertical list of full-width selectable cards.

The screen should feel balanced, symmetrical, and mobile-first.

## Riding Options

Keep these exact labels and order:

Skier

Snowboarder

Telemark

Cross-country

Adaptive

Interested (Learning)

None of the Above (Experience)

Do NOT rename any of them.

Do NOT change the values submitted to the backend.

The existing selection behavior should remain exactly the same:
- Single select
- Tap to select
- Tap another option switches selection
- Existing stored value mappings remain unchanged

## Layout

Replace the current multi-column/wrapping pill layout with a vertical stack of equal-width selectable cards.

Example:

[ Skier ]

[ Snowboarder ]

[ Telemark ]

[ Cross-country ]

[ Adaptive ]

[ Interested (Learning) ]

[ None of the Above (Experience) ]

All cards should:
- Match width
- Match height
- Use existing BaseLodge design system
- Have consistent spacing
- Be optimized for mobile

## Skill Level Section

Keep existing logic.

Show skill level for:
- Skier
- Snowboarder
- Telemark
- Cross-country
- Adaptive

Hide skill level for:
- Interested (Learning)
- None of the Above (Experience)

Replace the current skill level presentation with the following labels:

Beginner (Green)

Intermediate (Blue)

Advanced (Black)

Expert (Double Black)

Do NOT change stored values.

Keep existing selection behavior.

## Visual Requirements

- Preserve current colors
- Preserve current typography
- Preserve current selected/unselected states
- Preserve accessibility
- Preserve mobile responsiveness
- Preserve onboarding progress indicator

The goal is simply:
1. Full-width stacked riding options
2. Full-width stacked skill options
3. Cleaner visual symmetry
4. No backend changes

## Testing

Verify:

- Single-select riding still works
- Single-select skill level still works
- Existing values save correctly
- Interested (Learning) hides skill level
- None of the Above (Experience) hides skill level
- Mobile layout looks correct on iPhone-width screens
- No console errors
- No onboarding regressions

Do not finish until you've fully tested the implementation and confirmed all acceptance criteria above are working.