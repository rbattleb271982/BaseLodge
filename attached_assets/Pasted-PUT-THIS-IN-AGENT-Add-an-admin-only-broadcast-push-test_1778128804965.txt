PUT THIS IN AGENT

Add an admin-only broadcast push test route for BaseLodge.

Context:
- /admin/test-push-all currently sends only to active tokens for the logged-in admin user.
- I want a separate broadcast route that sends to every active push token in the database across all users.
- This is for admin QA/testing only.
- iOS APNs works.
- Android FCM works.
- Do not break /admin/test-push.
- Do not break /admin/test-push-all.

Create this route:

GET /admin/test-push-broadcast

Default behavior:
- Visiting /admin/test-push-broadcast with no query params should send the standard test message to every active token.

Default message:
- title: BaseLodge
- body: Test push from BaseLodge

Customization behavior:
- The route should also support optional query params:
  - title
  - body

Example:
- /admin/test-push-broadcast?title=Hi%20Schmitty&body=Does%20tapping%20this%20open%20your%20app%3F

Important:
- The clean/default test URL should remain:
  /admin/test-push-broadcast
- Custom title/body should be optional, not required.

Requirements:
1. Must require login and admin access.
2. Query all active PushDeviceToken rows across all users.
3. Send to every active token:
   - platform="ios" via existing APNs helper
   - platform="android" via existing FCM helper
4. Skip unsupported platform values safely.
5. Include optional data payload:
   - source: "admin_test_push_broadcast"
6. Return safe JSON showing:
   - route
   - admin_user_id
   - title_used
   - body_used
   - total_active_tokens
   - unique_users_targeted
   - ios_attempted
   - android_attempted
   - total_success
   - total_failed
   - unsupported_platforms
   - per-token safe result:
     - token_id
     - user_id
     - platform
     - provider
     - token_preview only
     - success/failure
     - safe error, if any
7. Never expose:
   - full tokens
   - APNs keys
   - Firebase secrets
   - service account values
8. Keep logs safe:
   - token preview only
   - user_id
   - platform
   - provider
   - success/failure
9. Handle:
   - zero active tokens
   - invalid platform
   - per-token failures without stopping the whole broadcast

Run:
- Python syntax check
- app startup/import validation
- route registration check if possible

At the end, report:
- files changed
- exact default URL to test
- exact example URL for a custom message
- whether Replit deploy is required
- whether GitHub push/pull is required
- whether npx cap sync android is required
- whether Android Studio rebuild is required