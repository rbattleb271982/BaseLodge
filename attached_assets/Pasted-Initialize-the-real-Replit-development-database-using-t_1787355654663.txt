Initialize the real Replit development database using the validated #311 bootstrap.
This is a controlled development-only initialization task.
Do not touch production.
Do not restart Flask until the development database is fully initialized and verified.
Preconditions
Use the already configured:
BASELODGE_RUNTIME_ENV=development
BASELODGE_DEVELOPMENT_DATABASE_URL
BASELODGE_MIGRATION_DATABASE_URL
BASELODGE_PRODUCTION_DATABASE_IDENTITY_HASH
The target must resolve to the verified Replit development database identity:
postgresql://helium:5432/heliumdb
with fingerprint:
b6d12084fe886e2c4690c5cbc6619d79f1afe7722f94c29eddcd87d113d8c05e
It must not match the protected production fingerprint:
732b2adf0aaed1c390923f6c581c036d4c20d96ef45b64a893491d04fe0b4ab5
If any identity check fails, STOP.
1. Verify the target is still empty
Before writing anything, confirm:
0 BaseLodge application tables
no BaseLodge enum types
no alembic_version
target is PostgreSQL
target is the configured development database
If the database is no longer empty, STOP and report exactly what exists.
2. Run the validated BL-60 bootstrap
Run the committed #311 bootstrap runner against the verified development database only.
Require:
BASELODGE_BOOTSTRAP_MODE=1
and the explicit development bootstrap database URL.
Do not import Flask or app.py.
Verify the bootstrap contract passes before continuing.
3. Stamp BL-60
After successful bootstrap, use the standalone Alembic path and stamp:
bl60_mtn_filter_edu
Do not use Flask-Migrate/app import.
4. Upgrade to current head
Using:
BASELODGE_RUNTIME_ENV=development
BASELODGE_MIGRATION_MODE=1
BASELODGE_MIGRATION_DATABASE_URL pointing to the same verified development DB
run the normal migration path:
bl60_mtn_filter_edu
→ bl305_ski_day_foundation
→ bl306_mpv_fk_reconcile
Verify current revision becomes:
bl306_mpv_fk_reconcile
5. Verify the initialized development schema
Confirm:
all BL-60 tables exist
ski_day exists
SkiDay FKs have the intended actions
MountainPageView now has:
resort FK with ON DELETE CASCADE
user FK with ON DELETE SET NULL
no orphan FK violations
alembic_version is correct
no bootstrap contract errors occurred
6. Reference data
Do not copy production data.
Do not import production users, trips, push tokens, availability, analytics, or private data.
Do not seed resort/reference data unless there is already a reviewed safe non-production seed mechanism approved for this step.
If the schema initializes successfully but the app would lack resort/reference data, report that as the next separate task rather than improvising a seed.
7. Production safety
During this task:
no production writes
no production reads beyond any already established identity check that does not expose user data
no production migration
no production schema change
no OneSignal calls
no push changes
no BL-77 work
8. Restart gate
Do not restart Flask automatically.
After initialization, determine whether the development database is structurally ready for Flask startup.
If schema is correct but reference data is still missing, say so explicitly.
Leave:
Flask Server stopped
Project workflow stopped
I will approve restart separately.
Commit
This task should not require code changes.
Do not create a commit unless a genuine code fix is unexpectedly required.
If a code change appears necessary, STOP and report why before changing anything.
Return
Report:
development database identity verification
empty-target preflight result
bootstrap result
BL-60 stamp result
BL-305/BL-306 upgrade result
final Alembic revision
SkiDay verification
MountainPageView FK verification
FK integrity result
whether reference/seed data is still missing
whether Flask is structurally safe to restart
confirmation production was untouched
confirmation no push/OneSignal behavior changed
Flask Server status
Project workflow status
BL-77 status
End with exactly one:
DEVELOPMENT DATABASE INITIALIZED — READY FOR RESTART REVIEW
or
DEVELOPMENT DATABASE INITIALIZATION BLOCKED: [reason]