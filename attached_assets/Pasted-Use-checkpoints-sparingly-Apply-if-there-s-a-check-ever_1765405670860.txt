Use checkpoints sparingly. Apply “if there’s a check” everywhere. Do not duplicate UI or navigation.
STEP 1 — REMOVE OLD TRIPS PAGES & ROUTES
1A. In app.py, find routes that match any of the following:
/trips
/my_trips
/my-trips
/profile/trips
If any exist:
Comment out the old logic
Replace the body with:
return redirect(url_for('home'))
If no such routes exist, skip.
1B. In templates/, locate and deprecate old trip pages:
Files that MAY exist:
templates/my_trips.html
templates/trips.html
templates/trip_index.html
If found:
Comment out the contents using {# ... #} OR delete the file
Remove any floating “+” button found inside
Do NOT touch home.html — that is the new authoritative screen.
STEP 2 — REMOVE THE OLD FLOATING “+” BUTTON
Search all templates for:
A green floating button
A button with “+” value
A class like floating-button, fab, or similar
If found:
Remove the entire button block
Remove any associated <style> or JS for floating-button behavior
⚠️ Do NOT remove the “Add a Trip” button inside home.html.
Only remove the pre-existing floating + button.
STEP 3 — ADD OR UPDATE BOTTOM NAVIGATION IN SPECIFIC FILES
Because the app does not use base.html, the bottom nav must be added manually to each main template.
Add or update the bottom nav block at the bottom of these templates only:
templates/home.html
templates/profile.html
templates/friends.html
templates/friend_profile.html
Insert this block only once per file:
<nav class="main-nav">
    <a href="{{ url_for('home') }}" class="{% if request.path.startswith('/home') %}active{% endif %}">
        Home
    </a>
    <a href="{{ url_for('home') }}" class="{% if request.path.startswith('/home') %}active{% endif %}">
        Trips
    </a>
    <a href="{{ url_for('friends') }}" class="{% if request.path.startswith('/friends') or '/profile/' in request.path %}active{% endif %}">
        Friends
    </a>
    <a href="{{ url_for('profile') }}" class="{% if request.path == '/profile' %}active{% endif %}">
        Profile
    </a>
</nav>
Use “if there's a check”:
If a nav block already exists → update it, do not duplicate
If nav styles exist in CSS → reuse them, do not re-add duplicate styles
⚠️ Do NOT add nav to login, signup, reset password, or admin templates.
STEP 4 — UPDATE HOME TO BE THE ONLY TRIPS PAGE
Open templates/home.html.
Ensure these sections exist:
Tab Buttons
<div class="home-tabs">
    <button class="home-tab active" data-tab="my">My Trips</button>
    <button class="home-tab" data-tab="friends">Friends' Trips</button>
    <button class="home-tab" data-tab="all">All Trips</button>
</div>
Add a Trip Button
<a href="{{ url_for('trips') }}" class="add-trip-btn">Add a Trip</a>
(If the project later has /add_trip, this can be updated.)
Tab Panels
Ensure these placeholders exist and have NO floating + buttons:
<div id="tab-my" class="home-tab-panel active">
    <!-- My trips appear here -->
</div>

<div id="tab-friends" class="home-tab-panel">
    <!-- Friends' trips appear here -->
</div>

<div id="tab-all" class="home-tab-panel">
    <!-- Combined feed -->
</div>
Empty state appears ONLY in home.html
Remove duplicates elsewhere.
STEP 5 — ENSURE /PROFILE DOES NOT DISPLAY TRIPS
Open templates/profile.html.
If any of the following appear:
Trip cards
“My Trips” links
“Upcoming Trips”
Trip lists
Floating buttons related to trips
Remove them.
Profile should ONLY show:
Summary card
Edit fields
Segmented controls
Invite a Friend button
Settings / Logout
NOTHING trip-related
STEP 6 — FRIENDS TEMPLATE CLEANUP
Open templates/friends.html.
Ensure:
✔ Invite button
✔ Filter pills
✔ Friends list
Remove:
❌ Any trip cards
❌ Any add-trip CTAs
❌ Any old leftover navigation fragments
STEP 7 — UPDATE LINKS TO OLD TRIPS PAGES
Search all templates for:
url_for('my_trips')
url_for('trips')
/my-trips
/my_trips
Replace them as follows:
“My Trips” links → /home
“Add a Trip” links → /trips (creating/editing flow)
STEP 8 — UPDATE /HOME REDIRECT AFTER LOGIN
Open the login route in app.py.
Find the line that performs a redirect after successful login.
Update it to:
return redirect(url_for('home'))
If already correct, skip.
STEP 9 — CLEAN OUT UNUSED CSS OR JS
Only remove code that supports:
floating + button
old trips-page layouts
deprecated components
unused selectors like .my-trips, .fab, .floating-icon, etc.
Do NOT remove:
trip-card styles
home-tabs styles
segmented controls styles
nav styles
Use “if there’s a check”.
STEP 10 — SAVE ALL FILES & COMPLETE
Use no more than 2–3 checkpoints.