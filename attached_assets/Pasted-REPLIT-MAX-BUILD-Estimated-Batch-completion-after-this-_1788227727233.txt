REPLIT: MAX + BUILD
Estimated Batch completion after this step: ~45%

TASK: BL-168 — Harden auth session lifecycle

Implement the approved Task #409 plan.

Use the PLAN as the implementation contract. Do not re-investigate the entire auth system.

IMPLEMENT ONLY

1. Add versioned Flask-Login identities using existing password authentication state so password changes/reset invalidate older sessions and remember cookies.

2. Centralize successful authentication/session establishment across:
   - email signup
   - email/password login
   - password reset completion
   - Google OAuth

3. When establishing an authenticated session:
   - preserve only explicitly approved invite/onboarding return state
   - clear other pre-authentication session state
   - preserve each path’s current intended remember-me behavior

4. After an in-account password change:
   - invalidate prior sessions
   - log out the current session

5. After password-reset completion:
   - invalidate older sessions
   - establish a fresh valid authenticated session

6. Explicitly configure the remember cookie with the approved SameSite policy matching the existing session-cookie policy.

7. Centralize post-auth redirect validation so only trusted local destinations are accepted.

PRESERVE

- existing 7-day session lifetime
- existing 30-day remember lifetime
- signup’s current remember-me behavior
- invite flows
- native-shell auth/logout behavior
- deleted-user fail-closed behavior
- current logout semantics
- current reset-token behavior
- existing authentication UX

DO NOT IMPLEMENT

- BL-169 CSRF changes
- BL-170 rate limiting
- MFA
- OAuth redesign
- server-side session storage
- schema/index/migration changes
- Production changes

TEST

Add focused tests for:

- password change invalidates other authenticated clients
- password reset invalidates older sessions/remember cookies
- reset completion creates a fresh valid session
- pre-login session state is cleared except approved preserved state
- all four auth entry points use the hardened establishment path
- remember behavior remains correct per path
- external post-auth redirects are rejected
- remember-cookie SameSite is explicit
- deleted-user cookies still fail closed
- invite/native logout regressions remain intact

Run focused auth/session tests and directly adjacent regressions only.

Do NOT run the full test suite in this BUILD.

VERIFY

- py_compile
- git diff --check
- focused tests pass
- no schema/migration changes

Commit with:

BL-168 Harden auth session lifecycle

Return a concise implementation report with:
- files changed
- behavior changed
- focused test results
- commit hash
- any unresolved issue