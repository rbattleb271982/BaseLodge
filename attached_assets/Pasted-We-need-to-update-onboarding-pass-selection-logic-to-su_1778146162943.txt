We need to update onboarding pass selection logic to support multi-pass users, but cap the total number of selectable passes at 3.

Goals:

* Users can select up to 3 passes during onboarding
* 3 is a hard max
* Existing users should also be limited to 3 passes anywhere pass editing exists
* UX should feel intentional and premium, not cluttered

Implementation requirements:

1. DATA MODEL

* Inspect current User model + onboarding storage logic for pass_type
* Refactor to support multiple passes
* Recommended approach:

  * create a UserPass join table OR
  * use a normalized relational structure already in place if one exists
* Avoid comma-separated strings if possible

2. ONBOARDING UI

* Update onboarding pass selection screen
* Users should be able to tap/select multiple passes
* Selected passes should visually fill/highlight
* Once 3 are selected:

  * all remaining unselected passes become disabled
  * show subtle helper text:
    "You can select up to 3 passes."
* Tapping a selected pass again should deselect it

3. PROFILE + SETTINGS

* Any pass editing screen must respect the same 3-pass cap
* Existing single-pass assumptions throughout templates/routes/helpers need audit + update

4. APP LOGIC AUDIT
   Search entire codebase for:

* pass_type
* "No Pass"
* pass display helpers
* trip compatibility logic
* onboarding completion checks
* friend cards / profile identity rows
* overlap or matching logic

Update all areas to gracefully support:

* 0 passes
* 1 pass
* multiple passes

5. DISPLAY RULES
   Identity rows should render cleanly:
   Examples:

* Epic Pass
* Epic Pass · Ikon Pass
* Epic Pass · Indy Pass · Mountain Collective

Avoid wrapping/broken layouts on mobile.

6. COMPATIBILITY PREP
   Structure this cleanly for future logic:

* shared pass overlap
* compatibility scoring
* trip suggestions by pass ownership

7. QA REQUIREMENTS
   Fully test:

* onboarding
* editing passes later
* deselection
* cap enforcement
* mobile layout
* existing users with legacy single-pass data
* users with no pass selected
* trip cards
* profile cards
* friend rows
* any analytics or event tracking tied to onboarding

Do not overuse checkpoints. Use them sparingly only when truly necessary.

Do not finish until you have fully tested the entire multi-pass flow end-to-end in Replit and confirmed no regressions.
