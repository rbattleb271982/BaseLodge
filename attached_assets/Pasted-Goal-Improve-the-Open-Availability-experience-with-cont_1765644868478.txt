Goal: Improve the Open Availability experience with context-aware copy, well-designed empty states, and a placeholder for a future “Suggest a trip” affordance — without changing backend logic.
PART 1 — Context-aware header copy (implement now)
Locate the sentence rendered near the top of the profile experience:
“X friends are open to ski on your dates →”
Update rendering logic based on the active tab:
When the active tab is NOT “Open Availability”
Render the sentence as a clickable link
Keep the arrow (→)
Clicking navigates to the Open Availability tab
When the active tab IS “Open Availability”
Replace the sentence with non-clickable helper copy:
Plan a trip with your friends below
Remove the arrow
Remove all link styling and hover behavior
Ensure:
No duplicate links appear on the Open Availability tab
Styling matches existing secondary/helper copy
No layout shift occurs when switching tabs
PART 2 — Open Availability empty states (implement now)
Implement two distinct empty states on the Open Availability tab.
Empty State A — User has no open dates
Display encouraging + instructional copy such as:
Add open dates to see when friends are available to ski.
Show two CTAs:
“Edit open dates”
“Invite friends”
Empty State B — User has open dates, but no friends match
Display encouraging + instructional copy such as:
No friends are open on these dates yet. Try adding more dates or inviting friends.
Show two CTAs:
“Edit open dates”
“Invite friends”
Ensure:
Empty state copy is neutral, calm, and utility-focused
CTAs reuse existing routes/actions
No new backend logic is introduced
PART 3 — “Suggest a trip” placeholder (visual only, implement now)
Add a non-clickable inline sentence beneath the helper copy on the Open Availability tab:
You’ll be able to suggest a trip here soon.
This is display-only:
No click behavior
No routing
No modal
No backend logic
Style it as subtle helper text (same hierarchy as other secondary copy).
PART 4 — Explicit deferrals (DO NOT IMPLEMENT YET)
Add TODO comments in code for future enhancements, but do not implement them.
TODO: Suggest a trip (future)
Will eventually:
Be inline text (not a button)
Appear only when matching friends exist
Pre-fill both overlapping dates and matching friends
Interaction model to be defined later
TODO: Copy refinement
Current terminology is acceptable (“open dates”, “friends who are open”)
Revisit phrasing once Suggest a Trip becomes functional
PART 5 — Constraints (important)
Do NOT:
Add new routes
Add new backend logic
Modify database schema
Introduce new UI components beyond inline text
Change tab navigation logic
Keep changes:
UI-only
Localized to the profile + Open Availability tab
Safe for immediate deployment
EXPECTED RESULT
Profile header copy adapts correctly based on context
Open Availability tab feels intentional, not redundant
Empty states guide users clearly and optimistically
Future functionality is hinted at without confusing users
No scope creep, no deployment risk