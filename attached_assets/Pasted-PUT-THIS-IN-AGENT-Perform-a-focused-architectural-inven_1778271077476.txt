PUT THIS IN AGENT

Perform a focused architectural inventory of BaseLodge’s outbound messaging and notification systems.

Do NOT change any code.
Do NOT refactor anything.
This is read-only analysis only.

I need a structured implementation inventory covering:

1. All current outbound messaging entry points
- Routes
- Services
- Helpers
- Utilities
- Background jobs/workers, if any
- SDK-triggered messaging
- Admin tools
- Cron/scheduled logic

2. For every outbound flow, identify:
- Trigger source
- Channel: push, email, in-app
- Provider used: OneSignal, APNs, FCM, SendGrid, etc.
- Whether it uses abstraction or direct provider calls
- Whether preferences are checked
- Whether logging occurs
- Whether retries/idempotency exist
- Whether async/background processing exists

3. Identify all current provider-specific dependencies:
- OneSignal
- APNs
- FCM
- SendGrid
- Capacitor Push
- SDK integrations

4. Identify all current messaging-related models/tables:
- EmailLog
- PushDeviceToken
- Preference models/fields
- Any notification/inbox/event models
- Any audit/logging models

5. Identify whether any event-driven patterns already exist:
- Emitters
- Queues
- Signals
- Hooks
- Observers
- Async jobs
- Pub/sub style patterns

6. Identify all current admin/test/broadcast tooling:
- Test push routes
- Broadcast routes
- Bulk send logic
- Unsafe tooling
- Preference bypasses

7. Identify all current preference enforcement points and gaps.

8. Identify all current async/background infrastructure:
- Celery
- RQ
- APScheduler
- Threads
- Deferred jobs
- Cron-style behavior
- Retry systems

9. Produce:
- A clean architecture diagram of current state
- Biggest coupling risks
- Easiest wins
- Hardest future migration risks
- Recommended insertion point for a future centralized emit_event() service

10. Most importantly:
Identify what can remain untouched initially vs what MUST be refactored early.

Do not propose rewrites yet.
Do not implement anything.
Do not change files.
This is strictly a current-state architectural inventory and migration-readiness assessment.

Use checkpoints sparingly.