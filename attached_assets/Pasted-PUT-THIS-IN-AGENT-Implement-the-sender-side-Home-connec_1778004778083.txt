PUT THIS IN AGENT

Implement the sender-side Home connection moment using the existing activity/notification infrastructure added in the last pass.

Do not create a new notification system.
Do not change the accepting-user session-based card unless necessary.
Do not refactor Home broadly.
Use checkpoints sparingly.

Goal:
When User A sends a connection request and User B accepts it:
- User B sees “You and User A are now connected.” on Home immediately after accepting.
- User A also sees “You and User B are now connected.” on Home the next time they visit Home.
- The message should be one-time, dismissable, and not stale.

Use the existing Activity/notification record created by emit_connection_accepted_activity.

Implementation requirements:
1. Locate the Activity/notification record created when a connection is accepted.
2. Determine how it identifies:
   - recipient user
   - other connected user
   - activity type/action
   - read/dismissed state, if available
3. On the Home route, surface one pending connection-accepted activity for the current user if:
   - it belongs to the current user
   - it has not already been consumed/dismissed
   - it is recent enough to feel like a product moment
4. Render the same style Home card:
   “You and [Name] are now connected.”
5. Add dismissal/consumption logic so it does not reappear later.
6. Prefer the existing DismissedInsightCard pattern if available.
7. Do not show old connection events from weeks/months ago.

Also fix this tracked issue:
- flash message lost on connect_from_trip → friend_profile.html

Testing required:
1. User A sends request to User B.
2. User B accepts request.
3. User B sees the connection card on Home.
4. User A logs in or visits Home and sees the connection card.
5. Refresh Home after viewing/dismissing and confirm the card does not return.
6. Confirm notification feed still works for both users.
7. Confirm connect_from_trip → friend_profile.html message is no longer lost.

At the end, report:
- Files changed
- Routes touched
- Activity/notification logic used
- How stale messages are prevented
- How dismissal/consumption works
- Anything intentionally left for later