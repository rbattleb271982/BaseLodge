PUT THIS IN AGENT

GOAL
Add a Country → State/Province cascading filter to BOTH:
1) Wishlist Destinations
2) Mountains I’ve Visited

This must work identically in DEVELOPMENT and PRODUCTION.

CRITICAL CONSTRAINTS
- DO NOT modify ANY logic, templates, JS, or queries related to:
  - My Trips tab
  - Trip creation
  - Trip editing
- DO NOT alter trip filtering, trip state logic, or trip UI in any way
- Scope changes ONLY to:
  - Wishlist Destinations screen
  - Mountains I’ve Visited screen

CURRENT PROBLEM
Both screens currently:
- Have a single "All states" dropdown
- Do NOT allow filtering by country first
- Show mixed US + Canada resorts together
- Create confusion when provinces (AB, BC, etc.) appear alongside US states

REQUIRED UX (BOTH SCREENS)
1) Country dropdown (required first step)
   - Options:
     - All countries
     - United States
     - Canada
   - Default: All countries

2) State / Province dropdown (disabled until country is selected)
   - Label should dynamically change:
     - "State" when United States selected
     - "Province" when Canada selected
     - "State / Province" when All countries
   - Options populated ONLY from the selected country

3) Resort list behavior
   - Resort list must:
     - Reactively update when country changes
     - Reactively update when state/province changes
     - Never show provinces when US is selected
     - Never show US states when Canada is selected

4) Search behavior
   - Search must operate ONLY on the currently filtered resort set
   - No cross-country results

DATA REQUIREMENTS
- Use the canonical Resort table as the source of truth
- Filtering must rely on:
  - resort.country
  - resort.state / province field
- NO hardcoded lists
- NO duplication of resort data
- NO new schema fields

TECHNICAL REQUIREMENTS
- Implement shared, reusable filtering logic used by BOTH screens
- Frontend must:
  - Reset State/Province selection when Country changes
  - Reset resort list cleanly without stale results
- Backend must:
  - Support country + state filtering cleanly
  - Work identically in dev and prod environments

UI REQUIREMENTS
- Preserve existing visual design
- Do NOT change spacing, typography, checkbox styling, or Save behavior
- Only add the Country dropdown ABOVE the State/Province dropdown
- Keep existing search box placement

VALIDATION CHECKLIST (MUST PASS)
- Selecting Canada → only AB / BC / etc. appear
- Selecting United States → only US states appear
- Switching country clears state selection
- Search never leaks results across countries
- Wishlist Destinations and Mountains Visited behave identically
- My Trips tab is 100% unchanged

DELIVERABLES
1) Updated backend filtering logic
2) Updated frontend dropdown logic
3) Confirmation that My Trips code path was not touched
4) Manual test steps verifying behavior in BOTH dev and prod

Do NOT add extra features.
Do NOT refactor unrelated code.
Fix this cleanly and permanently.