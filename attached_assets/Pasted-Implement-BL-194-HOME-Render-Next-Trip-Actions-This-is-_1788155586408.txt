Implement BL-194 — HOME — Render Next Trip Actions.

This is BUILD.

Use the completed BL-189 canonical action contract.
Do not create another planning task.
Do not change action eligibility logic in templates.
Do not expand scope beyond BL-194.
Do not commit yet.

GOAL

Render the existing canonical Next Trip action contract on the Home card as a compact collapsible row.

The interaction should communicate:
Actions to take · N

Only render the row when `action_count > 0`.

The action list must come directly from:
`home_summary["next_trip"]["actions"]`

The template must NOT recompute eligibility.

PRODUCT CONTRACT

Placement:
- inside the existing compact Your Next Trip card
- at the bottom of that card
- visually subordinate to the trip summary
- above any card boundary/padding end
- do not move the existing `View trip` link

Collapsed state:
- `Actions to take · N`
- collapsed by default
- use the existing accessible Home disclosure idiom where possible
- use a down-arrow style disclosure indicator, not a chevron
- entire summary row should be comfortably tappable

Expanded state:
- show the canonical actions in server-provided order
- each action is a compact tappable row/link
- use the canonical action label
- link to the canonical action destination
- do not modify destinations client-side

Do not display internal:
- action key
- action type
- priority
- eligibility metadata

ZERO-ACTION STATE

If `action_count == 0`:
- render no Actions to take row
- do not show `Actions to take · 0`
- do not add a placeholder or empty-state message

BL-189 remains authoritative for zero-action eligibility.

APPROVED CURRENT ACTIONS

The UI must support the BL-189 actions generically, including:

- `Review RSVP`
- `Review join requests`

Do not hard-code eligibility around those labels.

Render whatever eligible canonical actions BL-189 supplies.

This is important so BL-83 and BL-84 can extend the contract later without requiring the Home template to be rewritten.

INTERACTION

Prefer the existing accessible disclosure primitive introduced for Home.

Requirements:
- collapsed by default
- keyboard accessible
- semantic native disclosure if that is the existing pattern
- no new JavaScript if not required
- no persistence of expanded/collapsed state required
- no analytics requirement in this task

Do not introduce a new accordion library or dependency.

DESIGN

Keep the Next Trip card compact.

The action section should not turn the card into a large multi-row panel when collapsed.

When expanded:
- clear separation between action rows
- compact spacing
- readable tap targets
- no excessive borders or containers
- no avatars
- no photos
- no image placeholders
- no decorative mountain art
- no profile bubbles
- no gratuitous icons

Do not add:
- Add pass
- Add gear
- profile actions
- generic Edit trip action
- generic View trip action inside the actions list

The existing `View trip` CTA remains separate.

DESTINATIONS

Use the canonical BL-189 destination exactly.

Current expected examples:
- Review RSVP → Trip Detail `#td-self-rsvp`
- Review join requests → Trip Detail `#td-join-requests`

Do not create new endpoints.
Do not add Home mutations.

ARROW / DISCLOSURE INDICATOR

Use a down-arrow visual treatment rather than the prior chevron idiom.

Keep it lightweight and consistent with other Home disclosures where practical.

Do not add a heavy icon dependency.

DATA / PERFORMANCE

No new Home queries.

Do not:
- load relationships in the template
- call helpers from Jinja that hit the database
- re-sort actions client-side
- re-filter actions in the template
- alter `action_count`

Use server-provided order exactly.

HOME BOUNDARIES

Do not modify:
- Next Trip selection
- BL-189 action eligibility
- About You & Gear
- Your Activity
- Friends’ Passes
- Happening
- Ideas
- Friends’ Trips
- navigation/header
- RSVP behavior
- join-request behavior

Do not change:
- privacy/authorization
- schema/migrations
- native configuration
- caching

FILES

Likely:
- `templates/partials/home/_next_trip.html`
- focused Home/Next Trip action presentation tests

If the existing disclosure macro needs a very small reusable extension to support this presentation, keep it backward compatible and confirm BL-188/190/191/192 behavior remains unchanged.

Avoid changes to `app.py` unless strictly necessary. The BL-189 data contract is already complete.

TESTS

Add or extend focused tests covering:

Rendering:
- action row renders when action_count > 0
- row does not render when action_count == 0
- collapsed summary displays correct singular/plural-safe count format
- approved display format remains `Actions to take · N`
- actions render in server-provided order
- canonical labels render
- canonical destinations render exactly
- action keys/types/priorities do not render

Disclosure:
- collapsed by default
- accessible disclosure semantics
- down-arrow indicator present
- no custom carousel/accordion dependency
- no unnecessary custom JavaScript

Eligibility boundary:
- template does not infer Interested/Going state
- template does not query pending join requests
- template only consumes `actions` and `action_count`

Regression:
- existing View trip CTA remains
- BL-193 compact card remains intact
- BL-189 action-contract tests remain intact
- About You/Gear disclosure intact
- Your Activity disclosure intact
- Friends’ Passes disclosure intact
- Happening carousel intact
- Home empty states intact

PERFORMANCE

Confirm:
- zero new Home queries
- no relationship lazy loading
- no client-side action re-sorting

MANUAL VALIDATION

Preview at mobile width and desktop width.

Confirm:
- collapsed row remains compact
- card height is reasonable
- down arrow is visually clear
- action rows are easy to tap
- expanded content does not clip
- `View trip` remains visible and separate
- no page-level horizontal overflow
- bottom navigation remains unaffected

VALIDATION

Run:
- focused BL-194 presentation tests
- BL-189 action-contract regressions
- BL-193 Next Trip regressions
- Home disclosure regressions
- Home empty-state regressions
- relevant full Home regressions
- `git diff --check`

Do not commit yet.

Return:

BL-194 IMPLEMENTATION RESULT

- Files changed:
- Actions row implemented:
- Hidden when action_count == 0:
- Collapsed by default:
- Summary copy:
- Down-arrow indicator:
- Existing Home disclosure primitive reused:
- Canonical action order preserved:
- Canonical destinations preserved:
- Template eligibility logic added:
- New Home queries added:
- Custom JavaScript added:
- New dependency added:
- Existing View trip CTA preserved:
- Other Home modules changed:
- Privacy/authorization changed:
- Schema/migration changed:
- Mobile validation:
- Desktop validation:
- Tests run:
- Tests passed:
- git diff --check:
- Scope deviations:
- Commit created: NO

If implementation is complete and clean, end with exactly:

BL-194 READY TO MERGE