Investigate all notification timing behavior across BaseLodge before we finalize the delay for Task #238.

Do not make any code changes.

I want a complete audit of every place in the app where a user-facing notification is sent or scheduled, including push notifications and any other messaging channels that share the same dispatch infrastructure.

Please trace all current notification events and identify whether each one is:

* Immediate
* Delayed
* Scheduled
* Batched
* Background-processed
* Retry-driven
* Triggered synchronously inside the request
* Triggered asynchronously after commit

For every notification/event, report:

1. Event name / constant
2. User action or system event that triggers it
3. Recipient
4. Delivery channel
5. Delivery strategy
6. Exact delay, if any
7. Where that delay is defined
8. Whether the delay is intentional product behavior or infrastructure/retry behavior
9. Whether the database/state is re-checked before delivery
10. Whether the notification survives app/server restarts
11. Whether duplicate suppression exists
12. Whether delivery writes a `MessageEventLog`
13. Deep link destination, if applicable

Please inspect all relevant infrastructure, including:

* `services/messaging_constants.py`
* `services/message_dispatch.py`
* `emit_messaging_event`
* OneSignal integration
* any queue/background worker
* scheduled jobs
* retry logic
* delayed delivery models/fields
* cron or task scheduler behavior
* any direct OneSignal calls outside the messaging service
* trip notifications
* friend/invite notifications
* join/leave/accept/decline notifications
* trip update/cancel notifications
* any reminder notifications
* any marketing or lifecycle push notifications if they use the same system

Also search the codebase for explicit timing values such as:

* `sleep`
* seconds/minutes delays
* `timedelta`
* `eta`
* `countdown`
* `schedule`
* `delay`
* `retry`
* `backoff`

I specifically want to know whether BaseLodge already has product-level delays like:

* 5 seconds
* 10 seconds
* 15 seconds
* 30 seconds
* 1 minute
* 5 minutes
* etc.

Do not confuse retry/backoff timing with intentional user-facing notification delays. Separate those clearly.

## Return

Give me:

1. A table of every notification event and its timing
2. All intentional product delays currently used
3. All infrastructure/retry delays currently used
4. Whether any existing event waits and re-checks state before sending
5. Whether BaseLodge already has a reliable delayed-job mechanism suitable for a 15-second join-request notification
6. Whether 15 seconds would be consistent or inconsistent with the rest of the app
7. Your recommendation for Task #238 based on the existing timing patterns
8. Exact files/functions involved

Stop after the audit. Do not implement Task #238 yet.
