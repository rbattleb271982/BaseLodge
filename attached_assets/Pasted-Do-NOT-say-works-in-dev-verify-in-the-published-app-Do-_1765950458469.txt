Do NOT say “works in dev” — verify in the published app
Do NOT guess — capture evidence (stack trace + DB counts)
Do NOT stop until both problems are fixed end-to-end
STEP A — CAPTURE THE REAL PROD ERROR (JONATHAN)
Enable full stack trace logging for production (Replit logs):
Add an error handler or ensure exceptions print traceback to stdout
Reproduce Jonathan login in the published app
Copy the exact traceback and identify:
file + line
the NoneType (or other) crash cause
Fix requirement:
The post-login redirect page must NOT assume optional data exists.
Add safe defaults for missing fields (e.g., empty lists for friends/trips, default strings, etc.)
Jonathan must reach the logged-in home screen without error.
STEP B — PROVE WHICH DATABASE PUBLISHED IS USING
In the running app startup logs, print:
whether DATABASE_URL exists (print only host/dbname, not credentials)
whether SQLite is being used
total counts:
User count
Friend/Connection count
SkiTrip count (or relevant)
Then, in the same environment, query and print:
user.id for Richard + Jonathan
how many friends Richard has according to the DB
This must be run from the published runtime, not just python in the shell.
STEP C — FIX SEEDED CONNECTIONS (RICHARD)
If Richard has 0 friends in the published DB:
Either the seed never ran on that DB OR it ran for a different “main” user.
Implement a one-time admin-safe route or CLI command that:
Finds Richard by email
Finds the 50 dummy users (by a recognizable pattern: created_by_seed flag, email domain, or name prefix)
Creates bidirectional Friend records between Richard and each dummy user
Commits once, with rollback on failure
Logs: “Created X friendships for Richard (user_id=Y)”
If the 50 dummy users do not exist in the published DB:
Re-run the seed in the published environment (same DB) OR create them again there.
But do NOT create duplicates if they already exist.
STEP D — HARD VERIFICATION (MUST DO)
After fixes, verify in the published app:
Jonathan can login with 12345678 and lands on home without error
Richard logs in and sees exactly 50 friends
Logout/login again: still 50 friends
Provide final output with:
Root cause for Jonathan 500 (exact line)
Root cause for missing friends (exact DB mismatch or missing seed)
What changed (files + functions)
Evidence: counts before/after