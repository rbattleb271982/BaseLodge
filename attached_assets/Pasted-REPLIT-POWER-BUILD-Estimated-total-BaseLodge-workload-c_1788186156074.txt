REPLIT: POWER + BUILD
Estimated total BaseLodge workload completed after this step: ~37%

BL-127 — Correct wishlist removal at_limit response

BUILD the approved BL-127 closeout.

Important investigation result:
The original BL-127 production bug has already been corrected by BL-123.

Current production logic already uses:

"at_limit": len(ids) >= WISHLIST_LIMIT

Canonical backend source:
- services/wishlist.py
- WISHLIST_LIMIT = 15

Commit b24c6041 from BL-123 already corrected the application behavior.

Therefore:

DO NOT modify production wishlist application logic unless a failing regression test demonstrates that the investigation was materially wrong.

DO NOT create or implement a separate Task #389.
Treat the tests below as the remaining BL-127 work.

Scope:
Add focused regression coverage for the existing wishlist removal response contract.

Required tests:

1. Cover successful removal where the resulting canonical wishlist count is 0.
Expected:
- success = true
- count = 0
- at_limit = false

2. Cover resulting canonical count 1.
Expected:
- success = true
- count = 1
- at_limit = false

This may already have coverage. Reuse or strengthen the existing test rather than creating redundant coverage.

3. Cover resulting canonical count 14.
Expected:
- success = true
- count = 14
- at_limit = false

4. Cover resulting canonical count 15.
Expected:
- success = true
- count = 15
- at_limit = true

5. Add a defensive legacy-data case where the stored wishlist contains more than 15 valid entries after the requested removal.
Expected canonical response:
- success = true
- count = 15
- at_limit = true

The response must reflect canonical product-visible wishlist state, not uncapped raw stored-list length.

Implementation constraints:

- Prefer parameterized tests where that keeps the suite clear.
- Exercise the real remove endpoint / existing wishlist helpers rather than reimplementing wishlist calculations inside the test.
- Preserve CSRF/auth behavior used by the existing endpoint tests.
- Do not change the add endpoint.
- Do not change frontend/template/native behavior.
- Do not replace canonical wishlist semantics with raw-list semantics.
- Do not modify WISHLIST_LIMIT.
- Do not introduce a generalized wishlist refactor.
- Do not change database/schema/migrations.
- Do not touch BL-70 migration/backfill work.
- Do not access Production or Supabase.
- Do not fix the known unrelated SQLite/PostgreSQL migration-test failure.

Validation:

Run:
1. The new BL-127 focused tests.
2. Existing wishlist endpoint/service tests, including BL-123 regressions.
3. Appropriate adjacent API/CSRF tests if the test file or fixtures overlap them.
4. Full suite if reasonably practical.

The known unrelated migration test failure may still appear. Report it separately and do not expand scope.

If all BL-127 behavior is already correct and the new tests pass without application-code changes, that is the desired outcome.

After verification:
- inspect git diff
- run git diff --check
- commit the BL-127 test coverage

Commit message must include the task ID, for example:

BL-127 Add wishlist removal limit regressions

Return:
- exact tests added/changed
- whether any production code changed
- focused/adjacent/full-suite results
- git diff --check result
- commit hash
- confirmation that BL-127 is complete from the code side

If implementation reveals that production behavior materially differs from the approved investigation, STOP rather than expanding scope.