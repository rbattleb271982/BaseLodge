PUT THIS IN AGENT

Goal:
Add system-based dark mode support to BaseLodge using CSS color tokens and the approved alpine dark palette. Do not change layout, HTML structure, routing, or business logic.

Constraints:
- Use standard CSS only (no Tailwind, no JS)
- Use CSS variables only (no hard-coded hex colors in components)
- Default behavior must follow prefers-color-scheme
- No manual theme toggle
- Do not touch admin pages
- Refactor only shared surfaces (cards, inputs, buttons, modals)

Steps:

1. At the very top of static/css/style.css, add the following light-mode tokens:

:root {
  --bg-primary: #ffffff;
  --bg-secondary: #f6f7f9;

  --card-bg: #ffffff;
  --modal-bg: #ffffff;

  --text-primary: #111111;
  --text-secondary: #555555;
  --text-tertiary: #888888;

  --border-subtle: #e5e7eb;
  --divider: #eceff3;

  --accent-primary: #3a7afe;
  --accent-primary-hover: #5a8cff;

  --accent-success: #4caf8e;
  --accent-warning: #f4b860;
  --accent-danger: #e26d5c;

  --shadow-soft: 0 6px 18px rgba(0,0,0,0.08);
  --shadow-card: 0 8px 24px rgba(0,0,0,0.12);
}

2. Immediately after, add the dark-mode overrides:

@media (prefers-color-scheme: dark) {
  :root {
    --bg-primary: #0E1114;
    --bg-secondary: #161A1E;

    --card-bg: #1C2127;
    --modal-bg: #20262D;

    --text-primary: #F5F7FA;
    --text-secondary: #B3BCC7;
    --text-tertiary: #7D8794;

    --border-subtle: #2A313A;
    --divider: #232933;

    --accent-primary: #4DA3FF;
    --accent-primary-hover: #6AB3FF;

    --accent-success: #4CAF8E;
    --accent-warning: #F4B860;
    --accent-danger: #E26D5C;

    --shadow-soft: 0 6px 18px rgba(0,0,0,0.45);
    --shadow-card: 0 8px 24px rgba(0,0,0,0.55);
  }
}

3. Refactor global surfaces to use tokens:

body {
  background: var(--bg-primary);
  color: var(--text-primary);
}

.card,
.trip-card,
.profile-card {
  background: var(--card-bg);
  border: 1px solid var(--border-subtle);
  box-shadow: var(--shadow-card);
}

4. Refactor inputs and forms:

input,
select,
textarea {
  background: var(--bg-secondary);
  color: var(--text-primary);
  border: 1px solid var(--border-subtle);
}

input::placeholder,
textarea::placeholder {
  color: var(--text-tertiary);
}

5. Refactor buttons:

.button-primary {
  background: var(--accent-primary);
  color: #0E1114;
}

.button-primary:hover {
  background: var(--accent-primary-hover);
}

.button-secondary {
  background: transparent;
  color: var(--text-primary);
  border: 1px solid var(--border-subtle);
}

6. Refactor modals and bottom sheets:

.modal,
.bottom-sheet {
  background: var(--modal-bg);
  border-top: 1px solid var(--border-subtle);
  box-shadow: var(--shadow-card);
}

Validation:
- Dark mode activates automatically via OS setting
- No pure black backgrounds introduced
- Trip cards, profile cards, inputs, and modals remain readable
- Light mode appearance remains unchanged
- No admin CSS affected