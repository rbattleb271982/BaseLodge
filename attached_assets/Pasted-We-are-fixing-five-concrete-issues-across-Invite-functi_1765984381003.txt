We are fixing five concrete issues across Invite functionality, Edit Profile sizing, and Open Availability UX.
This work is required for the app to be usable on mobile.
Do not skip steps. Do not add features beyond what’s described.
🔴 ISSUE 1 — INVITE TAB CRASH (STILL HAPPENING)
Current behavior
Tapping the Invite tab always crashes
Error shown:
(psycopg2.errors.UndefinedColumn)
column invite_token.uses_count does not exist
This occurs every time, even after cache clears.
REQUIRED BEHAVIOR
The Invite flow must work
Users must be able to:
Generate an invite
Share it
Have the recipient accept it
Become connected after acceptance
We do NOT need to show:
Invite history
Invite usage counts
Lists of past invites
REQUIRED FIX (NON-NEGOTIABLE)
Stop querying invite tokens on Invite tab load
Do not SELECT invite tokens when rendering the Invite screen
This avoids touching broken schema paths
Remove all references to multi-use invites
Remove every reference to:
uses_count
max_uses
This includes:
SQLAlchemy models
Queries
Any template logic
Single-use enforcement
Invite validity is determined by:
used_at IS NULL
On accept:
Set used_at = now()
Create bidirectional friendship
If reused:
Show friendly error
Do NOT crash
Invite screen scope
The Invite tab should:
Allow generating an invite link
Allow sharing
It should not attempt to render invite history
🔵 ISSUE 2 — EDIT PROFILE PAGE IS STILL TOO SMALL
Current behavior
Edit Profile content feels cramped
Inputs and typography feel smaller than the More page
Tap targets feel tight
REQUIRED CHANGE (LOCKED)
Make the Edit Profile page visually larger by scaling UI elements, not by widening the container.
Specifically:
Increase:
Font sizes
Input heights
Button heights
Match the visual scale of the “More” page
Keep the same layout structure and sections
Do NOT change backend logic
Do NOT add or remove fields
This is a UI scale fix, not a redesign.
🟣 ISSUE 3 — OPEN AVAIL: GROUP DATES BY MONTH
Current behavior
Open availability dates are shown as a flat list
Hard to scan
REQUIRED BEHAVIOR
Group open dates by month only, formatted like:
Jan 1,2,3,4
Feb 14,15,16
Rules:
Group by month name
Ignore year in display
Still group even if dates are not consecutive
If only one date exists in a month, still show the month
This grouping applies wherever open dates are summarized (Home / Open Avail views).
🟤 ISSUE 4 — OPEN AVAIL CALENDAR SELECTION COLOR
Current behavior
Selected dates appear light beige
This is visually weak and inconsistent
REQUIRED BEHAVIOR
ALL selected dates must be maroon
This includes:
Single selections
Multiple selections
Beige should not be used for selected states
Disabled / unavailable dates may still use lighter styling
⚫ ISSUE 5 — REMOVE “NO DATES SELECTED” COPY
Current behavior
Calendar shows “No dates selected” text
REQUIRED CHANGE
Remove this text entirely
No replacement copy
The UI should speak for itself
🔒 ISSUE 6 — SAVE OPEN DATES BUTTON STATE
REQUIRED BEHAVIOR
Save open dates button must be disabled
Until at least one date is selected
When no dates selected:
Button disabled
When ≥1 date selected:
Button enabled
Do NOT show error messages.
Prevent invalid submission instead.
🧪 ACCEPTANCE CRITERIA (ALL MUST PASS)
Invite
Invite tab no longer crashes
Invite link works
Accepting an invite connects users
No uses_count references anywhere
Edit Profile
Page feels visually larger
Inputs and text are easier to tap/read
Matches scale of More page
Open Availability
Dates grouped by month
Selected dates are maroon
“No dates selected” text removed
Save button disabled until valid
