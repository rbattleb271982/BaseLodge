PUT THIS IN AGENT

We have a suspected one-directional friend connection bug. 
DO NOT FIX ANYTHING. This task is DIAGNOSIS ONLY.

Observed behavior:
- User A sends an invite to User B.
- User B signs up and completes onboarding.
- User A sees User B in their Friends feed.
- User B does NOT see User A in their Friends feed.

Goal:
Identify where the asymmetry occurs and report findings so we can decide on the fix externally.

========================
DIAGNOSIS TASKS (READ-ONLY)
========================

1. Identify the friendship data model
   - Locate the model/table used for friend connections (e.g., Friend, Friendship, Connection).
   - Report:
     - Table name
     - Columns
     - Whether it appears directional (user_id → friend_id) or symmetric.

2. Inspect invite acceptance / signup flow
   - Trace the code path that runs when:
     a) A user signs up via an invite link, OR
     b) An invite is marked as accepted.
   - Report:
     - Exact file names and line numbers
     - Whether ONE or TWO friendship records are created
     - Which user IDs are written into which fields

3. Database inspection (NO WRITES)
   For a known test pair (User A, User B):

   - Query all friendship rows where user_id = User A
   - Query all friendship rows where user_id = User B

   Report results clearly as:
   - Rows A → *
   - Rows B → *

4. Inspect Friends feed query logic
   - Locate the query used to populate the Friends feed.
   - Report:
     - Exact WHERE clause logic
     - Whether it assumes:
       a) Bidirectional rows exist, or
       b) Symmetric matching on a single row

5. Identify the asymmetry point
   Based on findings, answer:
   - Is the asymmetry caused by:
     a) Only one friendship row being created?
     b) Feed query only checking one direction?
     c) Invite flow skipping reciprocal creation on signup?

========================
OUTPUT REQUIREMENTS
========================

Return a clear written diagnosis including:

- Friendship model structure
- Invite/signup code path summary
- Database row evidence showing asymmetry
- Friends feed query logic
- The single most likely root cause (NO FIX)

Do NOT:
- Modify schema
- Insert new rows
- Change queries
- Add guards or migrations

This is a read-only investigation only.