PUT THIS IN PLAN

Audit the current BaseLodge codebase and produce a complete seed-data planning report before creating any seed users.

I want to build a pseudo-live environment with purposeful seed users, realistic connection states, and realistic trip scenarios. Do not generate or insert seed data yet. First inspect the existing schema, route logic, enums, and UI dependencies so the plan matches what the app actually supports today.

Your task:
1. Inventory all user/profile fields that can be entered, displayed, or are required for onboarding and profile rendering
2. Inventory all relationship types supported in the app, including friend connections, friend requests, invitations, trip invites, participant roles, and participant statuses
3. Inventory all trip-related fields and statuses that affect rendering across Home, Trips, Friends, Profile, invite flows, and trip detail screens
4. Identify every meaningful product state that should be represented in seed data, including:
   - fully connected friends
   - pending outgoing request
   - pending incoming request
   - no connection
   - upcoming trip owner
   - accepted participant
   - pending trip invite
   - declined trip invite
   - overlapping trips
   - non-overlapping trips
   - no trips
   - past trips only
   - fully completed profile
   - partially completed profile
   - no wishlist
   - wishlist populated
   - no mountains visited
   - mountains visited populated
   - different rider types, pass types, and skill levels
5. Recommend the best seed cohort structure for this app, grouped into purposeful sets such as:
   - core social graph
   - edge cases
   - invite/trip workflow users
6. Recommend how many seed users should be in each cohort and why
7. For each recommended cohort, explain which screens and UX states it is intended to exercise
8. Identify any current schema gaps or logic issues that would make realistic seeding difficult or misleading
9. Recommend the safest implementation approach for deterministic seed data in this codebase, including whether it should be:
   - a standalone seed script
   - admin-only seed route
   - CLI command
   - reset-and-reseed flow
10. Tell me exactly which files, models, enums, and templates you inspected

Important requirements:
- Base the report only on what the app actually supports now
- Do not guess at unsupported states
- Do not create random fake data yet
- Do not modify the database yet
- Be explicit about gaps, ambiguities, or missing statuses
- Organize the final output as a structured seed-data plan I can approve before implementation
- Use checkpoints sparingly only if truly needed

Return the final answer in this structure:
1. Supported user/profile fields
2. Supported connection and invite states
3. Supported trip and participant states
4. Recommended seed cohorts
5. Recommended user counts
6. Implementation recommendation
7. Any blockers or schema mismatches