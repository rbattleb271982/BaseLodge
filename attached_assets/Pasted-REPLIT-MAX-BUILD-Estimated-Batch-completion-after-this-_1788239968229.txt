REPLIT: MAX + BUILD
Estimated Batch completion after this step: ~95%

TASK: BL-173 — Targeted social surface updates
STAGE 1 OF 2

Use Task #414 PLAN as the implementation contract.

IMPLEMENT ONLY:
1. Shared BL-172 refresh utility extraction/generalization
2. Friends targeted refreshes
3. Trip planning targeted refreshes

DO NOT implement Group Trip changes in this run.

GOAL

Reuse BL-172's canonical server-rendered targeted-refresh architecture across Friends and planning surfaces without changing product behavior or introducing a second frontend state system.

1. GENERALIZE BL-172 REFRESH ENGINE

Extract the reusable portions of the existing BL-172 targeted-refresh implementation into one configurable shared utility.

Preserve:
- canonical server-rendered HTML as source of truth
- per-region stale-response protection
- pending-control / duplicate-submit protection
- scroll restoration
- same-page vs redirect-away handling
- no automatic retries, including 429
- non-success handling
- disconnected-control cleanup
- same browser and Capacitor execution path

Keep Trip Detail-specific:
- edit-mode restoration
- modal rebinding/cleanup
- any Trip Detail-only hooks

Maintain compatibility with existing BL-172 behavior.

Do not rewrite BL-172 beyond what is necessary to extract the shared mechanism.

2. FRIENDS

Add the smallest stable canonical regions needed for:
- incoming requests
- bounded Friends directory
- suggested-friend state/counts as required

Convert only:

A. Incoming friend-request acceptance
- remove the current full-page reload
- refresh the necessary canonical Friends regions after confirmed success

B. Mobile pull-to-refresh
- refresh canonical bounded Friends content instead of reloading the entire page

Preserve:
- active tab
- URL-backed filters
- search
- scroll
- focus where practical
- bounded cursor paging
- existing suggestion preview state where applicable

Do not fetch the complete friend directory.

Do not rewrite actions that already update locally.

Keep navigation unchanged for:
- friend removal
- suggestion submission
- auth-expiry redirects
- friend-profile Connect PRG flow

3. PLANNING

Convert only:

A. Trip Detail planning-post creation
- after confirmed success, refresh only the canonical planning preview/count region

B. Dedicated Trip Planning create
C. Dedicated Trip Planning edit
D. Dedicated Trip Planning delete where the final post requires the canonical empty state

Use canonical server-rendered planning regions.

Composer behavior:
- close/reset only after successful mutation
- retain user's draft on validation/network/non-success errors
- do not retry failures or 429s

Do not alter planning product rules, visibility, notifications, or lifecycle behavior.

CRITICAL CONSTRAINTS

- Reuse existing mutation routes.
- Server remains source of truth.
- No client-side relationship/planning state model.
- No frontend framework.
- No redesign or copy changes.
- Preserve BL-133 visibility/authorization.
- Preserve BL-158/159 paging/bounded retrieval.
- Preserve BL-168 sessions.
- Preserve BL-169 CSRF.
- Preserve BL-170 limits and 429 behavior.
- Preserve notification semantics.
- No schema/index/migration changes.
- No Production, deployment, secrets, or persistent-data changes.
- Do not touch Group Trip implementation yet.

VALIDATION

Run focused tests only.

Cover:
- existing BL-172 targeted-refresh behavior still works
- shared utility stale-response protection
- pending/duplicate-submit behavior
- redirect-away behavior
- non-success/429 no-retry behavior
- Friends incoming accept refresh
- Friends bounded directory remains bounded
- Friends filters/tab/search state preservation
- Friends pull-to-refresh no longer reloads page
- planning create/edit/delete targeted refresh
- planning final-delete empty state
- Trip Detail planning preview/count refresh
- planning draft retained on failure
- CSRF remains correct
- browser/Capacitor use the same path

Also run:
- Python compilation for touched Python
- JS syntax checks for touched/shared JS
- git diff --check

DO NOT:
- run full test suite
- run broad browser automation
- stress test unrelated surfaces
- implement Group Trip work

If generalizing BL-172 would require a broad frontend rewrite, STOP and report rather than expanding scope.

COMMIT

Commit only this Stage 1 work with a clear BL-173 commit message.

RETURN

- shared utility changes
- Friends mutations converted
- Planning mutations converted
- files changed
- focused tests/results
- syntax/diff checks
- commit hash
- anything intentionally deferred to Stage 2
- confirmation no schema/migration/Production/persistent-data changes occurred