PUT THIS IN AGENT

This is a ONE-TIME, CONTROLLED PRODUCTION DATA MAINTENANCE TASK.

It includes:
1) Creating a mutual friend connection between two existing users
2) Deleting two known test users from the production database

DO NOT modify application logic.
DO NOT change models or migrations.
DO NOT generalize this into reusable code.
DO NOT delete any data other than what is explicitly listed.

================================
PART 1 — CREATE FRIEND CONNECTION
================================

USERS TO CONNECT

User A email: cbrown3855@gmail.com
User B email: richardbattlebaxter@gmail.com

Friend model is directional:
- user_id → friend_id

For a mutual friendship, TWO rows must exist:
- user_id = A, friend_id = B
- user_id = B, friend_id = A

TASK

1. Connect to the PRODUCTION database.
2. Look up both users by email and record their user IDs.
3. Check whether Friend rows already exist in either direction.
4. If BOTH rows already exist:
   - Do nothing
   - Report that users are already mutually connected
5. If ONE or BOTH rows are missing:
   - Insert ONLY the missing row(s)
   - Ensure no duplicate rows are created
6. Verify after insertion that:
   - Each user sees the other as a friend
   - Exactly two Friend rows exist between them

================================
PART 2 — DELETE TEST USERS
================================

USERS TO DELETE (PRODUCTION)

- richard@richard.com
- asdfa@gmail.com

CRITICAL SAFETY RULES

1. BEFORE deleting:
   - Look up each user by email
   - Report their user ID
   - Confirm they are NOT:
     - The same as User A or User B
     - Referenced as a friend of User A or User B

2. DELETE ORDER (IMPORTANT)
   - First delete all dependent rows referencing these users:
     - Friend rows
     - Trips
     - TripGuests
     - Invites
     - Any other foreign-key-linked records
   - Then delete the User rows themselves

3. Use hard deletes (this is test data cleanup).

4. If a user email does NOT exist:
   - Do nothing
   - Report that the user was already absent

================================
VERIFICATION (MANDATORY)
================================

After all operations:

- Confirm cbrown3855@gmail.com and richardbattlebaxter@gmail.com:
  - Exist
  - Are mutually connected
- Confirm richard@richard.com and asdfa@gmail.com:
  - No longer exist in the database
  - Have no remaining dependent rows

================================
OUTPUT REQUIREMENTS
================================

Report back with:

- User IDs for all four emails
- Friend rows that existed vs. were inserted
- Rows deleted per test user (by table)
- Final confirmation that:
  - The friend connection is mutual
  - Test users are fully removed
  - No unintended data was touched
