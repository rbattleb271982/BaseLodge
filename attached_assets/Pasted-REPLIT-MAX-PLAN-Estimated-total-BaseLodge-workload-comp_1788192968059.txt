REPLIT: MAX + PLAN
Estimated total BaseLodge workload completed after this step: ~38%

RESUME TASK: BL-80 — Preserve completed and cancelled trip history

The privacy stop condition from the previous BL-80 investigation has been resolved.

RESOLVED BLOCKER

Account-deletion logging no longer retains deleted-account email addresses.

Commit:
28d6d8050a6888f914719d5ab92857f00c27226d
Prevent deleted-account email logging

Validation:
- focused account-deletion, CSRF, privacy, and connection tests: 90 passed
- py_compile passed
- git diff --check passed
- no schema or migration changes
- no account-deletion behavior changes

The separate existing auth_restore email logging finding is OUT OF SCOPE for BL-80 and must not block this investigation.

CONTINUE THE PREVIOUS BL-80 PLAN.

Do not restart from scratch unnecessarily. Reuse any verified findings from the earlier investigation and complete the remaining requested analysis.

BL-80 requirements:

Goal:
Ensure past trips remain a reliable historical record rather than disappearing or losing important semantics after completion/cancellation.

Requirements:
- Define completed vs cancelled historical states.
- Preserve participant/date/trip references required for historical display.
- Do not duplicate the trip into a second historical object.
- Existing Trip Detail remains the canonical trip record.
- Respect deletion/privacy behavior.

USER DATA SAFETY GATE:
This changes retention/semantics of persisted trip/user data. Require compatibility review, migration/backfill decision, rollback strategy, tests, and post-change verification.

BL-140 history contract remains canonical:
docs/history-event-conventions.md

COMPLETE THE INVESTIGATION AND RETURN:

A. current trip lifecycle map
B. current deletion-path map
C. proposed completed semantics
D. proposed cancelled semantics
E. canonical record design
F. history event design
G. participant retention design
H. authorization/privacy rules
I. hard-delete/soft-delete recommendation
J. backfill decision
K. migration plan
L. concurrency plan
M. consumer impact matrix
N. exact implementation scope
O. exact out-of-scope list
P. test plan
Q. risks/open product decisions
R. recommended implementation tier

IMPORTANT QUESTIONS THAT STILL MUST BE RESOLVED

- Is completion persisted or derived from dates?
- Is cancellation currently just hard deletion?
- Which trip deletion paths are ordinary cancellation versus privacy/admin deletion?
- If cancelled trips remain canonical rows, which existing queries/consumers must explicitly exclude them?
- Which participant rows must survive terminal trip states?
- Can cancelled trips still render safely in Trip Detail?
- What authorization applies to former participants and private cancelled/completed trips?
- Does BL-80 need both canonical lifecycle state and append-only lifecycle events?
- What happens when dates on a previously completed trip move back into the future?
- Is cancellation reversible?
- What exact migration should be inserted before bl70_user_season_pass?
- Can BL-70 still be safely reparented based on persistent migration state?

MIGRATION SAFETY

Current expected graph:

bl52_trip_stay
→ bl78_rsvp_transition
→ bl79_friend_history
→ bl70_user_season_pass

Do not mutate any database during PLAN.

Do not access Supabase Development or Production.

Before recommending BL-70 reparenting for BUILD, inspect repository assumptions and clearly state that persistent Replit alembic_version must be rechecked immediately before any ancestry mutation.

If persistent/shared state has BL-70 applied, BUILD must STOP rather than reparent.

PRIVACY

The resolved account-deletion logging issue should no longer block BL-80.

The unrelated auth_restore email logging finding:
- is OUT OF SCOPE
- should be reported separately
- must not be silently changed
- must not halt BL-80 unless you discover a direct BL-80 privacy interaction

Do not implement yet.

If another material privacy, authorization, or data-loss defect directly affecting BL-80 is discovered, STOP and report it clearly rather than silently expanding scope.