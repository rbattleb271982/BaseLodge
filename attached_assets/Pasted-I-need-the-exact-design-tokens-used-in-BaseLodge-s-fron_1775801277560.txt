I need the exact design tokens used in BaseLodge's frontend. 
Search the codebase (CSS files, SCSS files, Tailwind config, 
inline styles in templates, or any style-related configuration) 
and give me the actual values for each of these:

COLORS:
- Primary brand color (the deep red/burgundy used on buttons, 
  active nav items, hero cards, and accent text)
- Background color (the warm ivory/off-white page background)
- Card/surface color (the white or near-white used on cards)
- Text primary color (the dark charcoal/near-black used for 
  headings and body text)
- Text secondary/muted color (the gray used for metadata, 
  subtitles, and helper text)
- Border/divider color (the light gray used between list rows)
- Error/destructive color (if one exists — the red used for 
  "Log Out" or delete actions)
- Success color (if one exists)
- The dark color used for the "Plan another trip" button 
  (appears to be a dark brown/charcoal, different from primary)

For each, give me:
- The exact hex value (e.g., #8B1A1A)
- Where you found it (file and line number)
- If it's defined as a CSS variable, give me the variable name

TYPOGRAPHY:
- Font family (or font stack) used for body text
- Font family used for headings (if different)
- Font sizes used for: page titles, section headers, body text, 
  small/meta text, button text
- Font weights used (regular, medium, bold, etc.)
- Line heights if explicitly set

SPACING:
- Base spacing unit (if using a system like 4px, 8px, etc.)
- Page horizontal padding (the left/right margin on mobile)
- Card internal padding
- Gap between cards/sections
- Gap between list rows

COMPONENTS:
- Border radius on cards
- Border radius on buttons
- Border radius on input fields
- Box shadow on cards (if any)
- Button height (primary buttons like "Invite friends")
- Input field height

BOTTOM NAV:
- Height of the bottom tab bar
- Icon size in the tab bar
- Active vs inactive colors for tab items
- Font size for tab labels

If the app uses Tailwind CSS, give me the tailwind.config.js 
content. If it uses custom CSS, give me the relevant CSS file 
contents. If styles are inline in templates, give me the most 
representative examples.

Format: plain text, organized by category above. Give me the 
raw values — do not editorialize or recommend changes.