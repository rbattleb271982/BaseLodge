Update the BaseLodge Home screen so the entire top profile summary and the next set of content below it match the attached mockup direction as closely as possible, using the existing editorial design system already in the app.

What to change on the Home screen:

1. Refine the top profile summary block so it feels like its own clearly separated module.

* Keep the existing content structure:
    * Welcome line
    * rider type / level / pass line
    * Complete your profile CTA
    * stats row
* Make this whole section feel more contained and visually delimited from the rest of the page.
* Increase the size and prominence of:
    * the rider meta line
    * the Complete your profile CTA
    * the stats numbers and labels
* Add stronger spacing and hierarchy between:
    * the welcome/header area
    * the CTA
    * the stats row
* The stats row should feel like a true summary band, not tiny footer text.
* Keep it elegant and editorial. Do not turn it into a heavy modern app card. Use soft containment, spacing, subtle rules, and hierarchy.

2. Restructure the content below the profile summary into a more cohesive system.
    This section should feel like one unified “activity and ideas” area instead of unrelated modules stacked on top of each other.
3. MY NEXT TRIP module

* Keep this as the primary activity module.
* Merge the current “You’re at Vail today. See who else is there.” style message into the My Next Trip card/module itself instead of showing it as a separate gray notification card.
* The module should support this hierarchy:
    * section label: MY NEXT TRIP
    * mountain/location
    * date/pass line
    * contextual live-status line like “You’re here today”
    * clear CTA like “See who else is here”
    * right-aligned action like Edit
* This should look like one coherent module, not a plain list row plus a separate alert underneath.

4. INVITES module

* Keep Invites as its own section below My Next Trip.
* Standardize trip invites and friend invites into one consistent visual pattern.
* They should use the same overall layout, spacing, and typography structure.
* Only the action differs:
    * trip invite can use “View”
    * friend invite can use “Accept”
* Tighten the content structure so it scans faster and feels cleaner.
* Preserve the existing functionality and route behavior.

5. IDEAS FOR YOU module

* Replace the current generic gray notification-card feeling with a more intentional product section called IDEAS FOR YOU.
* Existing idea content can stay logically similar, for example:
    * a friend has been to your wishlist mountain
    * you and friends have the same mountain on your wishlists
* These should feel like product intelligence cards, not system alerts.
* Use lighter, softer surfaces that fit the editorial design language.
* They should visually belong to the same Home screen system as the rest of the modules.
* Keep dismiss X controls if they already exist, but style them more cleanly.

6. Visual consistency rules

* Make the lower half of Home feel cohesive with the top summary block.
* Use one consistent system across these modules:
    * refined spacing
    * elegant dividers
    * editorial typography
    * subtle left rule/accent treatment where appropriate
    * restrained background fills
* Do not mix three unrelated UI languages.
* Keep the app premium, calm, and mobile-first.
* No chunky cards, no overly dark blocks, no generic dashboard styling.

7. Implementation constraints

* Reuse existing Home screen data and route logic wherever possible.
* Do not break current functionality for:
    * next trip
    * invites
    * ideas
    * dismiss actions
    * edit/view/accept links or buttons
* This is primarily a layout, hierarchy, and cohesion refactor.
* Preserve mobile responsiveness and keep it iOS-friendly for future wrapping in Capacitor.
* Follow the current BaseLodge visual system already established in the app.

8. Deliverable

* Update the relevant Home template(s), CSS, and any minimal supporting view logic needed so the Home screen matches this direction closely.
* Keep the implementation clean and production-appropriate.
* Only use checkpoints if I explicitly ask for them.

Do not finish until you have fully tested the updated Home screen in the browser and confirmed:

* the top profile summary is larger and more clearly separated
* My Next Trip now includes the contextual “you’re here today / see who else is here” type content inside the module
* Invites are visually unified
* Ideas for You feels intentional and cohesive
* spacing and typography look correct on mobile widths.