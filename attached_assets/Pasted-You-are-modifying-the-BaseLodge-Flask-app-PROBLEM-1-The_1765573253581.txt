You are modifying the BaseLodge Flask app.

PROBLEM:
1) The Home State dropdown on Edit Profile is not loading state options
2) The dropdown renders with a broken / floating iOS select UI that looks incorrect

GOAL:
- Ensure the Home State dropdown is populated correctly
- Make it visually consistent with other dropdowns
- Keep it mobile-friendly and native
- No redesign, no new libraries

IMPORTANT RULES:
- Do NOT change database schema
- Do NOT introduce a custom select component
- Use the same pattern as Gender / Birth Year dropdowns
- Fix root cause, not symptoms

--------------------------------------------------
STEP 1: BACKEND – FIX DATA FLOW
--------------------------------------------------

Locate the route that renders Edit Profile (likely /edit_profile).

Confirm:
- A list of U.S. states exists (static list OR database table)

Ensure the route passes:
- `states` → list of state names (strings)
- `user.home_state` → currently selected value (if any)

Example:
states = ["Alabama", "Alaska", ..., "Wyoming"]

Add this to the template context if missing.

--------------------------------------------------
STEP 2: TEMPLATE – FIX DROPDOWN OPTIONS
--------------------------------------------------

In edit_profile.html:

Ensure the Home State select:
- Iterates over `states`
- Renders <option> for each state
- Marks the user's current state as selected

Pattern should MATCH Gender / Birth Year selects exactly.

Do NOT use JS to populate options.

--------------------------------------------------
STEP 3: REMOVE BROKEN STYLING
--------------------------------------------------

Inspect CSS applied to the Home State <select>.

Remove or override:
- transform
- position: absolute
- overflow hidden on parent containers
- any custom pseudo-elements applied only to this select

The Home State dropdown should use the SAME base class as:
- Gender
- Birth Year
- Home Mountain

--------------------------------------------------
STEP 4: IOS SAFARI FIX
--------------------------------------------------

Ensure:
- The select is not wrapped in a div with transform or translate
- No z-index hacks specific to this field
- No custom dropdown arrow overlay

Let the browser handle the native picker cleanly.

--------------------------------------------------
STEP 5: VALIDATION
--------------------------------------------------

If Home State is optional:
- Remove required validation
- Do NOT show error state on load

--------------------------------------------------
STEP 6: TESTING
--------------------------------------------------

Manually verify on mobile:
1) Home State dropdown shows full state list
2) Selecting a state persists after Save
3) Dropdown behaves the same as Gender / Birth Year
4) No floating dark pill overlay
5) No console errors

STOP when complete.
