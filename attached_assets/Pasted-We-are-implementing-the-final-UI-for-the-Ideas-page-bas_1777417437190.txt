We are implementing the final UI for the Ideas page based on the approved design.

This is a FRONTEND-ONLY update (HTML + CSS + minor Jinja logic).
Do not change backend logic.

----------------------------------------
GLOBAL RULES
----------------------------------------

- Use existing design tokens (colors, spacing, fonts)
- Maintain mobile-first layout
- Do NOT introduce new components unless necessary
- Keep all logic tied to existing row data (resort, dates, friend_ids, idea_type)

----------------------------------------
1. HEADER + HERO COPY
----------------------------------------

File: trip_ideas.html

Replace the current title section with:

Eyebrow:
"Ideas"

Dynamic headline:

IF populated:
    "{n} friends, {n} days."

IF reengagement:
    "Nothing lined up — yet."

IF no availability:
    "Where to next."

Subtext:

IF no availability:
    "It starts with your dates. Add them once — we’ll do the rest."

IF reengagement + has availability:
    "We’re watching your dates. The moment something lines up, it’ll show here."

IF populated:
    "This is the trip that actually works."

----------------------------------------
2. STATE 1 (NO AVAILABILITY)
----------------------------------------

Show ONLY:

- Illustration (optional, keep existing if present)
- CTA button:
    "Add when you're free"

- Subtext:
    "Takes about thirty seconds. You can change it any time."

Remove:
- Any feed rows
- Any “in the meantime” section

----------------------------------------
3. STATE 2 (HAS AVAILABILITY, NO MATCHES)
----------------------------------------

Top:
- Headline + subtext (as above)

Add hint card:

Title:
"Speed it up"

Body:
"Invite the friends you actually ski with. Most people get their first match within a week."

Primary CTA:
"Invite friends"

Below that:

Section label:
"IN THE MEANTIME"

Render fallback rows (wishlist / friend saved)

Each row:
- Resort name (serif)
- Location
- One signal line (existing logic)
- Right-aligned:
    "View →"

----------------------------------------
4. STATE 3 (POPULATED)
----------------------------------------

----------------------------------------
4A. HERO CARD (TOP MATCH)
----------------------------------------

Take the strongest row (signal_type highest).

Render as large card:

- Burgundy background
- Rounded corners
- Padding: 20–24px

Content:

Label:
"TOP MATCH"

Resort name (large serif)

Dates:
"{start_date} — {end_date} · {n} nights"

Friend names (comma separated)

CTA button:
"Plan this trip"

Link:
prefilled /add-trip

IMPORTANT:
Do NOT include extra rows like "Conditions"

----------------------------------------
4B. SECONDARY SECTION
----------------------------------------

Label:
"ALSO LINING UP"

Render remaining rows as list

Each row:

LEFT:
- Resort name
- Location
- Signal lines (1–2 max)

RIGHT:
"Plan it →"

----------------------------------------
5. SIGNAL DISPLAY
----------------------------------------

Use existing idea_type

Render as text lines:

availability_overlap:
    "Dates overlap with {friend_name}"

wishlist_overlap:
    "On your wishlist"

friend_trip:
    "{n} friends going"

availability-only:
    "You’re free {date range}"

Limit to max 2 signals per row

----------------------------------------
6. ROW STRUCTURE
----------------------------------------

- Padding: ~18px vertical
- Border-bottom divider
- No divider on last row
- Entire row clickable
- CTA on right side

----------------------------------------
7. CTA STANDARDIZATION
----------------------------------------

Primary buttons:
- Burgundy background
- Cream text
- Rounded

Secondary CTAs:
- Text only
- Burgundy color
- "→" arrow

----------------------------------------

Do not refactor logic.
Do not add new routes.
Only adjust rendering and layout.

Focus on hierarchy:
1. Hero match
2. Supporting ideas
3. Actions