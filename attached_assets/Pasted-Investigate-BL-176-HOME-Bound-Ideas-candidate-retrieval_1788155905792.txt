Investigate BL-176 — HOME — Bound Ideas candidate retrieval.

This is PLAN / INVESTIGATION ONLY.

Do not implement yet.
Do not change application code.
Do not change templates.
Do not change schema or migrations.
Do not commit anything.

GOAL

Determine the safest way to bound Home Ideas candidate retrieval without changing existing Ideas eligibility, matching, ranking, suppression, privacy, or presentation semantics.

BL-177 already separated and bounded Happening retrieval.
Do not alter Happening.

The output of this investigation should define a build-ready architecture for BL-176 only.

1. MAP CURRENT IDEAS DATA FLOW

Trace the full Home Ideas path from `GET /home` through candidate retrieval, matching, ranking, suppression, and final card construction.

Identify:
- route code
- services/helpers
- database queries
- Python-side processing
- candidate collections
- deduplication
- ranking
- suppression
- final display cap
- current query counts
- any relationship loading
- any N+1 behavior

Distinguish raw candidate retrieval from final Ideas cards.

2. LOCK CURRENT IDEAS SEMANTICS

Document the current authoritative matching rules for all Ideas sources.

Based on current behavior, explicitly verify:

Friend-trip ideas:
- direct friends required
- public future friend trips qualify
- friend organizing trip qualifies
- friend attending trip qualifies
- Interested friend attendance qualifies
- Going friend attendance qualifies
- pending attendance excluded
- accepted legacy attendance excluded if that is current behavior
- declined attendance excluded
- removed attendance excluded
- past friend trips excluded
- private friend trips excluded
- resortless friend trips excluded
- past attendance windows excluded

Date-overlap ideas:
- shared future open dates qualify
- friends without availability excluded
- consecutive shared dates become overlap windows
- shared dates require mutual openness
- trip dates never establish availability
- passes never restrict date matching
- shared passes may add context only

Wishlist ideas:
- shared wishlist mountains create ideas
- wishlist matches require direct friends
- wishlist matches require no dates
- wishlist matches require no trips
- missing wishlist mountains excluded
- wishlist overlap selects its mountain

General:
- date overlaps may remain mountainless
- mountainless overlaps suggest choosing mountains
- larger friend groups rank higher
- preserve current ranking behavior exactly unless a current tie is undefined

Do not infer or simplify these rules.
Verify against code/tests.

3. IDENTIFY CURRENT SCALE PROBLEM

Quantify where the unbounded work happens.

For each candidate source:
- rows fetched
- query shape
- joins/eager loading
- Python materialization
- sort/group complexity
- friend-count dependency
- trip-count dependency
- availability-count dependency
- wishlist-count dependency

Explain which candidate populations grow with:
- total direct friends
- total future trips
- participant rows
- open-date rows
- wishlist rows

4. DETERMINE SAFE BOUNDING POINTS

Evaluate whether each Ideas source can be bounded independently before full materialization.

For each source, assess:
- safe SQL filtering
- safe ranking/windowing
- safe per-friend or per-concept dedupe
- whether final ranking requires global knowledge
- whether a simple LIMIT would change semantics
- whether finite overfetch would be heuristic/unsafe
- whether DB-side aggregation is practical

Do not recommend arbitrary limits unless equivalence can be proven.

5. PRESERVE CROSS-SOURCE RANKING

Identify how friend-trip, date-overlap, and wishlist ideas compete in the final Ideas ranking.

Determine:
- whether each source can be reduced independently
- whether the final ranking needs all candidates
- whether source-level top-K selection is safe
- whether ranking can move into SQL
- whether a canonical deterministic tie-break exists

If exact current equivalence cannot be maintained under a bound, return NEEDS PRODUCT/ARCHITECTURE DECISION.

6. SUPPRESSION / DEDUPLICATION

Document all current suppression and deduplication rules.

Identify:
- per-friend suppression
- per-trip suppression
- per-mountain suppression
- date-window merging
- cross-source suppression
- Ideas/Happening suppression behavior
- dismissed or previously-seen state, if any

Do not introduce new suppression.

7. PRIVACY / AUTHORIZATION

Confirm Ideas continues to respect:
- direct-friend boundaries
- public/private trip behavior
- attendance visibility
- availability visibility
- wishlist visibility
- any existing authorization constraints

No broader access may result from optimization.

BL-133 remains authoritative for authorization/visibility.

8. RELATIONSHIP TO BL-177

BL-177 already owns dedicated Happening retrieval.

Confirm BL-176 can be implemented without:
- changing `services/happening.py`
- changing Happening query semantics
- re-coupling Happening to Ideas raw candidates
- changing the five-card Happening bound

Happening must remain untouched.

9. QUERY / PERFORMANCE TARGET

Propose a measurable BL-176 performance contract.

Examples of useful proof:
- fixed maximum rows per candidate source
- bounded number of SQL queries
- DB-side grouping/ranking
- no full-population Python materialization
- no N+1
- no relationship lazy loading
- output-equivalence tests against the old engine

Do not introduce caching.
BL-146 remains authoritative for caching.

10. TEST STRATEGY

Propose tests covering at minimum:

Equivalence:
- friend-trip matching
- availability overlap matching
- wishlist matching
- mixed-source ranking
- mountainless overlap behavior
- group-size ranking
- shared-pass context without pass restriction
- all suppression/deduplication rules

Privacy:
- private trip exclusion
- non-direct friend exclusion
- resortless trip exclusion
- invalid attendance exclusion

Performance:
- large friend population
- large future-trip population
- large availability population
- large wishlist population
- bounded candidate rows/materialization
- explicit query-count assertions
- no N+1

Regression:
- BL-177 Happening behavior unchanged
- Home empty-state behavior unchanged
- existing Ideas presentation unchanged

11. FILES / ARCHITECTURE

Identify:
- likely files to change
- whether `services/ideas_engine.py` should remain authoritative
- whether a new helper/service is warranted
- whether route code should shrink
- tests to add/change
- files that should remain untouched

No schema/migration should be required.

12. STOP CONDITIONS

Return NEEDS PRODUCT/ARCHITECTURE DECISION if:
- bounding requires changing ranking semantics
- ranking requires a new product tie-break
- exact cross-source competition cannot be preserved safely
- privacy semantics must change
- schema/migration is required
- the only viable strategy is arbitrary finite overfetch
- optimization would re-couple Happening to Ideas

Return READY FOR BUILD only if there is a deterministic bounded architecture that preserves current Ideas behavior.

RETURN

CURRENT IDEAS DATA FLOW

CURRENT AUTHORITATIVE IDEAS SEMANTICS

CURRENT SCALE / BOTTLENECK

CANDIDATE SOURCES

CROSS-SOURCE RANKING

SUPPRESSION / DEDUPLICATION

SAFE BOUNDING OPTIONS

RECOMMENDED ARCHITECTURE

EXPECTED QUERY / MATERIALIZATION BOUND

PRIVACY / AUTHORIZATION CHECK

BL-177 ISOLATION CHECK

TEST PLAN

FILES EXPECTED TO CHANGE

RISKS / OPEN QUESTIONS

RECOMMENDED BUILD SCOPE

End with exactly one:

BL-176 READY FOR BUILD

or

BL-176 NEEDS PRODUCT/ARCHITECTURE DECISION