1️⃣ Update Bottom Navigation (templates/components/bottom_nav.html)
Replace the current 3-tab structure with 4 tabs:
Order must be:
Home
Friends
Invite
Settings
Rules:
Use appropriate icons or text labels (match existing style)
Remove “Trips” icon/tab completely
Settings should route to /profile
Invite should route to /invite
2️⃣ Add a NEW Invite page (templates/invite.html)
Create a new template:
templates/invite.html
Contents:
Page header: “Invite a Friend”
Two large buttons:
“Copy Invite Link” (placeholder, no JS needed)
“Show QR Code” (placeholder image)
Simple description:
“Share BaseLodge with your ski crew. Copy your invite link or let your friend scan your QR code.”
Match BaseLodge styling from styles.css
Include bottom_nav.html at the bottom
3️⃣ Add Invite Route to app.py
Inside app.py:
Create a new route:
@app.route('/invite')
@login_required
def invite():
    return render_template('invite.html')
Ensure this route does not require database logic yet — UI only.
4️⃣ Remove Deprecated Trips Page (templates/my_trips.html)
Delete or disable:
templates/my_trips.html
Redirects should point to /home.
5️⃣ Update Home Template (templates/home.html)
Modify home.html so:
The Trips UI (My Trips / Friends’ Trips / All Trips) remains inside Home.
Remove any references to a standalone “Trips” page.
At the top, redesign the header card:
Header Card Requirements
Warm neutral gradient background:
from #F8F3EE to white
Rounded corners
Padding consistent with design system
Contents:
Welcome back, {{ current_user.first_name }}

{{ current_user.pass_type }} · {{ current_user.rider_type }} · {{ current_user.skill_level }}
Mountains visited: {{ mountains_visited_count }}
Next Trip: {{ next_trip.mountain }} – {{ next_trip.date_range }}
If next_trip is missing, show:
“Next Trip: Not scheduled”
6️⃣ Update Friends List (templates/components/friend_row.html)
Modify the friend row component so each friend appears inline:
{{ friend.name }} — {{ friend.pass_type }} — {{ friend.rider_type }} — {{ friend.skill_level }}
Ensure:
Single row
No vertical stacking
Use the existing BaseLodge text styling
7️⃣ Update Settings Link (Profile)
In bottom_nav.html, link “Settings” to:
/profile
Do not use /settings.
8️⃣ Remove or disable any leftover references to "Trips" tab
Search across:
bottom_nav.html
home.html
app.py
Components folder
Ensure:
No remaining “Trips” navigation item
No routes pointing to /trips
All trip content should now live inside home.html only.
9️⃣ Ensure Mobile-First Layout Works
Across templates:
Add padding-bottom (pb-16 equivalent) above bottom nav
Add <div style="margin-bottom: 80px;"></div> where needed to prevent content clipping
10️⃣ Confirm Everything Renders
After updates:
Navigating to / → Home (with Trips UI)
/friends → Friends list
/invite → Invite screen
/profile → Settings page
Make sure bottom nav appears on all authenticated pages.