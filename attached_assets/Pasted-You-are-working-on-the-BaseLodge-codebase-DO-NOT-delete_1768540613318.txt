You are working on the BaseLodge codebase.

DO NOT delete, modify, or migrate anything yet.

Your task in this step is DISCOVERY ONLY.

We are preparing for a Supabase cleanup, but we must first understand
the current Replit structure.

====================
GOAL
====================
Inventory anything in Replit that overlaps with data now owned by Supabase,
specifically resort / mountain data.

====================
STEP 1 — DATABASE STRUCTURE
====================
Inspect the Replit database / ORM models and report:

1. List all tables or models related to:
   - resorts
   - mountains
   - locations
   - regions
   - passes (if tied to resorts)

For each table/model, report:
- Name
- Fields
- Primary key
- Foreign keys
- Whether it is read-only or written to

DO NOT change anything.

====================
STEP 2 — FILE & SCRIPT INVENTORY
====================
Search the codebase for:

- Seed scripts
- Import scripts
- Sync scripts
- JSON / CSV files
- Admin utilities

Specifically anything that:
- Creates resorts
- Updates resorts
- Merges or deduplicates resorts
- Loads resort data from files

List:
- File path
- Purpose
- Whether it writes to DB

====================
STEP 3 — ROUTE & SERVICE USAGE
====================
Search routes and services for:
- Resort reads
- Resort writes
- Admin resort CRUD
- Fallback logic (Supabase → Replit)

For each occurrence:
- File
- Function
- Read vs write
- Whether it is still used

====================
STEP 4 — CONFIG & FALLBACKS
====================
Check for:
- Dual DB config paths
- Feature flags
- try/except fallbacks
- Environment switches for data source

List all findings.

====================
OUTPUT FORMAT
====================
Produce a structured report with these sections:

1. Database / Models
2. Scripts & Static Data
3. Routes & Services
4. Config & Fallback Logic

For each item, explicitly mark one of:
- SAFE TO DELETE
- SAFE TO DISABLE
- REQUIRED (DO NOT TOUCH)
- NEEDS CONFIRMATION

Do NOT perform any deletions or edits.
Wait for explicit user approval after reporting.
