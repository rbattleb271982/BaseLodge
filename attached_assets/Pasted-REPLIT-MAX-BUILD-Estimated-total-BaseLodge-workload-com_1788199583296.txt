REPLIT: MAX + BUILD
Estimated total BaseLodge workload completed after this step: ~38%

TASK: Resolve the persistent Replit DB identity gate, then resume BL-87

BL-87 implementation previously stopped correctly at Hard Gate 1.

CONFIRMED SUPABASE DEVELOPMENT:

- target: supabase-development
- identity: sha256:2e1590206884…
- revision: bl80_trip_lifecycle
- BL-87 absent
- BL-70 absent

PERSISTENT REPLIT DB PREVIOUSLY VERIFIED DURING BL-80:

- built-in persistent Replit development database
- DATABASE_URL
- host: helium
- database: heliumdb
- PostgreSQL
- previously migrated explicitly to bl80_trip_lifecycle
- BL-70 was not applied

Current blocker:

BASELODGE_MIGRATION_REPLIT_IDENTITY_HASH is not configured.

The guarded resolver therefore refuses to inspect the Replit database.

==================================================
1. INSPECT THE EXISTING IDENTITY-GUARD CONTRACT
==================================================

Inspect:

- scripts/database_target.py
- runtime_config.py
- relevant migration-target tests
- existing environment/secret configuration conventions

Determine the exact canonical algorithm already used by the repository to compute the SHA-256 database identity hash.

DO NOT invent a second algorithm.

DO NOT weaken or bypass the guard.

DO NOT hard-code an allowlist in application code.

==================================================
2. IDENTIFY THE PERSISTENT REPLIT DATABASE
==================================================

Using the existing persistent Replit DATABASE_URL only:

- do not print the full database URL
- do not print credentials
- do not expose secrets

Establish the database identity using the SAME canonical identity logic used by the guard.

Confirm non-secret identity characteristics are consistent with the previously verified persistent Replit database, including where safely available:

- target type = persistent Replit database
- host = helium
- database = heliumdb
- PostgreSQL server reachable
- identity hash computed through the repository’s canonical method

If the target does NOT match the known persistent Replit database characteristics:

STOP.

Do not configure the hash.
Do not proceed with BL-87.

==================================================
3. CONFIGURE THE REPLIT MIGRATION IDENTITY HASH
==================================================

If and only if the identity is consistent with the known persistent Replit DB:

Configure:

BASELODGE_MIGRATION_REPLIT_IDENTITY_HASH

to the canonical SHA-256 identity hash.

Use the repository/Replit-supported secret or environment mechanism intended for migration target identity configuration.

Do NOT:

- modify application runtime architecture
- change DATABASE_URL
- change BASELODGE_DEVELOPMENT_DATABASE_URL
- change Flask workflow command
- expose the full DB URL
- expose credentials
- weaken target verification

This configuration exists solely to allow the guard to recognize the intended persistent Replit database.

==================================================
4. RE-RUN HARD GATE 1
==================================================

After configuration, run the guarded read-only verification for the persistent Replit database.

Confirm:

- target verification = PASS
- identity matches the newly configured expected SHA-256
- revision = bl80_trip_lifecycle
- wishlist_resort_event absent
- BL-87 absent
- user_season_pass absent
- BL-70 absent

Also re-confirm Supabase Development remains:

- identity sha256:2e1590206884…
- revision = bl80_trip_lifecycle
- BL-87 absent
- BL-70 absent

Do NOT access Production.

If either persistent environment is not exactly compatible with the approved ancestry:

STOP.

==================================================
5. IF HARD GATE PASSES, RESUME BL-87
==================================================

If BOTH:

- persistent Replit DB
- Supabase Development

pass Hard Gate 1 and remain before BL-70:

resume the already-approved BL-87 implementation from Task #399.

Do not create another plan.

Implement the approved design exactly:

- `WishlistResortEvent`
- `wishlist_resort_event`
- added/removed events
- settings/mountain_detail bounded sources
- nullable actor provenance
- subject and resort cascade semantics
- actor SET NULL
- timezone-aware timestamp
- required constraints/indexes
- transaction-neutral locked wishlist service
- replace/add/remove operations
- deterministic bulk diff semantics
- no events for no-op/reorder/normalization/admin merge
- privacy/account deletion integration
- resort lifecycle behavior
- numeric-string wishlist deletion-guard fix
- no synthetic backfill

==================================================
6. MIGRATION GRAPH
==================================================

Create:

bl87_wishlist_history

Approved ancestry:

bl52_trip_stay
→ bl78_rsvp_transition
→ bl79_friend_history
→ bl80_trip_lifecycle
→ bl87_wishlist_history
→ bl70_user_season_pass

Set:

BL-87 down_revision = bl80_trip_lifecycle

Change ONLY BL-70 down_revision to:

bl87_wishlist_history

Do not modify BL-70 migration body.

Do not create a parallel head.

==================================================
7. DO NOT ROLL OUT BL-87
==================================================

Even after implementation succeeds:

DO NOT apply BL-87 to:

- persistent Replit DB
- Supabase Development
- Production

This BUILD is code + migration creation + tests only.

Do not run unrestricted `alembic upgrade head`.

Production remains completely untouched.

==================================================
8. VALIDATION
==================================================

Run the full approved BL-87 validation set, including:

- event semantics
- deterministic replacement diffs
- no-op behavior
- repeated add/remove/add lifecycle
- rollback atomicity
- privacy deletion
- actor anonymization
- resort inactivation/deletion
- numeric-string deletion guard
- no backfill
- migration ancestry
- single Alembic head
- empty downgrade
- non-empty destructive downgrade refusal
- PostgreSQL concurrency
- current-state consumer isolation
- adjacent wishlist/Home/Ideas/privacy/resort tests
- full suite

Also run:

- py_compile
- git diff --check

Known unrelated SQLite migration-guard failure may remain if unchanged.

==================================================
9. COMMIT
==================================================

If implementation and validation pass:

Commit:

BL-87 Preserve wishlist transition history

Exclude unrelated pasted prompt assets.

==================================================
10. RETURN
==================================================

Return:

A. canonical Replit DB identity method used
B. confirmation identity guard was configured without exposing credentials
C. guarded persistent Replit target verification result
D. persistent Replit revision / BL-87 / BL-70 state
E. reconfirmed Supabase Development state
F. confirmation Hard Gate 1 passed
G. BL-87 model/schema implementation
H. wishlist service implementation
I. mutation-path integration
J. privacy/resort lifecycle behavior
K. numeric-string deletion-guard fix
L. no-backfill confirmation
M. final migration graph and single-head status
N. focused/adjacent/full tests
O. PostgreSQL concurrency tests
P. py_compile / git diff --check
Q. commit hash
R. confirmation no persistent DB received BL-87 migration
S. confirmation Production was untouched
T. whether BL-87 is code-side complete and ready for a separate Development rollout

If the Replit identity cannot be established safely, STOP rather than bypassing the guard.