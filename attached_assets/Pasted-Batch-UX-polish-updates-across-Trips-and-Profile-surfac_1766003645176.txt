Batch UX polish updates across Trips and Profile surfaces to improve clarity, correctness, and social visibility.
This is a UI behavior + display batch, not new feature development.
🔴 TASK 1 — ADD A TRIP: CALENDAR PLACEHOLDER
On the Add a Trip page:
Update the date input / calendar trigger to show a placeholder:
mm/dd/yy
Placeholder should be visible when no date is selected
This is a visual hint only (not a value)
🟠 TASK 2 — OPEN DATES CALENDAR SELECTION STATE (BUG FIX)
Fix the calendar selection behavior for Open Dates.
Required behavior
When a date is selected:
It should immediately render in maroon
When a date is unselected:
It should immediately return to default state
There should be no intermediate beige/half-selected state
Ensure selection state is visually correct on:
First selection
Subsequent selections
Deselection
🟡 TASK 3 — OPEN DATES SELECTED SUMMARY
On the Open Dates selection screen:
Add a summary display:
Below the calendar
Above the “Save Open Dates” button
Format
Group selected dates by month
Sort dates numerically
Example:
Jan: 1, 2, 3, 4
Feb: 14, 15
Behavior
Updates live as dates are selected/unselected
Read-only (not interactive)
Matches style used elsewhere in the app for date summaries
🟢 TASK 4 — HOME / PROFILE: MOUNTAINS VISITED PILLS
On the Home screen / profile view:
Display Mountains Visited as pill boxes, using the same visual style as Equipment pills
Behavior
If user has mountains:
Show pill list of mountain names
If user has none:
Show a pill or CTA that says:
+ Mountains Visited
Rules
Display-only (no inline editing)
Do not change how Mountains Visited are edited
Keep spacing consistent with Equipment pills
🔵 CONSTRAINTS
Do not add new data models
Do not refactor unrelated logic
Do not introduce new colors
Reuse existing pill styles
Keep all changes mobile-first
✅ FINAL VERIFICATION CHECKLIST
Before marking complete, manually verify:
Add Trip date input shows mm/dd/yy when empty
Selecting a date turns it maroon immediately
Deselecting returns it to default
No beige intermediate state appears
Selected dates summary renders correctly and updates live
Home/Profile shows Mountains Visited pills
+ Mountains Visited appears when empty
No regressions in Trips or Profile flows