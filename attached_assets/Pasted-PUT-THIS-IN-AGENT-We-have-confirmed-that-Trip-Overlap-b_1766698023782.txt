PUT THIS IN AGENT

We have confirmed that Trip Overlap backend logic is correct.
The remaining issue is UI COPY and semantic clarity between:

- Trip Overlaps (same resort + overlapping dates)
- Availability (shared open dates, no resort requirement)

This task is COPY-ONLY and light naming cleanup.
❌ Do NOT change backend logic
❌ Do NOT change queries or conditions

–––––––––––––––––
A) FRIEND TRIP DETAILS SCREEN (HIGH PRIORITY)
–––––––––––––––––

File: templates/friend_trip_details.html (around lines 89–101)

Current copy:
"{{ friends_open_on_trip|length }} friend(s) open during this trip"

Problem:
- Ambiguous phrasing
- Can be misinterpreted as trip overlap

Required change:
- Replace with availability-explicit language
- Do NOT use the word “overlap”

Use this copy (preferred):
"{{ friends_open_on_trip|length }} friend(s) are available during these dates"

Ensure:
- This copy clearly communicates availability only
- No implication of shared trips or same resort

–––––––––––––––––
B) HOME SCREEN — AVAILABILITY CARD (MEDIUM PRIORITY)
–––––––––––––––––

File: templates/home.html (around lines 397–401)

Current copy:
"You and X friend(s) have overlapping availability"

Problem:
- Uses the word “overlapping,” which collides conceptually with Trip Overlaps

Required change:
- Replace with clearer availability language

Use this copy (preferred):
"You and X friend(s) are available on the same dates"

Keep:
- Existing link behavior (“See availability →”)
- Existing routing to Open Availability tab

–––––––––––––––––
C) DEVELOPER-FACING NAMING CLEANUP (LOW PRIORITY)
–––––––––––––––––

File: templates/friend_profile.html (availability section)

Optional but recommended:
- Rename CSS classes and variables that include “overlap” but represent availability

Examples:
- .overlap-callout → .availability-callout
- .overlap-callout-text → .availability-callout-text
- has_availability_overlap → has_shared_availability

Notes:
- This is for code clarity only
- No user-facing text changes required here

–––––––––––––––––
D) CONSTRAINTS
–––––––––––––––––

- Do NOT change:
  • Trip Overlaps tab
  • Trip overlap copy (“Same resort, overlapping dates”)
  • Backend logic
  • Data models

- This task is strictly about:
  • Copy clarity
  • Semantic separation
  • Preventing user confusion

Deliverable:
- Updated templates with corrected copy
- Brief confirmation of changes applied