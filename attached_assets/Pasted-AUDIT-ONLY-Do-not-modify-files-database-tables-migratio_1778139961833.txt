AUDIT ONLY. Do not modify files, database tables, migrations, seed data, or production data.

I need a full backend data audit for BaseLodge’s resort, ski pass, mountain, state, region, and pass-mapping system.

Goal:
Understand exactly where resort/mountain/pass data currently lives, which fields are used by the app, and what would break if we moved to a cleaner canonical structure.

Please inspect the codebase and report:

1. All database tables and models related to:
   - resorts / mountains
   - ski passes
   - resort-pass mappings
   - states
   - regions
   - countries
   - trip destination fields
   - user pass fields

2. For each related model/table, list:
   - table name
   - columns
   - relationships
   - whether the field appears to be source-of-truth or duplicated/legacy

3. Identify every place in the app that reads or writes:
   - resort.pass_brands
   - resort.pass_brands_json
   - resort_pass
   - user.pass_type
   - ski_trip.pass_type
   - resort.state / state_full / state_code / state_name
   - resort.country / country_code / country_name
   - resort.brand

4. Identify every route/template/service that depends on pass or resort data, especially:
   - onboarding
   - profile edit
   - add/edit trip
   - Friends filtering
   - Friends’ Trips mountain filter
   - Home signals
   - Ideas/recommendations
   - Season Snapshot
   - admin/debug tools
   - seed scripts
   - migrations

5. Identify data risks:
   - duplicate pass storage
   - inconsistent pass names
   - resorts with no pass mappings
   - inactive resorts still shown
   - JSON fields that may drift from relationship table
   - trip records storing pass text separately from resort data
   - any hardcoded pass lists in templates or Python

6. Recommend the safest future structure, but do not implement it yet:
   - resort = all mountains/destinations
   - pass = canonical ski pass list
   - resort_pass = relationship table
   - no pass data duplicated in resort JSON fields long-term

7. Recommend a safe migration/import strategy using an Excel or CSV master file:
   - Resorts tab
   - Passes tab
   - ResortPassMap tab
   - validation checks before import
   - dry-run mode
   - backup/export before write
   - admin-only import or script-only import

8. Output:
   - A clear audit report
   - A list of files inspected
   - A list of exact code locations that would need to change later
   - A “do not touch yet” list for anything risky
   - A recommended phased plan

Important:
This is an audit only. Do not change code. Do not change database data. Do not create migrations. Do not delete pass_brands or pass_brands_json yet. Do not alter production data.