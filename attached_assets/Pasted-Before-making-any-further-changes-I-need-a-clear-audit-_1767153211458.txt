Before making any further changes, I need a clear audit of the CURRENT Admin → Resort Data Curation structure as it exists right now.

Do NOT implement anything in this step.
This is a read-only assessment.

Please document the following:

================================
PART 1 — FRONTEND STRUCTURE
================================

1. Filters
• List all filters that currently exist (Country, Status, State/Region, Pass Brand, Search, etc.)
• For each filter:
  - Where state is stored (URL params, JS state, form submit)
  - Whether it affects:
    • Table rendering
    • Excel export
    • Bulk actions
• Note any filters that are UI-only but not enforced in queries

2. Selection & Bulk Actions
• Describe how row selection currently works
• Confirm:
  - How selected IDs are tracked
  - How “Select All” behaves (visible rows vs all rows)
• List all bulk actions currently wired:
  - Activate
  - Deactivate
  - Merge
  - Delete
  - Pass Brand update
• Identify which bulk actions are currently broken or disconnected

3. Inline Edits
• List all inline-editable fields
  - Status
  - Pass Brands
• For each:
  - Endpoint used
  - Whether AJAX or full reload
  - Whether filters are preserved

================================
PART 2 — BACKEND STRUCTURE
================================

4. Endpoints
List all admin-related endpoints touching resorts, including:
• Table rendering
• Inline edits
• Bulk actions
• Excel export
• Excel import

For each endpoint:
• Route
• Purpose
• HTTP method
• ORM vs Core usage

5. Query Logic
• Identify the single source of truth for:
  - Filtering logic
  - Export logic
• Confirm whether the same query logic is reused or duplicated

================================
PART 3 — REGRESSIONS & GAPS
================================

6. Known regressions
Based on current code, explicitly confirm:
• Why bulk actions stopped working
• Whether selection state is being reset
• Whether filters are interfering with bulk logic

7. Risk areas
Call out:
• Any duplicated logic
• Any fragile JS bindings
• Any areas likely to regress again

================================
PART 4 — SUMMARY
================================

Provide:
• A concise diagram or bullet list of the current structure
• A list of what is WORKING
• A list of what is BROKEN
• A recommendation for the correct order of fixes

Do not propose new features.
Do not implement changes yet.
Stop after the audit.
