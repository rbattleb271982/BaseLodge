PUT THIS IN AGENT

We confirmed the bug in friend_trip_details (app.py ~2571–2591):
The "You'll be there X days" callout currently sums overlapping days across ALL of the current user's trips,
regardless of resort/mountain. This is incorrect.

Correct definition:
"You'll be there X days" must ONLY mean:
- You have a trip at the SAME mountain/resort as the friend’s trip
- AND your trip dates overlap with the friend trip dates
- X = total overlapping days at that SAME mountain/resort

Scope:
- Fix ONLY the logic that computes overlap_days / has_overlap for the friend_trip_details page
- Do NOT touch Trip Overlaps tab logic (already correct)
- Do NOT involve availability/open_dates for this callout

Implementation requirements:

A) Resort matching
1) Filter your_trips to SAME mountain as the friend trip BEFORE computing overlap days.
   Use the same field used elsewhere for resort identity (currently SkiTrip.mountain string).
   Example:
   matching_trips = [t for t in your_trips if t.mountain == trip.mountain]

2) Only compute overlapping days against matching_trips.

B) Day counting
3) Keep the existing overlapping_days() helper (inclusive bounds) for computing day intersections.
4) Compute overlap_days as the SUM of overlapping_days(...) across matching_trips only.

C) Rendering rule
5) has_overlap should be true ONLY if overlap_days > 0 after same-mountain filtering.
6) If overlap_days == 0, do NOT render the callout box at all.

D) Copy adjustment (small but important)
7) Update the callout text to remove ambiguity:
   - If overlap_days == 1: "You'll both be there 1 day."
   - Else: "You'll both be there {{ overlap_days }} days."

(Replace "You'll be there X days during this time." with the above so it clearly implies shared presence.)

E) Validation
8) Test these cases in the browser:
   - Friend trip at Niseko; user has overlapping trip dates at Vail => callout MUST NOT show
   - Friend trip at Beaver Creek; user has Beaver Creek trip overlapping 3 days => shows "You'll both be there 3 days."
   - Edge case where overlap is only one boundary day => shows 1 day (inclusive logic)

Do not finish until you've fully tested the user flow end-to-end in the browser with at least one negative case and one positive case.
Only use checkpoints if I say so.