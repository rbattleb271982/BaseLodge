PUT THIS IN AGENT

We have a recurring critical bug on /add_trip where the Country dropdown renders with no options, causing State = “No Options”, Resort disabled, and the calendar not re-initializing.

Fix this permanently with the following requirements:

1. COUNTRY DROPDOWN (HARD REQUIREMENT)
- The Country <select> MUST always be populated on page load.
- Do NOT rely on prior selections, edit mode, or JS side-effects.
- Populate it directly from `states_by_country.keys()` server-side.
- Render options like:
  <option value="">Choose country</option>
  <option value="US">United States</option>
  <option value="CA">Canada</option>

2. DEFAULT COUNTRY
- If no country is set (new trip), default to "US".
- This default must be applied BOTH:
  - in the rendered HTML `selected` attribute
  - AND in JS initialization logic

3. JS INITIALIZATION (DEFENSIVE)
- On DOMContentLoaded:
  - Read the country value
  - If empty or invalid, force-set it to "US"
  - Immediately populate the State dropdown from statesByCountry[country]
- Do NOT wait for a change event to populate State.

4. STATE DROPDOWN
- State options must always regenerate when country is set or changed.
- Clear + repopulate safely.
- If country changes, also clear Resort and reset calendar.

5. RESORT + CALENDAR RESET
- If country OR state changes:
  - Clear selected resort
  - Disable resort search until valid
  - Fully reinitialize the calendar (destroy + rebuild if needed)

6. EDIT MODE SAFETY
- If editing an existing trip:
  - Country → State → Resort must hydrate in order
  - No early returns or skipped logic allowed
- Explicitly guard against `isEditMode` bypassing initialization

7. FAIL-LOUD DEBUG
- If statesByCountry[country] is undefined:
  - console.error with country value
  - fallback to US instead of silently failing

8. FINAL CHECK
- Hard refresh, incognito, and mobile Safari must all:
  - Show United States preselected
  - Show populated State dropdown immediately
  - Allow Resort selection
  - Show calendar without requiring any manual interaction

This bug must not be able to regress again.
Do not introduce new conditionals that skip initialization.
Test new trip + edit trip paths fully.