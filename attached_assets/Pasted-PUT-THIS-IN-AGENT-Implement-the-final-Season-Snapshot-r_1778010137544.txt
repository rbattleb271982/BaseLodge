PUT THIS IN AGENT

Implement the final Season Snapshot redesign using the approved static HTML mock.

Use the uploaded mock as the visual source of truth. Convert it into the real Flask/Jinja template at:

templates/season_snapshot.html

Reference mock details:
- Compact maroon header
- Title: [Name]’s Winter
- Meta line: rider type · skill level · pass
- Attribution line near top of cream body: “on BaseLodge”
- Month-grouped destination list
- 1:1 poster card
- No truncation
- Bottom nav remains from base_app.html, do not mock it manually

Important:
Do not use the standalone HTML document structure from the mock.
Do not include <!doctype>, html, head, body, or nav-mock.
Do not add the mocked bottom nav.
This must remain a Jinja template extending base_app.html.

Architecture constraints:
- Keep `{% extends "base_app.html" %}`
- Keep CSS scoped in `{% block head %}`
- Keep content in `{% block content %}`
- Do not modify app.py unless absolutely necessary
- Do not modify base_app.html
- Do not modify static/styles.css unless absolutely necessary
- No React
- No new libraries
- No schema changes

Use existing data:
- user.first_name
- user|identity_line
- all_upcoming
- group_trips_by_month(all_upcoming)
- trip.resort.name preferred, trip.mountain fallback

Convert mock content dynamically:
- “Richard” becomes `{{ user.first_name }}`
- “Skier · Advanced · No Pass” comes from `user|identity_line`
- “on BaseLodge” stays as static attribution text
- Month/destination rows come from `group_trips_by_month(all_upcoming)`
- Destination should use `trip.resort.name if trip.resort else trip.mountain`

Overflow behavior:
- Keep the 1:1 card.
- No internal scrolling.
- Show nearest/upcoming trips first using the existing sorted `all_upcoming` order.
- Use compact density if there are more trips.
- Show `+ X more` after the visible cap.
- Do not let rows overflow outside the card.

Visual requirements:
- Match the approved mock closely.
- Maroon header: #5C1219
- Cream background: #F5F1E8
- Charcoal text: #1A1A1A
- Accent maroon: #A8152B
- Serif title
- Clean sans meta/month labels
- Destination names should be readable and not truncated.
- Remove `white-space: nowrap`, `overflow: hidden`, and `text-overflow: ellipsis` from destination names.
- Use wrapping instead.

Back button:
- Keep a minimal back arrow outside the card.
- It should route back safely, preferably to the previous page or existing app route behavior.

Testing:
1. Visit /season-snapshot while logged in.
2. Confirm no raw Jinja appears.
3. Confirm user name renders.
4. Confirm identity/pass line renders.
5. Confirm “on BaseLodge” appears near the top of the cream body, not the bottom.
6. Confirm Big Sky Resort does not truncate.
7. Confirm long destination names wrap cleanly.
8. Confirm 1:1 card sizing works on mobile.
9. Confirm bottom nav still comes from base_app.html.
10. Confirm no global layout regressions.

At the end, report:
- Files changed
- Whether app.py was untouched
- How identity/pass line is parsed
- Visible trip cap used
- How overflow “+ X more” is calculated
- Tests completed