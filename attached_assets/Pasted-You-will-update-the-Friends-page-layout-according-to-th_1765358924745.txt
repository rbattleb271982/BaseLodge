You will update the Friends page layout according to the new UX spec.

========================
TASK: FRIENDS PAGE UPDATE
========================

FILES TO MODIFY:
1. templates/friends.html
2. static/styles.css (or styles.css depending on project structure)

============================
REQUIREMENTS – FRIENDS PAGE
============================

1. REMOVE the Trips/Friends toggle from friends.html entirely.

2. The Friends page should ONLY show:
   - Page title: “Friends”
   - Back to Home link
   - Invite a Friend button (TOP of page)
   - List of friends (simple inline rows)

3. FRIEND ROW FORMAT (INLINE, SINGLE-LINE):
   Alex Peak — Snowboarder — Ikon

4. FRIEND ROW STYLING:
   - Use **Option A**: Gray card background (same as current friend cards)
   - Entire row should be one line (wrap only if needed)
   - Name bold, rider + pass normal weight
   - Use an EM DASH (—) between each item
   - Card padding and rounded corners should match existing design system

5. INVITE BUTTON:
   - Place IMMEDIATELY under the page title
   - Use PRIMARY button styling (.auth-button)
   - Text: “Invite a Friend”
   - Clicking it should call the same JS function currently used for profile’s “Invite a Friend”

6. FRIEND LIST:
   - No header: DO NOT display “10 Friends”
   - Just directly list the friend cards
   - Maintain vertical spacing like existing .friends-list

7. REMOVE the "Friends' Upcoming Trips" section from friends.html entirely.
   (Trips will only be seen on the profile page under “Friends’ Trips” tab.)

============================
REQUIREMENTS – CSS UPDATES
============================

1. Add a new class .friend-inline to force inline row format:
   - display: flex
   - flex-wrap: wrap (so it wraps gracefully on small screens)
   - font-size: 0.95rem
   - gap: 4px between text items

2. Friend card should be:
   - background-color: var(--bg)
   - border-radius: 12px
   - padding: 16px

3. Remove any obsolete toggle classes from friends.html.

============================
EDIT PROFILE & HOME PAGE NOTES
============================
Do NOT change profile.html or edit_profile.html yet.
Current task is ONLY updating friends.html and required CSS.

============================
OUTPUT FORMAT
============================
Show me:
1. The updated friends.html (full file, not partial)
2. The updated CSS additions (only the added or modified blocks)

DO NOT modify anything else in the project.

