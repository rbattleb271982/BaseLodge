Now I want to convert the audit into an actionable implementation plan.

Focus ONLY on the Event system and give me a precise, code-level rollout plan.

--------------------------------------------------
SECTION 1 — EVENT STANDARDIZATION
--------------------------------------------------

1. List all current event names already used in emit_event(...)
2. Identify inconsistencies in naming or structure
3. Propose a clean, consistent naming convention for all events

--------------------------------------------------
SECTION 2 — EVENT COVERAGE GAP ANALYSIS
--------------------------------------------------

1. Compare:
   - all user actions in the app
   vs
   - events currently emitted

2. List:
   - which actions already emit events
   - which actions DO NOT but should

3. Prioritize:
   - top 10 missing events to implement first

--------------------------------------------------
SECTION 3 — EVENT PAYLOAD RULES
--------------------------------------------------

1. Inspect current payload usage
2. Identify any cases where:
   - names
   - emails
   - or other PII
   are included

3. Propose strict payload guidelines:
   - what is allowed
   - what is forbidden

4. Show examples of:
   - GOOD payload
   - BAD payload

--------------------------------------------------
SECTION 4 — IMPLEMENTATION PLAN (CODE LEVEL)
--------------------------------------------------

For each of the top 10 missing events:

1. exact file
2. exact function
3. where emit_event should be inserted
4. what event_name should be
5. what payload should include

Be very explicit so I can implement this directly.

--------------------------------------------------
SECTION 5 — ACTIVITY vs EVENT STRATEGY
--------------------------------------------------

1. Should every Activity also emit an Event?
2. If yes:
   - where in the code should this linkage happen?
3. If no:
   - define which actions should emit Activity vs Event vs both

--------------------------------------------------
FINAL OUTPUT
--------------------------------------------------

End with:
- the 5 most important event implementations to do FIRST
- anything that could break existing functionality if done incorrectly