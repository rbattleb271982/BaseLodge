REPLIT: MAX + BUILD
Estimated total BaseLodge workload completed after this step: ~38%

TASK: BL-87 — Preserve wishlist add/remove history

The BL-87 investigation and architecture are approved.

Proceed to implementation.

Canonical plan:
Task #399 — Preserve wishlist add/remove history

Canonical history conventions:
docs/history-event-conventions.md

==================================================
APPROVED DESIGN
==================================================

CURRENT STATE

`User.wish_list_resorts` remains the sole operational source of truth.

Do NOT replace it with a relational current-state table.

History must never determine:
- current wishlist membership
- authorization
- visibility
- Ideas
- matching
- recommendations
- social proof
- eligibility

HISTORY

Add private append-only:

`wishlist_resort_event`

Model:
`WishlistResortEvent`

Columns:
- id — immutable database-generated PK
- user_id — required subject user
- resort_id — required subject resort
- actor_user_id — nullable actor provenance
- event_type — added / removed
- source — settings / mountain_detail
- occurred_at — server-generated timezone-aware timestamp

FKs:
- user_id → user.id ON DELETE CASCADE
- resort_id → resort.id ON DELETE CASCADE
- actor_user_id → user.id ON DELETE SET NULL

Constraints:
- bounded event_type
- bounded source
- no user/resort/type uniqueness constraint

Indexes:
- user + occurred_at
- user + resort + occurred_at

Event ID is deterministic tie-breaker.

No copied resort name.
No copied user PII.
No arbitrary metadata.
No free-text source.

==================================================
HARD GATE 1 — MIGRATION SAFETY
==================================================

Before editing BL-70 ancestry:

Verify BOTH authorized persistent environments:

1. Persistent Replit DB
2. Supabase Development

Expected current revision for both:
bl80_trip_lifecycle

Confirm:
- BL-87 absent
- BL-70 absent

Do NOT access Production.

If either environment has BL-70 applied or a migration state inconsistent with the approved graph:

STOP.

Do not edit BL-70 ancestry.
Do not create a parallel migration branch.
Return the exact discovered state.

==================================================
MIGRATION
==================================================

If preflight passes:

Create:

bl87_wishlist_history

Approved graph:

bl52_trip_stay
→ bl78_rsvp_transition
→ bl79_friend_history
→ bl80_trip_lifecycle
→ bl87_wishlist_history
→ bl70_user_season_pass

BL-87:

down_revision = "bl80_trip_lifecycle"

Reparent ONLY BL-70’s down_revision to:

bl87_wishlist_history

Do not modify BL-70 migration body.

Migration must:
- create empty wishlist_resort_event table
- create required constraints
- create required FKs
- create required indexes
- perform NO backfill
- perform NO User.wish_list_resorts data rewrite

Existing wishlist membership remains current truth with pre-cutover event history unknown.

DOWNGRADE

Downgrade to BL-80:
- succeeds if wishlist_resort_event is empty
- refuses if retained events exist

Do not silently destroy wishlist history.

==================================================
WISHLIST SERVICE
==================================================

Extend the existing wishlist service.

Add transaction-neutral operations for:

1. replace entire wishlist
2. add one resort
3. remove one resort

Each operation must:

- lock the subject User row using PostgreSQL SELECT ... FOR UPDATE
- refresh authoritative current state under the lock
- canonicalize/normalize current wishlist state
- validate the requested state using existing product rules
- compute exact membership changes
- update User.wish_list_resorts
- append one event per REAL membership transition
- flush without committing
- return structured change/result data

Routes retain commit/rollback ownership.

Do not commit inside the service.

==================================================
CURRENT-STATE RULES
==================================================

Preserve existing behavior:

- canonical entries are positive integer resort IDs
- maximum canonical size = 15
- first-seen ordering preserved
- duplicates removed
- malformed IDs removed during canonicalization
- product-visible reads only expose active non-region resorts

Do not silently change existing validation or UI semantics.

History tracks MEMBERSHIP only, not ordering.

==================================================
EVENT SEMANTICS
==================================================

ADDED:
resort absent from canonical membership before mutation and present after mutation

REMOVED:
resort present before mutation and absent after mutation

Sources:

settings
mountain_detail

Actor:
authenticated user explicitly passed as actor_user_id

Currently actor == subject user on product paths.

Do not add:
api
system
admin
onboarding

unless actual repository behavior proves one is necessary during BUILD and you STOP for review before expanding.

==================================================
BULK / ORDER / NO-OP RULES
==================================================

WHOLE-LIST REPLACEMENT

Treat current state as ordered list for storage, but set membership for history.

For mixed replacement:

- removals emitted in prior canonical order
- additions emitted in requested canonical order

Example:

3 adds + 2 removes = exactly 5 events

REORDER ONLY:
- current list order may change
- emit zero events

IDENTICAL REPLACEMENT:
- zero events

DUPLICATE ADD:
- zero events

ABSENT REMOVE:
- zero events

NORMALIZATION-ONLY REPAIR:
- zero events

MALFORMED LEGACY CLEANUP:
- zero events

ADMIN RESORT MERGE NORMALIZATION:
- zero user transition events

LEGITIMATE REPEATED LIFECYCLE:

added
removed
added

must produce three separate events.

==================================================
MUTATION PATH INTEGRATION
==================================================

Integrate every real product mutation path identified in PLAN:

1. `/settings/wish-list/save`
   source = settings

2. `/api/wishlist/add`
   source = mountain_detail

3. `/api/wishlist/remove`
   source = mountain_detail

4. Admin resort merge
   route through deterministic locked maintenance handling
   NO wishlist history event

Do not invent onboarding/profile mutation paths.

Preserve existing response shapes and user-visible behavior.

Existing best-effort analytics should remain tied only to actual current-state changes and remain outside history atomicity.

==================================================
CONCURRENCY / IDEMPOTENCY
==================================================

All user wishlist mutations serialize on the User row.

Required semantics:

- concurrent distinct adds serialize safely
- 15-item limit checked under lock
- concurrent add/remove uses last-serialized-operation semantics
- replace vs single mutation serialized
- replace vs replace serialized
- retry against unchanged committed state becomes no-op
- no duplicate event on retry
- no external idempotency key

Do not use event-table uniqueness to suppress valid repeated lifecycles.

Use disposable PostgreSQL coverage.

SQLite is not sufficient for lock validation.

==================================================
PRIVACY / ACCOUNT DELETION
==================================================

Subject user deletion:
- erase their wishlist history

Actor-only deletion:
- actor_user_id becomes NULL on otherwise surviving events

Privacy deletion:
- must NOT append removed events
- must NOT fabricate history

Current wishlist JSON disappears with User deletion as before.

BL-87 account-deletion edits must remain narrowly limited to wishlist-history cleanup.

Do NOT absorb Task #77’s broader FK cleanup scope.

==================================================
RESORT LIFECYCLE
==================================================

INACTIVATION

- retain Resort row
- retain wishlist events
- current-state reads may hide inactive resort
- emit no removed event

TRUE HARD DELETE

- cascade wishlist_resort_event rows for that resort
- do not fabricate removals

RESORT MERGE

- maintenance normalization only
- no user transition events

==================================================
LEGACY NUMERIC-STRING DELETION SAFETY FIX
==================================================

The investigation found a directly related defect:

Resort hard-delete reference checks currently use raw integer membership and can miss legacy canonical numeric-string wishlist IDs.

Example conceptual issue:

wishlist storage may contain:
"123"

while deletion guard checks only:
123

Canonical wishlist logic treats these as the same resort reference.

Fix this narrowly.

Requirements:

- resort hard-delete reference detection must canonicalize wishlist IDs using the same canonical semantics as the wishlist service
- canonical numeric strings must count as references
- malformed/noncanonical junk must not create false references
- do not broaden into unrelated resort cleanup
- add focused regression tests proving a resort cannot be hard-deleted while referenced through a canonical numeric-string wishlist ID

This deletion-safety fix is IN SCOPE because it directly protects wishlist subject/history integrity.

==================================================
BACKFILL
==================================================

NO synthetic backfill.

Do not create events for current wishlist membership.

Existing data cannot prove:
- add timestamp
- source
- prior removal/re-add cycles

After migration:
wishlist_resort_event must be empty.

Pre-cutover history remains unknown.

==================================================
CONSUMER ISOLATION
==================================================

Current-state consumers must continue using User.wish_list_resorts only.

Audit and preserve behavior for:

- Profile
- Wishlist settings display
- Friend wishlist
- Friend profile overlap
- Mountain pages
- Home
- Ideas
- Matching
- Recommendations
- Social visibility
- Notifications
- Existing analytics
- Admin reports

Do not make any consumer read wishlist history.

No public/admin history route or UI.

No conversion analysis in this task.

==================================================
TESTS
==================================================

Add focused tests covering:

EVENT SEMANTICS
- add -> one added event
- remove -> one removed event
- duplicate add -> zero
- absent remove -> zero
- added -> removed -> added -> three ordered events

REPLACEMENT
- deterministic mixed diff
- exact event count
- removal order = prior canonical order
- addition order = requested canonical order
- reorder-only -> zero
- identical replacement -> zero
- normalization-only -> zero

VALIDATION
- invalid resort
- inactive resort behavior
- region behavior
- nonexistent resort
- 15-item limit

ATOMICITY
- current wishlist + event insert commit together
- forced rollback leaves both unchanged

PRIVACY
- subject user deletion erases history
- actor-only deletion anonymizes where applicable
- privacy deletion creates no removed event

RESORT LIFECYCLE
- inactivation preserves history
- hard deletion cascades history
- numeric-string wishlist reference blocks unsafe hard deletion
- resort merge normalization creates no history

MIGRATION
- correct graph
- one head
- explicit upgrade to BL-87 excludes BL-70
- no synthetic backfill
- empty downgrade succeeds
- non-empty downgrade refuses destructive loss

POSTGRESQL CONCURRENCY
- add/add
- add/remove
- replace/replace
- replace/single

CONSUMER ISOLATION
- Ideas/matching/current-state consumers do not consult history
- current wishlist behavior unchanged

Run strong adjacent wishlist, privacy, Ideas/Home, resort lifecycle, and migration regressions.

Run the full suite because BL-87 touches:
- persisted user data
- central wishlist mutations
- privacy deletion
- resort deletion
- migration ancestry

Known pre-existing SQLite migration-guard incompatibility may remain if unchanged and clearly unrelated.

Also run:
- py_compile
- git diff --check

==================================================
DATABASE / ROLLOUT SAFETY
==================================================

Do NOT apply BL-87 migration to:

- persistent Replit DB
- Supabase Development
- Production

during implementation.

Read-only migration-version checks for persistent Replit and explicitly authorized Supabase Development are allowed ONLY for ancestry preflight.

Do not access Production.

Do not run unrestricted upgrade head.

BL-87 implementation and rollout remain separate.

==================================================
ROLLOUT RUNBOOK
==================================================

Document future rollout gates for Development and later Production.

Development future rollout should include:

- authorized target identity
- current revision = bl80_trip_lifecycle
- BL-70 absence
- privacy-safe wishlist legacy data diagnostics
- fresh verified backup
- explicit upgrade to bl87_wishlist_history
- unchanged User/current wishlist data
- empty event table immediately after migration
- BL-70 still absent
- authenticated add/remove/settings smoke tests

Production:
- separate approval
- separate read-only preflight
- separate verified backup
- explicit target revision
- no synthetic events
- count preservation
- rollback decision

Do not perform rollout in this BUILD.

==================================================
ARCHITECTURAL REVIEW
==================================================

Before commit, explicitly review:

- current wishlist remains operational truth
- no history consumer leaked into Ideas/matching/auth
- no fabricated events from cleanup/reorder/admin merge
- no duplicate events from retries
- repeated lifecycles remain valid
- privacy deletion removes identifying history
- resort hard-delete guard handles numeric-string legacy IDs
- BL-70 ancestry is correct
- no Production access
- no migration applied to persistent environments

If a material Critical/High defect remains:

STOP and report it.

Do not commit unsafe partial implementation.

==================================================
COMMIT
==================================================

If validation passes, commit with:

BL-87 Preserve wishlist transition history

Exclude unrelated pasted prompt assets.

RETURN

A. migration preflight results
B. exact model/schema changes
C. wishlist service behavior
D. mutation path integration
E. event semantics
F. bulk/order/no-op behavior
G. concurrency behavior
H. privacy/account deletion behavior
I. resort lifecycle behavior
J. numeric-string deletion-guard fix
K. no-backfill confirmation
L. migration graph and sole head
M. focused/adjacent/full test results
N. disposable PostgreSQL concurrency results
O. architecture review result
P. py_compile / git diff --check
Q. commit hash
R. unrelated findings
S. explicit statement whether BL-87 is code-side complete and migration-safe to close

Do not begin another task.
Do not roll out BL-87 to Development or Production.