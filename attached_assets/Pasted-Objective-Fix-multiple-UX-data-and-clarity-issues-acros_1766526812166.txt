Objective
Fix multiple UX, data, and clarity issues across Trips, Friends, Profile, and Typography. Do not introduce new features. Do not change business logic beyond what is explicitly stated. Preserve existing data. Use checkpoints only if absolutely required.
1. My Trips — Tile Card Invite Summary
Requirement
On the My Trips page, each trip tile must display a short summary of invite status:
Number of friends invited
Number of friends accepted
Rules
Show this summary directly on the tile card (no click required)
Example format:
3 invited · 1 accepted
Do not show declined or pending breakdowns here
Full invitee list and statuses should remain inside the trip detail page only
Implementation notes
Use existing invite / guest / participant data
Do not duplicate queries unnecessarily
This is a display-only change on the tile
2. Trip Disappearing After Invite Acceptance (BUG FIX)
Bug
After a friend accepts a trip invite, the trip sometimes disappears after navigation.
Fix
Ensure that once a user accepts a trip invite:
The trip is persistently associated with that user
The trip never disappears unless explicitly deleted or left
Acceptance must:
Add the user as a trip participant
Not rely on transient invite state
Audit any filters that remove trips based on:
Invite state
Ownership vs participation
Upcoming / past logic
This is a data + query correctness fix.
3. Global Typography Increase
Requirement
Increase font sizes across the entire app to improve readability.
Rules
Do this at the CSS system level, not per-page hacks
Increase:
Base body font size
Secondary / metadata text
Tile card labels
Maintain visual hierarchy (headers > body > metadata)
Slightly increase line-height where appropriate
Do not break layouts or cause overflow
4. Terrain vs “How I Like to Ride” — Single Source of Truth
Problem
Onboarding uses “How I Like to Ride”
Profile uses “Terrain”
These must represent the same underlying data.
Fix
Map both onboarding and profile UI to the same user field
Onboarding selection must:
Persist to the user profile
Display correctly on the profile page
Editing terrain on the profile must:
Update the same field used by onboarding
Remove any duplicated or shadow fields
Result
One preference. Multiple surfaces. No divergence.
5. “Add Equipment” / “Add Boots” Not Clickable (BUG FIX)
Bug
On the Trip page, “Add Equipment” and “Add Boots” appear interactive but are not clickable.
Fix
These must be fully clickable CTAs
Clicking either must deep-link the user to:
Profile → Equipment section
Auto-scroll and/or highlight the relevant subsection if possible
Do not create a new modal
Do not duplicate equipment editing logic
Rule
If “Add X” is visible, it must be actionable.
6. Friends Screen — Clarify Trip Invites Toggle
Problem
On the Friends list:
“Trip Invites” label is left-aligned
Toggle is right-aligned
It’s unclear what the toggle controls
Fix
Replace the ambiguous label + toggle with sentence-style clarity.
Required copy pattern
Can invite me to trips        [ ON | OFF ]
Rules
The toggle must clearly apply to this specific friend
Keep text and toggle visually connected on one row
Do not rely on alignment alone to convey meaning
If there is a section header, it should be contextual (e.g., “Trip Invites”), not the control label itself
No behavior change — this is a clarity and copy fix only.
Constraints
Do not add new database tables unless required to fix the disappearing-trip bug
Do not rename routes unnecessarily
Do not change permissions or invite logic beyond what’s described
Test:
Trip creation
Invite → accept flow
Navigation persistence
Equipment CTA navigation
Friends toggle behavior
Mobile layout after font-size increase
Definition of Done
Trip tiles show invite + acceptance counts
Accepted trips never disappear
Fonts are visibly larger everywhere
Terrain preferences are consistent across onboarding + profile
“Add Equipment” / “Add Boots” work
Friends screen toggle meaning is immediately obvious