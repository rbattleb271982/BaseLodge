Investigate the leftover development-only PostgreSQL enum activitytype that is blocking the #311 bootstrap.
This is an investigation only. Do not drop, alter, rename, recreate, or otherwise modify the enum yet.
Do not restart Flask.
Do not touch production.
Current verified state
The real Replit development database is:
postgresql://helium:5432/heliumdb
It currently has:
0 public tables
no alembic_version
one BaseLodge-specific public enum:
activitytype
Values:
trip_created
trip_updated
trip_overlap
connection_accepted
trip_invite_received
trip_invite_accepted
trip_invite_declined
friend_joined_trip
carpool_offered
join_request_received
join_request_accepted
join_request_declined
The #311 bootstrap correctly refused to run because the target was not completely empty.
Goal
Determine exactly where activitytype came from and whether it is safe to remove from this development-only empty database so the validated bootstrap can proceed.
Hard safety constraints
During this investigation:
no production queries that mutate anything
no production schema changes
no development DB writes
do not drop the enum
do not alter the enum
do not run the bootstrap
do not stamp Alembic
do not run migrations
do not start Flask
do not import app.py
do not touch push/OneSignal
do not resume BL-77
Read-only catalog queries against the verified development database are allowed.
Investigate
Confirm activitytype exists only in the development database being inspected.
Search the repository for:
activitytype
ActivityType
PostgreSQL enum creation for these values
Identify which historical migration, startup routine, model declaration, or prior development bootstrap created it.
Determine whether activitytype is part of the approved BL-60 bootstrap contract.
Determine whether any current table, view, function, sequence, domain, composite type, or other database object depends on it.
Confirm again that the development database contains no application tables or rows whose interpretation depends on this enum.
Determine whether dropping only this enum would restore the database to the exact empty state required by scripts/bootstrap_bl60.py.
Check whether any other user-created objects exist that the earlier preflight may not have surfaced:
enum types
domains
functions
sequences
views
materialized views
tables
extensions created specifically by BaseLodge
Determine whether the enum may have been left behind by the failed historical Alembic bootstrap attempt.
Confirm whether PostgreSQL transactional rollback could plausibly have left the enum behind, or whether it must predate that failed attempt.
Safety determination
I want an explicit answer to:
Can we safely drop only public.activitytype from this verified development database and then rerun the #311 bootstrap preflight?
To answer Yes, prove all of the following:
database is development, not production
no table or other object depends on the enum
enum is not part of the approved BL-60 bootstrap contract
no application data would be lost
no other BaseLodge schema object remains
removal would return the DB to the expected empty-bootstrap state
Return
Provenance
Explain where activitytype came from.
Dependency check
List every object depending on it, if any.
Development DB inventory
List any other user-created objects still present.
BL-60 compatibility
State whether activitytype belongs in the approved #311 BL-60 contract.
Cleanup recommendation
End with exactly one:
SAFE TO DROP DEVELOPMENT-ONLY public.activitytype AND RETRY BOOTSTRAP
or
DO NOT DROP activitytype: [reason]
Do not perform the cleanup during this investigation.