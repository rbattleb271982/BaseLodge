╔════════════════════════════════════════════════════╗
║ BaseLodge Trip Detail Edit Rows Error Fix         ║
╚════════════════════════════════════════════════════╝

Problem:
On a planned trip detail screen, tapping the editable summary rows causes an error on the next page.

The rows shown include:
- Location: “CA, United States”
- Pass: “Ikon”
- Trip length: “2 nights”
- Status: “Planning”
- Visibility: “Public”

Goal:
Fix the destination pages/routes for these trip edit rows so every tappable row opens a valid edit screen or valid existing flow without throwing an error.

Important:
Do not redesign the trip detail page.
Do not change the visual styling of the rows.
Do not change the trip model unless absolutely necessary.
Do not remove the rows unless a row is intentionally display-only.
Do not introduce a new trip planning flow.

Tasks:

1. Locate the template/rendered page that displays these trip detail summary rows.

2. For each clickable row, inspect the href/url_for target:
   - Location row
   - Pass row
   - Trip length / dates row
   - Status row
   - Visibility row

3. Identify which target route is failing and why:
   - missing route
   - wrong endpoint name
   - missing trip_id parameter
   - wrong parameter name
   - route exists but template missing
   - route exists but query fails
   - authorization/login guard issue
   - POST/GET mismatch

4. Fix each broken row so tapping it works.

Expected behavior:
- Location row should open a valid edit location/destination screen or existing trip edit flow.
- Pass row should open a valid edit pass screen or existing trip edit flow.
- Trip length row should open a valid edit dates/nights screen or existing trip edit flow.
- Status row should open a valid edit status screen or existing trip edit flow.
- Visibility row should open a valid edit visibility/privacy screen or existing trip edit flow.

If the app already has one general trip edit page, route all rows safely to that page with an anchor/query parameter if needed.

If a specific edit page does not exist yet, create the smallest safe GET page needed to avoid errors, using the existing BaseLodge styling system.

5. Add defensive handling:
   - If trip_id is missing or invalid, return a safe redirect or 404.
   - If the trip does not belong to the logged-in user, do not allow editing.
   - Do not expose another user’s trip edit screens.

6. Run checks:
   - Start the app successfully.
   - Confirm Python syntax passes.
   - Confirm tapping each of the five rows no longer errors.
   - Confirm each destination page loads for the trip owner.
   - Confirm non-owners cannot edit the trip.
   - Confirm no bottom nav or trip detail layout regressions.

Files likely involved:
- templates/trip_detail.html
- routes/trips.py or the trip-related route file
- templates/trip_edit*.html if they exist
- static/css/styles.css only if an existing edit page needs minor styling reuse

Do not use checkpoints unless necessary.

At the end, report:
- Which rows were broken
- What route each row now points to
- What files changed
- How you tested it