1️⃣ Tab Labels
Keep the labels exactly as they are.
Use:
My Trips
Friends’ Trips
Trip Overlaps
Open Avail.
Why
You’ve already solved mobile fit with pill styling + scroll
“Trip Overlaps” is explicit and reduces cognitive load
“Overlaps” alone becomes ambiguous out of context
“Open Avail.” is already abbreviated intentionally and consistently
Answer to Replit:
Keep existing tab labels unchanged.
2️⃣ Create a Trip CTA (when user has 1+ trips)
Choose: Outline style (transparent background, red border, red text).
Why
Preserves brand consistency
Clearly secondary to trip cards
Still discoverable and tappable
Matches the new outlined-pill language you’re introducing elsewhere
The lighter background + white text still reads as primary and competes visually.
Answer to Replit:
Use outline style for the de-emphasized CTA once the user has one or more trips.
3️⃣ Trip Card Pass Display (“Other”)
Keep “Other”. Do not rename it. Do not omit it.
Why
“Independent” is interpretive and potentially inaccurate
Some users genuinely don’t want to specify or don’t fit a label
Omitting the pass creates inconsistency across trip cards
“Other” is neutral, honest, and future-safe
You can always improve labeling later once pass taxonomy expands.
Answer to Replit:
Keep displaying “Other” as-is on trip cards.
4️⃣ Open Avail. Tab CTA (Empty State)
Style “Add Open Dates” as the primary CTA when the tab is empty.
Why
There is exactly one meaningful action in an empty Open Avail. state
Users won’t intuitively know they need to add dates otherwise
This mirrors “Create a trip” being primary when My Trips is empty
Once dates exist, it can return to a more subtle inline action.
Answer to Replit:
When Open Avail. is empty, style “Add Open Dates” as the primary CTA. When not empty, keep it subtle.
5️⃣ Trip Overlaps Explanation (most important)
Choose: Option A — supplement with a subtitle.
Final structure (LOCKED)
Keep the current primary line:
“You and Sarah overlap at Vail · Jan 15–17”
Then add a secondary explanation line below, muted:
“Same resort, overlapping dates”
Why this is the right call
Preserves the human-readable summary you already have
Adds clarity without rewriting everything
Keeps hierarchy clean:
Line 1 = what’s happening
Line 2 = why it’s happening
Scales cleanly to other cases:
“Same dates, different resorts”
“Partial date overlap at the same resort”
Option B overloads a single line and hurts scanability.
Answer to Replit:
Supplement the existing overlap sentence with a secondary explanation line (Option A), not a rewrite.
TL;DR — Copy/Paste Answers for Replit
You can literally send this:
• Keep tab labels exactly as they are.
• For Create a Trip when 1+ trips exist, use an outline style CTA.
• Keep “Other” as the pass label on trip cards.
• In Open Avail., style “Add Open Dates” as the primary CTA when empty.
• For Trip Overlaps, keep the current sentence and add a muted subtitle explaining the reason (Option A).