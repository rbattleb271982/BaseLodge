Update the Ideas page to match the Trips page structure and prioritize date management over inviting friends.

This is a FRONTEND-ONLY update (HTML + CSS + minor Jinja logic).
Do not change backend logic or routes.

Files:
- trip_ideas.html
- relevant CSS only

----------------------------------------
GLOBAL GOAL
----------------------------------------

Make the Ideas page feel like the same system as Trips:
- Page title on the left
- Action pill on the right
- State-driven content below
- Primary CTA focused on dates
- Invite friends as a persistent top action

No extra cards. No visual clutter.

----------------------------------------
1. HEADER STRUCTURE
----------------------------------------

Replace the current Ideas header with a Trips-style header:

LEFT:
- Page title: "Ideas"
- Must match the Trips title exactly:
  - same font family
  - same font size
  - same font weight
  - same color
  - same spacing
  - same vertical alignment

RIGHT:
- Pill button: "Invite friends"

STYLE:
- Match "Season Snapshot" pill exactly:
  - outlined burgundy
  - transparent/cream fill (NOT filled)
  - burgundy text
  - same border thickness
  - same border radius
  - same padding
  - same font size/weight
  - same vertical alignment

ACTION:
- Link to existing invite friends route

----------------------------------------
2. REMOVE OLD HEADER PATTERN
----------------------------------------

Remove:
- all-caps "IDEAS" eyebrow
- any duplicate or stacked header styles
- any spacing that pushes content down too far

----------------------------------------
3. STATE HEADLINE (REENGAGEMENT)
----------------------------------------

Below the header, render:

"Nothing lined up — yet."

STYLE:
- smaller than page title
- same left margin as header
- same ink color used across app
- tighter spacing (reduce gap from header)

----------------------------------------
4. STATE SUBTEXT
----------------------------------------

Replace current copy with:

"Your dates are the engine. Keep them current, and we’ll surface trips that line up."

STYLE:
- use muted text color (same as Trips subtext)
- slightly smaller than headline
- comfortable line height
- same left alignment

----------------------------------------
5. PRIMARY CTA (DATES)
----------------------------------------

Below subtext, add ONE primary button:

TEXT:
- If user has availability → "Edit dates"
- If user has no availability → "Add dates"

STYLE:
- match Trips "Plan a trip" button exactly:
  - burgundy fill
  - white text
  - same width
  - same height/padding
  - same border radius
  - same font size/weight
  - centered

ACTION:
- link to existing availability route

----------------------------------------
6. REMOVE SPEED IT UP CARD
----------------------------------------

Delete entirely:
- white background card
- border
- "SPEED IT UP" label
- invite explanation copy
- large Invite friends button

----------------------------------------
7. DO NOT ADD SECONDARY CTA
----------------------------------------

Do NOT add:
- "Invite friends →" under CTA
- any additional buttons

Invite friends is now ONLY in the header pill.

----------------------------------------
8. PRESERVE EXISTING FUNCTIONALITY
----------------------------------------

Do NOT change:
- populated state layout
- hero match card
- destination rows
- plan trip links
- availability logic
- fallback logic

Only adjust:
- header
- reengagement state layout
- CTA hierarchy

----------------------------------------
9. SPACING + ALIGNMENT
----------------------------------------

Ensure:
- consistent left/right padding with Trips page
- vertical spacing mirrors Trips structure
- no large empty gaps
- no extra containers or cards

----------------------------------------
FINAL RESULT
----------------------------------------

The Ideas page should now feel like:

Trips page structure +
Ideas intelligence

Hierarchy:

1. Page title (Ideas)
2. Header action (Invite friends)
3. State headline
4. Subtext
5. Primary CTA (dates)
6. Content below (existing logic)

Only use checkpoints if I explicitly say so.