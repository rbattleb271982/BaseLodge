Proceed with implementation.

Goal:
Match the attached approved mockup closely and fully remove the remaining old trip-detail structure.

The current implementation partially landed but still retains remnants of the old metadata-heavy layout. The attached mockup is the visual source of truth.

Requirements:

1. Top navigation
- Keep:
  - back chevron left
  - centered mountain name
  - Edit on right
- Preserve existing functionality.

2. Success modal
- Keep the centered success modal:
  - “Trip planned.”
  - “Your [Mountain] weekend is on the calendar.”
  - X dismiss
- No top banner/flash message.
- Keep the current dismiss behavior.
- Slightly reduce modal visual heaviness if easy:
  - slightly tighter vertical padding
  - slightly narrower visual footprint
- Keep it lightweight and Apple-like.

3. Hero section
Update the hero to match the mockup:

Keep:
- large linked mountain name → mountain detail page
- date line
- social proof pill (only when count > 0)
- metadata badge row

Remove:
- the separate PLANNING eyebrow/pill above the mountain name
- location/state line
- “N going” count line

Planning should appear ONLY once in the metadata badge row.

Date line should render:
“May 28–30, 2026 · 2 nights”

Night count should be computed inline using:
(trip.end_date - trip.start_date).days

Metadata badge row:
- “Visible to friends” or “Private”
- “Planning” or “Going”

Use lightweight inline SVG icons similar to the mockup.

4. Remove old metadata block entirely

Completely remove the old:
- Location
- Pass
- Trip Length
- Status
- Visibility

info-row block from the visible page.

This old settings-style structure should no longer render anywhere near the top of the page.

Also remove dead CSS related only to that removed block:
- .td-info-row
- .td-info-label
- .td-info-value
- .td-info-value-group
- .td-info-row--tappable

Retain:
- .td-info-chevron
- accordion styles
- modal styles
- person-row styles
- sticky-action styles

5. Trip setup section

Replace the current all-caps section-label styling.

Use:
Heading:
“Trip setup”

Subtitle:
“Let everyone know the plan.”

Important:
Do NOT introduce a serif font unless absolutely necessary to match the mockup.
Prefer the existing BaseLodge system/body heading style for consistency.

Inside the setup card/module:
- Pass
- Equipment
- Transportation
- Accommodations
- Lessons

Pass should be the FIRST row.

Pass row behavior:
- tappable link to edit trip form
- display pass brands using:
  trip.resort|mountain_passes
- fallback:
  “No pass”

Pass row should visually match the setup rows but should NOT behave like an accordion toggle.

Preserve ALL existing:
- expand/edit/save behaviors
- APIs
- dropdown interactions

for Equipment / Transportation / Accommodations / Lessons.

6. What’s next section

Replace the current next-section styling with the mockup structure.

Heading:
“What’s next?”

Subtitle:
“Keep the stoke going.”

Only ONE card row:
Title:
“See who’s going”

Subtitle:
“See friends heading to [Mountain].”

Route:
mountain detail page

Remove any:
- Invite friends
- Share trip
- extra CTA cards

Keep:
Who’s Going and Pending Invites sections BELOW this area if data exists.

7. Visual direction

Very important:
- no images
- no destination photography
- no travel-app aesthetic
- no booking-engine feel

Target:
- editorial
- lightweight
- Apple-like
- calm
- social trip hub

The page should feel like:
“your ski weekend”
NOT:
“trip settings.”

8. Additional guardrails

Make sure:
- no calendar renders anywhere on trip_detail.html
- attached mockup is followed closely
- spacing is tighter and more intentional than the current implementation
- hero area compresses upward slightly once the PLANNING eyebrow is removed

9. Relevant files

templates/trip_detail.html
app.py mountain_passes filter
app.py format_trip_dates helper

10. QA

Verify:
- no old metadata row block remains
- no calendar appears
- no PLANNING eyebrow appears
- Planning appears only once
- mountain name is linked
- Pass appears only inside Trip setup
- What’s next contains only the single See who’s going card
- success modal still works
- no images added
- existing accordion/edit behavior preserved

Use checkpoints sparingly.