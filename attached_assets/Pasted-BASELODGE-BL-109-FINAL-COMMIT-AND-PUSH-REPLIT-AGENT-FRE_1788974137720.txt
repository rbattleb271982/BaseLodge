BASELODGE — BL-109 FINAL COMMIT AND PUSH

REPLIT AGENT
FREE — BUILD

TASK PROGRESS
BL-109: ~90% → 100%

OBJECTIVE

Commit and push ONLY the approved BL-109 implementation files.

CRITICAL EXECUTION RULE

ONLY DO EXACTLY WHAT THIS PROMPT AUTHORIZES.

Do not modify code.
Do not modify tests.
Do not reformat unrelated files.
Do not create notes.
Do not create documentation.
Do not create attachments.
Do not modify `.replit`.
Do not stage `.replit`.
Do not stage any `attached_assets/Pasted-*` files.
Do not deploy.
Do not access Production.
Do not access any database.
Do not perform unrelated cleanup.

Never use:

git add .
git add -A

NEVER inspect, read, modify, copy, stage, or delete:

android/baselodge-release-key.jks


==================================================
EXPECTED APPROVED BL-109 FILES
==================================================

The only legitimate BL-109 files expected to be committed are:

- app.py
- services/happening.py
- templates/partials/home/_section_happening.html
- tests/test_home_empty_states.py
- tests/test_bl109_suggested_connection_confirmations.py

Known unrelated local files that MUST remain outside the commit:

- .replit
- attached_assets/Pasted-*

Do not stage or commit anything else.


==================================================
STEP 1 — VERIFY BEFORE STAGING
==================================================

Report:

1. current branch
2. local HEAD
3. origin/main HEAD
4. ahead/behind
5. git status --short

Expected baseline before commit:

local HEAD:
160d03e0eeb4257c3c975680731590cd13033254

origin/main:
160d03e0eeb4257c3c975680731590cd13033254

ahead:
0

behind:
0

If there are additional legitimate modified files beyond the five approved BL-109 paths listed above:

STOP and report them.

If there are only additional `.replit` or `attached_assets/Pasted-*` environmental-noise files, ignore them and continue.

Do NOT stop merely because those known noise files exist.


==================================================
STEP 2 — REVIEW APPROVED DIFF ONLY
==================================================

Inspect the diff only for the five approved BL-109 files.

Confirm:

- no schema changes
- no migration changes
- no dependency changes
- no native/iOS/Android changes
- no Production config changes
- no secrets/signing assets
- no unrelated feature work

Also confirm the approved copy remains:

"[First name] and [First name] connected"

"You suggested they knew each other."

Do not change anything during this review.

If the diff contains unrelated application work, STOP and report it.


==================================================
STEP 3 — STAGE ONLY APPROVED FILES
==================================================

Stage ONLY these exact paths:

app.py
services/happening.py
templates/partials/home/_section_happening.html
tests/test_home_empty_states.py
tests/test_bl109_suggested_connection_confirmations.py

Do not stage `.replit`.

Do not stage any `attached_assets/Pasted-*` file.

After staging, report:

git status --short

Then confirm that the staged set contains exactly the five approved BL-109 files and nothing else.

If anything else is staged:

STOP.
Do not commit.
Report the unexpected staged path.


==================================================
STEP 4 — COMMIT
==================================================

Create one commit with this exact commit message:

Close suggested connection loop

Do not amend any existing commit.

After commit, report:

- new commit hash
- commit subject
- files included in the commit


==================================================
STEP 5 — PUSH
==================================================

Push `main` normally to `origin/main`.

Do not force push.

Do not deploy.


==================================================
STEP 6 — VERIFY FINAL SYNC
==================================================

Report:

1. local HEAD
2. origin/main HEAD
3. ahead count
4. behind count
5. git status --short
6. commit hash
7. commit subject
8. exact files committed
9. confirmation `.replit` remains outside the commit
10. confirmation `attached_assets/Pasted-*` remain outside the commit
11. confirmation no deployment occurred
12. confirmation no Production/database action occurred


==================================================
TARGET FINAL STATE
==================================================

- local main = origin/main
- ahead = 0
- behind = 0
- BL-109 commit pushed
- only the five approved BL-109 files in the commit
- `.replit` remains local/uncommitted
- `attached_assets/Pasted-*` remain local/untracked
- no deployment
- no Production action


STOP after verification.