REPLIT: MAX + BUILD
Estimated Batch completion after this step: ~60%

TASK: BL-156 — Bound Home candidate retrieval

Task #406 investigation is approved.

Proceed directly to implementation.
Do not create another plan.

==================================================
PRIMARY GOAL
==================================================

Fix the confirmed Home Next Trip over-retrieval.

Current behavior materializes every eligible Next Trip candidate to display one:

10 eligible → 10 candidates → 1 displayed
50 eligible → 50 candidates → 1 displayed
100 eligible → 100 candidates → 1 displayed
500 eligible → 500 candidates → 1 displayed

Replace this with an exact bounded winner selection that retrieves at most:

- one earliest qualifying owned-trip candidate
- one earliest qualifying active-guest candidate
- one selected participant context

Then compare the two candidates using the existing effective-attendance ordering.

Visible Home behavior must remain identical.

==================================================
1. PRESERVE EXACT WINNER SEMANTICS
==================================================

The current Next Trip winner is the earliest effective attendance start across eligible owned and guest trips.

Preserve exactly:

OWNED TRIPS
- effective start = trip start date
- effective end = trip end date

GOING GUEST
- use participant attendance dates only when BOTH override dates exist
- otherwise use trip dates

INTERESTED GUEST
- use trip dates

Preserve:

- null-date behavior
- effective-end filtering
- active lifecycle requirements
- active RSVP requirements
- private-trip participant access
- owner/guest tie behavior
- existing stable ordering

Equal effective dates must produce the same winner as before.

==================================================
2. CANONICAL ATTENDANCE EXPRESSIONS
==================================================

Do not invent a second definition of effective attendance.

Reuse or narrowly codify SQL expressions matching the canonical attendance semantics in:

services/trip_attendance.py

Ensure SQL behavior matches the existing Python behavior.

Cover:

- Going with complete override
- Going with incomplete override
- Interested
- null dates
- effective end
- enum/status behavior

==================================================
3. BOUNDED OWNED CANDIDATE
==================================================

Retrieve only the earliest qualifying owned trip.

Requirements:

- current viewer is organizer/owner
- active lifecycle
- existing future eligibility
- same date/null semantics
- Resort data available as currently required
- deterministic ordering
- LIMIT 1

Do not load the viewer's full owned-trip collection.

==================================================
4. BOUNDED GUEST CANDIDATE
==================================================

Retrieve only the earliest qualifying non-owner active participation.

Requirements:

- participant belongs to current viewer
- Going or Interested only
- active trip lifecycle
- effective attendance remains current/future under existing rules
- private trips remain eligible because viewer is an active participant
- Resort context preserved
- deterministic effective-start ordering
- LIMIT 1

Filter in SQL before limiting.

Do not retrieve all participant rows and sort them in Python.

==================================================
5. TWO-CANDIDATE COMPARISON
==================================================

After retrieving the two bounded candidates:

- earliest owned candidate
- earliest guest candidate

compare only those two using the existing winner semantics.

The winner must be identical to the current all-candidate implementation.

Preserve owned-first behavior where the current stable ordering resolves an exact tie that way.

Do not introduce a new ranking system.

==================================================
6. PARTICIPANT CONTEXT
==================================================

Carry forward only the participant context needed for the selected guest candidate.

Preserve all existing Next Trip:

- actions
- RSVP state
- attendance dates
- links
- presentation logic

Do not change the Next Trip card.

==================================================
7. REMOVE UNBOUNDED MATERIALIZATION
==================================================

Remove the Home Next Trip path's need to materialize:

- all qualifying owned trips
- all active participant rows
- all qualifying guest trips
- the merged all-candidate list
- the full Python candidate sort

Do not remove data that other Home sections independently require.

Only remove/restructure retrieval that exists for Next Trip winner selection.

==================================================
8. CARDINALITY BUDGET
==================================================

Add deterministic tests for:

- 10 eligible trips
- 50 eligible trips
- 100 eligible trips
- 500 eligible trips

Regardless of source population, Next Trip must retrieve/materialize at most:

- 2 Trip ORM candidate objects
- 1 selected participant context
- 2 candidate retrieval statements

Final displayed Next Trip:

<= 1

Use ORM/SQL instrumentation with clean sessions so identity-map reuse cannot hide excess loading.

==================================================
9. REFERENCE EQUIVALENCE
==================================================

In tests, retain/build a reference selector representing the previous all-candidate behavior.

Compare bounded selection against the reference across:

- owned only
- guest only
- mixed owned + guest
- equal dates
- null dates
- Going complete overrides
- Going incomplete overrides
- Interested
- private guest trip
- past-effective guest attendance
- terminal trip
- inactive RSVP
- multiple owned trips
- multiple guest trips
- mixed large fixtures

The bounded helper and reference selector must choose the same visible trip and attendance context.

==================================================
10. HOME OUTPUT PARITY
==================================================

Verify equivalent Home fixtures produce the same visible:

- Next Trip
- Next Trip actions
- Happening
- Ideas
- Requests
- connection request
- connection toast
- availability summary
- supporting context

No layout or UI changes.

==================================================
11. HAPPENING
==================================================

Do NOT change Happening retrieval logic.

It already:

- uses one retrieval statement
- returns at most five final candidates
- applies correctness-sensitive ranking before LIMIT

Add/retain regression coverage showing:

10 / 50 / 100 / 500 source occurrences

still produce:

- one retrieval statement
- <= 5 returned candidates

Do not add arbitrary earlier limits.

==================================================
12. IDEAS
==================================================

Do NOT change Ideas retrieval logic.

It already:

- uses one retrieval statement
- returns at most five final candidates
- performs aggregation/deduplication/dismissal/booking suppression/social eligibility before final ranking

Add/retain regression coverage showing:

10 / 50 / 100 / 500 source concepts

still produce:

- one retrieval statement
- <= 5 returned candidates

Do not pre-limit individual Idea sources.

==================================================
13. PRIVACY / AUTHORIZATION
==================================================

BL-133 remains a hard boundary.

The guest candidate query may retrieve only the current viewer's active Going/Interested participant records.

Preserve:

- private-trip participant access
- public/private behavior
- reciprocal-current friendship semantics elsewhere on Home
- raw availability privacy
- wishlist privacy
- history never authorizes

Do not broaden retrieval merely to simplify winner selection.

==================================================
14. LIFECYCLE SAFETY
==================================================

BL-80 remains authoritative.

Ensure:

- completed trips do not become active Next Trip candidates
- cancelled trips do not become active Next Trip candidates
- terminal history never authorizes
- past-effective attendance is excluded exactly as before

==================================================
15. WISHLIST / AVAILABILITY SAFETY
==================================================

Do not modify Ideas wishlist or availability retrieval.

BL-87 current-state semantics remain unchanged.

History does not participate in eligibility.

Raw availability remains private.

==================================================
16. HOME QUERY BUDGET
==================================================

BL-161 established the equivalent warmed Home ceiling:

<= 25 SQL statements

The optimized Home route must remain at or below this ceiling.

Do not increase query count materially merely to reduce ORM materialization.

If the exact bounded implementation requires one additional stable query but still remains <=25, that is acceptable only if candidate cardinality and correctness goals are met.

==================================================
17. CROSS-DIALECT SAFETY
==================================================

The effective attendance SQL must behave correctly on the supported database paths.

Specifically verify:

- SQLite test behavior
- PostgreSQL-compatible SQL compilation/runtime where existing disposable PostgreSQL test infrastructure permits

Pay attention to:

- CASE expressions
- dates
- enum/status comparison
- NULL ordering
- deterministic tie ordering

Do not access persistent databases to test this.

==================================================
18. OUT OF SCOPE
==================================================

Do not implement:

- BL-158 My Trips pagination
- BL-159 Friends pagination
- Home redesign
- Happening ranking changes
- Ideas ranking changes
- arbitrary source pre-limits
- request/toast behavior changes
- global caching
- background precomputation
- denormalization
- generalized performance framework
- schema/index changes

==================================================
19. DATABASE SAFETY
==================================================

Expected:

NO schema change
NO migration
NO index change

Do not:

- create Alembic revisions
- apply BL-70
- mutate Supabase Development
- mutate persistent Replit
- access Production

If schema/index/migration work appears necessary:

STOP and report it.

==================================================
20. VALIDATION
==================================================

Run focused BL-156 tests covering:

- exact winner equivalence
- owned candidate selection
- guest candidate selection
- Going attendance overrides
- Interested behavior
- equal-date ties
- null dates
- private guest trips
- lifecycle exclusions
- RSVP exclusions
- 10/50/100/500 cardinality
- Home query budget
- Happening caps
- Ideas caps
- visible Home parity

Run adjacent regressions including:

- Home
- BL-80
- BL-87
- BL-133
- BL-161 query budgets
- trip attendance
- Happening
- Ideas

Then run the full suite.

Also run:

- py_compile
- git diff --check

==================================================
21. INDEPENDENT REVIEW
==================================================

Before commit perform an independent correctness/privacy/performance review.

Verify:

- bounded selector always matches previous winner semantics
- at most two candidate Trip objects are materialized
- at most one selected participant context is carried forward
- cardinality remains constant at 10/50/100/500
- effective attendance semantics are unchanged
- equal-date behavior is unchanged
- private guest access is unchanged
- BL-80 lifecycle behavior is unchanged
- BL-133 privacy is unchanged
- Happening unchanged
- Ideas unchanged
- Home visible output unchanged
- Home query count remains <=25
- no schema/index/migration
- no persistent database or Production access

If a material correctness/privacy regression remains:

STOP.

==================================================
22. COMMIT
==================================================

If implementation and validation pass:

Commit with:

BL-156 Bound Home Next Trip candidates

Exclude unrelated pasted prompt assets.

Do not begin BL-158, BL-159, or another backlog task.

==================================================
RETURN
==================================================

Return:

A. files changed
B. previous Next Trip retrieval behavior
C. new bounded retrieval behavior
D. effective-attendance implementation
E. owned candidate query
F. guest candidate query
G. two-candidate winner logic
H. equal-date compatibility
I. 10/50/100/500 candidate materialization results
J. Trip ORM load counts
K. participant-context load counts
L. candidate retrieval statement counts
M. reference-equivalence results
N. Home visible-output parity
O. Happening 10/50/100/500 results
P. Ideas 10/50/100/500 results
Q. warmed Home query count
R. SQLite/PostgreSQL compatibility verification
S. privacy/authorization verification
T. BL-80 lifecycle verification
U. BL-87/availability verification
V. focused test results
W. adjacent regression results
X. full-suite result
Y. py_compile / git diff --check / independent review
Z. commit hash and whether BL-156 is code-side complete and safe to close

Also explicitly confirm:

- no schema/index/migration
- BL-70 untouched
- persistent development databases untouched
- Production untouched