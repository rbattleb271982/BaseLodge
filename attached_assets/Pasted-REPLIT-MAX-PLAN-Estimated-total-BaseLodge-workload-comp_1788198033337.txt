REPLIT: MAX + PLAN
Estimated total BaseLodge workload completed after this step: ~38%

TASK: Resolve the Supabase Development backup gate so BL-80 migration can proceed

Current confirmed state:

- Saved Flask workflow starts successfully.
- Port 5000 issue is resolved.
- Workflow targets Supabase Development.
- Database connectivity works.
- Supabase Development is still at `bl52_trip_stay`.
- BL-78, BL-79, BL-80, and BL-70 schema are absent.
- Authenticated requests fail because:
  `column ski_trip.lifecycle_state does not exist`
- Production remains untouched.
- No migration has been applied to Supabase Development.

The ONLY blocker now is creating and verifying a safe pre-migration backup.

DO NOT migrate anything in this step.
DO NOT restart or modify the workflow.
DO NOT access Production.
DO NOT change application code or configuration.

Investigate the safest practical backup path.

1. POSTGRESQL 17 CLIENT

Supabase Development is PostgreSQL 17.6.

Determine the safest way to make available PostgreSQL 17-compatible:
- `pg_dump`
- `pg_restore`

Prefer a local client/tool install that does not alter application dependencies or runtime behavior.

Do not connect with it yet beyond harmless version verification.

2. SUPABASE-NATIVE BACKUP / RECOVERY

Inspect whether Supabase Development has an existing supported backup or restore capability that can satisfy this migration gate, such as:
- scheduled database backups
- downloadable backups
- point-in-time recovery
- another Supabase-supported recovery mechanism

Read-only inspection only.

Report:
- what exists
- latest available recovery point if visible
- whether it covers the current Development database
- whether restore/readability can be reasonably verified

3. PRIVATE STORAGE ALREADY AVAILABLE

Inspect currently available private storage destinations.

Do not create anything yet.

Check whether there is:
- an existing attached private App Storage bucket
- another private persistent storage location intended for backups

The previously referenced `baselodge-migration-backups` bucket currently does not resolve.

Do NOT create a replacement bucket unless later explicitly authorized.

4. LOCAL BACKUP OPTION

Assess whether a PostgreSQL 17 custom-format dump stored under:

`.local/backups/`

is sufficiently durable and private for this one Development migration.

Determine:
- whether `.local` survives normal Replit restarts/reloads
- whether it is excluded from Git
- whether a custom-format dump there could be verified with `pg_restore --list`
- whether that backup could actually be used to restore Supabase Development if the BL-80 migration failed

Do not create the dump in this PLAN.

5. RECOMMEND ONE BACKUP PATH

Choose the safest practical option in this order:

A. Supabase-native verified backup/recovery
B. PostgreSQL 17 custom-format dump in an already-existing durable private storage location
C. PostgreSQL 17 custom-format dump in `.local/backups/` if durability and restore viability are acceptable
D. Re-establish App Storage only if A-C are not safe

Do not turn this into a broad infrastructure project unless necessary.

RETURN

A. PostgreSQL 17 client solution
B. Supabase-native backup/recovery status
C. existing private storage options
D. `.local/backups/` durability assessment
E. recommended backup method
F. exact subsequent BUILD steps
G. whether BL-80 migration can safely resume after those steps

No mutation in this PLAN.