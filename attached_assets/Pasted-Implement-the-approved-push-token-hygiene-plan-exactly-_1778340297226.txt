Implement the approved push token hygiene plan exactly as specified below.
IMPORTANT:
Make surgical changes only
Do NOT refactor notification architecture
Do NOT modify MessageEventLog schema
Do NOT modify product push event wiring
Do NOT modify dedupe logic
Do NOT modify send_onesignal_push()
Do NOT modify _notify_push()
Do NOT change APNs or FCM send behavior
Do NOT add migrations
Preserve APNs direct admin test behavior
Preserve Android behavior
Preserve all inactive token rows as historical records
Use checkpoints sparingly
────────────────────────────────────────
GOAL
────────────────────────────────────────
Prevent stale duplicate active PushDeviceToken rows from causing:
OneSignal invalid_aliases errors
duplicate delivery issues
repeated stale subscriptions after TestFlight reinstalls
Desired behavior:
maximum 1 active token per user/platform
older tokens become inactive automatically
inactive rows remain in DB
existing push systems continue functioning unchanged
────────────────────────────────────────
IMPLEMENTATION TASKS
────────────────────────────────────────
UPDATE TOKEN REGISTRATION HYGIENE
File:
app.py
Function:
push_register_token()
Current behavior:
route looks up by (user_id, token)
new token inserts a new active row
older active rows remain active forever
Required new behavior:
When registering a token:
deactivate ALL OTHER active tokens for:
(same user_id, same platform)
EXCEPT the incoming token
Behavior rules:
do NOT delete rows
only set active=False
preserve timestamps/history
preserve apns_environment
preserve updated_at behavior for current token
preserve existing registration response payloads
Implementation detail:
After token lookup/upsert logic and before final commit:
query all other active tokens for:
user_id=current_user.id
platform=platform
token != incoming token
set active=False
Add WARNING logs for every deactivated token:
Example:
[PushToken] Deactivated stale token id=12 user=2 platform=ios
Use existing logging style/prefixes.
Result after fix:
user may only have 1 active ios token
user may only have 1 active android token
────────────────────────────────────────
ADD ADMIN CLEANUP ROUTE
Add new admin-only route:
GET /admin/push-token-dedup
Requirements:
@login_required
@admin_required
Behavior:
For every user/platform pair:
keep ONLY the most recently updated active token
deactivate all other active tokens
never delete rows
Most recent token logic:
newest updated_at wins
fallback newest created_at if needed
Return JSON summary:
{
"success": true,
"users_affected": X,
"tokens_deactivated": Y,
"details": [...]
}
Include:
user_id
platform
kept_token_id
deactivated_token_ids
Purpose:
immediately clean up existing stale TestFlight token buildup
especially user 2’s 12 active tokens
DO NOT:
call OneSignal APIs
remove rows
create migrations
────────────────────────────────────────
ADD PUSH TOKEN AUDIT VISIBILITY
Extend diagnostics visibility.
EITHER:
extend existing /admin/push-diagnostics
OR:
create /admin/push-token-audit
Your choice:
use the cleaner/safest option.
Required output:
Per user/platform:
active token count
token ids
updated_at timestamps
flag if count > 1
Example:
{
"user_id": 2,
"platform": "ios",
"active_count": 12,
"status": "DUPLICATE_ACTIVE_TOKENS"
}
Purpose:
make future accumulation visible immediately.
────────────────────────────────────────
DO NOT CHANGE THESE ROUTES
────────────────────────────────────────
These routes should continue functioning exactly as-is:
/admin/test-push
/admin/test-push-all
/admin/test-push-broadcast
Expected behavior after cleanup:
they naturally operate on the newest active token only
No logic rewrites needed.
────────────────────────────────────────
FILES EXPECTED TO CHANGE
────────────────────────────────────────
Likely:
app.py
Possibly:
templates/admin_push_diagnostics.html
(if diagnostics UI is extended)
Do NOT modify:
MessageEventLog model
send_onesignal_push()
_notify_push()
dedupe logic
push provider architecture
────────────────────────────────────────
TESTING REQUIREMENTS
────────────────────────────────────────
After implementation:
Registration hygiene
register a token
confirm only 1 active token exists per platform
confirm older rows become inactive
Cleanup route
run /admin/push-token-dedup
confirm user 2 cleanup occurs
confirm exactly 1 active ios token remains
Product push verification
trigger a real product push to user 2
confirm no invalid_aliases response from OneSignal
confirm MessageEventLog still records correctly
APNs direct routes
verify:
/admin/test-push
/admin/test-push-all
/admin/test-push-broadcast
all still function
Regression checks
no-token user still handled correctly
opted-out user still handled correctly
dedupe still functions
Android registration path still works
────────────────────────────────────────
FINAL RESPONSE FORMAT
────────────────────────────────────────
Return:
Exact files changed
Exact routes added
Summary of registration flow changes
Cleanup route output example
Diagnostics output example
Before/after active token counts for user 2
Test results
Any risks discovered
Do not stop at partial implementation.
Do not stop before testing.