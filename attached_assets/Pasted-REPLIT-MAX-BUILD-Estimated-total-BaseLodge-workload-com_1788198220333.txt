REPLIT: MAX + BUILD
Estimated total BaseLodge workload completed after this step: ~38%

TASK: #397 — Resolve Development Backup Gate

The plan is approved.

Implement `.local/tasks/...` / the approved #397 plan exactly as written.

GOAL

Create and verify a fresh pre-BL-80 logical backup of Supabase Development so BL-80 migration can proceed with a reliable recovery point.

Do NOT migrate anything in this step.

==================================================
1. RECONFIRM SUPABASE DEVELOPMENT
==================================================

Immediately before backup, run the guarded read-only target diagnostic.

Confirm:

- target is Supabase Development
- target identity matches the previously verified Development database
- PostgreSQL version is 17.x
- alembic_version = bl52_trip_stay
- BL-78 schema absent
- BL-79 schema absent
- BL-80 schema absent
- BL-70 schema absent

If any of those migration/schema assumptions are inconsistent:

STOP.

Do not create the backup.
Do not mutate the database.

Do not access Production.

==================================================
2. POSTGRESQL 17 CLIENT
==================================================

Use a temporary Nix tool environment containing PostgreSQL 17 client tools.

Do not change:
- application dependencies
- package runtime
- workflow configuration
- project database configuration

Verify before connecting:

- pg_dump major version 17
- pg_restore major version 17

If compatible PostgreSQL 17 tooling cannot be established safely:

STOP.

==================================================
3. PRIVATE APP STORAGE CUSTODY
==================================================

Use the already attached authenticated App Storage destination described in the approved plan.

Important:

- use the actual attached/default bucket identifier expected by the supported SDK/runtime
- do NOT pass a human-readable display name as though it were the SDK bucket ID
- do NOT create a new bucket
- do NOT change storage configuration

Verify authenticated listing under:

bl135/development/

before creating the backup.

If authenticated access to the intended private destination cannot be proven:

STOP.

==================================================
4. CREATE THE BACKUP
==================================================

Create a fresh PostgreSQL custom-format logical dump of Supabase Development.

Target object path should be timestamped under:

bl135/development/

and clearly identify it as the pre-BL-80 backup.

Stream directly to the private App Storage destination where supported.

Record:

- UTC timestamp
- object reference/path
- byte count
- SHA-256 checksum

Do not alter any Development data.

Do not apply any Alembic migration.

==================================================
5. VERIFY THE BACKUP INDEPENDENTLY
==================================================

After upload:

1. Confirm the object appears through authenticated listing.
2. Download/read it back through authenticated storage.
3. Verify downloaded byte size matches.
4. Verify SHA-256 matches the original.
5. Run PostgreSQL 17 `pg_restore --list`.
6. Confirm the inventory is readable and contains expected schema objects, including:
   - alembic_version
   - ski_trip
   - relevant existing trip/participant schema

Save appropriate checksum/inventory sidecars beside the dump if that is part of the approved plan.

The backup is not considered valid merely because upload succeeded.

It must pass:
- authenticated retrieval
- size verification
- checksum verification
- pg_restore inventory verification

==================================================
6. SAFETY
==================================================

DO NOT:

- apply BL-78
- apply BL-79
- apply BL-80
- apply BL-70
- run `alembic upgrade`
- restart Flask
- modify the saved workflow
- access Production
- change application code
- change secrets
- create another storage bucket
- use `.local/backups` as the primary recovery artifact

This task is backup-only.

If any required verification fails, STOP and report the failure rather than continuing toward migration.

==================================================
7. RETURN
==================================================

Return:

A. verified Supabase Development target identity
B. pre-backup alembic_version
C. PostgreSQL server version
D. pg_dump version
E. pg_restore version
F. authenticated App Storage destination verification
G. backup object reference
H. UTC timestamp
I. backup size
J. SHA-256
K. authenticated download verification
L. pg_restore inventory verification
M. confirmation expected Alembic/trip objects are present
N. confirmation Supabase Development received no writes
O. confirmation Production was untouched
P. explicit statement whether the BL-80 migration backup gate is now satisfied

Do not begin the BL-80 migration yet.