Investigate the safest way to move BaseLodge development from the Replit-managed development PostgreSQL database to a separate external non-production PostgreSQL database, so the Replit-managed Database feature can ultimately be removed from the project and Publish will no longer attempt to provision/synchronize a Replit production database.
INVESTIGATION ONLY. DO NOT IMPLEMENT OR CHANGE ANYTHING YET.
Current architecture
Development:
Replit-managed PostgreSQL
isolated and protected by BASELODGE_DEVELOPMENT_DATABASE_URL
currently initialized through bl317_startup_schema
contains the canonical development resort/reference data and the development-only test user
Production:
external Supabase PostgreSQL
currently at bl317_startup_schema
must remain completely unchanged
Replit Publish:
detects the managed development DB
requires/provisions a managed production counterpart
has no supported code-only bypass
Goal
Determine whether we can safely reach this architecture:
Development → external non-production Postgres
Production → existing external Supabase production
Replit managed Database integration → removed entirely
so normal Replit Publish deploys application code without generating/managing database schema.
Investigate external development DB options
Determine the safest supported non-production Postgres target, including:
A separate Supabase development project.
Another external PostgreSQL provider if materially safer/easier.
Whether an existing BaseLodge recovery/non-production Supabase project can be reused without risking confusion with production or historical recovery data.
Prefer a dedicated clean development database rather than repurposing a recovery database unless there is a compelling reason.
Existing development state
Inventory exactly what must be preserved or recreated from the current Replit development DB:
schema revision
695 resorts
245 resort-pass mappings
development-only user(s)
any other development-created rows
Determine whether it is safer to:
A. migrate/copy the current development DB
or
B. create a fresh external development DB using the already-built BaseLodge tooling:
#311 fresh BL-60 bootstrap
Alembic through BL-317
#313 safe reference-data importer
#315 safe development-user creator
Given how little unique development data exists, explicitly evaluate whether fresh reconstruction is safer than copying the Replit DB.
Runtime configuration
Determine the exact BaseLodge configuration required after migration:
BASELODGE_DEVELOPMENT_DATABASE_URL
BASELODGE_MIGRATION_DATABASE_URL
production identity hash
any new development identity hash
DATABASE_URL
Critically determine whether BaseLodge can completely stop depending on Replit’s managed DATABASE_URL.
Removing the Replit managed DB
Using current official Replit behavior, determine:
Exact UI path to remove/disconnect the Replit-managed development database after BaseLodge is confirmed on the external development DB.
Whether removing it removes DATABASE_URL.
Whether removing it deletes the DB immediately or soft-deletes it.
Recovery window.
Whether Publish stops generating managed-production database migrations once no Replit-managed database remains associated with the project.
Whether an existing autoscale deployment can then be republished normally.
Whether custom domains remain attached.
Deployment proof
Determine how we can prove before approving another production publish that Replit no longer sees a managed database/schema synchronization requirement.
Ideally identify a Publish UI state or API/status where:
no “Development database changes detected”
no “Generated migrations to apply to production database”
no production DB provisioning request
Safety sequence
Design the exact order so we never lose a working development environment:
Provision external development DB.
Bootstrap/migrate it.
Load reference data.
Create development-only user.
Point development runtime at it.
Verify Flask/Preview thoroughly.
Only then detach/remove Replit managed DB.
Verify Replit Publish no longer proposes database provisioning.
Only then publish #317 application code.
Adjust this if a safer sequence exists.
Hard constraints
Do not:
create an external DB yet
copy any data
modify current development DB
detach/delete Replit DB
modify DATABASE_URL
modify BaseLodge secrets
touch production Supabase
approve Publish
publish/redeploy
run migrations
restart production
touch push/OneSignal/APNs
resume BL-77
Return
Report:
recommended external development DB approach
fresh rebuild vs DB copy recommendation
exact development data that must be recreated
required environment/secrets changes
exact Replit managed DB removal procedure
recovery/rollback strategy
expected Publish behavior afterward
exact phased execution plan
risks/blockers
End with exactly one:
SAFE TO MIGRATE DEVELOPMENT OFF REPLIT MANAGED DATABASE USING THIS PLAN
or
DEVELOPMENT DATABASE MIGRATION BLOCKED: [reason]
Do not implement anything during this investigation.