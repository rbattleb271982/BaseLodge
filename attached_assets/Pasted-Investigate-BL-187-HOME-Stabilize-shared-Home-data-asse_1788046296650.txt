Investigate BL-187 — HOME — Stabilize shared Home data assembly.

This is PLAN/INVESTIGATION ONLY.

Do not implement yet.
Do not change application code.
Do not change database schema or migrations.
Do not change secrets or environment variables.
Do not commit anything.

Goal:
Create one consistent, read-only Home data assembly path for the summary modules in the current Home redesign, without changing existing product semantics.

Investigate the current Home request/data-loading architecture and produce an implementation-ready plan.

Specifically determine:

1. Current Home data flow
- Identify the route(s), controller logic, helpers, serializers, templates/components, and API calls involved in loading Home.
- Map what data is already loaded during a Home request.
- Identify all existing Home-level queries and aggregations.
- Identify where current/upcoming trip data is loaded and whether equivalent trip populations are later queried or counted again.

2. Existing canonical semantics to reuse
Trace the current authoritative source for:
- current user/profile identity
- rider/skier type
- skill level
- gear
- passes
- wishlist mountains
- trip counts
- mountains visited
- upcoming/current trips
- direct-friend relationships
- Friends' Passes counting

For each, identify:
- source model/table
- existing helper/query/serializer if one exists
- current eligibility/count semantics
- whether Home currently duplicates or reinterprets that logic

Do not invent Home-specific semantics where canonical behavior already exists.

3. Friends' Passes
Confirm the exact existing BL-19 behavior and identify how BL-187 should reuse it without changing semantics.

Preserve:
- Epic / Ikon / Other buckets
- unique connected-friend counting
- a friend with multiple qualifying passes may appear in multiple buckets, but once per bucket
- Other means canonical supported non-Epic/non-Ikon passes
- No Pass excluded
- all three buckets remain available
- Home count semantics remain equivalent to the corresponding Friends-filter result

Do not redesign the Friends' Passes UI in this task.

4. Trip reuse
Identify how BL-187 can reuse already-loaded current/upcoming trip data rather than performing equivalent route-level recounts later in the request.

Be explicit about:
- trip population being reused
- ownership vs attendance semantics
- active attendance statuses
- public/private handling where relevant
- date logic
- deduplication
- existing helper/query behavior that must remain authoritative

Do not add RSVP/action eligibility logic here.

5. Proposed shared Home summary contract
Design the smallest server-provided Home summary structure that later tasks can consume.

It should support, where appropriate:
- About You & Your Gear
- Your Activity
- Friends' Passes
- Your Next Trip

Do NOT implement:
- BL-188 disclosure UI
- BL-190 About You & Gear UI
- BL-191 Your Activity UI
- BL-192 Friends' Passes presentation changes
- BL-193 compact Next Trip UI
- BL-194 Next Trip Actions
- Ideas changes
- Happening changes

Show the proposed contract/shape and which existing canonical source supplies every field.

6. Query consolidation
Identify:
- redundant Home queries today
- queries that can safely be consolidated
- queries that should remain separate
- likely query-count impact

Do not optimize Ideas or Happening candidate retrieval here; BL-176 and BL-177 own those.

Do not introduce caching. BL-146 remains authoritative if caching is ever added.

7. Privacy and authorization
Confirm that the proposed refactor preserves existing privacy and authorization behavior.

Do not broaden visibility.
Do not create a new authorization model.
BL-133 remains authoritative for canonical authorization/visibility policy.

Call out any place where consolidation could accidentally alter:
- private-trip visibility
- friend eligibility
- participant visibility
- ownership semantics
- user-specific data boundaries

8. Tests
Propose focused tests covering:
- output equivalence before vs after consolidation
- query-count behavior
- trip reuse
- Friends' Passes semantic equivalence
- empty states
- users with no trips
- users with no gear/pass/wishlist
- multi-pass friends
- privacy/authorization preservation

Identify existing tests that should be extended instead of creating redundant test suites.

9. Exact implementation scope
Return:
- files that would likely change
- helpers/functions/routes that would likely be added or refactored
- tests that would change
- files that should explicitly remain untouched
- whether any schema/migration change is required

Schema/migration change should be NONE unless the investigation discovers an unexpected architecture issue.

10. Risk assessment
Classify implementation risk and identify any architecture discovery that would require us to stop and reconsider before Build.

Important boundaries:
- read-only Home assembly
- no product-semantic changes
- no visual redesign
- no Ideas ranking/matching changes
- no Happening ordering changes
- no new pass-history schema
- no RSVP/action eligibility changes
- no caching
- no Profile BL-165 expansion

Return a concise implementation plan with:

CURRENT STATE

REDUNDANCIES FOUND

CANONICAL SEMANTICS TO REUSE

PROPOSED HOME SUMMARY CONTRACT

QUERY CONSOLIDATION PLAN

PRIVACY / AUTHORIZATION CHECK

TEST PLAN

FILES EXPECTED TO CHANGE

RISKS / OPEN QUESTIONS

RECOMMENDED BUILD SCOPE

Do not implement.

End with exactly one:

BL-187 READY FOR BUILD

or

BL-187 NEEDS PRODUCT/ARCHITECTURE DECISION