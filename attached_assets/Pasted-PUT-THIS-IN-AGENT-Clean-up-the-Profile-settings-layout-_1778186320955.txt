PUT THIS IN AGENT

Clean up the Profile settings layout and create a clear Push notifications settings flow.

Current issue:
The Profile screen now shows both:
- Notifications / No new notifications
- Push notifications / Push notifications on

The Notifications row does not currently do anything useful and should be removed.

Desired Profile behavior:
1. Remove the “Notifications / No new notifications” row from the Profile screen.
2. Keep only one “Push notifications” row.
3. Move the “Push notifications” row into the Account section, directly above “Change password.”
4. The row should display the current push status as the subtitle:
   - Push notifications on
   - Push notifications off in BaseLodge
   - Notifications blocked at device level
   - Permission not yet granted
5. Tapping the Push notifications row should navigate to a dedicated Push notifications settings page or section where the existing push toggle lives.
6. Do not show the push toggle directly on the Profile screen.
7. Do not display Push notifications twice anywhere.

Push notifications settings page behavior:
1. Show a simple title: Push notifications
2. Show one master toggle for push notifications.
3. The toggle must use the existing user-level push_notifications_enabled preference.
4. When toggled off:
   - save push_notifications_enabled = false
   - backend OneSignal helper must skip this user
   - native OneSignal subscription should opt out where supported
5. When toggled on:
   - save push_notifications_enabled = true
   - backend OneSignal helper may send again
   - native OneSignal subscription should opt in where supported
   - if OS-level permission is not granted, request permission only because the user explicitly turned it on
6. Show short helper copy under the toggle:
   Trip invites, overlap alerts, and important updates.
7. Show current status text using the existing labels:
   - Push notifications on
   - Push notifications off in BaseLodge
   - Notifications blocked at device level
   - Permission not yet granted

Important:
- This must stay integrated with the existing OneSignal implementation.
- Do not create a separate notification system.
- Do not affect email preferences.
- Do not change existing APNs fallback routes.
- Do not expose OneSignal REST keys, device tokens, or private identifiers client-side.
- Browser/web users should not see OneSignal or permission prompt errors.
- Keep all OneSignal native calls gated to the native Capacitor runtime.
- Do not use checkpoints unless I explicitly ask for one.

After implementation, fully test:
- Profile screen shows Push notifications only once
- Notifications / No new notifications is gone
- Push notifications appears above Change password under Account
- Tapping Push notifications opens the correct push settings page/section
- Toggle saves correctly
- Status subtitle updates correctly
- Backend OneSignal sends still respect opt-out
- Native OneSignal opt-in/opt-out logic is still called where supported
- Browser has no OneSignal errors
- Python syntax passes
- templates compile
- Existing APNs routes still work
- /admin/test-onesignal-push still respects opt-out

Do not finish until you summarize exactly what files changed.