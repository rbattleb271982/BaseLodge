PUT THIS IN AGENT

Bug:
The new “Feedback” bottom navigation icon does not appear. The bottom navigation is hard-coded for 4 items (markup and/or CSS), so additional items are not rendered or are hidden.

Goal:
Update the bottom navigation to properly render 5 items (including Feedback) and make the nav future-proof so additional items can be added without layout breakage.

Constraints:
- Do NOT redesign the nav visually
- Keep existing icons, labels, spacing, and behavior
- Maintain mobile-first layout
- Do NOT break active state highlighting
- Use existing icon system

Implementation Steps:

1. Update Bottom Nav Markup
Locate the bottom navigation template (e.g. bottom_nav.html, layout.html, or base template).

Ensure the nav renders 5 items:
- Trips
- Friends
- Invite
- Profile
- Feedback

Add Feedback item with:
- Label: Feedback
- Icon: lightbulb (outline)
- Route: /feedback

2. Fix Layout to Support Dynamic Item Count
Remove any hard-coded assumptions of 4 items.

Specifically:
- Remove width: 25% from nav items
- Remove grid-template-columns: repeat(4, 1fr)

Replace with ONE of the following:
Option A (preferred):
- Use flexbox
- Each nav item: flex: 1

OR

Option B:
- grid-template-columns: repeat(auto-fit, minmax(0, 1fr))

This ensures 5 items render evenly.

3. Active State Logic
Ensure active state still works:
- Highlight icon + label when route matches
- Feedback should highlight when on /feedback

4. Icon Rendering
Verify the lightbulb icon:
- Uses same icon system as other nav icons
- Renders in outline style
- Inherits active/inactive color correctly
- No hard-coded fill colors

5. Validation Checklist (must all pass):
- Bottom nav shows 5 items
- All icons are visible and evenly spaced
- Labels are readable in dark mode
- Active state works for all tabs
- No overlap or clipping on small screens
- Works on mobile Safari

Do NOT:
- Do NOT hide icons with overflow
- Do NOT shrink labels
- Do NOT remove existing nav items
- Do NOT introduce horizontal scrolling

Deliverable:
Bottom navigation correctly displays 5 items including Feedback, with a layout that supports future expansion without breaking.