Investigate BL-177 — HOME — Bound Happening candidate retrieval.

This is PLAN / INVESTIGATION ONLY.

Do not implement yet.
Do not change application code.
Do not change database schema or migrations.
Do not change secrets or environment variables.
Do not commit anything.

Goal:
Identify how to bound Home Happening candidate retrieval so it no longer scales unnecessarily with the full friend-trip population, while preserving all existing Happening eligibility, privacy, ordering, suppression, and display semantics.

This task is performance-focused only.

1. Map the current Happening data flow.

Identify:
- Home route/controller entry point
- helpers/services involved
- database queries involved
- relationship loading
- friend-trip retrieval path
- any reuse of Ideas engine data
- where Happening eligibility is applied
- where ordering/ranking is applied
- where suppression/dismissal is applied
- where final candidate limiting occurs today, if at all

Be explicit about:
- current query count
- candidate population size before filtering
- candidate population size after filtering
- where database-side limits exist
- where in-memory filtering happens
- any N+1 behavior
- whether candidate retrieval can grow with all direct friends/all future trips

2. Preserve existing Happening semantics.

Do not change:
- what qualifies for Happening
- public/private rules
- direct-friend rules
- ownership/attendance semantics
- trip status rules
- date rules
- resort requirements
- ordering
- deduplication
- dismissal/suppression behavior
- presentation behavior

If any of these semantics are currently implicit or spread across multiple helpers, document the authoritative behavior rather than “cleaning it up.”

3. Relationship to Ideas.

Determine exactly how Happening currently reuses or depends on Ideas-engine friend-trip data.

Answer:
- Which query/data source is shared?
- Is Happening forced to inherit an oversized Ideas candidate population?
- Can Happening be bounded independently without changing Ideas?
- Would bounding shared retrieval alter BL-176 responsibilities?

BL-176 remains the dedicated Ideas candidate-bounding task.
Do not implement BL-176 here.

4. Candidate bounding options.

Identify the safest ways to reduce the number of Happening candidate rows loaded or processed.

Evaluate options such as:
- database-side date bounds
- database-side visibility/friend bounds
- result caps
- staged retrieval
- deterministic overfetch + final in-memory filtering
- reusable bounded helper
- splitting Happening retrieval from broader Ideas retrieval

For each viable option, explain:
- expected performance benefit
- semantic risk
- privacy risk
- ordering risk
- query-count impact
- whether it could exclude a candidate that currently should surface

Do not recommend arbitrary LIMIT without proving it preserves final ordering/eligibility.

5. Ordering safety.

Trace the exact existing Happening ordering.

Determine:
- fields used
- tie-breaking behavior
- whether ordering occurs before or after eligibility filtering
- whether a database-side bound can safely preserve the same final output
- what overfetch factor or staged strategy would be required, if any

If exact equivalence cannot be guaranteed with a simple bound, say so.

6. Dismissal/suppression.

Identify how dismissed/acknowledged Happening items are stored and filtered.

Determine:
- whether suppression can be applied before candidate retrieval
- whether doing so safely reduces rows
- whether suppression state is user-specific
- whether any privacy/cache boundary is relevant

Do not add caching.
BL-146 remains authoritative for cache safety.

7. Query and scale analysis.

Provide a practical estimate of current scaling characteristics.

For example:
- O(number of direct friends)
- O(number of future friend trips)
- number of rows returned before final filtering
- repeated queries or relationship loads

Then estimate the expected bounded behavior after the recommended change.

Use current architecture and code evidence, not theoretical redesign.

8. Tests.

Propose focused tests proving:
- Happening output equivalence before vs after optimization
- same ordering
- same deduplication
- same public/private handling
- same direct-friend eligibility
- same date eligibility
- same dismissal behavior
- same empty state
- large candidate populations remain bounded
- no N+1 query introduced
- query-count/candidate-count improvement
- BL-176 Ideas behavior remains untouched

Identify existing tests that should be extended rather than creating redundant suites.

9. Expected implementation scope.

Return:
- files likely to change
- helpers/functions likely to change
- tests likely to change
- files that should remain untouched
- whether schema/migration change is required

Schema/migration change should be NONE unless the investigation discovers an unexpected architecture issue.

10. Stop conditions.

Return NEEDS PRODUCT/ARCHITECTURE DECISION if:
- bounding candidates would change current Happening ordering
- exact eligibility cannot be preserved
- Happening and Ideas cannot be separated without a larger architecture change
- the task requires new privacy semantics
- a schema/migration is necessary
- a cache is necessary

Return READY FOR BUILD only if the implementation can be made performance-only with equivalent output semantics.

Return:

CURRENT HAPPENING DATA FLOW

CURRENT SCALE / BOTTLENECK

AUTHORITATIVE HAPPENING SEMANTICS

IDEAS / HAPPENING COUPLING

SAFE BOUNDING OPTIONS

RECOMMENDED APPROACH

ORDERING EQUIVALENCE CHECK

PRIVACY / SUPPRESSION CHECK

EXPECTED QUERY / CANDIDATE IMPACT

TEST PLAN

FILES EXPECTED TO CHANGE

RISKS / OPEN QUESTIONS

RECOMMENDED BUILD SCOPE

End with exactly one:

BL-177 READY FOR BUILD

or

BL-177 NEEDS PRODUCT/ARCHITECTURE DECISION