✅ Replit-Ready Responses
1. Calendar Implementation
Build a custom calendar using vanilla JS + CSS.
There is no existing calendar component in the app that should be reused.
This calendar has very specific UX rules (vertical months, continuous highlights, modal behavior) that would be harder to bend from a generic library.
Keep it lightweight and purpose-built.
Do not introduce a third-party calendar library.
2. Month Range
Use Option (b): Dynamic based on the friend’s furthest upcoming trip.
Details:
Calendar should start at the current month
Render months only as far as needed to fully display the friend’s latest upcoming trip
If a trip spans into a future month, that month must be rendered
Do not render empty future months “just in case”
This keeps the UI focused and avoids unnecessary scrolling.
3. Multiple Trip Highlighting
Use Option (a): All trips use the same highlight color.
Rationale:
This is a read-only awareness view
Users are scanning when the friend is traveling, not managing multiple trips
Color-coding trips introduces unnecessary cognitive load
If multiple trips overlap the same date:
The modal handles disambiguation (list of trips)
The calendar itself stays visually calm
4. Existing Implementation Check
✅ Yes — verify the current Friend Profile structure first.
Specifically:
Understand how the Upcoming Trips (List view) is currently rendered
Reuse the same data source and ordering
The Calendar view should be a visual alternative, not a parallel implementation
Important:
Do not refactor or relocate existing logic unless absolutely required
The List view remains the default and fallback
🔒 Summary (for Replit)
Custom calendar (vanilla JS/CSS)
Dynamic month range based on actual trips
Single highlight color for all trips
Inspect existing Upcoming Trips list before extending
Planning + Friend Profile Calendar can be implemented in parallel, but remain logically independent
Once this is done, stop and report:
Files touched
Any assumptions about trip date structures
Any edge cases discovered