PUT THIS IN ASSISTANT

We are now doing a MARKUP-ONLY refactor of templates/home.html.
Do NOT touch routes, database logic, or backend variables.

GOAL
- Remove “My Trips” from the tab navigation
- Make “My Trips” a standalone expandable section
- Keep Friends’ Trips / Overlaps / Open Availability as tabs
- Preserve all existing logic and data bindings

--------------------------------
STEP-BY-STEP INSTRUCTIONS
--------------------------------

1) REMOVE the “My Trips” button from the tabs navigation
   - Delete only the My Trips tab button
   - Leave Friends / Overlaps / Open Availability tabs unchanged
   - Do NOT change switchTab() logic yet

2) EXTRACT the existing “tab-my” block
   - Take the entire <div id="tab-my">…</div>
   - Move it ABOVE the tabs navigation
   - Wrap it in a new container:

     <div class="my-trips-section">
       <div class="my-trips-header">My Trips</div>
       <div class="my-trips-content">…existing tab-my content…</div>
     </div>

   - Remove role="tabpanel" from this block
   - Do NOT change any Jinja loops or conditionals inside

3) ADD expand / collapse behavior (frontend only)
   - my-trips-content should:
     • Be visible by default if my_trips|length <= 1
     • Be hidden by default if my_trips|length > 1
   - Clicking the my-trips-header toggles visibility
   - Add minimal inline JS for this (no libraries)

4) CREATE TRIP CTA behavior
   - Keep the existing empty-state “Create a trip” CTA exactly as-is
   - If my_trips exists, add a smaller “+ Create a trip” button near the My Trips header
   - This CTA should link to url_for('add_trip')

5) UPDATE trip card hierarchy inside My Trips
   - Swap the order so the mountain/resort name appears ABOVE the dates
   - Do NOT change formatting functions or data fields

--------------------------------
CONSTRAINTS
--------------------------------
- Do NOT touch backend logic
- Do NOT modify switchTab() for social tabs
- Do NOT modify CSS yet (we will do styling after)
- Do NOT introduce new data variables

After completing these steps, show me the updated templates/home.html.

END
