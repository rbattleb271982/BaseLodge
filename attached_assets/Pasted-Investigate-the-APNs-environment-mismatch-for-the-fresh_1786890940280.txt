Investigate the APNs environment mismatch for the fresh iPhone subscription. READ ONLY. Do not modify anything yet.
Current proven state:
Fresh Xcode-installed BaseLodge build
OneSignal-only architecture
onesignal-cordova-plugin 5.5.2
native iOS SDK 5.5.5
fresh subscription 1a821084-6d35-4397-9035-dcf0424c2b87
fresh APNs token prefix 523951ab
sdk=050505
session_count=1
test_type=1
invalid_identifier=true
enabled=false
notification_types=-18
The old subscription f10dcf69 had test_type=0; the fresh Xcode install has test_type=1.
I want you to determine whether BaseLodge/OneSignal is configured to send this sandbox/development APNs token through the wrong APNs environment.
Check, read-only:
The actual aps-environment entitlement in the current Xcode Debug build.
The provisioning profile/environment being used by the Xcode device build.
OneSignal app's current Apple/APNs configuration, including:
apns_env
whether credentials are .p8 or .p12
bundle ID
Team ID
Key ID where available
whether the APNs credential supports both Sandbox and Production
Whether OneSignal is correctly recognizing test_type=1 subscriptions as sandbox/development subscriptions for this app.
Whether the OneSignal app is configured production-only in a way that would cause a development token to be rejected.
Whether the BaseLodge backend has any APNs environment setting such as APNS_USE_SANDBOX, and whether that setting affects OneSignal sends, direct APNs sends, or both.
Compare the expected APNs environment for:
Xcode development install
TestFlight build
App Store production build
I want a definitive answer to:
Is 1a821084 invalid because the Xcode build produced a sandbox APNs token while OneSignal is configured/sending as production?
Return:
current Xcode entitlement/environment
current OneSignal APNs environment/config
whether they match
exact proven cause if they do not
smallest correct fix for development testing that does not compromise TestFlight/App Store production delivery
Do NOT change credentials, code, OneSignal settings, subscriptions, or secrets. Stop after the report.