PUT THIS IN AGENT

#193 - Onboarding Step 2 — Card Layout (6 Options)

Replace the current wrapping pill layout on onboarding Step 2 with a full-width vertical card stack for better mobile symmetry.

Also remove "Interested (Learning)" from onboarding only. Keep "None of the Above (Experience)" as the only non-discipline option.

Do not change:
- Steps 1, 3, or 4
- Edit Profile rider pills
- RIDER_TYPES
- Existing users/profiles
- Backend routes
- Models
- Stored values
- Validation logic
- Global stylesheets

Use checkpoints sparingly.

## Done Looks Like

Step 2 shows exactly 6 riding options as full-width stacked cards:

- Skier
- Snowboarder
- Telemark
- Cross-country
- Adaptive
- None of the Above (Experience)

"Interested (Learning)" is gone from onboarding.

Skill level options are also full-width stacked cards with these visible labels:

- Beginner (Green)
- Intermediate (Blue)
- Advanced (Black)
- Expert (Double Black)

Underlying `data-val` values stay unchanged:

- Beginner
- Intermediate
- Advanced
- Expert

Skill section behavior:

- Shows for Skier, Snowboarder, Telemark, Cross-country, Adaptive
- Hides only for None of the Above (Experience)

Reuse existing Social logic if it already supports this. Do not introduce new rider-type rules unless necessary.

Selection behavior remains unchanged:

- Tap selects
- Tap another switches selection
- Tap same again deselects
- Selected state remains bordeaux fill

Preserve:
- Header copy
- Progress indicator
- CTA button
- Typography
- Existing unselected styling

## Implementation

Relevant file:

`templates/identity_setup.html`

Update Step 2 only.

Replace the `ob-options--wrap` wrapping layout with a vertical full-width card stack.

Add scoped CSS inside the existing `{% block head %}` style block in `identity_setup.html`.

Do not edit `styles.css`.

Preserve existing button data attributes:
- `data-grp`
- `data-val`
- `data-target`

Preserve the existing JS selection behavior.

## Testing

Verify:

- Exactly 6 riding cards render
- "Interested (Learning)" is absent
- All riding cards are full-width and vertically stacked
- Skill cards are full-width and vertically stacked
- Skill labels display as Beginner (Green), Intermediate (Blue), Advanced (Black), Expert (Double Black)
- Skill section hides only for None of the Above (Experience)
- Skill section shows for all 5 riding disciplines
- Single-select behavior still works
- Stored values submit correctly
- Onboarding completes successfully
- Mobile layout looks balanced at iPhone width
- No console errors

Do not finish until you have fully tested the implementation and confirmed every acceptance criterion above is working.