╔══════════════════════════════════════════════════════════════╗
║ BASELODGE — HOME + IDEAS LOGIC AUDIT ONLY                  ║
╚══════════════════════════════════════════════════════════════╝

Goal:
Audit the current Home screen logic and the current Ideas tab logic so we can decide how to safely merge Ideas-style logic into Home later.

IMPORTANT:
- Do NOT make code changes.
- Do NOT redesign Home.
- Do NOT remove Ideas.
- Do NOT move any logic yet.
- This is an audit only.
- Only use checkpoints if absolutely necessary.

────────────────────────────────────
PART 1 — HOME SCREEN AUDIT
────────────────────────────────────

Find and document:

1. Which route renders Home
- file name
- route function name
- route URL
- blueprint name

2. Which template renders Home
- exact template path
- parent/base template if applicable

3. What data is passed into Home
For each variable, document:
- variable name
- where it is calculated
- model/table/source
- what it controls visually

4. Current Home sections
List each visible section/card and explain:
- what data powers it
- what condition makes it show or hide
- what happens when data is missing

5. Current trip logic
Document:
- how “next trip” is selected
- whether friend trips are included
- how countdown/dates are calculated
- how empty states work

6. Current social/friend logic
Document whether Home currently shows:
- friend activity
- trip invites
- connect requests
- overlap signals
- wishlist signals
- mountain signals

7. Current profile/stat logic
Document:
- how Trips / Mountains / Wishlist counts are calculated
- whether numbers are clickable
- whether counts are current-user-only or include friends

────────────────────────────────────
PART 2 — IDEAS TAB AUDIT
────────────────────────────────────

Find and document:

1. Which route renders Ideas
- file name
- route function name
- route URL
- blueprint name

2. Which template renders Ideas
- exact template path
- parent/base template if applicable

3. What data is passed into Ideas
For each variable, document:
- variable name
- where it is calculated
- model/table/source
- what it controls visually

4. Current Ideas sections/cards
List each visible section/card and explain:
- what data powers it
- what condition makes it show or hide
- what happens when data is missing

5. Current recommendation logic
Document:
- how Ideas are generated
- how ideas are grouped or deduped
- how ideas are ranked
- whether logic uses friend trips, availability, wishlist, pass type, mountain/resort data, or trip dates
- whether it filters out weak/duplicate ideas

6. Current empty/setup states
Document:
- what shows if user has no friends
- what shows if user has no availability
- what shows if user has no trips
- what shows if there are no recommendations

7. Current actions from Ideas
Document every CTA or link:
- where each routes
- whether it creates a trip
- whether it edits availability
- whether it invites friends
- whether it opens a mountain/resort/trip page

────────────────────────────────────
PART 3 — COMPARISON + MERGE READINESS
────────────────────────────────────

Return a side-by-side written audit with:

A. Files involved
B. Home route/template/data summary
C. Ideas route/template/data summary
D. Shared models/helpers/services
E. Logic that can safely be reused on Home
F. Logic that should stay Ideas-only for now
G. Risks before merging Ideas into Home
H. Recommended safest first Home module to add

Do not implement anything yet.