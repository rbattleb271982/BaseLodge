PUT THIS IN AGENT

We have confirmed:
- Production still has 118 resorts and only US countries are selectable.
- /admin/sync-resorts-from-dev was deployed but NOT executed in production.
- The canonical dataset used by the sync endpoint is incomplete (Holimont missing).

GOAL:
Fix the resort sync mechanism so it uses a COMPLETE canonical dataset (not a hardcoded subset),
then provide a SAFE way to run the sync in production.
Do NOT execute anything in production yet.

NON-NEGOTIABLES:
- Prod cannot read Dev DB directly. Canonical data must be shipped with the codebase.
- No deletes.
- No bidirectional sync.
- Idempotent.
- Provide a DRY RUN mode first.

TASKS:

1) Create the canonical resort dataset FROM DEV (export)
- Build a read-only export script that runs in DEV and outputs a file into the repo:
  data/canonical_resorts.json
- Export fields:
  name, state_code, country_code, pass_brands
- Normalization in export:
  name trimmed (keep original casing but also include a normalized_key)
  state_code upper
  country_code upper
  pass_brands as stored (string), allow NULL
- Include a stable unique key per resort:
  key = (name.strip().lower(), state_code.upper(), country_code.upper())

2) Update the production sync endpoint to use the canonical file (NOT hardcoded list)
- Replace any hardcoded CANONICAL_RESORTS list with reading from data/canonical_resorts.json.
- Endpoint:
  POST /admin/sync-resorts-from-canonical
- Admin protected.
- Idempotent upsert using the key (name/state_code/country_code).
- Actions:
  - INSERT if missing in PROD
  - UPDATE if exists but pass_brands differs (DEV/canonical overrides PROD)
  - SKIP otherwise
  - NO deletes
  - PROD-only resorts remain untouched

3) Add DRY RUN capability (required)
- Support query param or payload flag: dry_run=true
- In dry_run, compute counts and diff but DO NOT write anything.

4) Add clear response payload + logs
Return JSON:
{
  "dry_run": true/false,
  "canonical_count": <int>,
  "inserted": <int>,
  "updated": <int>,
  "skipped": <int>,
  "untouched_prod_only": <int>,
  "deleted": 0,
  "status": "success"
}
Also log examples of first 10 inserts and first 10 updates (keys only).

5) Verification steps (dev first, then prod readiness)
- In DEV: run dry_run and confirm it reports 0 inserts/updates when compared to dev DB.
- In PROD: we will first run dry_run and review expected inserts/updates before executing for real.

DELIVERABLES:
- data/canonical_resorts.json generated from DEV and committed
- New endpoint /admin/sync-reso
