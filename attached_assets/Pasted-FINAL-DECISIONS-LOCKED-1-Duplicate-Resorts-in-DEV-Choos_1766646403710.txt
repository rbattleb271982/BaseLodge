FINAL DECISIONS (LOCKED)
1. Duplicate Resorts in DEV
Choose: B — Deduplicate first, then export the clean canonical list.
Why (non-negotiable):
A “canonical” dataset cannot contain duplicates by definition.
If you export duplicates into prod, you are baking ambiguity into production.
Cleanup after prod sync is far more expensive and risky than cleanup before.
Rule:
DEV cleanup is part of canonicalization.
Canonical export must be one row per real-world resort.
Example:
❌ Niseko + Niseko United
✅ one canonical Niseko United
2. pass_brands Conflicts on Duplicates
Choose: B — Merge pass_brands (union) into the canonical record.
Why:
pass_brands is descriptive metadata, not a mutually exclusive enum.
If two DEV records disagree, that means knowledge is split, not that one is “wrong.”
Union preserves information; overwriting loses it.
Rule:
When deduplicating:
pass_brands = union(all non-null values)
Deduplicate + normalize (sorted, comma-separated)
Example:
Rusutsu: Ikon + Rusutsu: Other
→ Rusutsu: Ikon,Other
This avoids future rework.
3. PROD-Only Resorts (16 total)
Choose: A — Leave them completely untouched for now.
Why:
PROD-only resorts are unreviewed legacy data.
Automatically promoting them to canonical risks:
Wrong naming
Wrong geography
Wrong pass associations
Canonical source of truth must be intentional, not inherited.
Rule:
PROD-only resorts stay in PROD.
They are explicitly excluded from canonical export.
Handle them later as a separate, deliberate cleanup task.
This keeps blast radius low.
4. Endpoint Naming
Choose: A — Create a NEW endpoint. Do NOT replace the old one.
Why:
The existing /admin/sync-resorts-from-dev is proven unsafe (hardcoded subset).
Replacing it risks:
Confusion
Accidental execution
Losing audit history
Rule:
Old endpoint = deprecated (do not delete yet)
New endpoint = authoritative
Final name (lock this):
/admin/sync-resorts-from-canonical
This makes intent unambiguous.
SUMMARY — WHAT THIS MEANS OPERATIONALLY
Before any production execution:
Deduplicate DEV resorts
One row per real-world resort
Merge pass_brands on conflicts
Export canonical file
data/canonical_resorts.json
Clean, unique, authoritative
Use new endpoint
Reads from canonical file
Has dry_run=true
No deletes
Idempotent
Run PROD dry-run
Confirm Holimont appears
Confirm international countries appear
Confirm counts make sense
Only after that do you execute.