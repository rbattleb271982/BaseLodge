PUT THIS IN AGENT

Audit the current Ideas tab/page in BaseLodge and give me a full structural and infrastructure breakdown of how it works today.

I do NOT want code changes yet. I want a clear analysis of the existing implementation so we can decide how to evolve it into a real ideas engine with AI skills later.

Please inspect and report on all of the following:

1. Entry points and routing
- Which route(s) render the Ideas tab/page
- Which blueprint/file owns that route
- Whether the Ideas tab is a standalone page, a tab inside another page, or conditionally rendered UI
- The exact template(s) involved

2. Backend data flow
- What backend functions currently build the data shown on the Ideas tab
- Which helper functions/services are used
- Which models/tables are queried
- Whether the data is pulled directly in the route or passed through helper functions
- Whether there is any ranking, filtering, sorting, or deduping logic

3. Current “idea” logic
- What actually qualifies as an “idea” today
- Whether the page is powered by true computed suggestions or just placeholder / hardcoded cards
- Whether any overlap logic, friend matching logic, resort matching, availability logic, or recommendation logic already exists
- Whether any AI-related logic exists at all, even if partial or unused

4. Inputs available today
For the current Ideas page, identify which user data is already available and usable:
- user profile data
- pass data
- friend data
- trip data
- participant/invite data
- resort_id / mountain matching data
- wishlist data
- availability data
- location/home base data
- any scoring or metadata already present

For each one, tell me if it is:
- already wired into the Ideas experience
- available elsewhere in the app but not used here
- missing entirely

5. Frontend/UI structure
- Which HTML/Jinja templates and partials render the page
- Whether there is tab logic in the template or JS
- Any JavaScript used specifically for this page
- Whether the cards/components are reusable
- Whether the page already supports empty states, loading states, or CTA actions

6. Reuse opportunities
Please identify what can likely be reused for a future Ideas engine:
- overlap helpers
- trip counting helpers
- friend relationship logic
- resort matching logic
- card UI components
- ranking/sorting logic
- existing route patterns

7. Gaps / blockers
Call out what is missing or weak if we want to evolve this into a real Ideas tab with modular skills:
- missing data
- missing helper functions
- poor route structure
- duplicated logic
- incorrect assumptions
- weak overlap matching
- no saved-idea state
- no clean output object for idea cards

8. Final recommendation
At the end, give me:
A. a concise summary of how the current Ideas page works today
B. what should be kept
C. what should be refactored
D. what should be replaced entirely
E. the ideal architecture for turning this into a real skills-based ideas engine later

Important instructions:
- Do not make changes yet
- Do not rewrite files yet
- Do not simplify the explanation
- Be specific and reference actual files, routes, helpers, models, and templates
- If something is unclear, inspect more files before answering
- I want the output to read like a technical audit, not a guess
- Use checkpoints sparingly