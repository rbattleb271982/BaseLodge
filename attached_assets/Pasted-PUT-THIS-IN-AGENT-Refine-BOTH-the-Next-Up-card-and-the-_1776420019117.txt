PUT THIS IN AGENT

Refine BOTH the "Next Up" card and the stats section so they match the editorial, premium design language of the Home screen.

This is a polish pass, not a redesign.
Only update the populated Home state.

---

PART 1 — Next Up card (tight, editorial)

1. Remove button styling

* Replace "Edit" button with inline text:
  "Edit →"

* No border

* No background

* Use primary (bordeaux) text color

* Match style of "View →" in Friend Activity

---

2. Enforce true 2-row layout

Row 1:

* "IN 3 DAYS" (left)
* "Edit →" (right)

Row 2:

* "Vail, CO"
* "Apr 20–24" directly below, tightly grouped

---

3. Tighten spacing

* Reduce internal padding
* Reduce gaps between:

  * countdown → destination
  * destination → date

The card height should shrink noticeably

---

4. Reduce visual weight

* Keep blush background and border
* Slightly soften border (lighter or thinner)
* Ensure it doesn’t overpower header or stats

---

PART 2 — Stats section (align with empty state)

1. Remove old visual treatment

* Remove heavy underline styling under numbers/labels
* Remove anything that feels like clickable link styling for now

---

2. Match empty Home stats style

* label above, number below (same as empty state)
* consistent spacing between label and number
* consistent spacing between stat columns

---

3. Normalize labels

Use:

* TRIPS
* MOUNTAINS
* WISHLIST

(No “MOUNTAINS VISITED” or “WISHLIST MOUNTAINS”)

---

4. Reduce emphasis

* numbers should be present, but not dominant over the header
* use the same type-stat + type-label system already defined

---

5. Spacing

* ensure consistent gap:
  header → stats
  stats → Next Up

Stats should feel like a subtle data strip, not a feature block

---

PART 3 — Do NOT change

* header (already fixed)
* Friend Activity section
* empty Home state
* route logic
* conditionals

---

Goal:

The top half of populated Home should feel like ONE system:

* header (editorial)
* stats (subtle data strip)
* Next Up (tight featured moment)

No component should feel like it comes from a different design system.
