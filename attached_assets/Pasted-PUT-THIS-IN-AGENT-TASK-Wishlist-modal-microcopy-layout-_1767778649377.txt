PUT THIS IN AGENT

TASK: Wishlist modal microcopy, layout, and empty-state polish (NO logic redesign)

SCOPE:
- This is a UI + copy + spacing task only.
- Do NOT change data queries or matching logic.
- Assume wishlist_overlaps data already exists and is correct.

CHANGES TO IMPLEMENT:

1) WISHLIST MODAL — TITLE COPY
- Change modal header text from:
  "Wishlist matches"
- To EXACTLY:
  "Mountains on your wishlist"

No subtitle. Keep tone neutral and personal.

---

2) WISHLIST MODAL — PERSON ROW FORMAT
For each mountain section:

Mountain name:
- Example: "Deer Valley"
- Keep as section header
- REMOVE divider line beneath the mountain name

People listed under the mountain:
- Indent people visually under the mountain name
- Each person must appear on ONE line in this exact format:

  FirstName LastName · Skier/Snowboarder · SkillLevel · PassType

Example:
  Jonathan Schmitz · Skier · Advanced · Epic

NO line breaks
NO bullets
NO horizontal rules between people
Tighter vertical spacing than current implementation

---

3) WISHLIST MODAL — SPACING & VISUAL TWEAKS
- Mountain header spacing: standard section spacing
- Person rows: slightly smaller text than mountain header
- Remove any extra separators that visually disconnect the person from the mountain
- Modal should feel calm and compact, not list-heavy

---

4) IDEAS TAB — EMPTY STATE (USER HAS NO WISHLIST)
If the user has ZERO wishlist mountains:

Show a single card in the Ideas tab (NOT a modal):

Primary text:
  "Add mountains to your wishlist to find matches"

Secondary helper text (smaller, muted):
  "We’ll show you friends who want to ski the same places."

Right-aligned action text:
  "Add wishlist"

Interaction:
- Tapping the card or CTA navigates to the profile wishlist editor

---

5) IDEAS TAB — NO MATCHES (USER HAS WISHLIST BUT NO OVERLAPS)
If the user HAS wishlist mountains but ZERO overlaps with friends:

- DO NOT show the wishlist card
- DO NOT show empty text
- DO NOT show a modal trigger

This state should be completely hidden.

---

QA REQUIREMENTS (MANDATORY):
- Verify modal title text updates correctly
- Verify person rows are inline and indented under mountain
- Verify no divider line appears between mountain and people
- Verify empty-state card ONLY appears when wishlist is empty
- Verify no wishlist UI appears when wishlist exists but has no matches
- Verify no console errors
- Report back with confirmation only (no explanations)

DO NOT add new features.
DO NOT redesign the modal.
DO NOT change backend logic.