PUT THIS IN AGENT

Refine the Mountain Detail Page hero to the final approved design. This is a visual polish + small data wiring update only. Do not change page structure or underlying logic.

---

### GOAL

Make the hero:

* more compact
* more intentional
* slightly richer (with subtle social context)
* clearly separated from the list below

Do NOT add new sections or features.

---

### FINAL HERO STRUCTURE (TOP → BOTTOM)

1. Back link (existing, unchanged)

2. Mountain name

* Large serif
* Primary focal point

3. State

* Directly under the name
* Small, muted
* Uses full state name (not abbreviation)

  * Example: "Colorado", not "CO"

4. Social context (NEW, optional)
   Format:

* “6 friends going · 2 considering”

Rules:

* Only show if there is real data
* If both counts are zero → hide entirely
* If only one exists:

  * “6 friends going”
  * OR “2 considering”
* Small font
* Muted color (secondary text tone)
* No bold, no emphasis
* Must feel like context, not UI

Data source:

* Derived from the same dataset already powering the list
* Count distinct users:

  * Going = accepted
  * Considering = pending/invited (based on current logic)

---

5. Pass pills (updated style)

* Outline style (NOT filled)
* Bordeaux outline + text (#6b1a1a)
* Support multiple passes
* Tight spacing between pills

Examples:
[ Epic ]   [ Ikon ]
[ Ikon ]   [ Ikon Base ]   [ Ikon Base+ ]

Rules:

* Use Resort.get_pass_names()
* Normalize names if needed
* If no pass:
  show one ghost pill: “No major pass”

---

### SEPARATION FROM CONTENT

Add a clean transition between hero and list:

* Single hairline divider

  * 0.5px
  * subtle (rgba(0,0,0,0.1))
  * full width

Spacing:

* ~20–24px between pass pills and divider

Do NOT:

* add cards
* add section headers
* add backgrounds

---

### DESIGN REQUIREMENTS

* Keep cream background (#f5f0eb)
* Maintain editorial tone
* Reduce vertical height vs previous version
* Tighten spacing between:

  * name
  * state
  * social context
  * pills
* Ensure left alignment is consistent
* Keep everything calm and minimal

---

### DO NOT CHANGE

* Trip list structure
* Row design
* Date formatting
* Sorting logic
* Navigation behavior
* Backend logic

---

### DELIVER BACK

1. Files updated (template + CSS)
2. How social context counts were computed
3. Confirmation:

   * state now shows full name
   * pills are outline style
   * divider is added
   * hero height is reduced
4. Mobile validation confirmation (no overflow, spacing clean)

---

### IMPORTANT

* This is polish only
* Do not introduce new complexity
* Use checkpoints sparingly
* Do not finish until visually verified in mobile layout
