PUT THIS IN PLAN FIRST. THEN USE AGENT.
Use checkpoints sparingly only after meaningful milestones.

Goal:
Tighten the admin dashboard KPI tile height and add the percent-change/supporting delta line to the relevant KPI cards without making the typography feel small again.

Changes required:

1. Tighten vertical tile spacing
Reduce the mobile card height by tightening:
- card top/bottom padding
- gap between label and KPI number
- gap between KPI number and subtext
- gap above the sparkline

Keep the cards premium and readable, not cramped.

2. Add percent-change lines where useful
Add a small delta line under the subtext for KPI cards where a comparison exists or can be calculated cleanly.

Use the same style as the existing green line:
“+750% vs prior MTD”

Add comparable delta lines for:
- Total Users, if there is a useful prior-period comparison
- WAU, vs prior L7
- MAU, vs prior L30
- Trips L7, vs prior L7
- Trips L30, vs prior L30
- Mountain Page Views, vs prior L30 if possible
- Avg. Friends, only if a meaningful prior comparison exists

If a clean prior-period comparison is not currently available, do not fake it. Either omit that delta line or add the backend query needed if simple and safe.

3. Typography guidance
It is okay to reduce the mobile KPI number size slightly if needed for balance, but do not return to the old small sizing.

Target range:
- KPI value: 42–46px mobile
- KPI label: 17–18px mobile
- subtext: 16px mobile
- delta line: 14–15px mobile

4. Delta styling
Use:
- green for positive change
- muted gray for flat/no change
- avoid alarming red for now unless a negative comparison is important

Keep the text short:
- “+12% vs prior L7”
- “+8% vs prior L30”
- “Flat vs prior L7”

5. Preserve:
- 2-column grid
- current KPI order
- current card links
- current colors
- current backend metric logic unless adding simple prior-period comparison queries
- no major redesign

Done looks like:
- Tiles are shorter and more compact
- More dashboard content fits above the fold
- KPI numbers still feel large and premium
- Delta lines appear where they are accurate and useful
- No fake or placeholder percentage changes
- No clipping, overlap, or weird wrapping on mobile

Test:
- /admin loads cleanly
- Verify mobile layout at iPhone width
- Verify dense cards like NEW THIS MONTH and TRIPS L30 still fit cleanly
- Verify cards without sparkline or delta still maintain consistent visual height