Implement the safe development reference-data importer exactly as specified by the completed investigation.
This is an implementation task for non-production reference data only.
Do not restart Flask yet.
Do not touch production.
Do not resume BL-77.
Goal
Create a standalone, guarded importer that loads the reviewed canonical resort/reference data into development/test PostgreSQL without importing app.py, Flask, SQLAlchemy models, startup mutators, OneSignal, or any production data.
Canonical source
Use only:
attached_assets/BaseLodge_Resort_Master_IMPORT_READY_v3_1778641272852.xlsx
Required tabs:
Resorts_Cleaned
ResortPassMap_FIXED
Expected source counts:
695 resorts
245 distinct normalized resort/pass mappings
212 resorts with at least one mapping
483 resorts with no major-pass mapping
Do not use:
scripts/import_dev_resorts.py
scripts/backfill_resort_passes.py
db_init.py
production data
1. Build standalone importer
Create a new script dedicated to safe reference import, for example:
scripts/import_reference_data.py
It must:
never import app.py
never import Flask
never import SQLAlchemy models
never invoke startup mutators
never invoke OneSignal
use only side-effect-free runtime config, PostgreSQL driver, workbook parsing, and standard-library helpers
2. Hard safety guards
Require:
BASELODGE_RUNTIME_ENV is exactly development or test
BASELODGE_REFERENCE_IMPORT_MODE=1
BASELODGE_REFERENCE_IMPORT_DATABASE_URL
PostgreSQL only
production identity rejection using the existing #309 guard
in development, target identity must equal BASELODGE_DEVELOPMENT_DATABASE_URL
Do not print:
full DB URLs
usernames
passwords
credentials
If any guard fails, refuse before opening a write transaction.
3. Dry-run by default
Running the script with no apply flag must not write anything.
Make dry-run the default behavior.
Require an explicit:
--apply
for writes.
Dry-run should report:
workbook validation
planned resort count
planned resort-pass mapping count
country-code validation
duplicate findings
missing slug findings
4. Resort import behavior
Upsert resorts by unique slug.
Import the reviewed canonical fields needed by BaseLodge, including the canonical/compatibility geography fields identified in the investigation.
Do not silently create duplicate resort rows.
5. ResortPass import behavior
Preserve all 245 distinct mappings.
The current legacy importer is wrong because it collapses multiple mappings per resort.
Implement by unique:
(resort_slug, pass_name)
Do not overwrite:
Ikon + Other
Epic + Other
or any other valid multi-pass combination
Do not create a None/blank pass row.
No mapping means the resort has no major-pass mapping.
Reject duplicate identical mapping pairs in the source rather than silently collapsing them.
6. Country code reconciliation
The workbook contains:
AD
AT
GE
which are currently missing from utils/countries.py.
Add the minimum static country definitions required so those workbook resorts are represented consistently in the existing country selector.
Keep this change narrow to the missing canonical country codes.
Do not redesign country handling.
7. Transaction behavior
Live --apply must execute in one transaction.
On any validation or write failure:
rollback everything
leave the previous DB state intact
No partial reference import is acceptable.
8. Idempotency
Applying the same workbook twice must result in the same final state:
695 resort rows
245 resort-pass mappings
no duplicate resort slugs
no duplicate pass mappings
Existing rows should be updated deterministically by slug where appropriate.
9. Testing
Add focused tests for:
Workbook validation
required tabs
required fields
695 unique resort slugs
245 distinct valid mappings
allowed pass names only
no mapping slug missing from resorts
AD/AT/GE reconcile with country constants
Safety
missing import mode refuses
production runtime refuses
production identity refuses
development target mismatch refuses
non-PostgreSQL target refuses
importer source does not import Flask/app/models
Dry-run
default execution is non-mutating
reports planned counts
Disposable PostgreSQL apply
Start from a fresh #311/BL-306 test DB and verify:
695 resorts
245 resort-pass rows
no user rows created
no trip rows created
no SkiDays created
no push-device rows created
no analytics created
no orphan resort-pass rows
no duplicate slugs
no duplicate mappings
Rollback
Force a failure after writes begin and prove:
all inserted/updated resort data rolls back
all ResortPass changes roll back
Idempotency
Apply the workbook twice and verify stable exact row counts and mappings.
10. Validate before touching real development DB
First validate on disposable PostgreSQL.
Run:
focused reference-import tests
full regression suite against isolated test infrastructure
Do not import into the real Replit development DB yet.
Stop after implementation/testing and report readiness.
11. Workflow status
Keep:
Flask Server stopped
Project workflow stopped
Do not restart either.
12. Production safety
During this task:
no production import
no production reads of private/user data
no production migration
no production schema change
no OneSignal call
no production push-token operation
no BL-77 change
Commit requirement
If and only if:
importer implementation passes
disposable PostgreSQL apply passes
dry-run/refusal/rollback/idempotency tests pass
full regression suite passes
production remains untouched
real Replit development DB remains untouched
then commit.
The Git commit message MUST include Task #312.
Use:
#312: Add safe development reference-data importer
Do not omit #312.
Return
Report:
files changed
importer behavior
country-code changes
safety guards
dry-run result
disposable PostgreSQL import result
resort count
resort-pass mapping count
rollback result
idempotency result
focused test result
full regression result
confirmation production untouched
confirmation real Replit development DB untouched
Flask Server status
Project workflow status
BL-77 status
Git commit hash
exact Git commit message
End with exactly one:
REFERENCE IMPORTER VALIDATED — READY FOR EXPLICIT DEVELOPMENT DATA LOAD APPROVAL
or
REFERENCE IMPORTER NOT READY: [reason]