{% else %}
<section class="home-empty">

  <header class="home-empty-header">
    <h1 class="type-display home-empty-name">{{ user.first_name | format_name }}.</h1>

    {% if _id %}
      <p class="type-caption home-empty-identity">{{ _id | join(' · ') }}</p>
    {% endif %}

    {% if _eq_parts %}
      <a href="{{ url_for('settings_equipment') }}" class="home-empty-equipment">
        {{ _eq_parts | join(' · ') }}
      </a>
    {% else %}
      <a href="{{ url_for('settings_equipment') }}" class="home-empty-profile-link">
        Complete your profile
      </a>
    {% endif %}
  </header>

  <div class="home-empty-stats">
    <div class="home-empty-stats-tile">
      <span class="type-label home-empty-stats-label">Trips</span>
      <span class="type-stat home-empty-stats-num">{{ stat_trips_total or 0 }}</span>
    </div>
    <div class="home-empty-stats-tile">
      <span class="type-label home-empty-stats-label">Mountains</span>
      <span class="type-stat home-empty-stats-num">{{ stat_mountains or 0 }}</span>
    </div>
    <div class="home-empty-stats-tile">
      <span class="type-label home-empty-stats-label">Wishlist</span>
      <span class="type-stat home-empty-stats-num">{{ stat_wishlist or 0 }}</span>
    </div>
  </div>

  <div class="home-empty-editorial">
    <p class="type-tagline home-empty-tagline">
      The best days on the mountain don't just happen. They're made.
    </p>

    <p class="type-body home-empty-body">
      Your season lives here. Plan trips, see where your friends are headed, and find weekends that work.
    </p>

    <ol class="home-empty-list">
      <li class="home-empty-list-item">
        <span class="type-label home-empty-list-num">01</span>
        <span class="type-list home-empty-list-text">Your trips, past and upcoming</span>
      </li>
      <li class="home-empty-list-item">
        <span class="type-label home-empty-list-num">02</span>
        <span class="type-list home-empty-list-text">Friends' plans and availability</span>
      </li>
      <li class="home-empty-list-item">
        <span class="type-label home-empty-list-num">03</span>
        <span class="type-list home-empty-list-text">Overlapping weekends to ride together</span>
      </li>
      <li class="home-empty-list-item">
        <span class="type-label home-empty-list-num">04</span>
        <span class="type-list home-empty-list-text">Mountains you've visited or want to visit</span>
      </li>
    </ol>
  </div>

  <p class="type-caption home-empty-closing">Explore the menu below</p>

</section>
{% endif %}