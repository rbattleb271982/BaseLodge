Make the following coordinated UX + UI updates to the BaseLodge Flask project.
Use checkpoints only when truly necessary.
Do not change models.py or database schema in this pass — keep everything UI-first and use placeholders where needed.
1️⃣ Bottom Navigation → Add “More” Tab
File: templates/components/bottom_nav.html
Update the bottom navigation so the tab order is:
Home
Friends
Invite
More
Each item should:
Link to the appropriate route using url_for.
Use the existing styling (same structure/classes as current tabs).
Behavior:
Home → route that renders home.html (whatever it’s currently using).
Friends → route that renders friends.html.
Invite → route that renders invite.html (we’ll create if missing).
More → new route that will render more.html.
Remove any “Profile” or “Settings” tab from the bottom nav — those will now live under More.
2️⃣ Create “More” Screen
New template: templates/more.html
Route: add to app.py
In app.py, add a new route:
@app.route("/more")
@login_required
def more():
    return render_template("more.html")
Create templates/more.html with a simple, mobile-friendly layout:
Page header: “More”
A vertical list of full-width rows (no icons), each row clickable:
My Info → link to the route that currently renders profile.html (we may rename / adjust below).
Settings → link to new /settings route.
Invite a Friend → link to existing /invite route (see section 3).
Sign Out → link to logout route (whatever route currently logs the user out, e.g. /logout).
Each row should:
Have label text on the left.
A subtle “chevron” or > indicator on the right for clickable items, except “Sign Out” which can just be red text or bold.
Use existing typography/colors from static/styles.css. If needed, add simple classes like:
.list-section
.list-row
.list-row-label
.list-row-chevron
Ensure the bottom nav is included on this page using your standard include pattern.
3️⃣ Ensure Invite Screen Exists and Is Linked Correctly
Templates / routes:
If not already present, add:
@app.route("/invite")
@login_required
def invite():
    return render_template("invite.html")
Create templates/invite.html (or adjust existing) to contain:
Header: “Invite a Friend”
Short description:
“Share BaseLodge with your ski crew. Copy your invite link or let a friend scan your QR code.”
Two large buttons:
“Copy Invite Link” (placeholder; for now this can either do nothing or call a tiny JS function that copies a static URL like https://baselodge.com/invite).
“Show QR Code” (show a simple placeholder box or static image, not dynamic QR yet).
Include bottom nav.
In bottom_nav.html, ensure the Invite tab links to url_for("invite").
4️⃣ Update Onboarding (setup_profile.html)
File: templates/setup_profile.html
We want a two-step onboarding:
Screen 1 – Rider Type
Locate the first step content in setup_profile.html (the first screen in your onboarding flow).
Update the first question to:
Label: “What type of rider are you?”
Input: <select> dropdown.
name attribute should match the existing backend field for rider type (use whatever is there — e.g., rider_type).
Options (exact order):
Skier
Snowboarder
Cross-Country Skier
Snowshoer
Both Skier & Snowboarder
Style the dropdown using the same form-control classes already used in this template.
Screen 2 – Pass Type
The second onboarding screen should ask:
Label: “Which pass do you have?”
Input: <select> dropdown.
Use the existing field name for pass type (e.g., pass_type).
Include these options (exact text):
Epic
Epic Local
Epic Military
Epic Adaptive
Epic Northeast Value
Epic 4-Day
Epic 5-Day
Epic 7-Day
Ikon
Ikon Base
Ikon Session Pass (2-Day, 3-Day, 4-Day)
Indy
Mountain Collective
Other
None
Ensure:
The “Next” and “Back” buttons for onboarding steps still work.
The second screen remains simple and mobile-friendly.
Do not change the route/function names in app.py that handle this form. Only update the HTML structure and labels/inputs.
5️⃣ Home Screen – Text & “Add a Trip” Button
File: templates/home.html
Route: existing route (do not rename).
A. Welcome text
Find the heading near the top that says something like:
Welcome back, {{ current_user.first_name }}
Change it to:
Welcome, {{ current_user.first_name }}
B. Make the stat line larger
Find the line that displays the combined rider info, like:
{{ current_user.pass_type }} — {{ current_user.rider_type }} — {{ current_user.skill_level }}
Wrap it in an element with a larger font size and slightly bolder weight. For example:
<p class="stats-line">
  {{ current_user.pass_type }} — {{ current_user.rider_type }} — {{ current_user.skill_level }}
</p>
In static/styles.css, add or adjust a .stats-line class if needed:
.stats-line {
  font-size: 1.05rem; /* or slightly larger than body */
  font-weight: 600;
  margin-bottom: 0.5rem;
}
Keep it inline and on one line. Do not drastically change surrounding layout.
C. Fix “Add a Trip” button
In home.html, locate the “Add a trip” button.
Ensure it links to a route that exists. If no proper route exists yet:
Add a simple placeholder route to app.py:
@app.route("/add_trip")
@login_required
def add_trip():
    return render_template("add_trip.html")
Create templates/add_trip.html with:
Header: “Add a Trip (Coming Soon)”
Short body text: “Soon you’ll be able to add upcoming ski days here.”
Include bottom nav.
Update the button in home.html to:
<a href="{{ url_for('add_trip') }}" class="...">
  Add a trip
</a>
Use the same button styling as your other primary CTA buttons.
6️⃣ Convert Profile into “My Info” (Carv-style rows)
We’ll treat profile.html as the My Info page and link it from More.
Files:
templates/profile.html
app.py
A. Routing / naming
In app.py, find the route that renders profile.html (probably /profile).
Keep the route URL as /profile for now, but the nav/link from More should label it “My Info”.
When rendering profile.html, ensure you pass:
mountains_visited_count (placeholder int)
trips_count (placeholder int)
friends_count (placeholder int)
selected_mountains (placeholder list of names; see section 7)
If these variables are not yet computed, set them to simple defaults for now, e.g.:
mountains_visited_count = len(selected_mountains)  # if list exists, else 0
trips_count = 0
friends_count = 0
And include them in render_template("profile.html", ...).
B. Layout inside profile.html
Update the main heading of profile.html to “My Info” instead of “Profile” (if not already).
At the top of the page content, add a small stats section:
<div class="profile-stats">
  <div>Mountains visited: {{ mountains_visited_count }}</div>
  <div>Trips: {{ trips_count }}</div>
  <div>Friends: {{ friends_count }}</div>
</div>
Style it in styles.css as a compact row or stacked small text. It should be read-only and not clickable.
Below the stats, convert your editable fields into full-width, Carv-style rows:
Rows (in this order):
Rider Type
Skill Level
Pass Type
Gender
Home State
Birth Year
Gear (for now, this can be a non-interactive placeholder row that says “Coming soon” as the value, unless there is already a gear field in the backend).
For each row:
Left side: label (e.g., “Rider Type”).
Right side: current value (e.g., “Snowboarder”).
Use a > or chevron on the right for editable fields (Rider Type, Skill Level, Pass Type, Gender, Home State, Birth Year).
For now, you can leave the editing behavior as the existing form if present (e.g., a standard form submit). If profile.html currently uses a traditional <form> with inputs:
You can keep the form but visually wrap each input in a row-looking container, or
Show the values in static rows and place an “Edit” button at the bottom that goes to your existing edit_profile.html for now.
Important:
Do not introduce new backend fields like gear in models. For Gear, add a row like:
<div class="settings-row">
  <span class="settings-label">Gear</span>
  <span class="settings-value">Coming soon</span>
</div>
Later, we can wire this up when the DB supports it.
Add a “Mountains Visited” row in My Info that shows the count and links to the selector page (next section):
<div class="settings-row">
  <a href="{{ url_for('mountains_visited') }}" class="settings-link-row">
    <span class="settings-label">Mountains Visited</span>
    <span class="settings-value">{{ mountains_visited_count }}</span>
    <span class="settings-chevron">&gt;</span>
  </a>
</div>
This row is clickable and navigates to the mountains selection UI.
Style rows in styles.css with something like:
.settings-row,
.settings-link-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0.75rem 1rem;
  border-bottom: 1px solid #e5e5e5;
}

.settings-label {
  font-weight: 500;
}

.settings-value {
  color: #555;
}

.settings-chevron {
  margin-left: 0.5rem;
}
Adjust naming to match your existing CSS style conventions.
7️⃣ Mountains Visited Selector Page
We’re creating a new page where the user can:
Select a state.
See a list of mountains alphabetically for that state.
Check multiple mountains.
Save selections.
Files:
app.py
templates/mountains_visited.html
A. Backend: route + placeholder data
In app.py, add:
@app.route("/mountains-visited", methods=["GET", "POST"])
@login_required
def mountains_visited():
    # Placeholder structure: state -> list of mountains
    mountains_by_state = {
        "Colorado": [
            "Arapahoe Basin",
            "Aspen Highlands",
            "Aspen Snowmass",
            "Beaver Creek",
            "Breckenridge",
            "Copper Mountain",
            "Keystone",
            "Steamboat",
            "Telluride",
            "Vail",
            "Winter Park",
        ],
        "California": [
            "Heavenly",
            "Kirkwood",
            "Mammoth Mountain",
            "Northstar",
            "Palisades Tahoe",
        ],
        "Utah": [
            "Alta",
            "Brighton",
            "Deer Valley",
            "Park City",
            "Snowbird",
            "Solitude",
        ],
        "Wyoming": [
            "Jackson Hole",
        ],
    }

    # For now, use a simple placeholder list for selected mountains.
    # Later, this should be loaded from and saved to the database.
    selected_mountains = []  # TODO: replace with real user data

    if request.method == "POST":
        # Read selected mountains from form
        selected_mountains = request.form.getlist("mountains")
        # TODO: Save to DB when data model is ready

        # Redirect back to My Info or stay on this page
        return redirect(url_for("profile"))

    # Compute count for display if needed
    mountains_visited_count = len(selected_mountains)

    return render_template(
        "mountains_visited.html",
        mountains_by_state=mountains_by_state,
        selected_mountains=selected_mountains,
        mountains_visited_count=mountains_visited_count,
    )
Ensure request and redirect, url_for are imported at the top if not already.
B. Template: templates/mountains_visited.html
Create a new template:
Header: “Mountains Visited”
Under the header, show a line like:
“You’ve selected {{ mountains_visited_count }} mountains.”
If selected_mountains is non-empty, show them as a small pill list:
e.g., Vail • Jackson Hole • Mammoth Mountain
Add a <form method="POST"> that includes:
A <select name="state"> at the top to choose a state.
Options generated from mountains_by_state.keys(), sorted alphabetically.
Below the select, display the list of mountains for the currently selected state as checkboxes:
For now, you can treat the first state alphabetically as the default selected when rendering, or use a query parameter or hidden input to track the currently chosen state.
Each checkbox:
<label class="mountain-option">
  <input
    type="checkbox"
    name="mountains"
    value="{{ mountain }}"
    {% if mountain in selected_mountains %}checked{% endif %}
  />
  {{ mountain }}
</label>
At the bottom, include a full-width Save button that submits the form via POST.
Make sure the page is mobile-friendly:
Use vertical stacking.
Adequate padding.
Scrollable list of mountains.
Include the bottom nav if that’s your pattern, or not — your choice, but be consistent with how other secondary pages behave.
8️⃣ Settings Page (Lightweight)
Files:
app.py
templates/settings.html
In app.py, add:
@app.route("/settings")
@login_required
def settings():
    return render_template("settings.html")
Create templates/settings.html with:
Header: “Settings”
A simple vertical list of rows:
Notifications (placeholder toggle or text “Coming soon”)
Location Sharing (placeholder)
Units (text “Imperial / Metric (coming soon)”)
All of these can just be static rows for now, no real backend behavior.
Ensure the More menu links “Settings” to this route with url_for("settings").
9️⃣ CSS Tweaks (static/styles.css)
File: static/styles.css
Add or adjust classes to support:
Carv-style full-width rows for More, My Info, Settings, and Mountains Visited checkboxes.
Slightly bigger stat line on Home (.stats-line).
Profile stats row (.profile-stats).
Settings list rows (.settings-row, .settings-label, .settings-value, .settings-chevron).
Mountain option labels (.mountain-option).
Keep everything consistent with existing typography and spacing. Do not introduce a completely new visual system; extend the existing one.
After making these changes, please:
Ensure all templates still render without error.
Verify navigation:
Bottom nav → Home / Friends / Invite / More.
More → My Info / Settings / Invite a Friend / Sign Out.
My Info → shows stats and profile rows and has a working “Mountains Visited” row.
“Mountains Visited” → shows state dropdown, mountains checkboxes, Save button.
Confirm “Add a trip” no longer breaks and routes somewhere safe (even if it’s just a placeholder page).