PUT THIS IN AGENT

We need to STOP and diagnose the logic behind the
"You'll be there X days during this time."
callout on the Friend Trip Details screen.

This is the SECOND time this has surfaced, so this request is
STRICTLY READ-ONLY.

❌ Do NOT modify logic
❌ Do NOT propose fixes
❌ Do NOT update copy
❌ Do NOT refactor

–––––––––––––––––
A) IDENTIFY THE SOURCE
–––––––––––––––––

1) Identify EXACTLY where the value for
"You'll be there X days"
is computed.

Provide:
- Function name
- File name
- Line numbers
- Variable name(s) passed to the template

–––––––––––––––––
B) DATA USED (CRITICAL)
–––––––––––––––––

2) List ALL data sources used to compute X:
   - Current user's SkiTrip records?
   - Friend's SkiTrip records?
   - User.open_dates availability?
   - Any derived date sets?

3) Explicitly confirm:
   - Is SAME mountain/resort required?
   - If yes, how is that check implemented?
   - If no, why not?

–––––––––––––––––
C) DATE CALCULATION DETAILS
–––––––––––––––––

4) Document the exact date logic:
   - How are calendar days generated?
   - Inclusive vs exclusive bounds
   - How intersections are computed

5) Confirm whether this logic:
   - Counts days at ANY resort, or
   - Counts days ONLY at the friend’s resort

–––––––––––––––––
D) CONDITIONAL RENDERING
–––––––––––––––––

6) What condition causes the callout to render?
   - Is it shown when X > 0?
   - Is it always shown if a date range overlaps?
   - Is it tied to availability instead of trips?

7) Confirm whether the callout can appear when:
   - User has NO trip at the same resort
   - User is only "open" on those dates

–––––––––––––––––
E) CONCRETE WALKTHROUGH
–––––––––––––––––

8) Walk through the exact scenario shown:
   - Friend trip: Niseko, Feb 22–Feb 28
   - Current user: NO Niseko trip in that range
   - Explain step-by-step why the callout currently says:
     "You'll be there 5 days"

–––––––––––––––––
F) CONCLUSION
–––––––––––––––––

9) State clearly:
   - What X actually represents today
   - Whether this is:
     • Trip overlap logic
     • Availability logic
     • Mixed logic
     • Incorrect logic

Do NOT fix yet.
Return diagnosis only.