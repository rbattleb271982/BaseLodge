PUT THIS IN PLAN FIRST

Audit and redesign the BaseLodge Profile page structure to align with the newer Home-page identity system and remove redundant information.

Current issue:
The Home screen evolved into a cleaner, more editorial identity-first layout:
- stacked rider metadata
- cleaner hierarchy
- profile identity emphasized first

The Profile page still reflects an older utility/settings structure and now contains redundant information.

Examples of redundancy:
- Rider type / skill / pass repeated again inside “Profile details”
- Mountains visited count repeated
- Wishlist count repeated
- Profile header structure inconsistent with Home

Goal:
Make the Profile page feel like a natural continuation of the Home identity system while reducing duplication and improving clarity.

Do not make changes yet.

Audit requirements

1. Inspect the current Profile page structure
- templates/profile.html
- related partials/components
- profile card/header components if reused

2. Identify all duplicated information between:
- Home header
- Profile top section
- stats row
- profile detail rows

3. Recommend a cleaner information hierarchy.

Questions to answer:
- What should remain in the top identity section?
- What should move into editable detail rows?
- What should be removed entirely?
- Which stats should only exist once?
- Should Profile details become a true edit entrypoint rather than a repeated summary?

4. Recommend a revised Profile structure.

Example direction (not mandatory):
- Identity header (matching Home style)
- Stats row
- Editable modules:
  - Profile details
  - Gear
  - Mountains visited
  - Wish list
- Account/settings section

5. Determine whether:
- Home and Profile should share the same partial/component
- identity rendering should be centralized
- stats row should be shared
- profile card should become reusable

6. Mobile UX considerations
- Avoid excessive vertical space
- Avoid repeating the same metadata twice
- Preserve edit discoverability
- Keep the page feeling premium/editorial instead of settings-heavy

7. Deliver recommendations for:
- exact sections to remove
- exact sections to merge
- what the new Profile top should contain
- whether counts should remain in tiles only
- whether “Profile details” should show summary text or become cleaner CTA-style navigation

Do not implement yet.

Return:
- proposed information architecture
- affected templates/components
- recommended component reuse strategy
- migration/refactor approach
- risks/edge cases

Use checkpoints sparingly.
Do not finish until the full Profile page structure has been reviewed against the current Home layout.