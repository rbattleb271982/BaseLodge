PUT THIS IN AGENT

In the current dev environment, create one test friend account and fully connect it to `richardbattlebaxter@gmail.com` across all relevant relationship paths.

Goal:
Richard should have a realistic connected friend in dev so I can test all friend-related screens and flows.

Requirements:

1. Find the user with email:

* `richardbattlebaxter@gmail.com`

2. Create a new test user if one does not already exist.
   Use sensible test data for:

* first_name
* last_name
* email
* password
* rider/pass/profile basics
  Make the email obviously dev/test-only.

3. Create all relevant friendship/social connections needed so this test user behaves like a fully connected friend of Richard in the app.
   That includes whatever the current codebase expects for:

* accepted friendship
* bidirectional friend visibility if needed
* any accepted invite / participant / relationship records if those are required for friend-related features to show up correctly

4. Also create a realistic trip scenario for testing:

* the test friend should have created a trip
* that trip should be visible in the way friend trips normally appear in the app
* Richard should be invited to that trip
* set the invite/guest/participant state in whatever way the current codebase expects so the trip and invite surfaces behave realistically in dev

5. Do not guess blindly.
   Inspect the current friend/invite/social/trip logic and create only the records actually needed for a “real” connected friend + trip invite state in this app.

6. If there are multiple possible relationship tables involved, make sure Richard and the test user are connected in the way the app actually reads as a friend, and that the trip invitation is represented the way the app actually reads it.

7. Do not modify production.
   Dev environment only.

8. Report back with:

* test user email
* what records were created
* which tables were touched
* trip details created
* invite details created
* whether the setup is now sufficient for testing friend features and invited-trip flows in dev

Use checkpoints sparingly.
