Merge and close BL-123 / Task #380 — Normalize Wishlist Destinations.

The implementation is approved.

Do not create another planning task.
Do not modify product behavior beyond the completed BL-123 scope.
Do not include unrelated files.
Do not touch the untracked implementation brief.

1. PRE-MERGE VERIFY

Confirm the working tree contains only the intended BL-123 changes:

- app.py
- models.py
- services/wishlist.py
- services/ideas_retrieval.py
- templates/components/profile_card.html
- templates/admin_mountain_intelligence.html
- tests/test_wishlist_normalization.py
- tests/test_home_empty_states.py

If any unrelated tracked changes are present, STOP and report them.

Confirm:
- no .replit change remains
- no migration/schema file changed
- no dependency file changed
- supplied implementation brief remains untracked and untouched

2. FINAL VALIDATION

Run:

- focused wishlist tests
- Home / BL-176 regressions
- Mountain regressions
- admin merge regressions
- affected auth/CSRF tests
- git diff --check

The prior validation baseline was:

298 passed

If the final validation materially differs, STOP and report the failure rather than merging.

3. COMMIT

Create one commit containing only BL-123 changes.

Preferred commit message:

BL-123: normalize wishlist destination IDs

4. VERIFY COMMIT

Return:

- commit SHA
- commit message
- files committed
- tests run
- tests passed
- git diff --check result
- working tree state
- confirmation that unrelated untracked files remain untouched

5. NOTION CLOSEOUT

Update BL-123 / Task #380 to Done with:

- Git Commit = final commit SHA
- Tests = concise final test result
- Completed = current date

Do not create a new task.

6. FINAL STATE

Confirm:
- BL-123 merged
- no uncommitted BL-123 changes remain
- no migration was created
- no production data was changed
- no unrelated files were included

RETURN

BL-123 MERGE RESULT

- Commit:
- Message:
- Files committed:
- Tests:
- git diff --check:
- Working tree:
- Notion status:
- Git Commit recorded:
- Completed date recorded:
- Unrelated files untouched:

End exactly:

BL-123 MERGED