PUT THIS IN AGENT

Make a final UX polish pass on /season-snapshot so the page feels like a shareable “capture surface” instead of a dark app overlay.

Do not change backend logic.
Do not change grouping logic.
Do not change trip data.
Do not redesign the card itself.

This pass is ONLY about:
- page background
- instruction text
- composition
- ensuring correct trip formatting is preserved

---

CRITICAL: PRESERVE CARD DESIGN

Keep the card EXACTLY as currently implemented:
- “Richard’s Winter” headline
- serif typography
- cream card background
- identity line styling
- “— on BaseLodge —”
- spacing and layout
- bottom hairline

Do not restyle or restructure the card.

---

CRITICAL: PRESERVE TRIP FORMAT

Ensure the following format is used and NOT changed:

1. Month labels:
- Use abbreviated editorial format:
  DEC, JAN, FEB, MAR, APR

2. Trip rows:
- Left: Resort name + state
- Right: formatted date range

3. Date formatting:
- Same month:
  Dec 18 — 22
- Cross month:
  Dec 27 — Jan 2

4. No status labels:
- Remove ALL instances of:
  GOING, PLANNING, etc.
- Card should only show:
  month, destination, dates

---

REQUIRED CHANGES

1. Replace black background
- Remove the full black page background
- Replace with a soft editorial backdrop:
  - warm taupe / parchment / mushroom tone
  OR
  - very subtle warm gradient
- The page should feel like a “share studio,” not an app overlay

---

2. Add instruction text above card

Add ABOVE the card:

Primary line:
“Take a screenshot of the card below”

Secondary line:
“Share your season wherever you want”

Requirements:
- Keep both lines OUTSIDE the card
- Center aligned
- Subtle styling
- Do not overpower the card

---

3. Add subtle capture framing

Add a very subtle visual cue behind or around the card:
- faint outline OR
- soft framing area OR
- light studio-style backing

Requirements:
- No heavy borders
- No gimmicks
- Must feel premium and minimal
- Should gently indicate “this is the thing to capture”

---

4. Improve composition

- Center the card more intentionally in the viewport
- Reduce excessive empty space below
- Balance:
  instruction text → card → bottom spacing

The layout should feel deliberate and composed.

---

5. Keep everything else unchanged

Do NOT:
- modify the card structure
- add UI elements inside the card
- reintroduce headers or footers
- change typography inside the card
- change route logic or helpers

---

After updating, return:

1. What background treatment was used
2. What instruction text was added
3. How capture framing was implemented
4. Confirmation that:
   - month labels are DEC/JAN/etc.
   - date formatting matches spec
   - status labels are removed
5. How overall composition was improved
6. Any assumptions made