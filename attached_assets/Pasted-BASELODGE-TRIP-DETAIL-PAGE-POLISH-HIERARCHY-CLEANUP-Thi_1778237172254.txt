BASELODGE — TRIP DETAIL PAGE POLISH + HIERARCHY CLEANUP

This is a visual hierarchy / interaction cleanup pass.

Do NOT redesign the page.

The goal is to reduce visual noise, remove redundant edit affordances, tighten hierarchy, and clarify which elements are editable vs navigational.

━━━━━━━━━━━━━━━━━━━━
1. REMOVE TOP-RIGHT EDIT BUTTON
━━━━━━━━━━━━━━━━━━━━

Remove the top-right “Edit” action entirely.

The page now supports:
- inline editing where appropriate
- direct interaction on editable rows
- mountain navigation

The global Edit entry point is now redundant and conflicts with the cleaner inline-edit interaction model.

━━━━━━━━━━━━━━━━━━━━
2. MOUNTAIN TITLE = NAVIGATION ONLY
━━━━━━━━━━━━━━━━━━━━

The mountain title should navigate to the mountain/resort detail page.

It should NOT be editable from the trip detail page anymore.

Remove:
- mountain edit pencil icon
- any mountain edit interaction/sheet from this page

Preserve:
- navigation to the mountain detail page
- subtle tappable styling
- editorial/native feel

Do NOT:
- use blue hyperlinks
- use underlines
- make it look web-like

The mountain title should feel:
- navigational
- subtle
- tappable
- calm

━━━━━━━━━━━━━━━━━━━━
3. REMOVE EXTRA EDIT ICONS
━━━━━━━━━━━━━━━━━━━━

There are now too many explicit edit affordances on the page.

Remove:
- mountain pencil icon
- date pencil icon

Keep:
- subtle row/tap affordance only

Do NOT clutter the page with edit glyphs when the interaction is already obvious.

━━━━━━━━━━━━━━━━━━━━
4. REMOVE PASS ICON
━━━━━━━━━━━━━━━━━━━━

Remove the decorative Pass icon from the Pass row.

The icon adds visual noise without adding meaning.

Keep:
- Pass label
- pass value
- pass edit behavior

━━━━━━━━━━━━━━━━━━━━
5. REMOVE REDUNDANT SECTION HEADINGS
━━━━━━━━━━━━━━━━━━━━

The page structure is now strong enough that some headings are unnecessary.

Remove:
- “What’s next?”

Let the “See who’s going” card stand on its own.

Evaluate whether:
- “Trip setup”

is still necessary.

Preferred:
If removing it still feels visually clear, remove it too.

The cards themselves already communicate the grouping.

━━━━━━━━━━━━━━━━━━━━
6. CLEAN UP SEE WHO’S GOING COPY
━━━━━━━━━━━━━━━━━━━━

Replace:
“See friends heading to Vail.”

With:
“Friends heading to Vail”

Cleaner.
Less conversational.
More native.

━━━━━━━━━━━━━━━━━━━━
7. TIGHTEN CARD DENSITY SLIGHTLY
━━━━━━━━━━━━━━━━━━━━

The setup card still has slightly too much vertical spaciousness.

Reduce:
- row vertical padding
- spacing between rows
- overall row height slightly

Target:
~10–15% tighter density

Do NOT:
- make rows cramped
- reduce tap target usability
- redesign the card

The goal is:
less “web app spacing”
more “native settings density”

━━━━━━━━━━━━━━━━━━━━
8. SOFTEN DEFAULT/EMPTY VALUES SLIGHTLY
━━━━━━━━━━━━━━━━━━━━

Values like:
- No pass
- Not set
- No lesson
- Not needed

should have slightly softer visual emphasis than actively selected values.

Do NOT:
- reduce contrast excessively
- make them hard to read

Just reduce visual dominance slightly.

━━━━━━━━━━━━━━━━━━━━
9. EVALUATE METADATA BAR DIVIDERS
━━━━━━━━━━━━━━━━━━━━

The metadata bar:
- Visible to friends
- Planning

may no longer need strong divider/border treatment.

Evaluate whether:
- center divider
- borders
- heavy separators

can be softened slightly now that the page hierarchy is cleaner.

Do NOT redesign the metadata bar completely.

━━━━━━━━━━━━━━━━━━━━
10. PRESERVE EXISTING INTERACTION MODEL
━━━━━━━━━━━━━━━━━━━━

Keep:
- editable dates
- editable pass
- editable setup rows
- setup accordions
- “Visible to friends”
- Planning status
- bottom sheet behaviors
- mountain detail navigation

Do NOT:
- break any existing editing flow
- change trip logic
- change notifications
- introduce Tailwind
- do dead CSS cleanup yet

This is a polish/hierarchy pass only.

Use checkpoints sparingly.