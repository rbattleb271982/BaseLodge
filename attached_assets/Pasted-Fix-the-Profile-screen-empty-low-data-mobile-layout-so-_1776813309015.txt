Fix the Profile screen empty / low-data mobile layout so it fits on screen without unnecessary scrolling, and fix the broken profile name rendering in the header.
Scope
Profile screen only
Mobile-first
Empty / low-data state specifically
Do not redesign the page
Problems to fix
The Profile screen currently requires scrolling even when there is very little information shown
The header name is rendering incorrectly as Createaccount instead of the user’s real display name
The screen has too much vertical spacing between sections and rows in the empty / low-data state
What to do
1. Fix the header name bug
Ensure the Profile header shows the actual user’s name
Do not show Createaccount
Use the correct display name / first name logic already used elsewhere in the app
Verify for fullseed@gmail.com and other seeded users
2. Reduce vertical space so the page fits without scrolling
Tighten the Profile screen layout in the empty / low-data state by:
reducing top spacing under the header
reducing vertical spacing between the stat bar and the first section
reducing vertical spacing between section headers (ACTIVITY, PROFILE, ACCOUNT) and their rows
reducing row padding slightly
tightening the gaps between each row group
Do this carefully so it still feels premium and readable.
3. Keep the existing structure
Do NOT remove these sections:
Notifications
Profile details
Equipment
Mountains visited
Wish list
Change password
Log out
Delete account
But make the layout compact enough that this low-data state does not need scrolling on a standard iPhone viewport.
4. Preserve hierarchy
Keep section labels visible
Keep row titles readable
Keep secondary text muted
Do not make the screen feel cramped or dense
Do not shrink fonts aggressively just to force fit
Constraints
Do not redesign the profile page
Do not change copy
Do not remove rows
Do not change business logic
Only tighten layout and fix the broken name rendering
Use checkpoints sparingly
Testing
Verify on mobile that:
The correct user name appears in the header
The low-data Profile screen fits without scrolling on a standard iPhone viewport
The page still feels clean and readable
The bottom nav remains visible and properly spaced