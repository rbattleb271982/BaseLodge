REPLIT: MAX + PLAN
Estimated Batch completion after this step: ~90%

TASK: BL-159 — Bound Friends / Friends’ Trips retrieval with progressive loading

INVESTIGATE AND DESIGN ONLY.
Do not implement yet.

==================================================
GOAL
==================================================

Measure how Friends-related pages and the Friends’ Trips collection scale as:

- reciprocal friend count grows
- friend-trip count grows
- participant records grow
- grouped Friends’ Trips results grow

Then design the smallest safe bounding/progressive-loading solution.

BL-161 established that authenticated Friends query shape was:

- 19 SQL statements before BL-161
- 18 after BL-161's duplicate participant retrieval cleanup
- 4 User SELECTs in the original measurement

BL-158 is now complete and deliberately left Friends’ Trips unchanged.

Do not assume BL-158's exact pagination architecture should be copied.
Measure BL-159 first.

==================================================
1. FIND AUTHORITATIVE BL-159 REQUIREMENTS
==================================================

Locate any existing BL-159 task/backlog requirements.

Use them as authoritative if found.

Review related completed work only for boundaries:

- BL-80 terminal trip history
- BL-133 authenticated social visibility
- BL-155 Trip Detail N+1 cleanup
- BL-161 authenticated-page query overhead
- BL-156 Home candidate bounding
- BL-158 My Trips progressive loading

Do not modify those completed systems unless required for a narrowly shared helper and proven regression-safe.

==================================================
2. IDENTIFY BL-159 SURFACES
==================================================

Determine exactly which current product surfaces BL-159 should cover.

At minimum investigate:

A. Friends page/list
B. Friends’ Trips tab/section on My Trips

Also identify any other route that clearly uses the same unbounded collection and belongs naturally to BL-159.

Do NOT automatically absorb:

- Friend Profile
- Home
- Ideas
- Mountain Detail
- Trip Detail

Report those separately if relevant.

==================================================
3. MAP FRIENDS PAGE PIPELINE
==================================================

Trace the authenticated Friends page end to end:

SOURCE RECORDS
→ RECIPROCAL FRIEND AUTHORIZATION
→ DATABASE RETRIEVAL
→ RELATED DATA
→ PYTHON PROCESSING
→ SORTING/GROUPING
→ TEMPLATE DATA
→ RENDERED ROWS/CARDS

Document:

- reciprocal-current Friend logic
- User retrieval
- pass context
- trip context
- availability/wishlist context if present
- request/invite context
- displayed counts
- sorting
- filtering
- empty states

==================================================
4. MAP FRIENDS’ TRIPS PIPELINE
==================================================

Trace the Friends’ Trips tab on My Trips independently.

Document:

- friend eligibility
- public/private trip rules
- organizer trips
- Going participation
- trip lifecycle
- attendance state
- Resort requirements
- grouping
- month/destination grouping
- sorting
- deduplication
- rendered cards/rows
- displayed counts

Identify exactly what BL-158 left unbounded.

==================================================
5. MEASURE FRIEND-COUNT SCALING
==================================================

Use disposable fixtures only.

At minimum measure reciprocal friend populations of:

- 10
- 50
- 100
- 500

For the Friends page measure:

- SQL statements
- User ORM loads
- Friend ORM loads
- participant/trip loads
- Resort loads
- Python objects/materialized rows
- rendered friend rows/cards
- approximate HTML size
- directional request time

Timing is diagnostic only.

==================================================
6. MEASURE FRIEND-TRIP SCALING
==================================================

For Friends’ Trips test eligible source/result populations around:

- 10
- 50
- 100
- 500

Where practical distinguish:

A. one trip per friend
B. many trips for one friend
C. many friends with many trips
D. organizer-trip sources
E. Going-participant sources
F. mixed organizer + participant sources

Measure:

- SQL statements
- Trip loads
- User loads
- participant loads
- Resort loads
- Python grouping structures
- final rendered rows/groups
- HTML size

==================================================
7. DETERMINE THE ACTUAL PROBLEM
==================================================

Do not assume the Friends page and Friends’ Trips have the same issue.

For each surface determine whether growth occurs in:

- SQL query count
- SQL rows
- ORM materialization
- Python grouping
- template rendering
- HTML payload
- browser DOM size

Quantify the dominant scaling problem.

Classify each independently.

==================================================
8. CHECK FOR N+1 BEHAVIOR
==================================================

BL-161 removed one known duplicate Friends participant retrieval.

Now test whether remaining rendering causes per-friend or per-trip lazy loading.

Measure normalized SQL at 10/50/100/500.

Specifically inspect access to:

- User
- participant relationships
- Resort
- passes
- wishlist
- availability
- trip counts/context

If SQL count grows with rendered rows, identify the exact lazy relationship or helper causing it.

==================================================
9. FRIENDS PAGE PRODUCT SEMANTICS
==================================================

Document exact current behavior:

- ordering
- labels
- actions
- links
- pass display
- trip/activity context
- counts
- request states
- empty states

Determine whether every friend must be immediately visible or whether progressive loading is product-safe.

Do not redesign the Friends page.

==================================================
10. FRIENDS’ TRIPS PRODUCT SEMANTICS
==================================================

Document exact grouping and ordering.

Determine whether the UI groups by:

- month
- trip
- destination
- friend
- RSVP/status

or another current structure.

Establish the deterministic ordering necessary for any bounding solution.

Do not flatten or redesign grouping merely to make pagination easier.

==================================================
11. PRIVACY / AUTHORIZATION
==================================================

BL-133 is a hard boundary.

Confirmed friendship means:

reciprocal CURRENT Friend rows.

The following must never authorize:

- one-sided Friend row
- Pending relationship
- Declined relationship
- former friendship
- stale/history relationship
- historical FriendConnectionEvent records

Friends’ Trips must preserve current public-trip social visibility.

Private friend trips must not become visible merely because pagination/retrieval is consolidated.

==================================================
12. TRIP / RSVP ELIGIBILITY
==================================================

For Friends’ Trips verify current handling of:

- organizer trip
- Going
- Interested
- Pending
- Declined
- Removed
- public/private
- active lifecycle
- Completed
- Cancelled
- past trip
- future trip
- missing Resort

Preserve current behavior exactly.

BL-80 terminal semantics remain authoritative.

==================================================
13. DUPLICATION
==================================================

Determine whether a conceptual Friends’ Trip can enter through multiple paths, for example:

- friend organizes trip
- another friend attends same trip
- multiple friends attend same trip

Document current grouping/deduplication semantics.

Any pagination boundary must preserve the same conceptual grouping.

Do not page raw source rows if doing so can split or duplicate a conceptual displayed group incorrectly.

==================================================
14. PAGINATION BOUNDARY
==================================================

If Friends’ Trips requires bounding, determine what should actually be paginated:

- raw source rows
- unique trips
- grouped trip concepts
- month groups
- another existing display unit

The pagination unit must match current visible semantics.

Do not choose the easiest SQL unit if it causes incomplete groups.

==================================================
15. FRIENDS PAGE BOUNDARY
==================================================

If the Friends list itself requires bounding, determine the correct page unit and ordering.

Evaluate whether:

- all friends should remain visible
- first N + Load more is appropriate
- another progressive-loading pattern is safer

Base the decision on measured scale and current product behavior.

==================================================
16. BL-158 REUSE ASSESSMENT
==================================================

Inspect the completed BL-158 implementation:

- services/my_trips_paging.py
- cursor encoding/validation
- server-rendered fragment pattern
- Load more JavaScript behavior
- row partial architecture

Determine what can be reused safely.

Prefer small shared primitives only if they avoid duplication without creating a generalized pagination framework.

Do NOT refactor BL-158 merely to make BL-159 aesthetically cleaner.

==================================================
17. OFFSET VS KEYSET
==================================================

If pagination is recommended, evaluate:

A. OFFSET/LIMIT
B. keyset/cursor

Consider:

- grouping semantics
- deterministic ordering
- mutations between requests
- expected scale
- implementation complexity
- BL-158 precedent

Use keyset only where its correctness benefit justifies it.

==================================================
18. PAGE SIZE
==================================================

If progressive loading is recommended, measure/evaluate reasonable page sizes such as:

10
15
20

The correct page size may differ between:

- Friends
- Friends’ Trips

Explain the recommendation based on rendered density and grouping.

Do not automatically copy BL-158's 20.

==================================================
19. PROGRESSIVE LOADING UX
==================================================

Prefer BaseLodge's newly established Load more interaction if appropriate.

Determine whether each surface should use:

- Load more
- grouped Load more
- another existing pattern

No infinite scroll.

No frontend framework.

No visual redesign.

==================================================
20. COUNTS
==================================================

Identify every displayed or behavioral count.

Distinguish:

TOTAL AUTHORIZED RESULTS

from

CURRENTLY LOADED RESULTS.

Pagination must not accidentally change a total friend count or grouped trip count into the current page length.

==================================================
21. RELATED DATA BATCHING
==================================================

For the proposed visible page/group, determine how to load required related context without N+1 behavior.

Potential context includes:

- Users
- Trips
- Resorts
- participant states
- passes
- active friend counts
- trip attendees

Only load data actually required by current UI.

Do not introduce speculative enrichment.

==================================================
22. LARGE NETWORK BEHAVIOR
==================================================

Create representative stress fixtures such as:

- 500 reciprocal friends, few trips
- few friends, 500 eligible friend trips
- 100 friends with multiple trips each
- many friends attending the same trip
- one friend with many trips
- mixed organizer/Going sources

Measure both source cardinality and final visible display units.

==================================================
23. MUTATION BETWEEN PAGE LOADS
==================================================

Reason through:

- friendship removed
- friendship becomes one-sided
- trip becomes private
- trip cancelled/completed
- RSVP changes
- new trip created

Every progressive request must reauthorize against current state.

Do not rely on the first page's authorization snapshot.

==================================================
24. PERFORMANCE BUDGETS
==================================================

Recommend deterministic budgets based on the findings.

Examples:

- maximum hydrated friends per page
- maximum hydrated trip display units per page
- maximum related objects
- stable SQL count across 10/50/100/500
- no per-card/per-friend SQL
- bounded rendered HTML

Do not make wall-clock timing an acceptance threshold.

Use BL-161's existing Friends query measurements as regression context, not an arbitrary requirement if the architecture legitimately changes.

==================================================
25. EQUIVALENCE TESTING
==================================================

For datasets smaller than the chosen page size:

before/after visible output should remain effectively identical.

For larger datasets:

concatenating all pages must reproduce the complete deterministic reference output.

For grouped Friends’ Trips, equivalence must include:

- group membership
- trip identity
- friend identity/context
- ordering
- labels
- links
- counts

==================================================
26. OUT OF SCOPE
==================================================

Do not implement:

- Home changes
- Ideas changes
- Friend Profile redesign
- Trip Detail changes
- My Trips viewer-feed changes from BL-158
- new friend search
- friend recommendations redesign
- new trip filters
- global caching
- infinite scrolling
- frontend framework
- generalized pagination framework
- schema denormalization

==================================================
27. SCHEMA / DATABASE SAFETY
==================================================

Strong expectation:

NO schema change
NO migration
NO index change

Do not:

- create Alembic revisions
- apply BL-70
- mutate Supabase Development
- mutate persistent Replit
- access Production

If an index/schema change appears useful, report it separately.

==================================================
28. TEST PLAN
==================================================

Design deterministic coverage for:

- 10/50/100/500 friends
- 10/50/100/500 friend-trip sources/display units
- one friend/many trips
- many friends/one trip
- mixed organizer/participant sources
- reciprocal-current friendship
- one-sided relationship
- former friendship
- private trip
- public trip
- lifecycle states
- RSVP states
- grouping/deduplication
- exact page boundary
- page size + 1
- final partial page
- invalid cursor if used
- authorization changes between requests
- no duplicates
- no missing results
- full concatenation equivalence
- SQL/cardinality budgets

==================================================
29. PRIORITIZE FINDINGS
==================================================

Classify each finding:

HIGH
- materially growing work with a correctness-safe bound

MEDIUM
- measurable but not worth complexity yet

LOW
- theoretical/already adequately bounded

It is acceptable for the correct BL-159 implementation to address only one of the investigated surfaces if the other does not demonstrate a meaningful scaling problem.

==================================================
30. DELIVERABLE
==================================================

Return:

A. authoritative BL-159 requirements
B. BL-159 surfaces
C. Friends page pipeline
D. Friends’ Trips pipeline
E. Friends page current semantics
F. Friends’ Trips current semantics/grouping
G. 10/50/100/500 friend scaling
H. 10/50/100/500 friend-trip scaling
I. SQL growth findings
J. ORM/materialization findings
K. HTML/render growth
L. N+1 findings
M. privacy/authorization findings
N. lifecycle/RSVP findings
O. deduplication/grouping findings
P. confirmed HIGH/MEDIUM/LOW problems
Q. recommended pagination/bounding boundaries
R. BL-158 reuse recommendation
S. OFFSET vs keyset recommendation
T. recommended page sizes
U. progressive-loading UX
V. endpoint/client architecture
W. proposed cardinality/query budgets
X. correctness/equivalence strategy
Y. exact implementation scope and explicit exclusions
Z. recommended Replit tier for BUILD

Do not implement yet.