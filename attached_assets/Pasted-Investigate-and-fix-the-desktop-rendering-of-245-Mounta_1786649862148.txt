Investigate and fix the desktop rendering of #245 — Mountains Visited + Wishlist Resort Selector.
The new resort selector works conceptually, but on desktop it is rendering incorrectly. See the attached screenshot.
Observed problems:
Country accordion renders as a native gray button instead of the intended BaseLodge-styled row.
Chevron is rendering as a huge black browser/default-style icon.
United States and 8 visited are running together.
State accordion rows such as AK, AL, AZ, CA render as tiny native buttons.
Colorado renders incorrectly with the same oversized default chevron.
Resort content is not laid out as an intentional accordion.
Large portions of the page are empty.
Important: Do not change the approved information architecture or functionality from #245. The intended hierarchy remains:
United States → State → Resort
with:
U.S. first
countries alphabetical after the U.S.
states/provinces alphabetical
resorts alphabetical
checkbox selection
search
existing visited/wishlist endpoints
15-resort wishlist limit
44px minimum tap targets
First investigate the root cause. Specifically inspect:
Whether the .rs-* CSS appended in static/styles.css is actually being loaded on desktop.
Whether any existing desktop/media-query rules are overriding the .rs-* styles.
Whether the generated HTML/classes in templates/partials/resort_selector.html match the CSS selectors.
Whether the accordion controls are using native <button> styling because required CSS properties are missing or overridden.
Whether the CSS is malformed or nested incorrectly, causing the later .rs-* rules to be ignored.
Whether the JavaScript-generated markup differs between desktop and mobile.
Do not make unrelated UI changes.
After identifying the cause, make the smallest targeted fix necessary so the selector renders correctly on desktop while preserving the existing mobile presentation and all #245 functionality.
Then provide:
root cause
exact files changed
exact CSS/HTML/JS changes
confirmation that mobile behavior was preserved
confirmation that no backend/routes/database functionality changed
screenshot/visual verification if available
Do not publish or push to GitHub.