╔════════════════════════════════════════════════════╗
║ BaseLodge Row Semantics Cleanup                   ║
╚════════════════════════════════════════════════════╝

Goal:
Normalize row interaction semantics so the app feels more consistent and intentional.

This is a focused cleanup pass:
- Chevron-right means navigate to another page
- Caret-down means expand/collapse inline

Do NOT:
- redesign the Trip Detail page
- change routes
- change database models
- change trip data logic
- alter non-row layout structure
- introduce new visual styles beyond the minimal icon/behavior cleanup
- use checkpoints unless necessary

Tasks:

1. Find Trip Detail expandable rows
Locate the Trip Detail screen rows for:
- Equipment
- Transportation
- Accommodations
- Lessons

These rows expand inline instead of navigating.

2. Replace navigation chevrons with expand carets
For those inline-expand rows:
- replace chevron-right / arrow-right / › with a down caret
- use a clear expand/collapse indicator, such as ▾
- rotate or swap indicator when open if the existing JS structure supports it safely

Important:
- Chevron-right should remain only on rows that navigate to another page.
- Do not change tappable behavior beyond icon semantics.

3. Keep true navigation rows unchanged
Do not change chevrons on rows that actually navigate, such as:
- Profile rows
- Trip edit rows
- Mountain rows
- Friend rows
- Settings rows

4. Add single-open accordion behavior on Trip Detail
When the user opens one Trip Detail expandable row:
- close any previously open expandable row
- keep only one of Equipment / Transportation / Accommodations / Lessons open at a time

If the existing code makes this risky, do the icon semantics first and report why accordion behavior should be deferred.

5. Keep accessibility intact
For each expandable row:
- ensure aria-expanded updates if it already exists
- add aria-expanded if absent and straightforward
- ensure button/link semantics remain valid
- do not make non-button divs worse for accessibility

6. Testing requirements
- Start app successfully
- Confirm Python syntax passes
- Confirm Jinja templates parse
- Open a planned Trip Detail page
- Confirm expandable rows show caret-down, not chevron-right
- Confirm true navigation rows still show chevron-right
- Tap Equipment, Transportation, Accommodations, Lessons
- Confirm each expands correctly
- Confirm opening one closes the previous one
- Confirm no visual layout regression
- Confirm no route or edit-page behavior changed

At the end, report:
- Files changed
- Which rows were updated
- Whether accordion behavior was implemented or deferred
- How it was tested