The audit summary is too high-level. Redo the audit with a much deeper, migration-ready report. Do not make any code changes.

Goal:
I need a complete outbound messaging architecture audit for BaseLodge so I can decide whether and how to migrate email/notification orchestration into OneSignal.

Do not edit files. Do not refactor. Do not create migrations. Do not change environment variables.

This audit must include exact code locations and enough detail that we can safely plan a migration later.

Search the entire codebase for these terms:
- sendgrid
- SendGridAPIClient
- Mail(
- sg.send
- SENDGRID
- email
- EmailLog
- email_service
- reset
- password
- token
- notification
- push
- PushDeviceToken
- APNS
- apns
- FCM
- firebase
- OneSignal
- ONESIGNAL
- invite
- friend
- trip invite
- feedback
- contact
- cron
- scheduler
- lifecycle
- reminder
- digest
- weekly
- journey

For every result that relates to outbound messaging, document:

1. Exact file path
2. Function name or route name
3. Channel:
   - email
   - push
   - in-app
   - admin/debug
   - logging only
4. Provider:
   - SendGrid
   - OneSignal
   - APNs
   - FCM
   - local/database only
   - unclear
5. Trigger:
   - user action
   - admin action
   - scheduled/manual
   - app startup
   - background/lifecycle
6. Whether it is active, partial, dead, duplicate, test-only, or unclear
7. Environment variables required
8. Database models/tables touched
9. User-facing copy or subject line, if present
10. Migration notes:
   - easy to move to OneSignal
   - should stay transactional
   - needs product decision
   - should be removed later
   - should be consolidated behind a service

Deliver a full report with these sections:

A. Executive Summary
- Current provider responsibilities
- What is production-critical
- What is test/admin-only
- What is duplicate or risky
- What is safe to migrate later vs should be handled carefully

B. Full Inventory Table
Columns:
- Flow / Message
- Channel
- Current Provider
- Trigger
- File Path
- Function / Route
- Env Vars
- DB Models
- Status
- User-facing Copy
- Migration Notes

C. File-by-File Findings
For each file with messaging logic, summarize what it contains and why it matters.

D. Provider Map
Clearly separate:
- SendGrid responsibilities
- OneSignal responsibilities
- APNs direct responsibilities
- FCM direct responsibilities
- In-app/database-only notification responsibilities

E. Password Reset Deep Dive
Because this is production-critical, explain:
- full request flow
- token creation/validation flow
- email send flow
- template/body/subject
- failure handling
- logging
- why this should or should not move to OneSignal immediately

F. Push Notification Deep Dive
Explain:
- iOS path
- Android path
- token registration
- device model
- test/admin push routes
- production push routes
- duplicate/legacy paths if any
- how OneSignal overlaps with APNs/FCM

G. Invite / Friend / Trip Messaging Deep Dive
Explain:
- what sends actual outbound messages
- what only creates in-app/database records
- what currently has no outbound send
- what could become future OneSignal journeys

H. EmailLog / Tracking / State
Explain:
- what is logged
- where it is logged
- whether it tracks successful delivery or just attempted sends
- whether it would still be useful after OneSignal migration

I. Migration Readiness
Create three buckets:
1. Can move to OneSignal soon
2. Should stay as-is for now
3. Needs cleanup/product decision first

J. Risks and Questions
List open risks, unknowns, and questions that must be answered before any implementation.

K. Recommended Next Step
Recommend the next implementation phase, but do not implement it.

Verification requirements:
- Include the exact commands/searches you ran.
- Include counts or summaries of matches where useful.
- Do not finish until you have searched the whole codebase and produced the full report.
- No code changes.
- Use checkpoints sparingly.