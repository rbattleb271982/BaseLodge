Make a very small cleanup to the Trips tab only in BaseLodge.
Goal
Keep the current Trips tab layout exactly the same, but standardize the trip status copy and CTA copy.
Scope
Only update:
the Trips tab templates / row rendering
any Trips-tab-specific status formatting used by:
My Trips
Friends’ Trips
the My Trips CTA copy if needed
Do not redesign anything.
Requirements
1. Keep the existing Trips tab UI
Do not redesign the page
Do not change row layout
Do not turn rows into cards
Do not add badges, chips, icons, or new metadata
Keep the current editorial list style exactly as-is
Keep the current tab structure:
My Trips
Friends’ Trips
Keep the mountain filter exactly as-is
2. Standardize trip status wording on Trips tab rows only
On Trips tab row displays only, use:
planning -> Planning
going -> Going
Rules:
Do not show Confirmed
Do not show You're going
Do not show You're Going
Do not show Upcoming
Do not use any other trip-status wording on Trips tab rows
If code currently falls back from null to planning, keep that behavior, but display Planning
3. My Trips row format
Keep the secondary line as:
date · status
Example:
Apr 20–24 · Planning
Apr 20–24 · Going
Rules:
Do not add friend counts
Do not add overlap language
Do not add social copy
Keep My Trips focused only on the user’s own trip
4. CTA copy
For My Trips, the CTA should say:
Plan a trip
If the empty state currently uses different wording, change it to:
Plan a trip
Keep the CTA in the same place it is now.
5. Friends’ Trips
Keep pure date ordering as it currently works
Keep rows visually uniform
Do not add overlap labels
Do not add same-mountain labels
Do not add social intelligence copy
Keep this tab as a simple browse view of friends’ upcoming trips
Before editing
Quickly inspect the current Trips tab files and find the exact places where:
trip status text is rendered for My Trips
trip status text is rendered for Friends’ Trips
the My Trips CTA text is rendered
Deliverable
After making the change, report back with:
files changed
exact text changed
confirmation that no layout/visual redesign was made
Important
Trips tab only
minimal edit only
do not do extra cleanup
use checkpoints sparingly