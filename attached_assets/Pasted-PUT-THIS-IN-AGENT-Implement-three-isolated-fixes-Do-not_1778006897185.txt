PUT THIS IN AGENT

Implement three isolated fixes.

Do not refactor unrelated routes.
Do not change visual design except the auth/login spacing fix.
Use checkpoints sparingly.

FIX 1 — Availability date save should return to Ideas

Issue:
add_open_dates POST currently redirects to Home with tab=open, but this route is only entered from Ideas.

Change:
In app.py:
- In add_open_dates POST handler, change:
  redirect(url_for("home", tab="open"))
  to:
  redirect(url_for("trip_ideas"))

In templates/add_open_dates.html:
- Update the JS fetch fallback redirect from home?tab=open to trip_ideas.

Preserve:
- Existing save logic
- Existing validation
- Existing back button behavior

Test:
- From Ideas, edit availability dates.
- Save.
- Confirm user returns to Ideas.
- Confirm dates are saved.

FIX 2 — Jonathan Friends tab crash

Issue:
Friends route references seven_days_ago but never defines it.

Error:
NameError: name 'seven_days_ago' is not defined

Change:
In app.py friends route, near the top after today_str is defined, add:

seven_days_ago = datetime.now() - timedelta(days=7)

Do not change the friend trip logic otherwise.

Test:
- Log in as Jonathan.
- Visit Friends tab.
- Confirm no crash.
- Confirm friend trips render or empty state appears.
- Confirm Richard’s Friends tab still works.

FIX 3 — Move auth/login screen down on iPhone

Issue:
On iPhone/TestFlight, the logged-out auth screen starts too high under the Dynamic Island/status bar.

Inspect:
- auth/login template
- logged-out base template
- auth-specific CSS
- safe-area handling

Change:
- Add more top padding to the auth page wrapper using safe-area-aware spacing.
- Move the whole auth content stack down.
- Keep this auth-specific if possible.
- Do not affect logged-in app pages.

Suggested direction:
Use the correct existing selector and apply something like:

padding-top: calc(env(safe-area-inset-top, 0px) + 56px);

But inspect current spacing first and choose the right value/selector.

Test:
- Open login screen on iPhone/TestFlight.
- Confirm BaseLodge logo/title block sits comfortably below Dynamic Island.
- Confirm form still fits.
- Confirm create account tab works.
- Confirm logged-in pages are unaffected.

Final report:
- Files changed
- Exact redirect changes
- Friends route line added
- Auth selector changed
- Tests completed