Please implement the following features and fixes in the exact order below. Some features depend on earlier steps, so order matters.
================================
1. RESTORE THE HOME INTRO CARD
================================
The Home screen previously had an intro card that displayed the user’s basic info. It was removed when the “Next Trip” card was deleted. Please restore the intro card without restoring anything related to Next Trip.
Add back a card above the segmented tabs in home.html that displays:
“Welcome back, {{ user.first_name }}”
Rider type
Pass type
Skill level
Upcoming trip count (already computed in backend, or compute it here)
Use this example structure:
<div class="home-intro-card bl-card">
  <h2>Welcome back, {{ current_user.first_name }} 👋</h2>

  <div class="home-intro-details">
    {{ current_user.rider_type or "Rider" }} •
    {{ current_user.pass_type or "No pass selected" }} •
    {{ current_user.skill_level or "Skill not set" }}
  </div>

  <div class="home-intro-sub">
    {{ upcoming_trips|length }} upcoming trip{% if upcoming_trips|length != 1 %}s{% endif %}
  </div>
</div>
Styling:
Use existing .bl-card spacing/padding. No new colors needed.
====================================
2. ADD THE INVITE LINK + SHARE UI
====================================
On the Invite page (where the QR code currently lives), add two buttons ABOVE the QR code:
A. “Copy Invite Link”
B. “Share” (native mobile share sheet)
Layout example:
<h1 class="invite-header">Invite a Friend</h1>

<div class="invite-buttons">
  <button id="copyInvite" class="bl-button-primary">Copy Invite Link</button>
  <button id="shareInvite" class="bl-button-secondary">Share</button>
</div>

<div class="invite-qr-section bl-card">
  <img src="{{ url_for('my_qr') }}" class="qr-image" alt="Your QR Code">
</div>
JS:
<script>
const inviteUrl = "{{ url_for('invite_link', user_id=current_user.id, _external=True) }}";

document.getElementById("copyInvite").onclick = () => {
  navigator.clipboard.writeText(inviteUrl);
  alert("Invite link copied!");
};

document.getElementById("shareInvite").onclick = async () => {
  if (navigator.share) {
    await navigator.share({
      title: "Join me on BaseLodge!",
      text: "Use this link to connect with me on BaseLodge:",
      url: inviteUrl,
    });
  } else {
    alert("Sharing not supported on this device.");
  }
};
</script>
========================================
3. BACKEND: ADD /invite/<user_id> ROUTE
========================================
This route should behave EXACTLY like the QR flow:
If the scanner is not logged in:
Redirect to signup/login with next= pointing back to /invite/<id>
If logged in:
Automatically create bidirectional friendship
Then show the existing “connect_success.html” page
Add to app.py:
@app.route("/invite/<int:user_id>")
def invite_link(user_id):
    inviter = User.query.get_or_404(user_id)

    # If not logged in → send to login/signup
    if not current_user.is_authenticated:
        return redirect(url_for("signup", next=url_for("invite_link", user_id=user_id)))

    # Prevent connecting to self
    if current_user.id == inviter.id:
        return render_template("connect_self.html")

    # Check if already friends
    existing = Friend.query.filter_by(user_id=current_user.id, friend_id=inviter.id).first()
    if existing:
        return render_template("already_friends.html", friend=inviter)

    # Create mutual friendship
    pair1 = Friend(user_id=current_user.id, friend_id=inviter.id)
    pair2 = Friend(user_id=inviter.id, friend_id=current_user.id)

    db.session.add(pair1)
    db.session.add(pair2)
    db.session.commit()

    return render_template("connect_success.html", friend=inviter)
===================================================================
4. SIGNUP & LOGIN ROUTES MUST RESPECT ?next= AND REDIRECT PROPERLY
===================================================================
Modify both login and signup routes:
After validating credentials OR successfully creating a new user:
next_url = request.args.get("next") or session.get("next_after_setup")

if next_url and current_user.profile_setup_complete:
    session.pop("next_after_setup", None)
    return redirect(next_url)

elif next_url:
    session["next_after_setup"] = next_url
After profile setup completes:
If session["next_after_setup"] exists → redirect there.
This preserves all invite flows:
QR scan
Invite link
Future referral codes
============================================
5. INVITE PAGE HEADER SHOULD READ: “Invite a Friend”
============================================
Update the template where the QR lives so the header is exactly:
Invite a Friend
Use your existing typography classes.
=======================
6. QA + VISUAL CHECKS
=======================
After applying all changes, please verify:
Home intro card is restored and styled properly
Invite page has:
Copy Invite Link
Share button
QR code
Invite link redirects correctly:
Logged out → signup → auto-connect
Logged in → auto-connect immediately
Friendships are created both directions
Success screen loads after connection
No regressions to QR flow
No console errors
Mobile preview works