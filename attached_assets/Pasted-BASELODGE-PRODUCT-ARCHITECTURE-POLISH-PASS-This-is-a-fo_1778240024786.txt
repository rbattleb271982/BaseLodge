BASELODGE — PRODUCT + ARCHITECTURE POLISH PASS

This is a focused cleanup/polish pass from the holistic audit.

Do NOT redesign the app.

Implement only the following items.

━━━━━━━━━━━━━━━━━━━━
1. MOUNTAIN DETAIL — ADD “PLAN A TRIP HERE →”
━━━━━━━━━━━━━━━━━━━━

Add a single action on the mountain detail page:

“Plan a trip here →”

Placement:
- directly below the mountain hero/meta area
- above the social/who’s-going section
- visible without becoming a giant primary CTA
- should feel like a natural continuation of the mountain experience

Behavior:
- opens the Plan a trip / add_trip flow
- pre-fills the selected mountain/resort if possible
- user must still choose dates and save normally

Do NOT:
- auto-create a trip
- add a giant filled button
- clutter the mountain page

The interaction should feel:
- editorial
- subtle
- native
- intentional

━━━━━━━━━━━━━━━━━━━━
2. REMOVE DEAD HOME MODULE SYSTEM
━━━━━━━━━━━━━━━━━━━━

Audit confirmed:
- resolve_home_modules() is unreachable
- _module_*.html partials are unused
- no templates include them

Remove:
- resolve_home_modules() from app.py
- unused _module_*.html partials
- any leftover comments referencing the old module system

This is dead architecture cleanup only.

Do NOT touch the current Home feed logic.

━━━━━━━━━━━━━━━━━━━━
3. FIX history.back() FALLBACK
━━━━━━━━━━━━━━━━━━━━

Current issue:
mountain_detail.html uses:

javascript:history.back()

This breaks for:
- deep links
- push notifications
- cold launches
- direct URL entry

Fix:
- add a safe fallback URL to the Mountains page
- preserve natural history.back() behavior when history exists

Preferred behavior:
- if history.length > 1 → use history.back()
- otherwise → navigate safely to Mountains

Do NOT break:
- existing back navigation feel
- swipe-back behavior

━━━━━━━━━━━━━━━━━━━━
4. FLASH SYSTEM — PHASE A CLEANUP
━━━━━━━━━━━━━━━━━━━━

Audit confirmed:
friends.html and friend_profile.html suppress the shared flash system and render custom inline flash blocks.

Fix:
- remove the suppressor override in:
  - friends.html
  - friend_profile.html
- remove the duplicate inline flash markup
- allow these pages to inherit the shared base flash system from base_app.html

Also:
- ensure the shared dismiss-on-interaction behavior works correctly on these pages
- preserve current visual spacing/padding consistency

Do NOT:
- change flash colors
- redesign flash appearance
- remove flash functionality

Do NOT touch:
- trip_detail category-filtered flash behavior
- change_password flash behavior
- edit_trip flash behavior

Those are intentionally excluded from this pass.

━━━━━━━━━━━━━━━━━━━━
5. HOME PILL COPY
━━━━━━━━━━━━━━━━━━━━

Change:

“Add dates”

To:

“Add your availability”

Reason:
“Add dates” is ambiguous and can sound like planning a trip instead of adding open availability.

Do NOT change behavior.

━━━━━━━━━━━━━━━━━━━━
GUARDRAILS
━━━━━━━━━━━━━━━━━━━━

Do NOT:
- redesign pages
- change bottom nav
- change trip logic
- change Home feed logic
- touch Season Snapshot
- change Cancel trip behavior
- start SkiTrip.mountain migration work
- do broad dead CSS cleanup yet
- introduce Tailwind

This should be:
- a polish pass
- architecture cleanup
- consistency cleanup
- deep-link robustness improvement

Use checkpoints sparingly.

Do not finish until you have tested:
- mountain detail CTA prefill flow
- deep-link fallback on mountain detail
- Friends page flash dismissal
- Friend Profile flash dismissal
- Home page still renders correctly after dead module removal
- “Add your availability” pill displays correctly