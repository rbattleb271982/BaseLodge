Implement BL-195 — HOME — Convert Happening to a horizontal dismissible carousel.

This is BUILD.

Do not create another planning task.
Use the completed BL-177 bounded Happening retrieval.
Do not change Happening eligibility, ordering, privacy, dismissal semantics, or retrieval.

Goal:
Redesign only the Happening presentation on Home so it becomes a compact horizontal carousel with dismissible cards.

PRODUCT CONTRACT

Section title:
Happening

Presentation:
- horizontal scrolling carousel
- cards approximately 75–80% of the available viewport width
- next card should visibly peek into view
- compact vertical footprint
- existing dismiss action remains available
- dismiss control should sit in the upper-right of each card

Do not add “See all” unless there is already a real destination for it.
Do not create a new destination just for this task.

CARD CONTENT

Reuse the existing Happening card content and semantics.

Do not change:
- what information qualifies for the card
- trip eligibility
- friend identity semantics
- copy meaning
- ordering
- deduplication
- dismissal behavior

You may restructure markup/CSS only as needed to achieve the approved carousel presentation.

DESIGN CONSTRAINTS

Do NOT add:
- photographs
- image placeholders
- thumbnails
- profile bubbles
- avatars
- decorative mountain illustrations
- gratuitous decorative icons
- bookmarks
- new badges that introduce semantics

Keep the existing warm/clean Home visual language.

The carousel should feel elevated but restrained.

CAROUSEL BEHAVIOR

Use native horizontal scrolling where possible.

Preferred behavior:
- horizontal overflow
- touch/swipe friendly
- visible next-card peek
- snap behavior is acceptable if lightweight
- no heavy carousel library
- no new dependency

Avoid custom JavaScript unless strictly necessary.

If current dismissal behavior uses JavaScript, preserve it.
Do not rewrite dismissal logic merely for presentation.

DISMISSAL

Preserve the existing dismiss control and behavior exactly.

Requirements:
- X in the upper-right
- dismissal remains user-specific
- dismissing one card removes that card using the current behavior
- do not change server-side dismissal semantics
- do not introduce older-trip fallback
- do not change BL-177 winner-before-dismissal behavior

BL-177 remains authoritative for Happening retrieval.

EMPTY STATE

Preserve current empty behavior.

Do not fabricate carousel cards.
Do not add a new empty-state product concept unless already present.

HOME BOUNDARIES

Do not alter:
- About You & Your Gear
- Your Activity
- Friends’ Passes
- Your Next Trip
- Ideas
- Friends’ Trips
- navigation/header
- Home retrieval
- BL-177 Happening query
- BL-176 Ideas retrieval
- privacy/authorization
- schema/migrations
- native configuration

DATA / PERFORMANCE

Do not add Home queries.

Consume the existing Happening results from BL-177.

Do not:
- query additional trip data
- load relationships in the template
- change the five-card bound
- alter ordering in the template
- re-sort candidates client-side

TESTS

Add or extend focused tests covering at minimum:
- Happening section renders when candidates exist
- horizontal carousel wrapper exists
- cards render in server-provided order
- maximum five cards remain represented
- next-card peek styling/structure exists
- dismiss control remains present
- existing dismissal endpoint/behavior remains unchanged
- empty state remains correct
- no new Home queries
- no new carousel dependency
- no change to Happening retrieval semantics
- BL-177 regressions remain intact
- BL-190/191/192/193 remain intact

If practical, include responsive markup/CSS assertions rather than brittle pixel-specific tests.

MANUAL VALIDATION

Preview Home at:
- mobile-width viewport
- wider desktop/web viewport

Confirm:
- horizontal swipe/scroll works
- next card peeks
- no clipped text
- dismiss X remains accessible
- carousel does not create page-level horizontal overflow
- bottom nav remains unaffected

Do not commit yet.

Return:

BL-195 IMPLEMENTATION RESULT

- Files changed:
- Happening carousel implemented:
- Approximate card width:
- Next-card peek implemented:
- Native horizontal scrolling used:
- New carousel dependency added:
- Custom carousel JavaScript added:
- Existing dismissal behavior preserved:
- Happening ordering changed:
- Happening retrieval changed:
- New Home queries added:
- Other Home modules changed:
- Privacy/authorization changed:
- Schema/migration changed:
- Mobile validation:
- Desktop validation:
- Tests run:
- Tests passed:
- git diff --check:
- Scope deviations:
- Commit created: NO

If complete and clean, end with exactly:

BL-195 READY TO MERGE