✅ ANSWERS FOR REPLIT

1. Country Options Scope

Answer: B — Show all countries that have resorts in the database.

Rationale (important):
	•	We already synced US, CA, JP, FR, CH, AT, IT into production.
	•	Hiding countries that exist in the canonical Resort table would:
	•	Create confusion
	•	Break user expectations (especially for wishlist)
	•	Force a rework later when international expansion becomes visible
	•	The filter should be data-driven, not hardcoded.

Implementation rule:
	•	Populate Country dropdown dynamically from SELECT DISTINCT resort.country
	•	Still include “All countries” as the default option at the top.

✅ This applies to both dev and prod.

⸻

2. State / Province Label for International

Answer: Use a single generic label:

“State / Province / Region”

Rationale:
	•	Avoids fragile conditional logic for:
	•	Japan (prefectures)
	•	France (regions)
	•	Switzerland / Austria (cantons / regions)
	•	Keeps UI consistent and scalable
	•	Prevents future rework as more countries are added

Behavior rules stay the same:
	•	Dropdown is disabled until a country is selected
	•	Options are populated only from that country
	•	Label does not change dynamically per country

✅ Simple, correct, future-proof.

⸻

3. Template Locations

Answer: Yes — those are correct.
	•	Mountains I’ve Visited
	•	/settings/visited
	•	Wishlist Destinations
	•	/settings/wishlist

Constraint reminder (restate this):
	•	Changes must be limited to these templates and their supporting routes
	•	Do NOT touch:
	•	My Trips
	•	Trip creation
	•	Trip editing
	•	Any shared trip components

⸻

4. Existing Filter State Persistence

Answer: No persistence. Always reset on page load.

Desired behavior:
	•	On every page load:
	•	Country = All countries
	•	State / Province / Region = unselected
	•	Full resort list shown
	•	No localStorage
	•	No cookies
	•	No user preference saving

Rationale:
	•	These are selection tools, not preferences
	•	Avoids hidden state bugs
	•	Keeps behavior predictable across devices and sessions

⸻

✅ FINAL CONFIRMATION SUMMARY (FOR REPLIT)
	•	Country dropdown is dynamic (all countries with resorts)
	•	Label is always “State / Province / Region”
	•	Templates are /settings/visited and /settings/wishlist
	•	Filters reset on every load
	•	My Trips remains untouched

If you want, next step I can:
	•	Tighten acceptance tests Replit should run
	•	Or give you a one-line “lockfile” instruction they can paste at the top of the PR to prevent accidental My Trips changes