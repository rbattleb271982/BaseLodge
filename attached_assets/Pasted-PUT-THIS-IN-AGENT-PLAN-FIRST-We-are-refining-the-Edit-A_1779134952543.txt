PUT THIS IN AGENT

PLAN FIRST.

We are refining the Edit Availability screen.

IMPORTANT:

Do NOT redesign the page.

Do NOT change calendar behavior.

Do NOT change availability logic.

Do NOT introduce new features.

Do NOT use checkpoints unless truly necessary.

This is a lightweight UX polish pass only.

==================================================
GOAL
==================================================

Reduce visual clutter and remove unnecessary empty space while keeping the screen feeling clean.

Current issues:

1. The intro area above the calendar is too tall.
2. There is too much dead space before users reach the primary action area.
3. "How it works" feels disconnected and right-aligned.
4. The explanatory copy is longer than necessary.

We want the screen to feel tighter, more mobile-first, and easier to scan.

==================================================
COPY CHANGES
==================================================

Replace:

"Add one or more date ranges to unlock better suggestions and friend overlaps."

and

"Adding your availability helps us suggest trips and mountains that fit your schedule and makes it easier to plan with friends."

with:

"Add dates to unlock better trip suggestions and friend overlaps."

Remove the second paragraph entirely.

==================================================
HOW IT WORKS
==================================================

Remove the standalone right-aligned:

"How it works"

Replace with a small left-aligned inline link directly below the new sentence:

"Learn how it works →"

Requirements:

- left aligned
- sit directly below intro copy
- use existing maroon link styling
- route to existing How It Works page
- feel like lightweight helper text
- no large spacing around it

==================================================
SPACING REFINEMENT
==================================================

Reduce vertical spacing in the hero/introduction section.

Tighten:

- top padding under navbar
- spacing between intro text and helper link
- spacing between helper link and calendar card

Keep enough breathing room but remove dead space.

Target:

Users should reach the calendar faster without making the screen feel crowded.

==================================================
DO NOT TOUCH
==================================================

Do not modify:

- calendar interactions
- month navigation
- date selection behavior
- save logic
- selected date range card
- bottom nav
- route behavior

==================================================
RETURN
==================================================

1. Files to modify

2. Current spacing values

3. Proposed spacing changes

4. Copy updates

5. Risks

Do not modify code until PLAN is approved.