PUT THIS IN ASSISTANT

We are now performing a MARKUP-ONLY refactor in templates/home.html.
All backend logic, routes, and database behavior must remain untouched.

GOAL
- Remove “My Trips” from the tab navigation
- Make “My Trips” a standalone expandable section above the tabs
- Preserve Friends’ Trips / Trip Overlaps / Open Availability as tabbed views
- Keep all existing data bindings and JS behavior intact

--------------------------------
INSTRUCTIONS
--------------------------------

1) REMOVE “My Trips” from the tabs navigation
   - Delete only the My Trips tab button from .bl-tabs-wrapper
   - Leave Friends’ Trips, Trip Overlaps, and Open Availability buttons unchanged
   - Do NOT change switchTab() logic

2) EXTRACT the existing My Trips tab panel
   - Take the entire <div id="tab-my" ...> block
   - Move it ABOVE the tabs navigation
   - Remove role="tabpanel" and tab-related display logic
   - Wrap it in a new container:

     <div class="my-trips-section">
       <div class="my-trips-header">
         <span>My Trips</span>
         <span class="my-trips-toggle">▾</span>
       </div>
       <div class="my-trips-content">
         <!-- existing My Trips content -->
       </div>
     </div>

   - Do NOT modify the Jinja loops, conditionals, or links inside

3) ADD expand / collapse behavior for My Trips
   - Add minimal inline JS:
     • If my_trips|length > 1 → collapsed by default
     • If my_trips|length <= 1 → expanded by default
   - Clicking .my-trips-header toggles visibility of .my-trips-content
   - No backend state, no URL params

4) CREATE TRIP CTA behavior
   - Keep the existing empty-state “Create a trip” CTA exactly as-is
   - If my_trips exists, add a smaller “+ Create a trip” button
     inside the My Trips header area
   - Link to url_for('add_trip')

5) UPDATE My Trips card hierarchy
   - Inside each My Trips card, swap content order so:
     • Mountain / resort name appears first
     • Trip dates appear beneath
   - Do NOT change data fields or helper functions

--------------------------------
CONSTRAINTS
--------------------------------
- Do NOT touch routes, models, or queries
- Do NOT refactor CSS yet
- Do NOT change Friends / Overlaps / Open Availability markup
- Do NOT remove any existing JS except tab-my handling

After completing these steps, show me the updated templates/home.html.

END
