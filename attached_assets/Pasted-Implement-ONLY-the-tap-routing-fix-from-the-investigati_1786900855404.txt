Implement ONLY the tap-routing fix from the investigation report.

Current proven state:

- Push delivery works.
- The notification physically appears on the iPhone.
- The exact payload contains:
  {"url":"/friends"}
- The remaining bug is only that tapping the notification opens Home instead of /friends.
- Root cause: the tap-routing block resolves OSPlugin to a dead Capacitor proxy instead of the real Cordova OneSignal bridge, so the OneSignal tap callback never fires.

Do not change OneSignal delivery, APNs, subscriptions, permissions, entitlements, dependencies, backend send logic, or any other file.

Make ONLY these changes in:

static/js/bl-native.js

1. Fix OSPlugin resolution so the real Cordova bridge is preferred before registerPlugin:

Before:
if (!OSPlugin && typeof Cap.registerPlugin === 'function') OSPlugin = Cap.registerPlugin('OneSignal');

Replace with:
if (!OSPlugin && window.plugins && window.plugins.OneSignal) OSPlugin = window.plugins.OneSignal;
if (!OSPlugin && typeof Cap.registerPlugin === 'function') OSPlugin = Cap.registerPlugin('OneSignal');

2. Add the Cordova v5 click listener using:

OSPlugin.Notifications.addClickListener(...)

The callback must:

- clear badge/notifications using the existing helper
- pass the event into the existing _doNavFromPush(...)
- use source name:
  cordova_click_listener

Use the existing _extractPushUrl and _pushNavDone logic unchanged.

Do not redesign routing.
Do not remove existing fallbacks.
Do not change the payload format.
Do not touch app.py.

3. Add/retain diagnostic logs proving:
- the Cordova addClickListener registered
- navigation destination
- navigation source

4. Run:
- node --check static/js/bl-native.js
- all relevant JS routing tests
- full Python test suite

5. Verify only static/js/bl-native.js changed.

If all tests pass:
- commit with message:
  BL-12: Fix OneSignal notification tap routing
- push to origin/main

Do NOT publish yet.
Do NOT send another notification yet.

Return only:

- exact diff summary
- syntax-check result
- JS routing test result
- full Python test result
- commit SHA
- push result
- confirmation only static/js/bl-native.js changed

Stop there.