REPLIT: MAX + BUILD
Estimated Batch completion after this step: ~80%

TASK: BL-169 — Enforce Explicit CSRF Contract
TASK #410

Implement the approved Task #410 plan.

Treat the completed BL-169 investigation as the implementation contract.
Do not repeat the audit.

IMPLEMENT

1. Apply the existing CSRF validator centrally to all unsafe methods:
   POST, PUT, PATCH, DELETE.

   Validation must occur before application mutation or other handler side effects.

2. Add an explicit CSRF exemption mechanism.
   Keep it empty unless the existing architecture proves a legitimate independently authenticated unsafe endpoint requires exemption.

3. Close the confirmed gaps from the investigation, including:
   - authentication/password recovery
   - participant settings
   - group-trip mutations
   - invite/social mutations
   - connection-toast state
   - mountain view tracking

4. Add CSRF tokens to the auth/password-recovery forms that currently lack them.

5. Standardize the identified profile/native callers on the existing canonical CSRF token/header mechanism.

6. Make native push mutations explicitly use the canonical CSRF-aware request path rather than depending solely on fetch monkey-patch timing.

7. Remove side effects from GET.
   Convert the identified state-changing admin GET execution routes to CSRF-protected unsafe methods.
   Preserve GET only where genuinely read-only.

PRESERVE

- existing CSRF token format and validator
- existing form/header transports
- current authorization rules
- existing response/payload contracts where possible
- OAuth callback behavior
- BL-132 protections
- web and Capacitor behavior
- BL-168 auth/session lifecycle behavior

DO NOT

- implement BL-170 rate limiting
- redesign authentication or APIs
- add blanket CSRF exemptions
- add schema/index/migration changes
- touch Production or persistent data
- run the full test suite

TEST

Add focused coverage proving:

- every unsafe route is centrally protected or explicitly exempt
- missing/incorrect tokens return 403 before side effects
- confirmed gap routes work with valid tokens
- auth/reset forms work normally
- native push behavior remains functional
- admin GET routes no longer cause mutations/side effects
- converted admin unsafe methods require valid CSRF
- BL-132 and BL-168 security behavior remains intact

Run focused CSRF tests and directly adjacent regressions only.

VERIFY

- focused tests pass
- py_compile
- git diff --check
- no schema/migration changes

Do not add broad stress testing or run unrelated regression suites.

Commit:

BL-169 Enforce explicit CSRF contract

Return only:
- files changed
- key behavior changes
- focused test count/result
- verification result
- commit hash
- unresolved issues, if any