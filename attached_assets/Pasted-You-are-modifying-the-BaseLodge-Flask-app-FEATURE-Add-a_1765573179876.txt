You are modifying the BaseLodge Flask app.

FEATURE:
Add a "Filter by state" DROPDOWN to the Mountains Visited page that filters
the mountain list in real time.

IMPORTANT RULES:
- Do NOT remove or replace search
- Do NOT change routes
- Do NOT introduce new pages
- Do NOT require a Save / Apply button
- Must work on mobile
- Must be safe and backward compatible

--------------------------------------------------
STEP 1: IDENTIFY DATA SOURCE
--------------------------------------------------

Locate where the Mountains Visited list is generated.

Confirm:
- Mountains have a `state` field OR
- Mountains can be extended to include `state` safely

If some mountains do NOT have a state:
- They should appear ONLY when "All states" is selected

--------------------------------------------------
STEP 2: BACKEND – PROVIDE STATE OPTIONS
--------------------------------------------------

In the route that renders the Mountains Visited page:

- Query all DISTINCT states from the mountains dataset
- Sort alphabetically
- Pass to the template as `states`

Example values:
- Colorado
- Utah
- California
- New York
etc.

Also pass:
- selected_state (default = "all")

--------------------------------------------------
STEP 3: TEMPLATE – ADD DROPDOWN UI
--------------------------------------------------

At the top of the Mountains Visited page (above search):

Add a dropdown:

Label:
"Filter by state"

Options:
- All states (value="all") — default
- One option per state from `states`

This should be a native <select> element.

--------------------------------------------------
STEP 4: FILTERING LOGIC
--------------------------------------------------

Filtering should work as follows:

- When the dropdown value changes:
  - Filter the mountain list immediately
  - NO page reload
  - NO submit button

Implementation:
- Use JavaScript to:
  - Read selected state
  - Show only mountains matching that state
  - Always keep search active on the filtered list

Mountains without a state:
- Show ONLY when "All states" is selected

--------------------------------------------------
STEP 5: SEARCH + FILTER INTERACTION
--------------------------------------------------

Ensure:
- Search filters within the currently selected state
- Changing state does NOT clear the search input
- Clearing the state (back to All) restores full list

--------------------------------------------------
STEP 6: SAFETY & UX
--------------------------------------------------

- Default behavior must remain unchanged if JS fails
- No breaking changes to form submission
- No visual regression
- Mobile tap targets must remain usable

--------------------------------------------------
STEP 7: OPTIONAL CONTEXT (LIGHT)
--------------------------------------------------

Optionally display below dropdown:
"X mountains"

Where X updates dynamically based on the filter.

--------------------------------------------------
STEP 8: TESTING
--------------------------------------------------

Manually verify:
1. Default view shows all mountains
2. Selecting a state filters list instantly
3. Search works within filtered results
4. Clearing state restores full list
5. Mountains with no state are hidden when a state is selected

STOP when complete.
